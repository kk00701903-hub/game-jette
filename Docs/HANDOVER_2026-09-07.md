# 인수인계 — 2026-09-07 Cowork(Claude) 세션

> **먼저 읽을 것: `Docs/HANDOVER_FINAL_2026-09-15.md`** — 87차까지의 현재 상태·규칙·절차·남은 일을 한 장으로 정리한 최종 인수인계. 이 파일은 회차별 상세 이력.

프로젝트: 「너와 나의 주파수」(Coast Run) — Unity 6000.5.10f1, **모바일 세로 게임**. `C:\dev\game`, repo `kk00701903-hub/game-costrunner` (main).

> **원칙: 모든 영상·사진·UI 에셋은 모바일 세로(9:16) 기준으로 만든다.** 스틸 1080×1920 원본 → 720×1280 처리본, 영상 9:16 세로. 가로(16:9) 에셋은 넣지 않는다.

> **원칙(86차-2, 사용자 지시): UI 시안 그림에 글자를 굽지 않는다 — 다국어.** 시안(기부·캐릭터 정보·배너 등)을 배경 PNG 로 만들 때는 **프레임·아이콘·꽃·알약 틀·버튼 틀만** 남기고 제목·라벨·버튼 글자·안내문은 전부 지운다(PIL 로 그 자리를 주변색으로 메움). 글자는 **코드에서 `Loc.T(ko, en)` 으로 시안 좌표에 그린다**(`Place(rt,x0,y0,x1,y1)`, 720×1280 디자인 좌표, `resizeTextForBestFit` 로 영어가 길어도 칸 안에). 아이콘 글리프(☕·✦·★·♥)는 폰트 문자이므로 굽지 않아도 된다. 예: `UI/DonateUI.cs`·`UI/StatusUI.cs`(제목 「캐릭터 정보」·「스탯」·「생활」·스탯 라벨 7+2·「닫기」 전부 코드). **옛 배경 중 글자가 구워진 것**(더보기 포토카드·뮤직 컬렉션 버튼 글자 등, 48차 D 참고)은 같은 방식으로 글자를 지우고 코드로 옮기는 것이 남은 일.

---

## 1. 이 세션에서 한 것

### 1-1. 분석·정리
- 루트 `README.md`·`Assets/_CoastRun/COMPLETION.md`·`Art/PIPELINE_STATUS.md`는 9/3 기준으로 낡음(루트 README는 옛 「347」 지시서). 실제 코드는 `Assets/_CoastRun/`(C# 144개, 약 3만 줄).
- 진행 이력은 `Tools/Telegram/queue.txt`(3차~11차 보고)가 가장 정확. 작업결과 문서는 프로젝트 밖 「송전탑/」 폴더.
- 툴체인: Unity 6000.5.10f1(URP 17.5, Purchasing 5.4.3, glTFast, uGUI), Blender 5.2.1(blender-mcp), Photoshop/Illustrator 2026(MCP, `.cursor/mcp.json`), Firefly(스틸·영상), Kling API(`Tools/KlingGen`), ACE-Step 1.5+Demucs(BGM/SFX, `C:\dev\ACE-Step-1.5`), Mixamo, Pretendard, Cursor 에이전트.

### 1-2. 커밋 (origin/main보다 앞서 있음 — 푸시 안 함)
| 커밋 | 내용 |
|---|---|
| `201d896` feat(story) | 챕터 사이·엔딩 영상 6편, 오프닝 6컷 60초 (8·11차 미커밋분) |
| `1534a68` feat(art) | 세로 UI 아트, 실사 원경, 현무암 바닥, Pretendard, Blender 펫, 엔딩 CG |
| `da4ddf7` feat(run) | 노을 규칙, 플로우 busy 가드·워치독·CoastErrorOverlay, CoastPrefs |
| `1a9c855` feat(ui) | 9~10차 순회 수정, 글자 ×1.3 |
| `56e8ace` chore(build) | version code 14 |
| `bf3b873` feat(opening) | **오프닝 1:37** — BGM_Opening = Suno 「돌아온 제주」 0:00~1:37(1:33 페이드), 9컷 재구성, `make_opening_memory.py`, `send_pending.py` |
| `a83bdfa` feat(art) | 오프닝 새 4컷 스틸(Firefly) `Cut_Open_4~7.png` |

### 1-3. 오프닝(OpeningCinematic.cs) 현재 구조
9컷 × 10초 + 타이틀 카드(음악 끝 97초까지 유지, 탭으로 넘겼으면 2.2초).
컷 = (클립, 스틸, 대체배경, 자막). 클립 `StreamingAssets/Opening/<clip>.mp4` 있으면 재생, 없으면 스틸 켄번즈.

| # | 클립 | 스틸 | 자막 |
|---|---|---|---|
| 1 | open_1 | Cut_Open_1 | 열두 살 봄, 송전탑 아래서 처음 만났다. |
| 2 | open_7 ✅ | Cut_Open_4 돌담길 | 제주의 여름은 짧았고, 우리는 늘 바빴다. |
| 3 | open_8 ✅ | Cut_Open_5 구슬치기 | 유리구슬 한 알에 온 세상을 걸던 나이. |
| 4 | open_9 ✅ | Cut_Open_6 딱지치기 | 딱지를 다 잃어도 그 애가 웃으면 괜찮았다. |
| 5 | open_2 | Cut_Open_2 | 그 애는 늘 내 앞에 섰다. 한 번도 이유를 말하지 않고. |
| 6 | open_10 ✅ | Cut_Open_7 첫사랑·귤 | 말하지 못한 게 하나 있었다. 그때도, 지금도. |
| 7 | open_3 | Cut_Open_3 | 여섯 해 뒤, 그가 돌아왔다. 딱 1년만. |
| 8 | open_4 | Cut_CH01_Close | 노을이 지기 전에 송전탑까지 달리면… |
| 9 | open_6 | Cut_END_A3 | 열아홉 살, 마지막 1년의 이야기. (+타이틀) |

open_5.mp4(여름 축제)는 더 이상 쓰지 않음(파일은 남아 있음).

---

## 2. 남은 과제
1. ~~오프닝 영상 4편~~ **완료** — Firefly 웹(Veo 3.1, 9:16, 8초)으로 생성 → ffmpeg `setpts=1.25*PTS`로 10초 슬로우 → `open_7~10.mp4`. 원본 8초는 `Tools/FireflyVideo/raw/open_N_firefly.mp4`(gitignore). 에디터/실기기에서 오프닝 전체 1:37 재생 확인만 남음.
2. 실기기 검증(11차 워치독·에러 오버레이 확인) — 폰 필요.
3. APK 용량(230~240MB; Resources 342MB: BGM 69·Rig 58·Card 18·Video 13) → AAB/PAD 또는 압축. `Builds/last_build.txt` "1383 MB" 계측 오류 확인.
4. 카드 사진 실물 교체(Card_01~30), 문서 3종 현행화(README·COMPLETION·PIPELINE_STATUS).
5. `git push` — 사용자 PC에서만 가능(샌드박스는 403). 이번 세션 커밋은 push 완료, 영상 커밋은 다시 push 필요.

---

## 3. 새 채팅에서 이어갈 때 — 절차

### 3-1. Firefly 웹 (영상 생성)
- **Claude 앱 내장 브라우저**에 Adobe ID로 로그인돼 있음(프로필 유지). `preview_start`로 `https://firefly.adobe.com/generate/video` 열면 바로 사용 가능.
- 로그인이 풀렸으면: 그 페이지에서 JS `window.adobeIMS.signIn({redirect_uri: location.href})` 실행 → Sign in 화면 → **사용자가 Adobe ID 이메일+비밀번호로 직접 로그인**(Google 로그인은 QR 휴대폰 인증에 막힘). `auth.services.adobe.com` 사이트 접근은 허용돼 있음.
- 사용자 Chrome은 읽기 전용, Chrome 확장은 미연결 — 조작 불가.
- 설정: 모델 Veo 3.1 Fast(기본), **가로세로비율 → 세로(9:16)**, 재생 시간 8초 또는 10초, 프레임 → 첫 번째 = 해당 `Cut_Open_N.png`. 프롬프트는 `Tools/KlingGen/make_opening_memory.py`의 `SHOTS` vid_prompt 참고.
- 첫 프레임 업로드: 네이티브 파일 선택창은 못 쓰므로 JS로 `HTMLInputElement.prototype.click`을 가로채 input을 잡은 뒤, GitHub raw(`raw.githubusercontent.com/kk00701903-hub/game-costrunner/main/...`, CORS 허용)에서 fetch한 blob을 `DataTransfer`로 `input.files`에 넣고 change 이벤트. 프롬프트는 textarea(shadow DOM)에 native setter로 주입.
- 다운로드: 「다운로드」 버튼은 느리거나 안 됨 → 페이지의 `<video>`(shadow DOM, blob URL)를 fetch해 `<a download>` 클릭. 파일은 `~/Downloads/<uuid>.tmp`로 떨어짐(내용은 정상 mp4). `~/Downloads` 폴더 접근 권한 요청 후 device_bash에서 ffmpeg로 처리.

### 3-2. Kling API (대안, PC에서만)
```
python Tools\KlingGen\make_opening_memory.py --skip-stills
```
Cowork 샌드박스·PC 리눅스 VM 모두 `api.klingai.com` 차단(403). PC 터미널에서만 된다.

### 3-3. Firefly 스틸 (Photoshop)
Photoshop(컴퓨터 사용 full 허용) → 새 파일 1080×1920 → 컨텍스트 바 「이미지 생성」 → 프롬프트 → 파일 › 내보내기 › PNG로 빠른 내보내기 → `Tools/FireflyArt/Open_N_raw.png` → `python Tools/FireflyArt/process_title.py`(720×1280으로 `Resources/CoastRun/Cut_Open_N.png`). ※ process_title.py는 UI_Title_Gate.png도 다시 쓰므로 실행 후 `git checkout -- Assets/Resources/CoastRun/UI_Title_Gate.png`.
스타일 프롬프트 접두: `storybook watercolor illustration, soft painterly edges, warm nostalgic summer light, Jeju island Korea, a girl about twelve with short brown hair and a thin boy with short black hair, no text`.

### 3-4. 텔레그램 보고
`Tools/Telegram/queue.txt`에 한 줄 append → PC에서 `Tools\Telegram\send_pending.bat`(또는 `watch.py`) 실행. 샌드박스에서는 `api.telegram.org` 차단이라 직접 전송 불가. 큐 48~50번이 미전송 상태.

### 3-5. git
`.git/index.lock`이 남아 있으면(Unity/Cursor 잔재) 삭제 권한 받아서 지운 뒤 진행. 커밋 메시지는 기존 문체(영문 소문자 제목 + 이유를 쓴 본문). `Assets/_Recovery/`는 Unity 씬 복구 파일 — 커밋하지 않는다.


### 3-6. 안드로이드 에뮬레이터 / 실기기 (adb) — 2026-09-14 추가
- **Cursor 에는 에뮬 MCP 가 없다.** Cursor 전역(`C:\Users\ares2\.cursor\mcp.json`: photoshop·illustrator·blender)과 프로젝트(`.cursor/mcp.json`) 둘 다 확인 — Cursor 에이전트는 **터미널 auto-run 권한**으로 `adb`/`emulator.exe` 를 PowerShell 에서 직접 돌렸을 뿐이다(`~/.cursor/projects/c-dev-game/terminals/172669.txt` 등).
- Claude 쪽 동등 권한 = **`android` 로컬 MCP 서버** `Tools/Mcp/android_mcp_server.py`(의존성 없음, unity 서버와 같은 stdio 골격). 도구 13개: `android_ping / devices / avds / start_avd / kill_avd / install / launch / stop_app / screencap / logcat / input / shell / props`.
- 경로·기본값은 `.env` 의 `ANDROID_SDK_ROOT / ANDROID_ADB / ANDROID_EMULATOR / ANDROID_AVD(CoastRun_ARM64) / ANDROID_AVD_ALT(CoastRun_API30) / ANDROID_PKG(com.jette.coastrun) / ANDROID_ACTIVITY`. 비어 있으면 `%LOCALAPPDATA%\Android\Sdk` 기본값.
- 등록: `Tools/Mcp/claude_desktop_config.json` 과 `.cursor/mcp.json` 에 `android` 항목 추가됨. Claude 데스크톱은 **앱 설정 → 개발자 → 구성 편집** 에 `Tools/Mcp/claude_desktop_mcp_android.json` 의 `android` 항목을 붙여 넣고 앱 완전 재시작(트레이까지).
- 전형적 흐름(실기기 USB): `android_ping` → `android_install` → `android_launch`(boot=run) → `android_screencap`(name=device_run) → `android_logcat`.
- **제약(변함 없음, EMU_VISUAL.md)**: 이 PC 에뮬레이터로는 Unity 6 ARM APK 를 볼 수 없다(x86_64 AVD=SIGILL, ARM64 AVD=QEMU2 부팅 불가). 시각 확인은 실기기 adb 또는 에디터 Play + `unity_shot`.
- PC 직접 점검: `python Tools\Mcp\android_mcp_server.py --selftest` (2026-09-14 결과: adb 1.0.41 OK · emulator OK · AVD 2개 · 연결 기기 0 · APK 존재).
- 원격 세션에서 `.env` 는 remote 도구로 못 쓴다(차단) → `Tools/Mcp/env_android.txt` 를 만들고 RunBat(`menu Coast Run/Dev/Run - Tools/_clip/run.bat`)로 PC 에서 append 했다. 같은 방식으로 다른 .env 항목도 추가 가능(findstr 로 중복 방지).

### 3-7. Blender MCP 연결 — 2026-09-14 (컴퓨터 사용으로 직접 연결한 기록)
- 증상: Blender 5.2.1 을 띄워도 `blender` MCP(`uvx blender-mcp`, 127.0.0.1:9876)가 "Could not connect to Blender. Make sure the Blender addon is running" — **애드온이 설치는 돼 있으나(`%APPDATA%\Blender Foundation\Blender\5.2\scripts\addons\blender_mcp.py`) 활성화가 안 된 상태**였다(`bpy.context.preferences.addons` 에 blender_mcp 없음).
- 컴퓨터 사용 절차(승인 카드는 정상 동작함 — "Blender 5.2"(launcher) + "Blender"(blender.exe) 두 항목을 `computer_resolve_access` 로 풀어서 함께 요청해야 창이 마스킹되지 않는다. "Blender" 단독 이름은 안 풀리고 `blender.exe` 전체 경로로 풀린다):
  1. `computer_open_application("Blender 5.2")` → 스플래시는 뷰포트 빈 곳 클릭으로 닫기.
  2. 사이드바(N) 의 BlenderMCP 탭은 **Claude 앱 창이 화면 오른쪽을 덮어 탭이 안 보인다** → 대신 **Scripting 워크스페이스의 Python 콘솔**(왼쪽 아래)에서 처리했다.
  3. 콘솔 입력: `bpy.ops.preferences.addon_enable(module="blender_mcp")` → 애드온 register 시 서버가 **자동 시작**된다("BlenderMCP server started on localhost:9876").
  4. `bpy.ops.wm.save_userpref()` 로 저장 → 다음 Blender 실행부터는 켜자마자 자동 연결(애드온 `register()` 의 auto_start).
  5. 검증: `blender.get_scene_info` 가 Cube/Light/Camera 반환. Layout 워크스페이스로 되돌려 둠.
- 다음부터: Blender 만 실행하면 된다. 그래도 안 붙으면 콘솔에서 `bpy.ops.blendermcp.start_server()` 또는 위 3번 다시.
- 참고: `scene.blendermcp_server_running` 플래그는 register 시점에 갱신이 안 돼 False 로 남을 수 있음(서버는 떠 있음) — `bpy.context.scene.blendermcp_server_running = bpy.types.blendermcp_server.running` 으로 맞춤.

---

## 4. 14차 — 러닝 화면 리빌딩 1회전 (서브웨이 서퍼급 목표) — 2026-09-07 저녁
커밋 `cacc628` 카메라/점선 · `0d53e95` 수국·만국기·아웃라인·SSAO · `354e8ca` HUD 2줄 · `fc08a35` 30초 리듬 · `9b0d31e` 흑돼지·스프링·풀링 (+ 13차 `7faa55e` 시인성/클리핑/그림자/HUD 안전영역/파사드).
- 카메라 `RunnerCameraRig`: offset (0.05,1.75,-4.6), lookAhead 13, FOV 60~74. 레인 스냅 `PlayerController.LaneEaseScale` 1.2 (0.18s).
- 가독성 규칙: 먹는 것 = `PickupGlow`(가산 광원)+부유, 피하는 것 = `HazardRing`(빨간 링)+흰 아웃라인(`ObstacleOutline` 메시 / `ChromaUnlit _OutlineOn` 그림).
- 곡면 월드: `CoastChromaUnlit`에 `CoastCurveWorld` 적용(바닥 뚫림의 원인이었음). 블롭 그림자·광원·링은 `CoastMaterials.CreateTexturedTransparentCurved`.
- 거리 연출 `StreetDressing`: 수국(돌담 앞), 만국기(타일당 1줄), 난간 화단. `LookDevMenu`(Coast Run>Look): SSAO 피처·섀도맵 2048.
- HUD: 하트 3개(`RunHudChrome.BuildHealthBar`), 중앙 여정/노을 바(`UI_FinalDestinationController.BuildProgressBar` anchors 0.265~0.72, y -84), 우측 점수·코인. 안전영역 `Screen.safeArea`.
- 리듬 `RunRhythm`: 420m 마디 = 쉬움/코인 라인/위기. `ObstacleSpawner.RowGap`·`CoinSpawner.SpawnPattern`이 읽음.
- 펫 `PetKind.BlackPig`(부활 1회) — `HealthSystem.Apply`에서 `PetCompanion.TryRevive()`.
- 남은 백로그(2회전): 해녀모자/의상, 킥보드 NPC 장애물, 건물 LOD, Mixamo 24fps 달리기 클립, 목표 이미지 색 보정(따뜻한 노란 벽·청록 바다), 바다 쪽 야자·등대 강화, 하늘 구름 볼륨.

### 4-1. 2~3회전 (2026-09-07 밤)
- `8ba0395` 차양·간판(`StreetDressing.ShopFront`)·갈매기·햇살(`SkyDressing`)·슬라임 장애물 · `016a412` 킥보드 아이 NPC(`OncomingCar.Kind.Scooter`), 주인공 모자 제거(`SkaterRig.AttachBucketHat` 호출 주석) · `ad0303b` 목표 이미지풍 상가 파사드 10종.
- 파사드: `Tex_Facade_C~H` 교체 + `I~L` 추가(정사각 1024, 바닥 정렬 크롭). 키트 FBX(`Bldg_C~H`)는 `JejuKit.SpawnBuilding`에서 1.3배 — 연속 상가벽. 원본 시트(2688×1536, 2304×1792)는 PC `Downloads/shop_*.jpg`, `shop2_*.jpg`.
- 하늘: `Sky_Backdrop_NOON.png`를 파이어플라이 셀셰이딩 적운 하늘(2048×1536)로 교체(원본 `Downloads/_cmp/sky_3.jpg`, 다른 후보 sky_0~2).
- **Kling 웹**: 로그인·레퍼런스 주입(base64→File→input.files)·프롬프트까지 되지만 무료 계정은 피크 시간 제출 차단("non-member users cannot submit new tasks temporarily"). 유료 전환 후 같은 탭에서 Generate만 누르면 됨. 브라우저 파일 입력 훅: `HTMLInputElement.prototype.click` 가로채기 → `window.__capturedInput`.
- Firefly 웹 다운로드: `<img src=blob:>`를 fetch → `<a download>` 클릭 → `Downloads/*.tmp`(가끔 저장 대화상자가 클로드 앱에 뜸 — 사용자가 취소해야 함).
- 남은 것: 원경(등대·풍력) 그림 교체(후보 `Downloads/_cmp/ff_far1.jpg` 672×384 저해상 — 표준 모드로 재생성 필요), 건물 LOD, 달리기 클립, 구름 볼륨, 색 보정.

### 4-2. 4회전 (2026-09-07 밤, Kling 유료 전환 후)
- 커밋 `c8f4311` 거리 소품 6종 · `687ba7d` 소품 5종 · `796b3e6` 잉크 테두리/코인/스텀블 · `59251c1` 점프 패드/젤리/킥보드.
- **주인공 선명도**: Mixamo 리그(SkinnedMeshRenderer)엔 `CelOutlineHint`가 안 붙고 있었음 → `CoastRun/InkOutline` 셰이더(노멀 압출, Cull Front)로 리그 렌더러 복제 셸. 폭 0.012 m.
- **아이템**: `Obs_Coin_Gold/Silver`(별 엠블럼) 빌보드 — `CoinPickup` 회전은 가로 스케일로. `Obs_Jelly_{Strawberry,Soda,Lemon,Lime,Grape}` = Kling 슬라임 색상 회전(PIL HSV). `Obs_Heart/Star/Potion` 재작성.
- **피격 모션**: `CoastPlayerVisual.ApplyGameplayPose` SoftHit — Trip 28° 앞 고꾸라짐+스쿼시, Bounce 반대쪽 젖힘. `_hitStartTime`.
- **점프 패드**: `JumpPad.cs`(트리거→`PlayerController.LaunchFromPad(1.5)`), `ObstacleSpawner.SpawnJumpPadSection`(패드+4 m 간격 낮은 장애물 3줄, 5~9줄마다 70%). 그림 `Obs_JumpPad`(62° 눕힌 빌보드, `Obs_JumpPadSmall`·`Obs_Ramp` 예비).
- **킥보드**: `scooterFromChapter=1` — 1챕터부터 마주 오는 킥보드(밴·버스는 3챕터부터 그대로).
- **디버그 메뉴**: Coast Run/Debug/God mode(%#&g, PlayerPrefs `CoastRun.Debug.God`), Spawn jump pad ahead(%#&j), Dump scene(%#&d → Tools/scene_dump.txt).
- 확인 절차 메모: 스크립트 편집 후 유니티가 자동 감지 못할 때가 있음 → Ctrl+R(Refresh) 후 Ctrl+P. 컴퓨터 제어 권한은 ~15분마다 만료되어 재승인 필요.
- 관광객 커플 장애물은 발이 바닥에 정확히 닿음(RoadPlacement.Snap이 렌더러 최하단을 도로에 맞춤) — 추가 수정 없음.

### 4-3. 5회전 (2026-09-07 밤)
- 커밋 `cd4b4c5` 수집 연출·코인 / `ae3d6fb` Mixamo 달리기 클립 / `36b942e` 텔레그램 메뉴.
- **달리기 모션**: Mixamo "Running (Running With Intention)" 클립을 사이드 브라우저(Mixamo, Adobe 개인 ID 로그인)에서 In Place·30fps·Without Skin으로 받아 `Rig/Anim_Run.fbx` 교체(takeName mixamo.com, 0~21프레임). 예전 21프레임 자작 클립은 `Downloads/_cmp/Anim_Run_old.fbx`. 콘솔의 "Avatar Rig Configuration mis-match 36mm" 경고는 무해.
- **코인**: 0.8 m, 흰 테두리(ChromaUnlit outline), 글로우 1.0/0.22. **수집 연출** `JuiceDirector.PlayCoinCollect(vis,pos,amount,tint)`: 별 반짝이 파티클(절차 텍스처) 16~22개 + 가산 링 플래시 0.28초, 젤리는 작은 링만.
- **텔레그램 보고 주기**: 시작·50%·완료 3회(사용자 지시). 권한 만료 시 `[권한요청]` 줄을 큐에 넣어 알림.
- **유니티가 닫혀 있을 때**: computer_open_application("Unity")는 허브만 뜸 → Unity Hub(실행 파일 `c:\program files\unity hub\unity hub.exe`) 권한을 받아 프로젝트 목록의 game 클릭으로 연다.

### 4-4. 6회전 — 사용자 4건 피드백 (2026-09-07 밤)
- 커밋 `89f846c` 접지/선명도/앞쪽 터짐/돌 방파제 · `cc323d6` 클링 파사드 C~G.
- ① 떠다님: 원인은 발 미끄러짐 → `SkaterRig` 애니메이터 speed = clamp(Speed/11, 0.9, 1.55) + 반주기마다 `JuiceDirector.PuffStep` 발먼지.
- ② 뿌옇음: FXAA→SMAA High(`CoastRunBootstrap`), 블룸 threshold 1.2/intensity 0.55, 잉크 폭 0.017.
- ③ 레퍼런스: 바다쪽 `SeaWall`(Tex_Stonewall_Jeju 2.4 m 타일)+크림 캡+하늘색 철난간, 절벽면도 돌. 키트 건물 1.5배·10 m 로트. 파사드 `Tex_Facade_C~G` = Kling(레퍼런스 이미지 참조) 상가 입면 자연 비율 크롭(원본 `Downloads/_cmp/kf_0.png`). H~L은 Firefly 그대로.
- ④ 뒤에서 터짐: `PlayCoinCollect`가 카메라 쪽 0.9 m·위 0.45 m로 당겨서 스폰, 링/파티클 ZTest Always·큐 3500(`CoastUnlitCurved` `_ZTest` 프로퍼티 추가).
- 주의: 윈도우 IME(Textinputhost) 창이 앞에 있으면 컴퓨터 제어가 전부 막힘 → 사용자가 Esc.

## 4-5. 14차 7·8회전 (2026-09-07 밤 ~ 09-08 새벽, 자율 야간 작업)

### 7회전 (커밋 32db45e)
- 아이템 수집을 물리 트리거 → 경로 거리 판정(`Economy/PickupReach.cs`)으로. 몸 앞 1.1 m에서 먹고 터짐은 가슴 앞(카메라 쪽). 지나친 코인/젤리는 1.2 m 뒤에서 회수(예전 30~40 m → 카메라 앞 거대 아이템의 원인).
- `Obs_*.png` 49장 전부 오프라인 알파 키잉(`Tools/Art/key_to_alpha.py`, 원본은 `Tools/Art/_keybackup/`). 밉맵·트라이리니어·aniso 8. `CoastChromaUnlit`는 알파 기반(잔여 마젠타만 안전장치). → 멀리서 도트처럼 반짝이던 아른거림·분홍 테두리 제거.
- SSAO 풀해상도/블러 High, MSAA 4. 차선 점선·횡단보도·맨홀을 바닥에서 1 cm 띄움(z-파이팅).
- HUD: 하트 3개 → 하트 1 + 초록 게이지 + 숫자. 전화 버튼 숨김(`UI_PhoneOverlay` 아이콘 alpha 0).
- 포장 텍스처 줄눈을 이끼색+AO로(원본 `_keybackup/Tex_Pavement_Cream.png`). 관광객 커플은 Kling으로 발까지 있는 전신 재생성.

### 8회전 (커밋 91748b7, dc27359, c5ea604) — 사용자 1~10번 지시
1. 건물: `JejuKit.WallColorRule` 민트/베이지/블루 3색이 10 m 단위로 순환(파사드 그림엔 55%). 집마다 간판·차양(`StreetDressing.ShopFront`), 2/3는 2층 발코니 난간+수국 화분(`Balcony`), 밑동 접지 그림자(`ContactShadow`). 말랑이(Obs_Slime) 1.15 m, 젤리 픽업 1.2배+흰 테두리.
2. 펫: 주인공 옆·살짝 앞(0.95, 0, 0.7). 진행 방향은 스무딩 없이 따라감(SmoothDamp 지연으로 카메라 앞까지 처지던 것).
3·4. 메인: `UI_Title_Gate.png` = 노을 커플 키아트(로고 없음). 제목은 글자(`AlbumTable.AlbumKo/AlbumEn` + lang 표 "Our Frequency"). 메뉴 3개(이어하기/새로하기/더보기) 아래 한 줄, 더보기 → 오른쪽에서 열이 슬라이드(챕터/노을 달리기/컬렉션/오프닝/설정).
5. 스케줄 카드: 삽화 + 항목 이름만(214px). 수치는 고른 순간 토스트. 삽화 없는 항목(job_salon, job_market, job_sashimi, job_night_delivery, job_hall, job_dangsan, job_tower_watch, job_lighthouse, job_tower_fix, job_dj_assist, 교육 항목들)은 Kling으로 `Sched_<id>.png` 4:3 생성하면 자동 반영 — **다음 회전 과제**.
6. 챕터 카드: 250×320 가로 스크롤, 챕터 번호+그림만.
7. 달리기: 클립 재생 속도 = 속도/7.5(1.0~1.9), 앞기울기 5~12°, 발 디딤마다 스프링 튐, 먼지 확대, 카메라 bob 0.045.
8. 오프닝 스틸 6장(Cut_Open_1/2/4/5/6/7) Kling 2K 재생성 — 사용자 준 12살 아이들 그림(`Tools/Art/ref_kids_12.png`) 기준 옷·머리 고정 프롬프트(원본은 `_keybackup/opening/`).
9. 말랑이 마스터(Kling) → Obs_Slime(진한 초록) + Jelly 5색(색상 이동). 
10. `CoastHudLayout.TextScale` 1.4, 최소 19px.
- 에디터: `Coast Run/Scenes` 단축키 Shift+F1(타이틀)/F2(런)/F5(육성)/F9(부트).

### Kling 웹 작업 절차(내장 브라우저)
- `preview_start https://app.klingai.com` → Image Generation. 프롬프트는 `.ProseMirror`에 `execCommand('insertText')`, Generate 버튼 클릭. 결과는 `.creation-stream-item`에 프롬프트와 함께 나타남(완료 후 새로고침 필요할 때 있음). 이미지 URL의 `:WxH.webp` 접미사를 떼면 원본 PNG. 페이지에서 fetch→blob→`a.download` 하면 사용자 `Downloads`에 저장(연결 폴더). 배경색은 (234,0,114) 딥핑크로 나옴.

## 4-6. 14차 9~14회전 (2026-09-08 오전)
- 9: 골드런식 관용(몸 65%/장애물 70% 판정, 레인 0.20s ease-out, 선입력 0.25s, 계단식 가속 30s +6% 상한 1.7배), 오프닝 강제재생 해제(더보기›오프닝), 테두리 남색.
- 10: 점프대 하늘 코인 아치(`CoinSpawner.SpawnAirArc`, 픽업 높이 판정), 그림 소품 세로 명암·뒷면 두께.
- 11: Blender 장애물 키트 `Tools/blender/obstacle_kit.py`(Shift+F7) → `Models/Obs3_*`(말랑이·콘·바리케이드·크레이트·버스·밴). `ObstacleCatalog/ObstacleHazard/OncomingCar`가 있으면 우선 사용 + 잉크 헐. 건물 잉크 셸 `BuildingOutline`.
- 12: 팔레트 5+1(`JejuKit.Cream/Sky/Pink/Mint/Lemon/Coral`), 사진 텍스처 OFF, 선화 파사드 `Tex_FacadeLine_*`, 차선 레몬/스카이. 경고 = 화면 배지 `WarnHud` + 바닥 셰브론 띠.
- 13: 파트별 상가 키트 `Tools/blender/town_kit.py`(Shift+F8) → `Shop_A~F`(벽/트림/창틀/유리/문/차양 슬랫/지붕 재질 분리, Unity 에서 팔레트 MPB). 파스텔 라이팅 `PastelLit`. 장애물 밀도: 줄 간격 1.0→0.62s, 2레인 22→50%, 열린 레인 점프 장애물 `SpawnLowFill`.
- 14: 필지 다양화 `World/JejuLots.cs`(상가 50 / 제주 집 House_A·B 22 / 공터 14 / 공원 14, 카페 테라스 Prop_CafeSet+파라솔, 정자 Prop_Pavilion). 경고는 주인공 레인 앞 5~22 m 가장 가까운 장애물 하나만(`ObstacleWarning.Pick`, 레인 바꾸면 이동). 장애물 충돌 시 팡(`ObstacleHazard.Pop` + `JuiceDirector.PlayObstaclePop`).
- 다음 후보: 건물 정면 클로즈업 점검(차양 각·간판), 사진 실제 제주 마을 참고해 집 지붕·돌담 비율, 메타 재구성(마을 빌드업), APK 100MB.

## 4-7. 15차 1~2회전 (2026-09-08 오전) — "건물이 사진처럼", 차 경고, 팡 터짐

- **FShop 키트**(`Tools/blender/town_kit.py` `uv_box`/`fshop`, Shift+F8): `FShop_Sq`(6.4×6.4) / `FShop_Tall`(8×8) / `FShop_Wide`(9.6×5.6).
  본체 상자의 **정면(+X)과 양 옆면(±Y)** 에 `FacadeFront` 재질(UV 0..1)로 같은 그림을 씌움(골드런식 그림 상자). 뒤/위/아래는 `Wall`.
  옆·뒤 현무암 띠(`Stone`, 1.1 m), 흰 코니스+계단식 기와 `Roof`, 모서리 `Trim` 기둥, 화분(`Frame`+`Leaf`), 문턱 `Concrete`.
- **파사드 그림**: `Assets/Resources/CoastRun/Tex_Facade_M~X.png`(12장, Kling text-to-image 3:4, 지붕 위 잘라내고 1024px).
  프롬프트 골자: "Front elevation of a two-storey Jeju … stylized 3D mobile game render like Talking Tom Gold Run, straight-on orthographic, pastel <색> wall, black basalt stone lower wall, <색> clay tile roof, Korean signboard, striped awning, no people, no sky, flat front only".
  원본 Kling 다운로드는 `Downloads/fac_new_*.png`, `*.tmp`(목록 `Tools/Art/_fac_new_list*.txt`).
- `JejuKit.SpawnFShop`: `SqTex {M,N,O,P,R,T,V,X}`, `TallTex {U,X}`, `WideTex {Q,W,S,Q}`; `FacadeWall`(옆면 단색), `FacadeRoof`(3D 지붕색) 표. `SpawnBuilding`은 FShop이 있으면 **항상** FShop(파트 키트 Shop_A~F는 폴백).
- 장애물: `OncomingCar.Spawn`에도 `ObstacleWarning.Attach` → 차·버스도 '!' 경고. `ObstacleWarning.Pick`이 매 프레임 레인 재계산, 26 m부터.
- 팡 터짐: `JuiceDirector.PlayObstaclePop` — 절차 텍스처 별/하트/연기 파티클(`MakePop/SpawnPop`) + 흔들림 + 링 플래시. 플레이 중 별 버스트 확인됨(하이어라키에 PopStar/PopHeart/PopPuff(Clone)).
- 커밋: 1b5f231(1회전), f5fe6e3(2회전).
- 남은 것: 집(House_A/B)도 그림 파사드(Q/W 1층 기와집)로 바꾸면 거리 전체가 사진풍이 됨. 파사드 12장 중 옆면에 간판이 반복돼 보이는 것은 옆면용 UV(0.6..1)로 조정 여지.
- 3회전(15차-3, 커밋 cba3fc4): 집 필지도 `FShop_Low`(8×4, 1층) + Q/W 기와집 그림으로 교체(`JejuKit.SpawnFHouse`, `JejuLots.House`는 키트 폴백 유지). 이제 거리 전체가 그림 파사드.

## 4-8. 16차 (2026-09-08 오전) — 지붕색·높낮이·간판 한글·필지 다양성 (커밋 a0c1793)

- 간판: `Tools/Art/facade_signs.py` — 원본 `Tools/Art/_facade_src/Tex_Facade_K.png`의 엉망 한글 영역을 보드색으로 덮고 Pretendard-ExtraBold로 상호를 그려 `Assets/Resources/CoastRun/Tex_Facade_K.png`로 출력. 상호·보드 좌표는 스크립트 `SIGNS` 표(비율). 새 파사드를 추가하면 표에 한 줄 추가 후 `python3 Tools/Art/facade_signs.py K`.
- 모델(Shift+F8): `FShop_Sq / Sq_Flat / Sq_Gable / Tall(3층=그림 2층+단색 1층) / Tall_Flat / Wide / Wide_Flat / Low / Low_Gable`. `fshop(name,w,h,depth,storeys,roof,extra)`.
- `JejuKit.SpawnFShop`: 해시(`_h`)로 높이 등급(1층 15/2층 45/3층 25/넓은 15%)·지붕형·그림·스케일(0.92~1.10) 결정. 지붕색 `JejuRoofs` 9색. `ApplyFacade` 공용.
- **Recolor 버그 수정**: 블렌더 join 메시는 렌더러 하나에 서브재질 여러 개 → 재질 인덱스별 `SetPropertyBlock(mpb, i)`. 이전엔 마지막 색(흰 Trim)이 전체를 덮어 지붕이 전부 흰색이었다.
- 필지: `PromenadeSegmentBuilder._shopStreak` — 상가 2연속이면 다음 필지는 집/공터/공원. 비율 상가 45 / 집 22 / 공터 15 / 공원 18. 그림 상가 시드는 `rng.Next(1000000)`.

## 4-9. 17차 (2026-09-08 오후) — 빨래줄 활공·타격감·난이도·픽업 아트 (커밋 cba92dd, 17-2)

- **빨래줄** `Economy/ClothesLine.cs`: `ObstacleSpawner.SpawnJumpPadSection`에서 점프대 2개 이상 지난 뒤 60%로 z+6.5 m에 스폰(그때는 하늘 코인 아치 대신). 트리거(줄 높이 3.4±0.9 m, 앞뒤 2.6 m)에 **공중 상태**로 닿으면 `PlayerController.GrabLine(3.4 s, 3.4 m, ×1.55)` → 활공(무적, 레인 이동 가능), `CoinSpawner.SpawnGlideLine`으로 줄 높이 코인(앞 1/3 잡은 레인, 뒤는 옆 레인 물결), `GlideRope`(LineRenderer 집라인), 빨래 한 장이 머리 위에 딸려 감. 에디터 디버그: 플레이 중 **L** = 12 m 앞 점프대+빨래줄. 그림 `Obs_Laundry.png`(옷걸이+빨래) 있으면 빌보드.
- **타격감** `ObstacleHazard.Pop`: 파편은 주인공↔장애물 접점(45%)에서, 파티클 시뮬레이션 공간 = 주인공(`JuiceDirector.MakePop` Custom space) → 뒤에 남지 않음. 장애물 본체는 주인공에 붙여 0.06 s 납작→0.16 s 펑. `PlayObstaclePop`: 히트스톱 0.04×0.07 s, Shake 0.32, FOV −5, `CoastPrefs.Vibrate()`(모바일 Handheld.Vibrate).
- **난이도** `RunTuning.HitDamage` 14→30(체력 스탯으로 30→18), `HealthSystem.potionHeal` 25→40, `JellySpawner.potionEvery` 180→110 m. 차·버스 `carFromChapter` 3→1, 버스 비율 1챕터 22%.
- **픽업 아트** Kling 재생성(프롬프트: "glossy stylized 3D mobile game pickup icon like Talking Tom Gold Run … thick clean dark outline … isolated on flat solid magenta"): `Obs_Coin_Gold/Silver`, `Obs_Jelly_Strawberry/Soda/Lemon/Lime/Grape`, `Obs_Potion`, `Obs_Laundry`. 키잉은 새 스크립트 `Tools/Art/key_bg.py`(모서리 배경색 샘플링 → 색상 창 ±16° + 채도 → **테두리와 연결된 영역만** 제거 → bbox 크롭). 원본 `Tools/Art/_keybackup/`(이전 것은 `_keybackup_r16/`).
- Kling 주의: 제출 직후 스트림에 안 보여도 크레딧은 차감·생성됨(늦게 나타남). 스트림은 가상화라 오래된 항목은 스크롤/새로고침 필요. Assets 페이지는 캡션이 없어 썸네일로 식별.

## 4-10. 18차 (2026-09-08 오후) — 허들 팡 + UI/UX 통합 점검 (커밋 191f1f4)

- **팡 안 터지던 장애물** = `DuckHazard`(OverheadBar·Clothesline·LanternString, 웅크려 피하는 것들). `ObstacleHazard.PopFrom(Transform)`(정적, 루트 탐색+팡)로 공용화해 DuckHazard도 호출. 차·버스는 `OncomingCar`가 ObstacleHazard를 붙이므로 이미 팡.
- **화면 비율**: `CoastPortraitViewport`는 9:16보다 긴 화면에서 카메라 rect를 꽉 채움(전엔 위아래 검은 띠). `CoastUiCanvas` 스케일러 `matchWidthOrHeight` 1→0(폭 720 기준). 노치는 `CoastPortraitSafeArea`. 검증: Game 뷰 "Phone 20x9 (1080×2400)" 프리셋(추가해 둠) + 16:9.
- 고친 화면: 타이틀 3버튼(20:9에서 좌우 잘림 → OK), 설정/캐릭터 선택/기록 카드(`CreateOverlayPanel` 카드 anchor 0.06~0.94 → 닫기가 카드 밖으로 안 나감), 챕터 선택(`OpenTimeline`: 탭 y −96, 제목 뒤 어두운 띠, 시놉 19px, 카드 212×290 3장, 카드 위치는 실제 캔버스 높이 기준), 육성 독 라벨 13px Bold, 업적 행 56/18px.
- **에디터 입력 이슈**: 원격 제어에서 플레이 중 왼쪽 클릭이 유니티에 안 들어오는 현상(키보드·오른쪽 클릭은 됨, 재시작해도 동일). 우회: `CoastDebugClicker`(EventSystem에 자동 부착, 에디터 전용) — **K** 키로 마우스 아래 UI에 클릭 이벤트 직접 전달. `CollectionUI`의 닫기는 K로도 안 닫혔음(Escape도) — 원인 미확인, 실기기 확인 필요.
- 아직 남은 UX 메모(30년차 시선): 컬렉션 '레코드' 탭 트랙 리스트 글자 12~14px로 촘촘함(행 확대 필요), 육성 화면 우측 독 6개가 캐릭터 그림 위에 겹침(그림 폭 544→500 또는 독을 그림 밖으로), 타이틀 '더보기' 컬럼 5개는 2단(플레이/기타)으로 나누면 더 깔끔, 러닝 HUD 20:9에서 상단 하늘이 35%라 카메라 피치 −2° 고려. `PlayerController.DebugGod`(PlayerPrefs CoastRun.Debug.God)이 켜져 있으면 무피격이 되니 실기기 테스트 전 확인.
- 18차-2: 갤럭시 S26 = 2340×1080(19.5:9). 긴 폰에서 세로 FOV를 9:16 가로 시야 유지하도록 넓히고(RunnerCameraRig.TallFov) 1.8~2° 내려다봄 — 도로 폭이 좁아지고 하늘만 늘던 것 보정. Game 뷰 프리셋 'Galaxy S26 (1080x2340)'은 추가 팝업 OK가 원격에서 안 눌려 못 넣음(수동으로 + → 1080×2340 추가 권장).

## 4-11. 18차-3 — 화면 기준 해상도 확정 (사용자 지시)

- 사용자 지시: 세로 9:16, **기준 1080×1920**, Canvas Scaler Scale With Screen Size / Match Width Or Height **0.5**, 16:9~21:9(폴드 22:9) 대응, Safe Area 스크립트 필수.
- 구현(`UI/CoastUiCanvas.cs`): 스케일러를 위 값으로. 기존 배치 코드는 720×1280 단위라 `HudInset`을 `DesignScale = 1.5` 배 스케일로 두고, `CoastPortraitSafeArea.Apply()`가 매 프레임(그리고 Awake에서 즉시) 카메라 rect ∩ `Screen.safeArea`를 스케일러와 같은 로그 평균 식으로 디자인 단위로 환산해 inset 크기를 정한다. 그 안의 UI는 손대지 않아도 그대로.
- 폭 계산(디자인 단위): 9:16 → 720, 19.5:9(S26) → 652, 20:9 → 648, 22:9(폴드) → ~615. 고정 폭 요소는 600 이하로: 타이틀 3버튼 188×3+10×2, 대화상자·바인더 616, 챕터 카드는 실제 패널 폭으로 3등분.
- 검증: 20:9 프리셋에서 타이틀·러닝 HUD·일시정지 확인. 16:9와 S26(1080×2340)은 수동 확인 권장.
- 조작: "러닝 중 좌우 버튼 안 눌림" → 게임엔 좌우 버튼이 없고 스와이프뿐이었음. `MobileSwipeInput`에 탭 폴백(0.22 s 이내, 이동 < 0.6 cm: 좌/우 1/3 = 레인, 가운데 = 점프) 추가, UI(일시정지) 위에서 시작한 터치는 조작으로 안 씀. 스와이프 임계 0.6 cm(dpi 기반)는 유지.
- 18차-4: 편집기 Game 뷰 기준을 1920×1080 Portrait(=1080×1920)로 되돌림. 20:9 프리셋은 검증용으로만 남김. 1080×1920에서 타이틀·육성·챕터·러닝 확인. 노을 타이머 글자가 세로 잘림으로 안 보이던 것 수정(verticalOverflow).

## 4-13. 21차 (2026-09-08 밤) — 20장 흑백·슈퍼맨 활공·자동 배치·후처리 활성화 (커밋 a132a2d)

- **후처리가 여태 안 돌고 있었다**: `CoastRun_Renderer.asset`의 `postProcessData`가 비어 있어 URP 후처리(블룸·톤매핑·비네트·ColorAdjustments)가 20라운드 내내 no-op였다(주스 채도 킥, 기억 팝업 채도, 챕터 그레이드 전부 무효). URP 패키지 `PostProcessData.asset`(guid 41439944d30ece34e96484bdb6645b55)을 연결. 켜고 나니 예전 수치(ACES+대비16+채도26+비네트0.22+파란 그림자)는 너무 어둡고 진해서 `CoastPostStack`을 밝은 카툰 톤으로 재조정: Neutral 톤매핑, 노출 +0.15, 대비 4, 채도 10, 블룸 0.4/1.15, 비네트 0.10, SMH 거의 중립. 챕터 그레이드도 절반 이하로. **굽힌 VP_Base/VP_CH 에셋을 로드해도 코드 값으로 덮어쓴다**(코드가 진실).
- **20장 흑백** `Visual/MonochromeWorld.cs`: `StageManager.StartStage`에서 `Arm(ChapterIndex)`. 20장이면 주인공이 달리기 시작한 뒤 10초부터 2.5초 동안 전역 Volume(priority 30, saturation −100, contrast 12, exposure −0.15) weight 0→1. UI는 오버레이 캔버스라 컬러 유지. 시작 순간 `JuiceDirector.OnWorldFade()`(작은 흔들림+FOV −3+SoftHit). 에디터: 플레이 중 **B** = 즉시 흑백. `EnsureCamera`가 카메라 후처리 on·볼륨 레이어 마스크 보장.
- **슈퍼맨 활공** `Player/SkaterRig.cs`: `PlayerController.IsGliding`이면 `_glideBlend`로 몸을 78°(+출렁임) 눕히고 엉덩이 위치 보정, `LateUpdate`에서 휴머노이드 뼈를 직접 조준(`Aim`) — 오른팔 앞으로 곧게(주먹), 왼팔 옆구리 따라 뒤로, 다리 뒤로 곧게, 고개 앞. 믹사모 클립 없이 절차적. `RunnerCameraRig._glideCam`: 활공 중 카메라 +0.75 m 위·0.9 m 뒤로 빠지고 7° 내려다봄(높이감).
- **자동 배치** `Raising/RaisingUI.AutoPlan()`: 스케줄 시트 하단이 [닫기]/[자동 배치]/[실행] 3버튼(190px). 빈 칸만 채움. 칸마다: 스트레스 ≥70 → 휴식(스트레스 감소 큰 것) / 돈 <60 → 알바(돈−스트레스×0.5) / 그 외 → 체력·순발력·매력 중 최저 스탯을 올리는 카드(교육 → 자기계발 → 알바 순, 점수 = 상승×10 − 스트레스×0.4 + 시즌 보너스 6, 돈이 40 밑으로 떨어지는 교육 제외). 잠긴 카드(`LockReason`)·중복 제외, 칸을 채울 때마다 예상 스탯을 갱신해 다음 칸 판단. 토스트 "자동 배치 N칸 · 마음에 안 들면 칸을 눌러 바꿔". 슬롯 칩·시트 슬롯 이름은 best-fit(9~15px)로 긴 이름이 새지 않게.
- **타이틀 아트** `Resources/CoastRun/UI_Title_Gate.png`: Kling 3.0 이미지 참조(원본 사진을 Image1로)로 구도·포즈·의상 그대로 두고 귀여운 3D 게임 스타일로 재생성. 3세트 뽑음 — 유아 치비(너무 어림) / 10대 스타일라이즈(채택, 남자 아이돌풍) / 20대 사실풍(사진에 너무 가까움). 후보 원본 `Tools/Art/_title_candidates/`(teen_b_USED.png가 채택본, 1536×2720 → 1080×1920). 원본 사진은 `Tools/Art/_titlebackup/UI_Title_Gate_photo_r20.png`. Kling 참조 업로드 요령: 내장 브라우저는 파일 선택이 안 되므로 JS로 base64 → `File` → `input.el-upload__input`에 DataTransfer로 주입 후 change 이벤트(청크로 넣고 해시로 검증할 것 — 긴 문자열은 한두 글자가 빠진다).
- 21차-3 **말랑이 또렷하게**: 셰이더 텍셀 테두리는 원거리에서 밉맵과 함께 사라져 말랑이가 뿌옇게 보였다. `Tools/Art/bold_outline.py`로 그림 자체에 크기 비례 이중 테두리(짙은 남색 3.2% + 흰 외곽 2.0%)를 굽고 채도·대비 살짝 올림(원본 `Tools/Art/_keybackup_r21/`). 대상: 말랑이 5종·코인 2종·물약·하트·별. `PaintedProp`은 이 키들에 셰이더 테두리 1.5텍셀만(흰 링을 덮지 않게). 크기: 말랑이 0.3→0.36(대 0.42→0.48), 코인 0.8→0.9, 물약 1.0, 하트 0.9. 텍스처 mipBias −0.7.

## 4-14. 22차 (2026-09-08 밤) — 9항목: 테두리 3D·차 그림·타격감·왼발·골인 리본 연출·활공 회전·데미지 밸런스·7곡·포토카드 (커밋 19dc2bb → 22차-2)

- **① 말랑이 테두리 얇게+입체감** `bold_outline.py` v2: 남색 2.2%·흰 1.4%로 얇게, 안쪽 가장자리에 밝은 하이라이트/그림자 띠 + 위→아래 명암 그라데이션(구슬 느낌). 원본은 여전히 `_keybackup_r21/`.
- **② 차·버스 그림** Kling으로 정면 그림 생성(택시 `veh_taxi_0`, 버스 `veh_bus3d_0` — 3D 글로시 토이 스타일, 마젠타 배경) → `key_bg.py`로 키잉 → `Resources/CoastRun/Obs_Van.png`(택시)·`Obs_BusFront.png`(버스). 키잉 뒤 앞유리·바닥 그림자에 남은 마젠타는 후처리로 유리색(H=100)으로 돌리고 바닥 12%는 알파 페이드. `OncomingCar.BuildVisual`: **PaintedProp 그림을 최우선**(택시 1.8 m, 버스 2.6 m), Blender 키트는 그림 없을 때만. 예전 그림 백업 `Tools/Art/_keybackup_r22/`. Kling 원본 `Downloads/veh_*.png`.
- **③ 픽업 타격감** `Visual/PickupFloat.cs`(신규): 오버레이 캔버스에 "+N" 떠오르는 텍스트, 코인은 베지어로 HUD 코인 카운터까지 날아가 펀치 스케일. `JuiceDirector.PlayCoinCollect` 버스트·링 키움, 연속 픽업 스트릭마다 SFX 피치 +0.045(`CoastAudioManager.SfxPitchBoost`), 몸통 스쿼시.
- **④ 왼발** `SkaterRig.leftFootYawFix=22`, LateUpdate에서 LeftToes 기준 실제 오차의 85%를 되돌림.
- **⑤ 골인 연출** `World/FinishRibbon.cs`(신규, 기둥+줄무늬 테이프, 통과 시 `Break()`로 양쪽이 흔들리며 벌어짐). `StageManager.FinishThenClear`: 리본 파열 → `PlayerController.FinishRun()`(속도 7.5/s로 감속, 관성 이동) → `RunnerCameraRig.PlayFinishFrame`(앞쪽 3.6 m·1.55 m 높이에서 가슴 바라봄) → 0.75초 뒤 `SkaterRig.SetFinishPose(true)`(몸 180° 회전, 오른손 번쩍, 왼손 옆구리, 고개 14° 갸웃, 카메라 응시) → 0.8초 뒤 클리어. `UI/StageClearUI`는 팝업 카드가 아니라 인게임 오버레이: 상단 "STAGE CLEAR!" + 등급 배지, 먹은 아이템 칩(코인/말랑이/하트/물약/별 × N, 슬라이드 인), 하단 콤보/합계/지갑/여정 + 버튼. 업그레이드 상점은 정산에서 뺐다. `StageRunStats`에 Potions/Stars 추가. 에디터 PageDown = 결승 30 m 앞으로 워프.
- **⑥ 활공 회전** `ClothesLine`: 빨래에 매달리는 연출 삭제, 줄을 지나면 몸이 한 바퀴(`SpinSeconds` 0.6, smoothstep 360°) 돌며 양손 앞으로 뻗은 슈퍼맨 → 앞쪽에 `SpawnGlideLine`으로 코인 줄.
- **⑦ 데미지 밸런스** `ObstacleHazard.DamageMul` → `PlayerController.PendingHitDamageMul` → `HealthSystem.HandleHit`. `RunTuning.HitDamage = MaxHp×0.34×(1−0.22×체력01)`(일반 장애물 3방). 버스 ×99(즉사, mul≥50이면 max+1), 차 ×1.6(2방), 킥보드 ×0.8, 귤 ×0.7. `ObstacleSpawner`: 마주 오는 차 간격 5→3열, 버스 비율 0.3/0.4, 킥보드 0.25.
- **⑧ 레코드 7곡** `Content/AlbumTable.cs`: 1장 보조개(M7,타이틀) / 3장 솜사탕 둘이서(M2) / 6장 Good bye My first love(M4) / 9장 이별예감(M1) / 12장 남녀사랑이야기(M6,연주) / 16장 돌아온 제주(M5,타이틀) / 20장 별(M3,듀엣). **오디오 파일은 아직 없음** — `Resources/CoastRun/BGM/Track_M1..M7.ogg`에 넣으면 바로 재생. `Collection`·`CollectionUI`·업적 #38 7곡 기준.
- **⑨ 포토카드** 첨부 6장 + Kling 생성 4장 → `Resources/CoastRun/Card/Card_21..30.png`(`Tools/Art/make_photocard.py`: 폴라로이드 안쪽 사진 자동 검출 → 4:5 → 600×900 크림 카드 + 캡션 + 하트 + "NO.0xx • FILM"). 21 사려니숲 / 22 함덕 벤치 / 23 풍차 기타 / 24 노을 도로 / 25 애월 카페 / 26 귤밭 / 27 우도 자전거 / 28 성산 일출 / 29 유채꽃 / 30 함덕 노을. 원본 `Tools/Art/_photocard_src/`. **얼굴 참조 요령**: 두 사람 얼굴 시트 `couple_faces.jpg`를 base64 1000자 청크로 JS에 넣어(`window.__c[i]`, 청크마다 `h(s)=(v*31+charCode)>>>0` 해시를 파이썬 값과 대조) Kling Image1로 업로드 — Kling Uploads에 남아 있으니 재사용 가능. 프롬프트 "Photo of the SAME two people as Image1 (keep faces/hair/identity), change only clothes and Jeju location…". **주의**: 2장짜리 참조를 주면 결과도 2패널로 나온다 → 위/왼쪽 절반만 잘라 카드로.
- 미결: OST 7곡 오디오 파일, 추가 포토카드(동백·협재 등)는 같은 방법으로 더 뽑을 수 있음(크레딧 ~140).

## 4-15. 23차 (2026-09-09 새벽) — 진행 중, 컴파일 미확인 (이 세션에서 종료됨 → 다음 세션이 이어서)

### 사용자 지시 9항목
1. 1번 레이스 끝 **붉은 리본 확실히 보이게**, 리본 지난 뒤 하늘로 올라가는 느낌 제거, 뒤돌 때 **양손 모양** 검수, 얼굴은 메인페이지 얼굴로
2. 장애물 충돌 시 화면으로 **'꽈당'** 불쾌한 타격감(맞았다는 걸 알아야 피한다)
3. 리본 지나면 주변 코인·장애물 **싹 사라지고**, 뒤에서 **박수치는 사람들**
4. 펫 효과 눈에 보이게 — 코인 먹을 때 가끔 **"120%"** 표시
5. 클리어 화면에 **송전탑까지 몇 km** 남았는지 안 보임 → 보이게
6. **육성 페이지 디자인을 메인페이지 느낌**으로
7. 프린세스 메이커 집사처럼 기억 없는 주인공을 안내하는 **남자 꼬마(집사)** 를 육성 페이지에
8. 육성 페이지 주인공은 **움직이는 이미지**로
9. 게임 중 **꼬마 도움 버튼** 떴을 때 누르면 **3초 피버모드**(주변 코인·말랑이 다 먹음)

### 코드 반영 완료(1~5) — **아직 유니티 컴파일 확인 못 함, 커밋 안 함**
- `World/FinishRibbon.cs` 전면 교체: 기둥 3.4 m + 위 빨간 FINISH 현수막(TextMesh) + 풍선 + **가슴 높이(1.25 m) 두꺼운(0.5 m) 진빨강 언릿 리본**(흰 가장자리, 점선 무늬) + `SpawnCrowd`(결승 뒤 양옆에 `Crowd_A/B`(없으면 Tourists/Haenyeo) 빌보드 8장, 리본 끊길 때 튀어나와 박수 콩콩) + `JuiceDirector.OnFinishConfetti`.
- 관중 그림: `Resources/CoastRun/Obs_Crowd_A.png`, `Obs_Crowd_B.png` (Kling 생성 → key_bg 키잉, 원본 `Downloads/crowd_0/1.png`, `_keybackup/Obs_Crowd_*.png`).
- `Camera/RunnerCameraRig.PlayFinishFrame`: 카메라가 위로 붕 뜨던 것(→ '하늘로 올라가는' 느낌) 제거 — **리본 통과 지점 옆 발 기준 1.15 m 고정 '사진사' 카메라**, `PlayerController.FeetPosition` 추가. `FinishRun`이 `_hop=0`으로 두던 것도 수정(한 프레임 땅 밑으로 꺼짐).
- `Player/SkaterRig.FinishPoseLate`: 손목까지 Aim(RightMiddleProximal/LeftMiddleProximal) — 오른손 위로 곧게, 왼손은 옆구리에 손등 바깥.
- `Visual/JuiceDirector.SoftHitSequence`: 히트스톱 0.10 s + 흔들림 0.55 + 채도 −70 + FOV −9 + `PickupFloat.Impact("꽈당!")`.
- `Visual/PickupFloat.Impact`: 붉은 비네트 플래시 + 흰 번쩍 + 캔버스 전체 기우뚱 진동 + 큰 "꽈당!" 글자.
- 펫 120%: `PlayCoinCollect`에서 `PetCompanion.CoinBonus>1`이면 1.1초에 한 번 "120%"(하늘색) 플로팅.
- 리본 통과 정리: `StageManager.FinishThenClear` → `ObstacleSpawner.SetSuppressed(true)`(차도 제거), `CoinSpawner.ClearAhead`, `JellySpawner.ClearAhead`(신규). `ResetFinishPresentation`에서 suppress 해제. `ObstacleSpawner.Update` 시작에 **결승 −14 m ~ +60 m 행 금지** 가드(`StageManager.FinishPathZ`).
- `UI/StageClearUI`: 발 패널 350px, **"송전탑까지 N km" 주황 알약(22px 굵게, 탑 아이콘)** + 아래 시계·기록 줄; 배너 오른쪽 아래에 **메인페이지 얼굴 컷인**(`UI_Face_Girl.png`, 170px 원형) + 말풍선("해냈다!" 등 랜덤). `UI_Face_Boy.png`도 잘라 둠(집사/도윤용).
- 패치 스크립트: `Tools/_patch/patch23a.py`, `patch23b.py`, `patch23c.py` (이미 적용됨 — 다시 돌리면 MISSING으로 중단되니 재실행 금지).

### 코드 반영 완료(6~9) — 역시 컴파일 미확인 (커밋 23차 WIP-2)
- **⑥ 육성 페이지 메인 톤** `Raising/RaisingUI.cs`: 팔레트 교체(Wood→살구 크림 #F6D8B0, WoodDark→저녁 보라 #4A2F55, Gold→노을 코랄 #FF8A65, Navy/Ink→보라 계열; 이름은 호출부 호환용으로 그대로). 바탕은 `UI_Raising_Backdrop.png`(타이틀 키아트 블러 14px·밝기 0.92)를 깔고 크림 종이는 알파 0.42로 비쳐 보이게.
- **⑦ 집사 꼬마 '도담'** `Raising/RaisingButler.cs`(신규): 방 왼쪽 아래 190×300, `UI_Butler_Boy.png`(Kling 3D 토이, 키잉 원본 `_keybackup/UI_Butler_Boy.png`, 원본 `Downloads/butler_0/1.png`). 말풍선 400×96 + 꼬리, "집사 도담" 태그. `Refresh(save, firstVisit)`가 우선순위대로 조언을 만든다: 첫 방문 안내 → 번아웃/스트레스≥0.7 휴식 → 돈<60 알바 → 최저 스탯 교육 → 남은 하트 → [자동 배치] 안내 → 송전탑 한 줄. 탭하면 다음 조언 + 콩 뛰기. `RaisingUI.Refresh()`에서 호출, `Update()`에서 `Tick`(숨쉬기).
- **⑧ 움직이는 주인공** `RaisingUI.TickPortrait`: 2D 초상(Raise_Girl_*)을 발끝 피벗으로 숨쉬기(세로 1.6%, 1.9 Hz)·좌우 흔들림(±1.3°, 0.7 Hz)·미세 부유, 3.5~6.5초마다 눈 높이(-108~-132, ±68) 살구색 띠 0.11초 = 깜빡임, 탭하면 26px 콩 뛰기. `RefreshCharacter`의 회전/스케일은 `_charMoodScale`로 옮김. **업그레이드 경로**: Kling image-to-video로 초상 5초 루프를 뽑아 스프라이트 시트(6×4, 270×480)로 돌리면 진짜 Live2D급 — 참조 업로드는 base64 청크 방식(HANDOVER 4-13/4-14).
- **⑨ 꼬마 도움 → 피버** `Economy/FeverMode.cs`(신규): `StageManager.StartStage`에서 `Ensure()`. 스테이지 시작 9초 뒤, 이후 24초마다 6초 동안 화면 오른쪽 중간에 꼬마 얼굴 버튼(150px, `UI_Face_Butler.png`) + "도와줄까요?" 말풍선 + TAP 배지가 콩콩(마지막 1.5초 깜빡). 누르면 3초 피버: `FeverMode.MagnetBonus=14 m`를 `CoinPickup/JellyPickup` 자석 반경에 더하고 끌려오는 속도 ↑, `JuiceDirector.OnFeverStart/End`(채도 +35·FOV +7·속도선 연속·코인 버스트), `PickupFloat.Banner("FEVER!")`. 에디터/원격 키 **H** = 즉시 피버. 얼굴 아이콘 `UI_Face_Butler.png`(흉상 `UI_Butler_Bust.png`에서 원형).
- `PickupFloat`: 오버레이 캔버스 루트는 회전이 안 먹어 흔들림용 자식 `Fx` 컨테이너 추가(꽈당 연출이 그 위에 올라감).

### MCP 브릿지(사용자 요청: "권한 다시 묻지 말고 MCP로")
- `Assets/_CoastRun/Editor/CoastRemote.cs`: 에디터 안 TCP 127.0.0.1:47001 명령 서버(ping/status/refresh/play/stop/pause/menu/scene/shot/log/clearlog/warp/clear/retry/hit/lane/jump/crouch/tap/key/timescale/gameview).
- 런타임 훅: `Core/CoastRemoteKeys.cs`(디버그 키 주입 — `Input.GetKeyDown(디버그키)` → `CoastRemoteKeys.Down`으로 일괄 치환), `StageManager.DebugClear()`, `MobileSwipeInput.Inject()`.
- `Tools/Mcp/unity_mcp_server.py`: Claude 데스크톱 로컬 MCP(stdio, 의존성 없음). 도구 `unity_launch`(허브 없이 Unity.exe -projectPath로 직접 실행) / `unity_status` / `unity_compile` / `unity_cmd` / `unity_shot`(Tools/_shots/*.png) / `unity_log`.
- 설정 파일: `Tools/Mcp/claude_desktop_config.json` (= `Downloads/claude_desktop_config.json`; unity + blender + photoshop). **`%APPDATA%\Claude\claude_desktop_config.json`에 넣어야 하는데 그 폴더는 Cowork 브릿지가 보호 영역으로 막아 제가 못 씀.** 사용자가 직접 복사해야 함 — **경로를 추측하지 말고 앱 설정으로 연다: Claude 앱 → 설정 → 개발자 → 「구성 편집」버튼 → 열리는 폴더에 덮어쓰기**(9/9 확인: 재설치 후 `%APPDATA%\Claude` 폴더가 아예 없었음. 스토어판 등 설치 방식에 따라 설정 위치가 달라짐). 앱은 `Ctrl+Q`로 완전 종료 후 재실행. 승인 카드에 승인 버튼이 안 보이는 문제가 있었음(거부만 표시).
- 등록되면 `mcp__remote-devices__unity__*` 류로 도구가 붙는다(`get_device_info.localMcpServers`로 상태 확인). 그 뒤로는 computer_request_access를 쓰지 말 것.

### 이 세션이 끝난 이유·새 세션(다른 계정 채팅방)에서 먼저 할 일 — 2026-09-09 10:30
- 이 Claude 데스크톱 앱(1.49585.0)에서 **컴퓨터 제어 승인 카드에 "승인" 버튼이 안 그려짐**(거부만 표시) → 모든 `computer_request_access`가 거부로 떨어짐. 앱 하나만 요청해도 같음. 설정에서 "컴퓨터 제어" 기능은 켜져 있음(`bypassPermissionsGateByAccount`는 전부 false). 사용자가 이 채팅방을 지우고 **다른 계정 채팅방에서 다시 시작**하기로 함.
- 새 세션 첫 순서: ① `get_device_info`로 `localMcpServers`에 unity가 있는지 확인. 있으면 `unity_launch → unity_compile → unity_cmd/unity_shot`로 진행(승인 불필요). ② 없으면 컴퓨터 제어 승인을 **한 번** 요청해 보고(다른 계정이면 카드가 정상일 수 있음), 통과되면 즉시 유니티 열고 컴파일. ③ 그것도 막히면 사용자에게 `Downloads\claude_desktop_config.json`을 **앱 설정 → 개발자 → 「구성 편집」으로 열리는 폴더**에 복사 + 앱 Ctrl+Q 재시작을 부탁(이 파일에 unity·blender·photoshop 다 들어 있음). `%APPDATA%\Claude` 경로를 직접 안내하지 말 것 — 없는 경우가 있음.
- 코드 상태: 23차 ①~⑨ 전부 코드 반영, 커밋 `9f35db8`·`67e3828`·`7b0784f`. **유니티 컴파일 미확인** — 새 파일: `Editor/CoastRemote.cs`, `Core/CoastRemoteKeys.cs`, `Economy/FeverMode.cs`, `Raising/RaisingButler.cs`, `World/FinishRibbon.cs`(교체). 컴파일 에러가 나면 대부분 이 다섯 파일 + `PickupFloat.cs`(Impact/Banner/_fx) + `StageClearUI.cs`(얼굴 컷인·JourneyPill) 에서 날 것. 괄호 균형·API 시그니처는 검토했음.
- 사용자 최종 요청: **APK 업데이트해서 폰 테스트**(`Coast Run/Build/Android APK (IL2CPP, ARM64+ARMv7)` = Ctrl+Shift+Alt+K, 결과 `Builds/CoastRun.apk`, 현재 있는 건 9/6 구버전) + 그 폴더 열어 주기. 컴파일 → 플레이 확인(아래 순서) → 빌드 순.
- git: `.git/*.lock`을 VM에서 지울 수 없어 `mv`로 `_stale_*.lock`으로 비켜 둔 뒤 커밋했음. 다음 커밋도 `HEAD.lock`/`index.lock`이 있으면 `mv`로 치우고 진행.

### 2026-09-09 13:30 추가 — 승인 카드 버그 재확인·설정 폴더 위치
- 앱을 **재설치해도** 1.49585.0 그대로라 승인 카드(거부만 표시) 증상 동일. 계정을 바꿔도 동일 → 앱 버전 버그로 결론. 새 버전 나올 때까지 computer_request_access는 시도만 1회.
- 재설치 후 `%APPDATA%\Claude` 폴더가 **존재하지 않았음**(Win+R로 확인). 설정 파일 위치는 **Claude 앱 → 설정 → 개발자 → 「구성 편집」**으로 열리는 폴더가 정답. 사용자가 그 폴더에 `Downloads\claude_desktop_config.json`을 덮어썼음(13:30). 앱 Ctrl+Q 재시작 후 `get_device_info.localMcpServers`에 unity·blender·photoshop이 뜨는지 확인할 것.
- 텔레그램: 워처(`Tools/Telegram/watch.py`)가 꺼져 있어 queue 102~104번 미전송. 보고는 큐에 append만 하고, 사용자에게 `Coast Run/Telegram/Start watcher`(Ctrl+Shift+Alt+T) 실행을 부탁. VM은 api.telegram.org 403.
- Kling MCP 추가(9/9 13:40): `Tools/Mcp/kling_mcp_server.py`(kling_ping / kling_image / kling_image2video / kling_status / kling_wait / kling_download, .env 키 사용). `Tools/Mcp/claude_desktop_config.json`에 `kling` 항목 추가됨 — 앱 설정 → 개발자 → 구성 편집 폴더에 이 파일을 다시 덮어써야 반영. 등록되면 `mcp__remote-devices__kling__*`로 붙고, 웹(claude.ai)에서도 이 PC에 링크된 세션이면 같이 쓸 수 있다.
- 이 세션 작업: 전체 코드베이스 점검 리포트(`Docs/CODE_AUDIT_2026-09-09.md`) 작성 중.

### 2026-09-09 14:40 — 24차(MCP만으로 진행, 승인 카드 없이)
- MCP 4개(unity/blender/photoshop/kling) 등록 완료. 설정 파일 위치는 **앱 설정→개발자→구성 편집**. `kling_ping` OK(bearer). `unity_compile` 에러 0 → 23차 코드 컴파일 확인 끝.
- 24차 수정(전부 컴파일 확인):
  ① `PromenadeSegmentBuilder.cs:514` 만국기 4.3→5.2 m(카메라가 줄 밑 지날 때 깃발이 파란 판처럼 크게 뜨던 것)
  ② `RunnerCameraRig.PlayFinishFrame` 앵커 −2.2→−2.8, 시선 1.0→1.3 m(정산 헤더에 얼굴 가림)
  ③ `PetCompanion.SetHidden` + `StageManager.FinishThenClear/ResetFinishPresentation`에서 골인 중 펫 숨김
  ④ `FinishRibbon.Update` 끊긴 테이프가 1.2 s 뒤 바닥으로 떨어져 눕는다(고정 카메라 앞을 붉게 가리던 것) — **플레이 확인 아직**
  ⑤ `Editor/CoastRemote.cs` 브릿지가 플레이 진입 리로드마다 죽던 문제: beforeAssemblyReload에서 Stop + 대기 중 Serve 연결 강제 종료 + ReuseAddress + 재시도 60회. **컴파일·검증 아직** — 유니티가 UPM IPC 실패로 멈춰 있음
- 검증 캡처: `Tools/_shots/audit_*.png`, `fix_run_*.png`, `fin_*.png`. 리본 게이트·관중은 fin_0에서 확인.
- 브릿지가 죽으면: 사용자에게 Unity 플레이 정지(Ctrl+P)+창 클릭 부탁 → 재컴파일되며 살아난다. Unity가 아예 안 뜨면 Editor.log의 "Could not connect to IPC stream Upm-*" 확인(이전 프로세스 잔존).
- **역할 변경(9/9 15:00, 사용자 지시): git 커밋과 APK 빌드는 앞으로 사용자가 Cursor에서 직접 한다. Claude는 파일 수정·검증·HANDOVER 갱신·텔레그램 큐까지만.** 커밋 대상 목록은 HANDOVER에 적어 둔다.
- 다음: ④⑤ 플레이 확인 → (사용자) 커밋 "24차" → (사용자) APK 빌드(`Coast Run/Build/Android APK (IL2CPP, ARM64+ARMv7)` = `unity_cmd "menu ..."`) → 폰 테스트.

### 2026-09-09 16:00 — 24차 후반: 점검 리포트 치명·높음 항목 수정 (전부 컴파일·플레이 확인, 커밋은 사용자)
- ④ 리본: 테이프 낙하 + **현수막(Banner/Trim/FINISH 라벨)을 통과 0.9 s 뒤 위로 올리며 숨김**(`FinishRibbon.cs`, unscaled time — 정산 UI가 timeScale 0이라 얼어 있던 것). 정산 화면 v4_fin.png 깨끗함.
- ⑤ 브릿지(`CoastRemote.cs`): 플레이 진입 리로드에서 살아남는 것 확인(2회). 원인은 리로드 직전 대기 중이던 Serve 연결이 47001 점유 → beforeAssemblyReload Stop + 연결 강제 종료 + ReuseAddress + 재시도 60회.
- 점검 2-1 `CoastMaterials`: 추적 리스트를 **에디터 전용·약참조**로(빌드에선 추적 없음) + `StageManager.BeginStage`에서 `Resources.UnloadUnusedAssets()` → 타일/픽업 머티리얼이 스테이지마다 해제된다.
- 점검 2-3 `ObstacleSpawner.ResetForStage`: `_root` 자식 전부 파괴 + `_rowsUntilPad/_padsSinceLine` 리셋(재도전 두 겹 행). 플레이 확인(v2_retry_*.png).
- 점검 3-2 `CoinSpawner`: `ResetForStage(stage, z)` 신설(코인 회수·시드 `System.Random(700+stage*6131)`), `UnityEngine.Random` 7곳 제거, `GameSession.HandleStageStart`에서 호출.
- 점검 2-4 `SaveManager`: 임시파일→`.bak`→교체 쓰기(`WriteAtomic`), 손상 시 `save_0.corrupt.json` 보존 후 `.bak` 로드, `Migrate()`(챕터 수 리사이즈·배열 보정·version 도장), `LastLoadRecovered` 플래그(타이틀 안내는 아직 미연결), profile도 `.bak`. `Delete()`가 `.bak`도 지움.
- 점검 2-5 `CoastUiCanvas` RaycastWatchdog: `ScrollRect` 하위는 건드리지 않음(도감 스크롤 죽던 것).
- 점검 3-1 `CoinWallet`: 코인마다 `PlayerPrefs.Save()` → dirty 플래그 + 5초 간격 LateUpdate flush + Pause/Quit/Disable 때 저장. `TrySpend`는 즉시 저장.
- 점검 5 `Editor/VideoFetch.cs`: 도메인 리로드 자동 실행 제거(메뉴 수동만).
- **미해결(다음)**: 점검 3-5 코인 이중 원장(CoinWallet PlayerPrefs vs SaveData.stats.money, 회차마다 500 재흡수), 3-4 골인 시 `player.enabled=false` 순서·`MobileSwipeInput.Tick` 입력 차단, 3-6 `CoastSea` 매 프레임 메시 할당, 3-11 IAP 복원, 3-12 `ChapterVN` 중복 재생 소프트락, 3-13 타임라인 엔딩 재생 통계 중복, 문서 정리(루트 README 재작성).
- 커밋 대상(24차 전체): Scripts/World/{PromenadeSegmentBuilder,FinishRibbon}.cs, Camera/RunnerCameraRig.cs, Player/PetCompanion.cs, Core/{StageManager,GameSession}.cs, Rendering/CoastMaterials.cs, Economy/{ObstacleSpawner,CoinSpawner,CoinWallet}.cs, Meta/SaveManager.cs, UI/CoastUiCanvas.cs, Editor/{CoastRemote,VideoFetch}.cs, Tools/Mcp/*, Docs/*, Tools/Telegram/queue.txt

### 2026-09-09 16:50 — 25차(사용자 피드백 5건, 컴파일·플레이 확인, 커밋은 사용자)
- ① 왼팔/왼손 파란색: 원인은 텍스처가 아니라(양팔 UV 모두 피부색 확인) **리그 ToonLit 의 _ShadowColor 가 ShadowCool(0.24,0.51,0.66)로 되돌아간 것** — 에디터 팔레트 OnValidate→`RefreshTracked` 가 SkaterRig 의 따뜻한 그림자(0.86,0.80,0.80)를 덮어썼다. `CoastMaterials.SetShadow(mat,color,threshold)` 신설(CustomShadow 플래그로 갱신에서 제외), SkaterRig·JejuKit 이 이걸 쓴다. 덤프(`Coast Run/Debug/Dump Player Materials` → Tools/_shots/player_mats.txt)로 전후 확인. 잉크 셸 폭도 0.017→0.010 + 카메라 거리 비례(`CoastInkOutline.shader`)로 손가락이 남색으로 덮이던 것 완화.
- ② 점프 중 왼발 왼쪽 치우침: `SkaterRig.LateUpdate` 에서 State==Air 일 때 왼다리(허벅지·정강이)를 오른다리의 좌우 거울상으로 0.7 블렌드. 22차-4 발끝 보정도 공중에서 동작.
- ③ 키보드 화살표: `MobileSwipeInput.PollKeyboard` 를 키 + Horizontal/Vertical 축 엣지검출 + 키패드 + 조이스틱 버튼까지 받도록 확장(블루투스 키보드가 D-Pad/축으로 오는 기기 대응). **에디터에선 Game 뷰가 포커스를 가져야 키가 들어간다.** activeInputHandler=0(구 Input Manager)라 새 Input System 전용 코드는 안 넣음.
- ④ 피격 피해: `RunTuning` HitDamage 34→45, 계수 0.34→0.45(체력 완화 0.22→0.15). 일반 장애물 2방 반, 체력 만렙 3방.
- ⑤ 정산 화면: `StageManager.FinishThenClear` 도착 모션(고정 카메라·돌아서기 포즈·펫 숨김) 끔(`FinishMotion=false`) — 리본만 끊고 0.9 s 감속 뒤 정산. `StageClearUI` 에서 합계/보유/콤보/"송전탑까지 km"/시계 제거(`ShowTotals=false`, `ShowJourney=false`), 발판 350→170. 남은 것: STAGE CLEAR 제목·스테이지명·아이템 칩·얼굴 초상+말풍선·버튼 2개. (사용자 말의 "메인화면 이미지"를 얼굴 초상으로 해석 — 타이틀 키아트를 배경으로 깔길 원하면 StageClearUI.Build 에 Image 하나 추가하면 됨)
- 커밋 대상 추가: Shaders/CoastInkOutline.shader, Rendering/{CelOutlineHint,CoastMaterials}.cs, Player/SkaterRig.cs, World/JejuKit.cs, Meta/RunTuning.cs, Input/MobileSwipeInput.cs, UI/StageClearUI.cs, Core/StageManager.cs, Editor/PlayerMaterialDump.cs(진단 메뉴, 유지해도 됨)

### 2026-09-09 17:40 — 26차: 스토리/러닝 모드 분리 · 체력 게이트 · 이벤트 컷씬 · 옛 컷씬 제거 (컴파일·타이틀/K-POP 진입 확인, 커밋은 사용자)
- 설계 문서: **`Docs/STORY_MODE_v6.md`** (모드·게이트 수식·이벤트 컷씬·대본 파이프라인).
- 새 파일: `Meta/StoryGate.cs`. 수정: `GameManager`(GateFail, PeekSave, AdvanceWeek 52주 초과 허용, AfterChapterContinue 주차 이어받기, StartStoryRun에서 오프닝 제거), `SaveData.ChapterRecord.gateFails`, `StoryCond`(게이트/필요체력), `RaisingUI.ExecuteWeek`(forced → 오프닝 이벤트 → 게이트 → 통과/연장; 스토리 카드·버튼도 게이트), `ArcadeRun.StartKpop/KpopMode`, `CoastBgmLibrary.Kpop`, `CoastAudioManager`(K-POP 풀트랙), `MainMenuController`(하단 K-POP 바, 버튼 행 92→150).
- 대본: `ChapterScript.Data.cs/.En.cs/Loc.cs` 를 플레이스홀더로 재생성(43씬 83줄). 옛 58씬은 `Docs/_archive_cutscenes_old/`. 편집 원본은 `Tools/Story/script/*.txt`. **사용자의 새 컷씬 대본은 아직 안 받음** — 받으면 txt로 옮기고 import.
- 확인: 타이틀 캡처(v26_title.png) K-POP 바 표시, 탭 → 02_Run 무한 러닝 진입(v26_kpop.png, 거리 HUD). 육성 게이트 흐름은 코드 리뷰만(플레이로 챕터 경계까지 가는 데 시간이 걸려 미확인) — 다음 세션에서 `RaisingDevMenu`로 주차 점프 후 확인 권장.
- 미결: BGM_KPOP_* 파일 없음(폴백으로 챕터 스템 재생). 타이틀 'v1.0' 라벨이 K-POP 바와 살짝 겹침.
- 커밋 대상 추가: 위 수정 파일 + Meta/StoryGate.cs + Story/ChapterScript.{Data,En}.cs + Core/Loc.cs + Tools/Story/script/*(43) + Docs/_archive_cutscenes_old/*(58) + Docs/STORY_MODE_v6.md

### 2026-09-09 18:20 — 26차 후반: 게이트 플레이 검증 완료 + 잔손질 (컴파일 0, 커밋은 사용자)
- **게이트 흐름 실기 확인** (`Coast Run/Debug/v2 세이브: 게이트 테스트` = Ctrl+Shift+Alt+G → F5 육성 → 스케줄 자동 배치 → 실행): 6주차 마지막 주 → "챕터 이벤트" 로그 → CH2 오프닝 컷씬(BG TowerDay 플레이스홀더) → `[게이트<0]` 줄만 나옴 → 육성 복귀, **7주차**로 연장(체력 25/36). 캡처 Tools/_shots/gate_9~11.png.
- `GameManager.GateFail`: 연장된 주로 `Save.week` 를 옮기고 페이즈/스케줄 초기화(같은 주차 반복 안 함).
- 플레이스홀더 대본 43씬에 BG 줄 추가(Open=TowerDay, Close=TowerSunset, PRO=HanulRoom, END_A=TowerSunset, END_B=TowerNight) — NARR만 있으면 화면이 검게 남던 것.
- `ChapterVN.Pressed`: 원격 키(Space/Return 진행, S 스킵)도 받음. `RaisingUI.DevKeys`: A/W/S/숫자 키도 원격 키로.
- `StageClearUI` 말풍선을 초상 아래로(폭 230, 16~24pt 자동 축소). 타이틀 `v1.0` 라벨을 우하단 구석으로(K-POP 바와 겹침 해소).
- 추가 커밋 대상: Editor/RaisingDevMenu.cs, Story/ChapterVN.cs, UI/StageClearUI.cs, UI/MainMenuController.cs, Meta/GameManager.cs, Story/ChapterScript.{Data,En}.cs, Tools/Story/script/*.txt

### 2026-09-09 19:00 — 27차 1단계: 톰 히어로식 쫀득 모션(스쿼시·스트레치·레인 오버슛·2차 스프링) (컴파일 0, 플레이 확인, 커밋은 사용자)
- 사용자 검토 요청("톰히어로 같은 쫀득한 모션") → 권장안대로 1단계 반영. 수치는 전부 `Core/RunConfig.cs`의 "Juice (27차)" 절에 노출.
  - `laneOvershoot 0.6`(레인 이동 easeOutBack — 목표를 살짝 지나쳤다가 튕겨 돌아옴), `jumpStretch 1.16`(점프 순간 세로 늘림), `landSquash 0.78`(착지 납작), `crouchSquash 0.86`(슬라이드 진입 납작), `stepSquash 1.2`(달릴 때 발 디딤 미세 바운스 배율), `squashStiffness 260 / squashDamping 14`(스프링), `secondaryAmount 1`(가방·머리 2차 움직임 배율).
- `Player/PlayerController.cs`: 레인 보간을 easeOutBack로(overshoot 0이면 기존 easeOutCubic). `OnCrouched` 이벤트 추가(웅크림 진입 1회).
- `Player/SkaterRig.cs`: `OnJumped/OnLanded/OnCrouched/OnLaneChanged` 구독 → 단일 스프링 `_squash`(0.6~1.35 클램프)로 루트 localScale(y=s, xz=1/√s, 부피 보존). 레인 이동 시 몸통 `_laneKick` 롤 + 가방 롤, 착지 시 가방 피치 스프링, 머리는 절반만 역보정(얼굴 찌그러짐 방지). 애니메이터가 뼈를 쓴 뒤 `LateUpdate` 첫 줄에서 얹음(`ApplyJuiceLate`), 활공/골인 블렌드 중엔 건너뜀. 가방은 `UpperChest→Chest→Spine` 순서로 찾은 뼈의 "Backpack" 자식(`HumanBodyBones.Spine2`는 없음 — 컴파일 에러 1회 수정).
- 플레이: K-POP 러닝모드에서 jump/lane/crouch 원격 명령으로 확인, 예외·경고 0. 캡처 Tools/_shots/j27_lane.png(레인 이동 중 몸통 기울기 + 가방 롤 보임). 스크린샷 지연 때문에 점프/착지 순간은 정지화면으로 잡기 어려움 — 에디터에서 직접 보고 RunConfig 수치 조절 권장(너무 과하면 landSquash 0.85, jumpStretch 1.10).
- 남은 단계(사용자 승인 시): 2단계 코인 픽업 팝(pickup squash), 3단계 피격 스트레치+텀블, 4단계 UI 이징(SimpleTween에 easeOutElastic/easeOutBack 추가 → 팝업·버튼·정산 숫자).
- 커밋 대상: Core/RunConfig.cs, Player/PlayerController.cs, Player/SkaterRig.cs, Docs/HANDOVER_2026-09-07.md, Tools/Telegram/queue.txt
- **주의(도구)**: `device_commit_files`는 같은 stagedPath를 두 번 커밋하면 첫 번째 내용이 캐시되어 다시 써진다 → 수정본은 새 폴더(edit6/…)에 복사해 커밋할 것.

### 2026-09-09 19:40 — 28차: 육성 화면 글자 겹침·집사 숨김 · [스토리]→[방 꾸미기] (컴파일 0, 플레이 확인, 커밋은 사용자)
- **① HUD 알약 겹침**: `RaisingUI.BuildHud` 네 알약 폭을 212/146/108(+컨디션 116)로 줄이고 날짜·G·챕터 글자는 13~20pt 자동 축소 → "CH 2"와 "♥부상"이 더 이상 겹치지 않음(r28_e.png).
- **① 집사 숨김**: `RaisingUI.ShowButler = false` — `RaisingButler` 코드는 그대로 두고 `SetVisible(false)`. 다시 켜려면 상수만 true.
- **② 방 꾸미기**(신규 `Meta/RoomDeco.cs`, `Raising/RaisingUI.Room.cs`):
  - 장식 12종: 상점 구매 6(화분 120G·벽시계 150·포스터 90·러그 200·조개 램프 180·책 더미 80) + 러닝 드롭 6(미니 돌하르방·감귤 바구니·해녀 인형·등대 모형·미니 서핑보드·별 조명 줄, price 0).
  - 슬롯 6개(왼쪽 벽/오른쪽 벽/창턱 좌·우/바닥 좌·우). 각 장식은 슬롯이 정해져 있고 [놓기]하면 그 자리 장식과 교체, [치우기]로 회수(보유 유지). 구매하면 바로 놓인다.
  - 저장은 `MetaProfile.decoOwnedMask / decoNewMask / roomSlots[6]`(회차를 넘어 유지, profile.json). `EnsureArrays`에서 `RoomDeco.Ensure`.
  - 드롭: 스토리 런 정산(`GameManager` MissionTable.Settle 직후) 45%, K-POP/무한 런 `ArcadeRun.Settle` 800 m 이상 25%. 새 장식이 있으면 육성 화면에서 토스트 + [방 꾸미기 ●] 표시, 팝업을 닫으면 NEW 해제.
  - 그림: `Resources/CoastRun/UI_Deco_<id>.png`(마젠타 키 지원)가 있으면 사용, 없으면 색 알약 + 한 글자 플레이스홀더(지금 상태). id: plant, clock, poster, rug, lamp, books, harubang, tangerine, haenyeo, lighthouse, surf, stars.
  - 슬롯 위치는 `RoomDeco.SlotLayout`(방 프레임 정규화 좌표) — 하트 명패(좌상단)·오른쪽 독 버튼을 피해 배치. 그림 넣은 뒤 조정.
  - 기존 [★ 스토리] 버튼의 기능(챕터 선택/타임라인)은 날짜 알약 탭·오른쪽 독 [챕터] 버튼으로 그대로 감.
- 플레이 확인: 구매 → 방에 표시 → 플레이 재시작 후에도 유지(r28_h~j.png). 예외 0.
- 커밋 대상: Meta/RoomDeco.cs(+meta), Raising/RaisingUI.Room.cs(+meta), Raising/RaisingUI.cs, Meta/SaveData.cs, Meta/GameManager.cs, Meta/ArcadeRun.cs, Docs/HANDOVER, Tools/Telegram/queue.txt
- 주의: 게이트 테스트 세이브(Ctrl+Shift+Alt+G)로 확인하느라 profile.json에 장식 4개(화분·벽시계·포스터·책)가 보유 상태로 남아 있음. 초기화하려면 profile.json의 decoOwnedMask/roomSlots를 지우면 됨.

### 2026-09-09 20:30 — 29차: 타이틀 K-POP 러닝모드 바를 시안(네온 글라스)대로 (컴파일 0, 플레이 확인, 커밋은 사용자)
- 사용자 시안: 다크 퍼플→핑크 유리 바, 네온 테두리, 왼쪽 헤드폰, 가운데 "K-POP 러닝모드" 그라데이션 글자, 이퀄라이저, 오른쪽 음표, 반짝이.
- 만든 방법: 클링(kling_image)으로 **헤드폰 아이콘**만 뽑고(`Tools/KlingGen/out/kpop_btn/headphones.png`; 음표는 클링 결과가 못 써서 폰트 글리프 ♪로), 바 본체·글로우·테두리·글자(Noto Sans CJK Black 그라데이션)·이퀄라이저·반짝이는 **Python(Pillow) 합성** → `Assets/Resources/CoastRun/UI_KpopBar.png`(1320×220, 알파). 합성 스크립트 `Tools/KlingGen/make_kpop_bar.py`(컨테이너에서 실행한 것 — 폰트 경로만 맞추면 PC에서도 돔).
  - 클링에 "버튼 통째로" 시키면 폰 목업/글자 깨짐이 나와서 못 씀. 아이콘 단품 + 코드 합성이 정답.
- `MainMenuController`: `UI_KpopBar` 그림이 있으면 660×110 Image 버튼(그림에 글로우 포함, 본체 620×75)으로, 없으면 기존 GlassButton 폴백. `UI/KpopBarPulse.cs`(신규): 1.0~1.03 숨쉬기 펄스 + 누를 때 스쿼시.
- 확인: k29_c.png(타이틀). 방 꾸미기 배치가 타이틀→이어하기 경로에서도 유지되는 것 확인(k29_g.png). 단, 'Play From Boot' 메뉴로 들어간 직후 한 번 배치가 0/6으로 보인 적 있음(보유는 유지) — 재현 안 됨, 다음에 부트 경로로 한 번 더 확인 필요.
- 커밋 대상: Assets/Resources/CoastRun/UI_KpopBar.png(+meta), UI/MainMenuController.cs, UI/KpopBarPulse.cs(+meta), Tools/KlingGen/make_kpop_bar.py, Tools/KlingGen/out/kpop_btn/*, Docs/HANDOVER, Tools/Telegram/queue.txt

### 2026-09-09 21:30 — 30차: 독 버튼 입체감 · 집 화면(방 꾸미기 v2: 자유 배치·러닝머신·베란다 화분·주인공 걷기·미니게임 3종) (컴파일 0, 플레이 확인, 커밋은 사용자)
- **독 버튼 입체감**(`RaisingUI.DockButton`): 오프셋 그림자(Drop) + 립 8px + 안쪽 어두운 테(Inner) + 위쪽 하이라이트 점(Shine) + 누르면 0.92로 눌리는 `PressSquash`(스프링).
- **집 화면 `Raising/HomeUI.cs`**(전체 화면 캔버스 sortingOrder 130, [방 꾸미기] 버튼 → `HomeUI.Open`): 헤더(G·나가기) / 탭 3개 / 장면 / 트레이.
  - [방] 방 그림 위에 가구·장식을 **자유 배치**(트레이 카드 [놓기] → 가운데 등장 → 끌어서 이동, 바닥 y 0.02~0.42 / 벽 0.45~0.85 클램프). 주인공(육성 초상 스프라이트)이 방 안을 **돌아다니고**(3~7초마다 랜덤 이동, 멀수록 작게, 걸을 때 콩콩) **바닥을 탭하면 걸어간다**. 앞뒤 정렬은 y 기준 형제 순서. **러닝머신** 탭 → 걸어가서 운동(체력 +2, 스트레스 +1, 페이즈당 1회 `treadmillStamp`).
  - [베란다] 하늘·바다·난간·선반 위 화분 4개. 빈 화분 탭 → 트레이에서 씨앗 선택(유채 20G/코스모스 40/수국 70/해바라기 90) → **페이즈마다 물 주기 1회**(waterStamp = week*4+phase) → 물 N번(3/5/7/9)이면 만개 → **팔기**(55/120/220/320G). 식물은 단계(씨앗·새싹·줄기·봉오리·꽃)별 프로시저럴 그림.
  - [놀이] `Raising/HomeMiniGames.cs`: **윷놀이**(윷 4개, 20칸 사각 트랙, 도담 AI, 잡기, 윷·모 한 번 더, 승리 60G) / **구슬치기**(흰 구슬을 새총처럼 끌어 쏘기, 원형 충돌·마찰, 마당 밖으로 밀어낸 구슬 ×8G, 3발) / **공기놀이**(왕복하는 손이 노란 구간일 때 [잡기], 5라운드 점점 빠르고 좁게, 성공 ×10G, 5/5 보너스 20G). 보상은 **주 3회**(`miniGameWeek/miniGamePlays`), 그 뒤는 연습.
- **데이터 `Meta/HomeData.cs`**: 가구 6종(침대 250·책상 220·소파 300·러닝머신 400·책장 180·창가 커튼 140) + RoomDeco 장식 12종 = `HomeData.All` 18종. 보유 비트는 `decoOwnedMask`(All 순서, 앞 12개는 RoomDeco와 동일). 배치는 `MetaProfile.homeItems[]{id,x,y}`(28차 `roomSlots`는 `HomeData.Ensure`에서 좌표로 한 번 이관). 화분 `SaveData.pots[4]{seed,growth,waterStamp}`, `treadmillStamp`, `miniGameWeek/Plays`, `flowersSold`.
- 육성 화면의 방(`RaisingUI.Room.RefreshRoomDeco`)도 같은 좌표로 그려서 집에서 옮긴 대로 보인다. 장식 그림은 여전히 플레이스홀더 — `Resources/CoastRun/UI_Deco_<id>.png` 넣으면 자동(id: 위 12 + bed, desk, sofa, treadmill, shelf, window).
- 참고한 문법: 플레이투게더·포켓캠프·헬로키티 아일랜드의 "트레이에서 꺼내 놓고 끌어 옮기기 + 캐릭터가 방 안을 돌아다님", 마이오아시스류의 "씨앗→물주기→판매".
- 플레이 확인: h30_c(방·끌기·걷기), h30_e~f(씨앗 심기·물주기), h30_g~j(놀이 탭·윷놀이 진행·공기놀이). 구슬치기의 드래그는 원격 탭으로 못 눌러 봤음 — **에디터에서 마우스로 한 번 확인 필요**. 예외 0.
- 커밋 대상: Meta/HomeData.cs(+meta), Raising/HomeUI.cs(+meta), Raising/HomeMiniGames.cs(+meta), Meta/SaveData.cs, Raising/RaisingUI.cs, Raising/RaisingUI.Room.cs, Docs/HANDOVER, Tools/Telegram/queue.txt
- 다음 후보: 가구 18종 그림(클링 아이콘 단품 + 마젠타 키), 러닝머신 애니(주인공 달리는 포즈 스프라이트 GirlSkater_Push 활용), 미니게임 효과음, 방 배경을 계절 그림으로.

### 2026-09-09 22:40 — 31차: K-POP 바 시안 재현(3회 반복) · Dreamy Room 오마주 · 활공 발끝 · 집사 튜토리얼 (컴파일 0, 플레이 확인, 커밋은 사용자)
- **K-POP 러닝모드 바 v2** — 사용자가 "시안과 다르다"고 해서 시안을 3배 확대해 다시 분석: 파스텔 라벤더→핑크 **반투명** 유리 바, **라인아트 네온 헤드폰**(시안색), "K-POP"은 시안→라벤더 그라데이션·"러닝모드"는 흰→라벤더, 가운데가 높은 **파형 이퀄라이저**, 흩어진 음표, 흰 4각 별. `Tools/KlingGen/make_kpop_bar.py`(v2, 순수 Pillow — 클링 없이 재생성 가능) → `UI_KpopBar.png` 1320×260. 게임 캡처(k31_a/b/c)와 시안을 나란히 놓고 3번 수정(헤드폰 크기·바 높이·투명도). `MainMenuController`: 바 660×130(y 82), 메뉴 줄 rowY 172.
- **방 꾸미기 = Dreamy Room 오마주**(`HomeData.Spot`, `HomeUI`): 장식 18종마다 '제자리' 좌표. 보유했지만 안 놓은 장식은 제자리에 **반투명 실루엣(맥동)**, 트레이 [놓기]하면 제자리로 **팡(스케일 바운스 + 반짝이 6알)**, 끌다가 제자리 근처(0.11)에 놓으면 **스냅** + "딱 맞는 자리!". 방 위쪽에 **"방 완성도 N/18" 진행 바**, 전부 놓으면 1회 **+300G**(`MetaProfile.homeCompleteRewarded`). 자유 배치(끌기)는 그대로 살려 둠.
- **빨래줄 활공 발끝**(`SkaterRig` 활공 포즈): 왼발이 바깥으로 꺾여 보이던 것 → 양발 `LeftToes/RightToes`를 `glideToeForward`(0.55: 앞 55%·아래 45%) 방향으로 Aim, 좌우 성분 0. 인스펙터 값. **실기 확인은 못 함** — 빨래줄 구간을 원격으로 못 잡음. 에디터에서 K-POP 러닝 → 빨래줄에서 확인 필요(과하면 0.35).
- **집사 튜토리얼**(`Raising/RaisingTutorial.cs`): 육성 화면 첫 진입 시(PlayerPrefs `CoastRun_RaisingTut`), 돌발 이벤트 팝업이 닫힌 뒤 시작. 어두운 막 4장으로 **대상만 밝게** + 노란 액자(맥동) + 집사 흉상(UI_Butler_Bust) + 말풍선 + [다음]/[건너뛰기]. 18단계: 인사 → 주차/G/챕터/컨디션 알약 → 하트 → 달리기/챕터/컬렉션/상점/리듬/홈 독 버튼 → 스탯 탭 → 스케줄 칸 3개(Chip0~2) → [스케줄]/[실행]/[방 꾸미기] → 마무리. 대상이 아래쪽이면 집사가 위로 올라감. **에디터 F1 = 다시 보기**(Return/Space = 다음). 튜토리얼 중엔 DevKeys 무시·EventSystem 선택 해제(엔터가 아래 버튼을 누르던 문제). 캡처 t31_g/h/i.
  - 주의: 투명 입력 막은 알파 0.03 — `CoastRaycastWatchdog`가 알파≈0 전면 이미지를 자동 해제하기 때문.
- 커밋 대상: Assets/Resources/CoastRun/UI_KpopBar.png, Tools/KlingGen/make_kpop_bar.py, UI/MainMenuController.cs, Player/SkaterRig.cs, Raising/RaisingTutorial.cs(+meta), Raising/RaisingUI.cs, Raising/HomeUI.cs, Meta/HomeData.cs, Meta/SaveData.cs, Docs/HANDOVER, Tools/Telegram/queue.txt

### 2026-09-09 23:10 — 32차: 러닝 HUD 숫자 넘침 (컴파일 0, 플레이 확인, 커밋은 사용자)
- 사용자 캡처: 코인 "12409"가 알약 밖으로, "거리 863 m" 바가 코인 알약과 겹침.
- `RunHudChrome`: 코인 알약 176→196px, 코인·점수 글자 **자동 축소**(Wrap+Truncate+BestFit — 기존 Overflow 설정에선 BestFit이 동작하지 않았음).
- `UI_FinalDestinationController`: 여정 바 오른쪽 끝 0.72→0.685(코인 알약과 분리), "거리" 라벨도 BestFit. 그리고 **K-POP/무한/오늘의 런에서는 CH5 HUD 벗기기(스테이지 17~20에서 점수/콤보/코인 알약 페이드) 안 함** — 무한 런에서 스테이지 인덱스가 19 이상이면 코인 알약이 사라지던 원인.
- 확인: hud32_c.png — 12655가 칸 안, 바와 코인 분리.
- 커밋 대상: UI/RunHudChrome.cs, UI/UI_FinalDestinationController.cs, Docs/HANDOVER, Tools/Telegram/queue.txt

### 2026-09-09 23:40 — 33차: K-POP 바 미세 조정 · 활공 하늘 코인 · 장애물 붉은 테두리 · 코인/말랑이 20% 축소 (컴파일 0, 커밋은 사용자)
- **K-POP 바**(`make_kpop_bar.py` v2.1): 헤드폰을 시안처럼 크게(K 1.28, 밴드가 이어컵에 닿게 182°~358° 아크, 둥근 네모 컵), 파형 이퀄라이저 25개 얇게, 바 알파 185, 바깥 음표 추가. 시안과 나란히 비교 compare4. 이 이상은 그림(폰트·질감) 차이라 코드로는 한계.
- **활공 하늘 코인**(`CoinSpawner.SpawnGlideLine`, `ClothesLine`): 잡은 지점 +3 m부터(전 6 m) 1.4 m 간격(전 1.8), 앞 절반은 잡은 레인 그대로, 뒤 절반은 옆 레인 물결. 높이 = 줄 +0.35 m ± 0.25 파도. `PickupReach` 위쪽 허용 1.25→1.6 m. **실기 미확인**(빨래줄 타이밍을 원격으로 못 맞춤) — 에디터에서 점프→빨래줄 잡기로 확인.
- **장애물 붉은 굵은 테두리**: `PaintedProp.Attach(outlineColor, outlineMul)` 인자 추가 → `ObstacleCatalog`/콘 `ObstacleHazard`에 `ObstacleOutline.Red`(0.88,0.12,0.14) × 3.0. 메시 헐 `ObstacleOutline`도 붉은색·1.10배. 코인·말랑이·빨래는 기존 남색 유지. (비둘기 `DuckHazard`는 자체 메시라 테두리 없음.)
- **코인 20%**: 그림 코인 0.9→0.72 m, 프로시저럴 0.5→0.4. **말랑이 20%**: `JellyPickup.BuildJelly` size×0.8(그림·캡슐 둘 다).
- 확인: k33_run.png(붉은 테두리 슬라임, 작아진 코인/젤리, HUD). 
- 커밋 대상: Rendering/PaintedProp.cs, Visual/ObstacleOutline.cs, Content/ObstacleCatalog.cs, Economy/ObstacleHazard.cs, Economy/CoinPickup.cs, Economy/CoinSpawner.cs, Economy/ClothesLine.cs, Economy/PickupReach.cs, Economy/JellyPickup.cs, Assets/Resources/CoastRun/UI_KpopBar.png, Tools/KlingGen/make_kpop_bar.py, Docs/HANDOVER, Tools/Telegram/queue.txt

### 2026-09-10 00:20 — 34차: 컷씬용 얼굴 레퍼런스 4장 · 옛 컷씬 잔여물 정리 (컴파일 0, 커밋은 사용자)
- **얼굴 레퍼런스**(`Tools/KlingGen/out/faces/`): 타이틀 그림(UI_Title_Gate)에서 두 얼굴을 잘라 `Tools/KlingGen/ref/ref_girl.png / ref_boy.png` → Kling **kling-v1-5** + `reference_type=face`(기본 kling-v1 은 image_reference 미지원 → 400). `Face_Hanul_20.png`, `Face_Doyun_20.png`(정면 재생성 1회), `Face_Hanul_12.png`, `Face_Doyun_12.png`. 컷씬 그림 생성 때 이 파일을 reference_image 로 넘기면 얼굴이 유지된다(fidelity 0.6~0.7, 어린이는 0.45).
- **옛 컷씬 잔여물 — 코드에서 전부 끊음**(파일 삭제는 이 세션에서 불가 → `Tools/cleanup_old_cutscenes.bat` 을 만들어 둠, 사용자가 실행):
  - `OpeningCinematic` 9컷(옛 문장 + Cut_Open_*) → 배경 3컷 플레이스홀더 "(오프닝 N/3 — 새 대본 자리)".
  - `StoryVideo.LegacyVideos = false` → VID_CH01_Open 등 옛 영상 재생 안 함.
  - `EndingController.EndingLetter` A/B 편지 5줄씩 → 플레이스홀더 3줄, 엔딩 라디오 멘트 → 플레이스홀더.
  - `StoryConfig.prologueBeats`(약속의 스마트폰…) → 플레이스홀더 2비트.
  - `story_data.json` 기억 조각 15개 title/body → "(기억 조각 R01 — 새 대본 자리)" (id·챕터·해금·BGM 키는 유지).
  - `Cut_*` 그림 폴백 제거: PhotocardTable.FallbackImage, RaisingUI.Popups.ChapterThumb, CollectionUI 앨범 재킷, UI_MemoryPopup → BG_* 배경. ChapterVN 의 `Cut_<id>` 로드는 파일이 없으면 null → BG 사용이라 그대로 둠.
  - **삭제 목록(bat)**: `Assets/Resources/CoastRun/Cut_*.png`(+meta, 32장), `Video/VID_*.mp4`(+meta, 10개), `Docs/_archive_cutscenes_old/`, `Docs/STORY_SCRIPT_2026-09-08.md`, `Docs/컷씬_스토리_요약_대화.md`, `Docs/_story_dump.txt`. 문서는 남기고 싶으면 bat 3단계 줄을 지우고 실행.
  - 남아 있는 옛 스토리 텍스트(컷씬은 아님): `RandomEventTable`(돌발 이벤트 "그 애가 병원 가는 날" 등), `RaisingButler` 조언, 주인공 말풍선, `PhotocardTable` 캡션, `AchievementTable`. 새 대본이 오면 같이 갈아끼울 것.
- 커밋 대상: Story/OpeningCinematic.cs, Story/StoryVideo.cs, Story/EndingController.cs, Story/StoryConfig.cs, Content/PhotocardTable.cs, Raising/RaisingUI.Popups.cs, UI/CollectionUI.cs, UI/UI_MemoryPopup.cs, Assets/Resources/CoastRun/story_data.json, Tools/cleanup_old_cutscenes.bat, Tools/KlingGen/ref/*, Tools/KlingGen/out/faces/*, Docs/HANDOVER, Tools/Telegram/queue.txt

### 2026-09-10 01:30 — 35차: 건물 앞 튀어나옴 · 붉은 테두리 +20% · 챕터별 풍경 배합(SceneMix) · 계절 날씨(눈·비·바람) · 하늘 10살 얼굴 (컴파일 0, 플레이 확인, 커밋은 사용자)
- **① 건물 쪽 튀어나옴**: 필지 피벗 앞선 `frontX` 를 도로에서 0.9→1.9 m 로(`BuildTownSideKit`). 돌담(0.55)·수국(0.7)·카페 테라스(1.0)·공원 잔디 박스가 도로 가장자리(x=−4)까지 나와 장애물처럼 보이던 것 — 전부 1 m 안쪽으로. s35_c.png.
- **② 장애물 붉은 테두리 +20%**: 그림 outlineMul 3.0→3.6, 메시 헐 1.10→1.12.
- **③ 챕터별 풍경 배합** — 신규 `World/SceneMix.cs`(챕터 20개 × 좌/우 확률표, 2~3타일 유지) + `World/PromenadeSegmentBuilder.Scenes.cs`(빌더). `PromenadeSegmentBuilder`는 partial 로 바꾸고 `Build()`에서 타일마다 `SceneMix.Pick` → 왼쪽 {상가·마을(집/공터/공원만)·숲·들판·현무암} / 오른쪽 {바다·무지개 방호벽·해변·풍력 발전기 바다·오름 언덕·숲·들판·현무암·상가(시장, 피벗 180°)}. 들판 종류(초원/유채/메밀/억새/귤밭)·숲 종류(삼나무/소나무/동백/활엽)는 챕터 프로필. 사진 레퍼런스(삼나무 숲길·해안도로·해변·풍력·현무암·오름) 그대로 챕터에 배분: 1 애월=무지개 방호벽, 2·6 이호=해변, 5 섭지코지=유채, 8 동문시장=양쪽 상가, 10 곶자왈=양쪽 숲, 12 다랑쉬=오름 언덕, 13 메밀, 15 억새, 18 동백, 19·20 풍력. 바다 챕터는 8블록마다 풍력 해안 1회.
  - 전부 프로시저럴(박스·구·킷 나무): 풍력 발전기는 `TurbineSpin` 으로 날개 회전, 무지개 블록 6색, 해변은 난간 없이 모래+비치 의자+파라솔, 오름은 기울인 초록 박스 두 장. 나중에 그림/모델로 갈아끼우면 됨. 왼쪽이 상가/마을일 때만 `SegmentDecorator`(자판기 등 거리 소품) 뿌림.
  - 디버그: `PromenadeSegmentBuilder.LastSceneLabel` 에 마지막 타일 장면 이름.
- **④ 계절 날씨**: `WeatherKind.Wind` 추가. `SeasonWeatherDirector.Roll` — 봄 맑음50/바람22/흐림15/비13, 여름 맑음45/소나기28/흐림15/안개12, 가을 맑음40/바람32/흐림15/비13, 겨울 눈42/맑음28/바람18/흐림12. 런 시작에 뽑고 **45~90초마다 다시 굴림**(같은 것 연속 회피). `WeatherFx`: 비/눈 시스템을 아래로 향하게(전엔 +Z 수평이었음 — 날씨가 늘 Clear 라 안 보였음), 바람이면 기울기 28°; 바람 파티클(봄 꽃잎 분홍 / 여름 초록 잎 / 가을 낙엽 주황 / 겨울 눈보라)이 왼쪽에서 도로를 가로질러 날림, 겨울 바람은 눈보라(눈 600/s, 속도 6.5). 오디오의 비/눈 앰비언스는 기존 훅 그대로. `GameSession`은 `RollWeather` 호출.
- **⑤ 하늘 12살 → 10살**: 더 통통·둥근 볼·아기 얼굴로 2장 뽑아 오른쪽 것을 `Face_Hanul_12.png` 로 교체(후보 hanul10_candidates.png, 원본 `out/faces/hanul10/`).
- 미확인: 날씨는 캡처에 안개/젖은 길만 잡힘(비·눈·바람 파티클은 에디터에서 계절 바꿔가며 확인 — `RunTuning.Season`). 챕터별 장면은 K-POP 런에서 현무암·상가·풍력 원경까지 확인(s35_b/c).
- 커밋 대상: World/SceneMix.cs(+meta), World/PromenadeSegmentBuilder.Scenes.cs(+meta), World/PromenadeSegmentBuilder.cs, Content/ContentIds.cs, Content/WeatherFx.cs, Content/SeasonWeatherDirector.cs, Core/GameSession.cs, Content/ObstacleCatalog.cs, Economy/ObstacleHazard.cs, Visual/ObstacleOutline.cs, Tools/KlingGen/out/faces/Face_Hanul_12.png(+hanul10/), Docs/HANDOVER, Tools/Telegram/queue.txt

### 2026-09-10 02:10 — 36차: 도착 실패 결과 화면 시안대로 (컴파일 0, 플레이 확인, 커밋은 사용자)
- `RunHudChrome.ShowRunOver` 전면 교체(체력 0 / 노을 놓침 → `GameSession.HandleRunOver`가 호출): 노을 진 하늘(진남색→주황 글로우→어두운 땅 + 반딧불 점) 위에 **리본 "아쉽지만 다음에!"**, 노란 "달리기 결과", 왼쪽에 **주저앉아 우는 하늘**(`UI_RunOver_Sad.png` — 클링 얼굴 레퍼런스로 뽑아 분홍 배경 키잉, 764×855 알파), 오른쪽 **별 3개**(진행률 30%↑ 1개, 66%↑ 2개, "실패 · 별 N/3"), 크림 패널 [결과]: 거리 / 점수(큰 글자) — 코인 / 아이템(포션+별) / 최고 콤보 / 하트(아이콘) — 주황 경고 "! 목표 거리 N m 미달 / 이번에는 실패했어요. 다음에는 더 멀리 달려봐요!" — [다시 도전하기](주황) [나가기 또는 육성으로 돌아가기](갈색). 이모지는 폰트에 없어 안 씀.
- 확인: ro36_c.png(1차 캡처). 그 뒤 글로우 알파 낮추고 패널 높이 690→560, 아이템/콤보 아이콘 교체 — 2차 캡처는 보너스 타임(무적)에 걸려 못 찍음, 코드만 반영.
- 커밋 대상: UI/RunHudChrome.cs, Assets/Resources/CoastRun/UI_RunOver_Sad.png(+meta), Tools/KlingGen/out/runover/*, Docs/HANDOVER, Tools/Telegram/queue.txt

### 2026-09-10 03:40 — 37차: 컷씬 대본 43씬(빈 화면 + 음악 큐 + 자막) · 음악 M1~M7 · 레코드 7장 · 비밀코드 1111 (컴파일 0, 플레이 확인, 커밋은 사용자)
- **음악 파일**: 받은 wav 7곡 → ffmpeg(libvorbis q4) → `Assets/Resources/CoastRun/BGM/BGM_M1.ogg … BGM_M7.ogg`(1.5~3.8 MB). M1 이별 예감 / M2 솜사탕 둘이서 / M3 별(듀엣) / M4 Goodbye My First Love / M5 돌아온 제주(Remastered) / M6 남녀 사랑 이야기(inst) / M7 보조개. `CoastBgmLibrary.Load("BGM_M6")` 로 읽힌다.
- **대본 형식에 음악 줄 추가**(`Tools/Story/cutscene_txt.py`): `[n] BGM | 곡키 | 볼륨 | 피치` → `VnLine("BGM", key, "", vol, pitch)`. 곡키 접미사 `M4s`=0.72배속(피치 0.72), `M4r`=차 안 라디오(볼륨 0.4), `M6w`=수중(0.45·피치 0.9), `정지`/`무음`/`∅` = 페이드아웃. 화자 영어 표(아빠·엄마·바다·꼬마·아이들·큰/마른 아저씨·기사) 추가. END_A/END_B 라벨은 아래 매핑으로.
- **ChapterVN**: `case "BGM"` → `VnMusic.Cue(key, vol, pitch)`(새 static 클래스 + `VnMusicPlayer` DontDestroyOnLoad, 2소스 크로스페이드 0.9s, 같은 곡이면 볼륨만). 씬 끝/스킵/Finish 에 `VnMusic.Stop(0.8)`. 그림 없는 컷은 `BG | Blank | | | 변형` → `BlankColor(변형)`: 세피아(회상)=갈색, 비=청회색, 밤=검푸름, 흰=흰 화면, 그 외(현재)=어두운 보라회색. 빈 컷도 두 번째부터 암전 전환(`_bgShown`). Finish 에서 `RecordTable.OnSceneWatched(씬)`.
- **대본 43씬**(`Tools/Story/script/*.txt`, 생성기 `/mnt/user-data/outputs/edit36/gen_scripts.py` 는 컨테이너 쪽 — 이후 편집은 txt 직접): 컷씬7_K드라마판.md 샷 리스트를 그대로 대사(SAY)+지문(NARR)+음악 큐로. 오프닝 「손」=`PRO`(M6→M5, 흰 화면, 현재 프레임 M6 저음량→타이틀 M5), 롱컷1 「하트」=`CH04_Open`(M6, 무전기 ∅), 롱컷2 「우리 기지」=`CH07_Open`(M6→박동 ∅), 롱컷3 「열두 개의 초」=`CH10_Open`(M6→급정지→M4 0.72배속), 롱컷4 「그 밤」=`CH13_Open`(M6 강약), 롱컷5 「스무 살」=`CH15_Open`(M4r→∅→M6→M6w), **`END_A`(전부 S = 기존 Happy) = 트루 엔딩 「주파수」**(M3 → 봄 M3 후렴 → M5 후렴 → 딸깍), **`END_B`(그 외 = 기존 Tragic) = 엔딩 A 「잡혀」**(∅ → 손 잡히는 순간 M6 → 액자 ∅ → 아빠 목소리 ∅ → 종 M5 후렴). 나머지 챕터 Open = chapter_summary 의 「오늘」 장면 요약(2~6줄) + 기존 체력 게이트 두 줄, Close = 「엇갈림/기억」 한두 줄. 챕터 제목 20개 갱신(이름·숨비소리·뒷모습·하트·통과·발자국·우리 기지·새 페인트·꺼진 초·열두 개의 초·빗속의 등·새 바퀴·그 밤·그 자리·스무 살·지나간다·조용해·둘 중 하나·국화·주파수 + Loc.cs 영어). 전체 591줄/EN 481줄. 앰비언스(탑 소리·빗소리·매미)는 큐로 안 넣음 — 나중에 `SFX` 줄 종류 추가하면 됨.
- **오프닝 진입**: `OpeningCinematic.Play` 는 `ChapterScript.Has("PRO")` 면 `ChapterVN.Play("PRO")` 로 위임(옛 3컷 플레이스홀더 안 씀). 타이틀 더보기 › 오프닝 / 첫 런 프롤로그 / 타임라인 CH1 「오프닝 다시 보기」 모두 같은 씬.
- **레코드**(`Meta/RecordTable.cs` + `UI/RecordsUI.cs`, 타이틀 더보기 › **레코드**): 판 7장(M1~M7), 코드로 그린 비닐(홈 명암·광택·색 라벨·번호·구멍). 해금된 판은 계속 돌고(판마다 속도 다름), 누르면 그 곡 재생(재생 중 판은 빠르게·크게·뒤 조명, 아래 패널에 곡명·설명·길이·진행 바; 다시 누르면 정지). 잠긴 판은 회색 + 해금 조건. 새로 열린 판은 빨간 점. 타이틀 곡은 들어갈 때 멈추고 닫으면 다시. **해금 규칙**: 롱컷 1 「하트」→M6, 롱컷 2 「우리 기지」→M5, 롱컷 3 「열두 개의 초」→M4, 롱컷 5 「스무 살」→M3(스토리 4곡 = 컷씬에 나오는 곡; 롱컷 4 「그 밤」은 M6 뿐이라 새 해금 없음 — 바꾸려면 `RecordTable.All[].unlockScene` 한 줄). 보너스 M1·M2·M7 은 `trackGrade` S급 ≥ 18/20(90%). `MetaProfile.recordMask / recordNewMask`. 더보기 버튼에 새 레코드 있으면 「레코드 •」.
- **비밀코드**(설정 › 비밀코드, 숫자 키패드 4자리, `MainMenuController.SecretCode = "1111"`): 맞으면 `RecordTable.UnlockAll` → `MetaProfile.devUnlockAll = true` + 레코드 7장 전부. 타임라인(`RaisingUI.Popups`): 막 탭 3개·미래 챕터 전부 열림, 미래 챕터 셀 → `DevChapterModal`: [▶ 이 챕터로 점프](`GameManager.DevJumpTo` — 본 진행의 week/chapter/phase 를 그 챕터 시작으로, 스탯은 현재 그대로, 재도전 중이면 본 진행으로 복귀) / [오프닝(컷씬) 보기]. 설정 버튼 라벨에 「· 전부 열림」 표시. 해제하려면 profile.json 의 devUnlockAll 을 false 로(UI 없음).
- 확인(Tools/_shots/r37_*): 레코드 화면·재생(records/records2), 프롤로그 빈 화면+자막(vn1), CH1 오프닝 새 대본(vn6), 비밀코드 키패드→전부 열림(code~code3), 타임라인 2·3막 열림·CH3/CH4 점프(tl2, dev, dev6 7주차 CH3, j2 9주차 CH4). 음악 재생은 로그(곡 없음 경고 0)로만 확인 — 귀로는 사용자 확인.
- 주의: 에디터 원격 `key S`(스킵)는 육성 화면 DevKeys(S=상점)가 먼저 먹는다 → VN 스킵은 SKIP 버튼 탭. 원격 tap 도구는 씬 전환 직후 두 번째 이벤트가 새 씬에 떨어질 때가 있음(집 화면이 열리던 것 — 실기 문제 아님).
- 커밋 대상: Assets/Resources/CoastRun/BGM/BGM_M1~M7.ogg(+meta 자동), Story/ChapterVN.cs, Story/OpeningCinematic.cs, Story/ChapterScript.Data.cs, Story/ChapterScript.En.cs, Core/Loc.cs, Meta/SaveData.cs, Meta/GameManager.cs, Meta/RecordTable.cs(+meta), UI/RecordsUI.cs(+meta), UI/MainMenuController.cs, Raising/RaisingUI.Popups.cs, Tools/Story/cutscene_txt.py, Tools/Story/script/*.txt(43), Docs/HANDOVER, Tools/Telegram/queue.txt

### 2026-09-10 00:40 — 38차: 15+5+2 항목(피드백 묶음) — 컴파일 0, 캡처로 확인한 것/못 한 것 아래 구분 (커밋은 사용자)
**확인(캡처 r38_*)**
- K-POP 런 사망 결과 → 시안 화면(아쉽지만 다음에!·우는 하늘·별·결과 패널·최고 기록 상자·버튼 2개)로 통합(r38_over). `GameSession.HandleStaminaDepleted` 아케이드 분기가 `RunHudChrome.ShowRunOver` 호출, `ShowRunOver`는 `ArcadeRun.Active`면 거리/점수/최고기록 기준(별 3 = 최고 갱신). 옛 '노을 달리기 결과' 카드(ArcadeResultUI)는 폴백만.
- 타이틀: 「새로하기」→「육성하기」(세이브 있으면 확인 모달 → 러닝 회차 바로 시작, 캐릭터 선택 페이지 미빌드), K-POP 바 오른쪽 **챕터 칩**(다음 챕터 `CH n 「제목」`, 누르면 챕터 선택=타임라인), 더보기에서 「노을 달리기」 삭제, 「레코드」는 컬렉션 › 레코드 탭으로(r38_title).
- 챕터 선택(타임라인) 시안화: 완료(금)/NEW(핑크)/잠김(회색) 태그, 별 5개(등급), 진행 바 ♥n/목표 +보상, 캐릭터 수집 얼굴 2개, 제목 분홍 알약 ♫, 「✕ 닫기」(r38_tl 비교). 남은 차이: 헤더 키아트(UI_Act1)는 옛 수채화 그대로 — 시안처럼 별·반짝이 밤배경으로 바꾸려면 Kling 재생성 필요. 카드 그림은 기존 챕터 썸네일.
- 펫 상점 시안화: 크림 제목 알약+주황 점, 파스텔 4색 카드, SSR 배지, 흰 틀 펫 그림, 금화 가격 알약, 파란 [구매](r38_shop).
- 방 꾸미기(HomeUI 방 탭): Kling 쿼터뷰 방 그림 `UI_Room_Iso.png`(정사각, 아래는 밤보라) — 옛 정면 방 그림 대체. 주인공 150×214 로 축소(r38_home). 후보 원본 `Tools/KlingGen/out/room/`. 남은 차이: 장식 아이콘이 아직 색 알약(UI_Deco_<id>.png 넣으면 자동), 시안처럼 방이 화면을 더 채우려면 그림 비율 조정.
- 컬렉션(밤하늘 보라 + 별 + 네온 파형 + 흰 탭 알약): **레코드 탭** = OST 7곡 리스트(파스텔 행, 디스크 아이콘, [S ▶]/■, COLLECTED n/7, NEW), 누르면 재생/정지. **포토카드 탭** = 잠김 카드(?, 자물쇠, 등급 배지), 등급 확률 표시(N 55/R 30/SR 12/SSR 3 %), 상세는 등급 프레임(SSR 무지개 4겹 + RARE PHOTOCARD). 디지털 앨범 구매/페이월 삭제(`Collection.CanPlayChapter` 항상 true). 실사 카드 21~26 기본 지급(`MetaProfile.starterCardsGiven`), 나머지는 **러닝 포토카드 아이템**(`PickupKind.Photocard`, `Obs_Photocard.png`, 420~640 m 마다) → `Collection.RollCardDrop`. 캡처는 육성에서 K 키로 열어 확인(r38_col 은 집 화면이 먼저 열려 컬렉션 캡처는 다음 세션에 재확인).
- 알바 카드 고를 때 뜨던 수치 토스트 삭제(RaisingUI.OnCardTapped).
**코드만 반영(캡처 미확인)**
- 키보드: `MobileSwipeInput` 3겹 입력(Input.GetKeyDown + 축 엣지 + **OnGUI KeyDown 이벤트**, 프레임 중복 제거) + 원격 화살표 키. 에디터는 게임 뷰에 포커스가 있어야 키가 들어온다(다른 창 클릭하면 안 먹음).
- 꼬마 집사 피버 3→5초(`FeverMode.Duration`).
- 장애물: 붉은 굵은 테두리 **원복**(잉크 선 1.05), 대신 **바닥 붉은 링**(`HazardRing`: 굵은 선 도넛, 더 빨강 (1,0.10,0.08), 반지름 ×1.25, 초당 2.2회 깜빡임). **전수조사**: `ObstacleCatalog.Spawn` 에서 링이 없는 장애물엔 바운드 크기로 자동 부착(마주 오는 차·오리 장애물 포함). 아이템(물약·별·하트·포토카드)은 발밑 **파란 링**.
- 배분: 신규 `Economy/RoadOccupancy.cs` — 장애물 앞뒤 3 m 같은 레인엔 코인·말랑이 없음(오리 장애물은 전 레인), 아이템은 장애물 4.5 m·다른 픽업 3.5 m 떨어진 자리로 미룸(FindClear), 장애물이 나중에 놓이면 근처 코인·말랑이 회수(`CoinSpawner/JellySpawner.RemoveNear`).
- 「출발!」 배너(`StageManager.AnnounceStart`, PickupFloat.Banner 0.9s) — 스테이지 시작/재도전마다.
- 돌하르방 밤 톤: 전용 밝은 현무암 재질 + `HareubangTone`(LightingT↑ 이면 기본색 최대 +55%).
- **챕터별 시각 고정표** `Content/ChapterClock.cs`(낮/오후/노을/저녁/밤, 1~20) — `StageManager.LoadStage` 가 lightingTStart 를 덮어쓴다. 밤 챕터는 노을 규칙이 되돌리지 않게 Max.
- **페인팅 사이드 스트립**(폴리곤 상가 대체): Kling 그림 `Side_Village.png`(마을 집 한 채+돌담+야자, 마젠타 키) / `Side_Shore.png`(현무암·풀·꽃·조개). `PromenadeSegmentBuilder.BuildPaintedSide` — 30 m 세로 띠 메시(ChromaUnlit, 양면), 왼쪽 상가/마을 → Village 띠(옛 킷 안 씀), 오른쪽 바위 → Shore, 바다 옆에도 Shore 띠. r38_run3 에서 왼쪽에 집 띠 확인(반복이 눈에 띔 — 변형 B 파일 `Tools/KlingGen/out/strips/` + `res/Side_*_A/B` 로 타일마다 섞어 쓰면 완화). 레퍼런스(ref_road)와 아직 거리 큼 — 하늘/바다/도로 텍스처는 그대로.
- 육성 안 「오프닝 다시 보기」 삭제(타임라인 챕터 모달·비밀코드 모달).
**못 한 것 / 남은 것**
- ①「바다에서 뛰는 것 같은 화면」(흰 반투명 도로) — 재현 못 함. 후보: 안개 날씨+젖은 톤, Beach 양쪽 장면. 다음 세션에 `RunTuning.Season`/날씨 바꿔가며 캡처.
- ⑦ 빨래줄 활공 코인 — 코드(`ClothesLine.OnTriggerEnter → CoinSpawner.SpawnGlideLine`)는 맞아 보임. 실기에서 안 보이면 `CoinSpawner.Instance` 가 다른 인스턴스(_root 없음)일 가능성 → 로그 추가 필요.
- 컬렉션 레코드/포토카드 탭 캡처, 「출발!」 배너, 파란/빨간 링, 코인 배분, 돌하르방 밤 톤, 키보드 — 캡처 검증 필요.
- 시안 대비 남은 것: 챕터 선택 헤더 키아트, 카드 썸네일(치비), 방 장식 아이콘 18개(UI_Deco_*), 레코드 화면 아래 치비 커플 그림, 도로/하늘 텍스처.
- 커밋 대상: Input/MobileSwipeInput.cs, Economy/{FeverMode,CoinSpawner,JellySpawner,RoadOccupancy(+meta),ObstacleHazard,JellyPickup}.cs, Content/{ObstacleCatalog,PhotocardTable,ChapterClock(+meta)}.cs, Visual/{HazardRing,ObstacleOutline}.cs, Core/{StageManager,GameSession}.cs, World/{JejuKit,HareubangTone(+meta),PromenadeSegmentBuilder,PromenadeSegmentBuilder.Scenes}.cs, Raising/{RaisingUI,RaisingUI.Popups,HomeUI}.cs, UI/{MainMenuController,CollectionUI,RecordsUI,RunHudChrome}.cs, Meta/{Collection,SaveData}.cs, Assets/Resources/CoastRun/{Obs_Photocard,UI_Room_Iso,Side_Shore,Side_Village}.png(+meta 자동), Tools/KlingGen/ref/ref_*.png(시안 6장), Docs/HANDOVER, Tools/Telegram/queue.txt

### 2026-09-10 16:50 — 39차: 환경·MCP·권한 점검 + FeverMode 컴파일 에러 수정 (컴파일 0, 커밋은 사용자)
**.env / 권한 (읽기만, 값은 바꾸지 않음)**
- `.env` 키: `PEXELS_API_KEY`(채움) · `KLING_ACCESS_KEY`(채움, Bearer 키와 같은 값) · `KLING_SECRET_KEY`(**비어 있음**) · `KLING_API_KEY`(채움) · `KLING_BASE_URL`=api.klingai.com · `TELEGRAM_BOT_TOKEN`(채움) · `TELEGRAM_ALLOWED_USER_ID`. Kling 클라이언트는 실제로 **bearer 모드**로 동작(`kling_ping`의 auth_mode) → `KLING_ACCESS_KEY`는 안 쓰이는 값이니 비워도 됨.
- `.env.example`에 `TELEGRAM_*` 두 줄이 빠져 있음 — 새 PC 세팅 시 빠지기 쉬우니 빈 값으로 추가 권장.
- `.gitignore`: `.env`, `.env.*`(`!.env.example`), `Tools/Telegram/queue.txt`·`sent.log`, `*환경 변수*.txt` 전부 제외 → 비밀값이 git에 들어갈 경로 없음. 코드에 하드코딩 없음(CODE_AUDIT §5와 일치). `.cursor/mcp.json`·`Tools/Mcp/claude_desktop_config.json`에는 키 없음(사용자 경로 `ares2`만 노출).
- MCP 등록 현황: Claude 데스크톱 = unity·blender·photoshop·kling 4개 / Cursor(`.cursor/mcp.json`) = photoshop·illustrator·blender 3개(unity·kling 없음). 미해결 보안 항목은 CODE_AUDIT §5 그대로(CoastRemote 47001 무인증, VideoFetch 자동 실행).
**MCP 연결 점검 결과**
- unity·kling·photoshop: OK. **blender: 처음엔 실패** — Blender 5.2.1 LTS에 "MCP for Blender" 애드온(v1.6, 프로토콜 5)이 **설치만 되고 비활성** 상태였음(5.2 업그레이드로 애드온 활성 목록이 초기화된 듯). Edit → Preferences → Add-ons → "MCP for Blender" 체크 → 바로 붙음(`get_addon_status` up_to_date, `get_scene_info` OK). 환경설정 자동 저장이라 다음 실행부터 유지될 것. 다시 안 붙으면 이 체크부터 확인. 애드온 텔레메트리는 켜져 있음(`telemetry_consent: true`).
- Blender 창은 `blender-launcher.exe`(허용 요청 시 이름 "Blender 5.2")가 아니라 **`blender.exe`**가 소유 → 컴퓨터 제어로 화면을 보려면 `blender.exe`도 따로 허용해야 함(안 하면 창이 마스킹됨).
**FeverMode 컴파일 에러 2건 수정**
- 증상: `Economy/FeverMode.cs(183,35)`·`(185,35)` CS1503 — `VacuumNearby()`가 `CoinPickup/JellyPickup.BeginFeverPull(Transform)`에 `PlayerController`(`_player`)를 그대로 넘김(38차 피버 5초 작업 뒤 생긴 것).
- 수정: `var pt = _player.transform;`로 받아 두 호출 모두 `pt` 전달. 다른 파일 변경 없음. Unity 재실행 후 `unity_compile` **에러 0**, 경고는 Input Manager 지원 종료 예고 1건뿐.
**⚠ `unity_launch`(MCP 직접 실행)로 Unity를 열면 UPM 서버가 안 뜸 — Hub로 열 것**
- 증상: `unity_launch`로 띄우면 매번 "Unity Package Manager Error — Failed to start the Unity Package Manager local server process … Windows Defender" 대화상자(Retry 눌러도 동일, 2회 재현). 같은 PC에서 **Unity Hub로 열면 정상**. Defender가 아니라 Claude 데스크톱 → MCP 서버(python) → Unity.exe 프로세스 체인에서 Unity의 자식(UPM 서버)이 못 뜨는 것으로 추정(24차의 "Upm IPC 실패"도 같은 원인일 가능성).
- 대응: 에디터가 꺼져 있으면 **컴퓨터 제어로 Unity Hub("unity hub.exe" 허용) → 프로젝트 `game` 클릭**으로 연다(9/10 이 방법으로 성공, 로딩 ~2분 뒤 브릿지 연결). `unity_launch`는 "이미 열려 있는지 확인" 용도로만. 대화상자가 떠 있으면 **Quit**으로 닫고 Hub로 다시 열기. 항구 조치 후보: `unity_mcp_server.py`의 `tool_launch`를 `"C:\Program Files\Unity Hub\Unity Hub.exe" -- --projectPath C:\dev\game`로 바꾸기(미적용).
- 이 세션 텔레그램 보고는 `queue.txt` append로만(device_bash 마운트가 안 돼 파일 통째로 커밋). 워처가 꺼져 있으면 미전송분은 `Coast Run/Telegram/Start watcher`(Ctrl+Shift+Alt+T).
- 커밋 대상: Assets/_CoastRun/Scripts/Economy/FeverMode.cs, Docs/HANDOVER, Tools/Telegram/queue.txt

### 2026-09-10 18:10 — 39차-2: 시안 4장(타이틀·로딩·일시정지·스테이지 클리어) 그대로 반영 (컴파일 0, 캡처 비교 확인, 커밋은 사용자)
사용자가 Meta AI 시안 4장을 주고 "똑같이 나올 때까지". Kling 재생성 대신 **시안 자체를 에셋으로** 썼다(가장 똑같이 나오는 길): 시안은 `Tools/KlingGen/ref/ref_{title,loading,pause,clear}.png`, 워터마크·"70%"를 Photoshop MCP(content-aware fill)로 지운 것이 `ref_*_clean.png`. 비교표 `Tools/_shots/r39_*.png`.
- **타이틀**: `UI_Title_Mock.png`(시안 통그림 1080×1920, 키아트는 기존과 동일)을 GateArt로 깔고, 스토리 모드/더보기/Play 판은 **투명 히트 영역**(`RunHudChrome.HitButton`; 좌하단 앵커, 중심 88/992·88/833·358/181). 제목·부제 라벨은 그림에 있으니 생략(`useMock`). CH 칩은 시안 자리(501/319)에 파란 `GlossyPill`로 덮어 실제 챕터를 그린다(세이브 없으면 CH 1, 탭 → 스토리 모드). 더보기 컬럼은 y 400에서 오른쪽 슬라이드. 그림이 없으면 옛 배치 그대로(`UI_Title_Mock` 지우면 원복).
- **로딩**: `UI_Loading_Mock.png`를 스플래시 통그림으로. "70%" 자리(373/1105)에 92pt 하늘색+흰 외곽선+파란 그림자 숫자를 실시간으로, 초록 바(x 125~420, y 980)는 어두운 덮개(`_loadCover`, 오른쪽 고정·왼쪽이 줄어듦)로 진행률 표현. `SetLoad(u)` 하나로 옛 바/라벨도 같이 갱신. 확인용으로 잠깐 8초로 늘려 캡처(r39_load2) 후 1.4초로 원복.
- **일시정지**: 시안 카드(제목·별·버튼 3개 포함)를 알파 크롭한 `UI_PauseCard.png`(1070×1330 → 653×811, 중심 +18) 한 장 + 투명 히트 버튼 3개(중심 y +135/−49/−232, 462×144). 딤 0.72→0.80. 버튼 글자는 그림에 구워져 있음(영문 로케일 미대응 — 그림 없으면 옛 코드 카드로 폴백).
- **스테이지 클리어**: 코드로 재현(배경이 실시간 게임이라). 제목 항상 "STAGE CLEAR!"(56pt 금색), 소제목 한 줄 `S01 첫 발 구르기`(스토리 모드는 기존 4줄 정보 유지, 자동 축소). 왼쪽 칩 4줄 **항상**(코인·말랑이·하트·물약, 347×60, 간격 70, 남색 반투명). 오른쪽 흰 테두리 원형 초상 `UI_Face_Ring.png`(시안 크롭, 160, 중심 622/295) + 흰 말풍선 "해냈다!"(233×50). 버튼 3개 `CoastUiArt.GlossyPill`(신규: 어두운 테두리+아랫입술+본체+윗광택, 590×141, r44) 핑크/주황/파랑 + 시안에서 뽑은 흰 아이콘 `Icon_Arrow/Refresh/Home.png` + **「메인으로」 신설**(타이틀로). 등급 배지·하단 띠 제거. 탭 확인: 메인으로 → 타이틀, Play 판 → K-POP 런, 일시정지 계속하기 OK.
- 남은 차이: 버튼 글자 굵기(시안은 더 굵은 라운드체), 스테이지 클리어 배경은 실시간 장면이라 시안의 밤하늘 톤과 다름, 타이틀 CH 칩 제목은 `ChapterScript.Title(1)`이 비어 "이름"으로 표시됨(시안 문구와 우연히 동일).
- **39차-3 코인 검게 덮임 수정**(사용자 지적, `Economy/CoinPickup.cs`): 38차 `Coin_Gold.fbx` 몸통(두께 ±0.17)을 쓰면서 ① 면 그림 쿼드(±0.13)가 잉크 림 캡(±0.145) 안에 묻혀 앞에서 검은 원판+금 별로 보였고 ② 인버티드 헐(Outline)이 FBX 와인딩 반대로 바깥에 그려졌다. → 면 그림 ±0.18(몸통 바깥), 잉크 림 1.06×0.15 + 색 검정→진한 금갈색, FBX 코인엔 헐 생략, 몸통은 공유 무조명 황금 재질(모든 서브메시 슬롯). 캡처 r39_coinfix17/18 — 열 지어 오는 코인·정면 코인 모두 황금.
- 커밋 대상: UI/{CoastUiArt,RunHudChrome,StageClearUI,MainMenuController}.cs, Economy/CoinPickup.cs, Assets/Resources/CoastRun/{UI_Title_Mock,UI_Loading_Mock,UI_PauseCard,UI_Face_Ring,Icon_Arrow,Icon_Refresh,Icon_Home}.png(+meta 자동), Tools/KlingGen/ref/ref_{title,loading,pause,clear}.png·ref_{title,loading}_clean.png, Docs/HANDOVER, Tools/Telegram/queue.txt

### 2026-09-10 19:30 — 39차-4: 타이틀 CHAPTER 칩 + K-POP 챕터 선택 페이지 (컴파일 0, 탭 확인, 커밋은 사용자)
- 사용자: 폰에서 「CH 1」 칩을 눌러도 반응 없음 → 칩을 **「CHAPTER ▾」**(오른쪽 위 분홍 배지에 현재 챕터 번호)로 바꾸고, 누르면 **`UI/KpopChapterSelect.cs`**(신규) 페이지: CH 1~20 카드 5×4(계절색 봄 노랑·여름 하늘·가을 주황·겨울 보라, 로케이션 이름, 스토리 트랙 별 ★, 마지막 클리어 다음 카드에 NEXT 태그), 선택 카드는 금 테두리+확대, 아래 「CH n 달리기!」(즉시 K-POP 러닝) / 「✕ 닫기」(선택만 저장).
- 자동 선택: `ArcadeRun.KpopChapter(gm)` = 사용자가 고른 챕터(PlayerPrefs `CoastRun_KpopPick`, 0=자동) 없으면 **마지막 클리어(`CoastRun_KpopLastClear`) + 1**(없으면 1, 20 넘으면 20). `SceneFlowController.EnterStageClear`에서 `ArcadeRun.NoteKpopClear(stage)`로 갱신(고른 챕터를 클리어하면 다음부턴 자동). `ArcadeRun.StartKpop(gm, chapter)` 오버로드 — 계절 = (챕터−1)/5, `StageIndex = chapter`. Play 판은 `StartKpop(gm)` → 자동 챕터.
- 캡처 `r39_chsel2.png`(페이지), `r39_chip3.png`(칩 배지 3). 에디터 PlayerPrefs에 pick=3이 남아 있음(테스트) — 신경 안 써도 됨.
- 커밋 대상: UI/KpopChapterSelect.cs(+meta), Meta/ArcadeRun.cs, Core/SceneFlowController.cs, UI/MainMenuController.cs, Docs/HANDOVER
- 이 세션 설계 문서: `Docs/RAISING_TAMAGOTCHI_v1/v2/v3.md`(육성 개편 제안, v3이 최신 — 사용자 결정 대기: 알바 카드 고르기 여부·자동 효율 −15%)

### 2026-09-10 20:10 — 39차-5: 러닝 상단 HUD 2줄 격자 정렬 (컴파일 0, 캡처 r39_hud4, 커밋은 사용자)
- 사용자 폰 캡처: 날씨 칩·점수 별 배지 겹침, 2줄 높이 제각각, 체력 숫자가 여정 바에 가림. **인셋 664 기준 격자**로 재배치:
  - 1줄 y −6 · 높이 60: 일시정지 6~66 │ 낮/밤·날씨 칩 78~342(왼쪽 앵커, 전엔 중앙 앵커라 점수 알약과 겹침) │ 점수 알약(스토리 런은 하트 목표 알약) 358~658.
  - 2줄 y −74 · 높이 52: 체력 6~206(트랙 174×44) │ 여정 바(`UI_FinalDestinationController.BuildProgressBar`, 노을 시계 포함) 216~456 │ 코인 190 468~658.
- 여정 바: 앵커 0.265~0.685 → 216/664~456/664, 높이 42→52, 왼쪽 ◀ 글리프 제거, 링 0~0.92 꽉 채움, 송전탑 배지 세로 중앙, 시계 라벨 13pt 한 줄 고정(전엔 best-fit+Wrap 때문에 "노을까지/2:04" 두 줄로 접힘). `RunHudChrome.BuildSunMeter`(예비)는 여전히 미호출이나 격자 값으로 맞춰 둠 + 남은 초 표기 추가.
- 주의: `RunHudChrome.cs`는 이 세션 중 사용자 쪽(Cursor)에서 날씨 칩(BuildWeatherChip)이 추가된 상태였음 — 디스크 최신본 위에 수정했으니 충돌 없음.
- 커밋 대상: UI/RunHudChrome.cs, UI/UI_FinalDestinationController.cs, Docs/HANDOVER

### 2026-09-10 20:40 — 39차-6: 챕터 선택 페이지 시안대로 + 잠김 회색 (컴파일 0, 캡처 r39_chsel4, 커밋은 사용자)
- 시안(Meta AI, `Tools/KlingGen/ref/ref_chapter.png`): 금색 입체 CHAPTER 제목 → 크롭 `UI_ChapterTitle.png`, 우상단 파란 X → 크롭 `UI_CloseX.png`(「닫기」 알약 제거), 카드 「챕터 N」+별만(로케이션 이름 삭제) 116×138 5×4, 봄·여름 2줄 붙이고 가을·겨울 2줄은 아래 묶음, 딤 0.66(뒤가 비침), 아래 핑크 「챕터 n 달리기!」 503×106.
- **잠김 규칙** `KpopChapterSelect.IsUnlocked(n, profile)`: n ≤ K-POP 마지막 클리어 + 1 이거나 스토리에서 이미 달린 트랙(`profile.trackGrade[n-1] > 0`). 잠긴 카드는 회색 + 「잠김」, 누르면 토스트("앞 챕터를 먼저 달려요")·선택 안 됨. 열리면 선택 유지. 에디터 프로필은 트랙 전부 S라 전부 컬러로 보임 — 새 프로필로 확인 필요.
- 커밋 대상: UI/KpopChapterSelect.cs, Assets/Resources/CoastRun/{UI_ChapterTitle,UI_CloseX}.png(+meta), Tools/KlingGen/ref/ref_chapter.png, Docs/HANDOVER

### 2026-09-10 20:55 — 40차-b: 러닝 꼬마 도움 버튼(FeverMode) 얼굴 30% 축소 + TAP! 분리·깜빡임 (컴파일 0, r40_tap1)
- `Economy/FeverMode.cs`: 얼굴 225→158, TAP! 알약을 얼굴 **아래**(원 하단 −6, 피벗 위)로 분리, `_tapCg` 알파 0.25~1.0 사인 깜빡임(초당 ~1.6회), 알약·글자도 raycast 켜서 눌림(Button 자식 버블). 사용자 쪽 40차 수정(얼굴 +120 위로, DismissOffer)은 유지.
- 커밋 대상: Economy/FeverMode.cs, Docs/HANDOVER

### 2026-09-10 21:40 — 40차-c: 펫 이미지 Kling 3D 귀여운 톤으로 재생성 + 실제 알파 컷아웃 (컴파일 0, r40_pet4/r40_coin1, 커밋은 사용자)
- 사용자: "펫 이미지 클링ai 로 3d 기준으로 귀엽게 다시 그려줘". `kling_image`로 3D 렌더 톤(픽사풍 치비, 큰 눈, 부드러운 라이팅, 마젠타 단색 배경) 프롬프트로 **Cat·Puppy·Seagull 3종 새로 생성**(원본 `Tools/KlingGen/out/pets/*_raw.png`, 이전 그림은 `out/pets/prev/*_prev.png`). BikerThug·BlackPig·Sparrow·WildGoose 4종은 이미 3D 톤이라 컷아웃만 다시 함.
- **Photoshop 「Select Subject」 MCP 미제공**(recipe_remove_background 실패) → 컨테이너 `rembg`(isnet-general-use)로 7장 전부 컷아웃, 가장자리 색 팽창(dilate) 후 **RGBA 실제 알파**로 `Assets/Resources/CoastRun/Obs_Pet_{BikerThug,BlackPig,Cat,Puppy,Seagull,Sparrow,WildGoose}.png` 교체(마젠타 없음).
- 왜 알파가 필수인가: 처음엔 RGB 마젠타 그대로 넣었더니 **어두운 보라 사각형**이 펫 뒤에 보임 — `PaintedProp`의 Painted_Back 쿼드가 어두운 틴트를 곱해서 마젠타가 키 거리 0.22 밖으로 밀려 안 잘림. `Tools/Art/key_to_alpha.py` 규약대로 알파가 있어야 함(14차-7과 같은 교훈).
- `Player/PetCompanion.cs` Build(): `PaintedProp.Available("Pet_"+kind)`면 FBX 대신 **스프라이트 빌보드**(`PaintedProp.Attach(_body, "Pet_"+kind, hp, replace:false, outline:true)`, 높이 BikerThug 1.1 / WildGoose 0.8 / 그 외 0.5), 없으면 기존 FBX 프리팹 경로 유지.
- 확인: 러닝 중 참새 펫이 주자 옆에 깨끗하게 뜸(r40_pet3/4, r40_coin1). 같은 캡처에서 코인 색도 재확인 — 금색 정상(r40_pet4에서 갈색으로 보인 건 노을 틴트 프레임, 회귀 아님).
- 커밋 대상: Player/PetCompanion.cs, Assets/Resources/CoastRun/Obs_Pet_*.png ×7(+meta), Tools/KlingGen/out/pets/*, Docs/HANDOVER

### 2026-09-10 21:10 — 41차: 장애물 효과 전부 제거 · 챕터 소개 즉시 · 펫 뒷모습 (컴파일 0, r41_run3/r41_sheet, 커밋은 사용자)
- **① 장애물 효과 제거**(사용자: "다 찾아서 지워주고 다시 나오지 않게"): 장애물에 붙던 효과 4가지 = 머리 위 빨간 「!」 배지 + 바닥 빨간 경고 띠 + 본체 색 펄스(`Visual/ObstacleWarning`), 발밑 빨간 링(`Visual/HazardRing`), 인버티드 헐 잉크 테두리(`Visual/ObstacleOutline`). 호출처가 6곳(ObstacleCatalog·ObstacleHazard·OncomingCar·DuckHazard)이라 지우는 대신 **킬스위치 `ObstacleWarning.Enabled = false`(static readonly) 하나**로 세 Attach 가 전부 조기 반환 → 컴포넌트 자체가 안 붙으므로 어디서 스폰돼도 다시 안 나온다. 아이템(물약·별·하트) 파란 링은 `color` 인자를 주는 경로라 그대로. 충돌 피드백(꽈당·팡 파편·거인 무적)은 장애물 "효과"가 아니라 타격 연출이라 남김 — 그것도 지우려면 `JuiceDirector.PlayObstaclePop/Stumble`.
- **② 챕터 소개 즉시**: `Core/StageManager.AnnounceStartCo` 가 베일(페이드) 알파 < 0.4 될 때까지 최대 3.5초 기다린 뒤 「챕터 N 시작!」을 띄웠음 → 대기 삭제, 첫 프레임에 바로 표시. PickupFloat 캔버스가 정렬 560(베일 500 위)이라 페이드 중에도 보임. 에디터 전용 경고 로그 `[StageManager] 챕터 N 소개 카드 표시 t=… veil=…`(unity_log 로 타이밍 확인용, 빌드엔 안 들어감).
- **③ 펫 뒷모습**: 주자를 뒤에서 보는 카메라인데 펫 스프라이트가 정면이라 화면을 마주봄 → Kling 으로 7종 **뒷모습(back view)** 재생성(`Tools/KlingGen/out/pets_back/*_raw*.png`; Puppy·BikerThug 는 정면으로 나와 2~3회 재생성, Puppy 는 `puppy3/…_1.png` 채택). rembg 컷아웃+색 팽창 → `Assets/Resources/CoastRun/Obs_Pet_*.png` 교체(RGBA). 정면 버전은 `Tools/KlingGen/out/pets/front/Obs_Pet_*_front.png` 로 보관. 캡처에서 참새 꼬리가 카메라 쪽(뒷모습) 확인.
- 확인: 러닝 캡처 r41_run3/run4 — 콘·바리케이드·젖은바닥 표지에 링/배지/테두리 없음, 참새 뒷모습.
- 커밋 대상: Visual/ObstacleWarning.cs, Visual/HazardRing.cs, Visual/ObstacleOutline.cs, Core/StageManager.cs, Assets/Resources/CoastRun/Obs_Pet_*.png ×7, Tools/KlingGen/out/pets_back/*, Tools/KlingGen/out/pets/front/*, Docs/HANDOVER

### 2026-09-10 21:45 — 42차: 사용자 6건(날씨 칩·글꼴 통일·동전 테두리·챕터 카드·이동거리 바·더보기 정리) (컴파일 0, 캡처 r42_*, 커밋은 사용자)
- **① 날씨 칩 「낮·안개」 vs 밤 하늘**: 원인 = `RunHudChrome.RefreshWeatherChip` 이 챕터 고정표 `ChapterClock.IsNight(챕터)` 만 읽어서, 노을 시계가 다 저물어 화면이 남색(블루아워)이어도 「낮」. 일괄 조사 결과 시간대 표기는 이 칩 한 곳뿐(여정 바 「노을까지/해가 졌어」는 별도 정상). → 칩이 **실제 조명 시각** `DynamicEnvironmentManager.LightingT` 를 읽어 낮(t<0.55) / **노을**(0.55~0.88, 주황 해) / 밤(≥0.88, 달) 3단계로 매 프레임 갱신. 환경 매니저 없으면 종전 고정표. 캡처 r42_night1: 해 진 뒤 「밤·맑음」+달.
- **② 글꼴 통일**: 일시정지 카드(`UI_PauseCard`)는 Kling **그림**이라 글꼴 파일이 없음. 그 둥글고 두꺼운 손글씨 톤에 가장 가까운 **무료 글꼴 BM Jua(주아체, SIL OFL 1.1 — 상업 사용·앱 임베드 무료, 재배포 시 라이선스 동봉)** 를 `Assets/Resources/CoastRun/Fonts/Jua-Regular.ttf`(+`LICENSE_Jua_OFL.txt`)로 넣고 `CoastHudLayout.Font()` 가 Jua → Pretendard → 내장 순으로 잡음. 모든 UI 가 `CoastHudLayout.MakeText`/`Font()` 를 쓰므로 한 곳 교체로 전체 통일(타이틀·더보기·컬렉션·러닝 HUD·VN 확인). **유료 글꼴 아님.** 주의: Jua 는 단일 굵기 — `HasRealBold` 로 가짜 볼드 끔(개별 `fontStyle=Bold` 지정 자리는 그대로라 약간 더 굵게 보임, 원하면 일괄 제거). 한글 2,350자 KS 완성형 커버(희귀 글자는 OS 폴백).
- **③ 동전 밤색 테두리**: `CoinPickup.AttachCoinSilhouette` 이 만들던 InkRim 실린더(진갈색 0.48/0.30/0.06)와 어두운 인버티드 헐을 **그리지 않음**(메서드 첫 줄 return). 금 몸통+면 그림만. 캡처 r42_k3 확인.
- **④ 「출발」만 뜨고 챕터 없음**: K-POP(아케이드) 런은 `StageManager.AnnounceStartCo` 가 `PickupFloat.Go("출발")` 만 띄웠음 → 스토리와 같은 **「챕터 N 시작! / 장소 / 제목」** 카드로 통일(아케이드는 StageIndex = 고른 챕터). 캡처하다 두 가지 버그 더 발견·수정: (a) 41차에서 카드를 페이드 중에 띄우자 `PickupFloat` 오브젝트가 그때의 활성 씬(타이틀)에 생성돼 타이틀 언로드와 함께 삭제 → `PickupFloat.BindToScene(런 씬)` 으로 이동. (b) 씬 로드 직후 긴 프레임(1초+)이 `unscaledDeltaTime` 으로 한 번에 더해져 카드가 0.3초 만에 끝남(로그 "카드 종료 1.80s" at +0.31s) → `GoSeq` 프레임당 최대 50 ms 클램프. 캡처 r42_f0: K-POP 시작에 「챕터 3 시작! 하늘네 마을 뒷모습」.
- **⑤ 상단 이동거리 바**: 아케이드 「거리 863 m」 → **「863 m」**(글자 제거), 라벨 13pt → **20pt**(체력 숫자 `_hpGaugeText` 20 과 동일). 스토리의 「노을까지 m:ss」도 20pt 한 줄(간격 한 칸). 캡처 r42_k1 「16 m」.
- **⑥ 더보기**: 「챕터 선택」 항목 삭제(챕터는 타이틀 CHAPTER 칩) · 「컬렉션」은 `CollectionUI.Open(null, 1)` 로 **포토카드(모은 사진) 탭**에서 열림 · 「레코드」 옆 새 항목 점(•) 제거 · 「오프닝」 → **「시네마」**: 신규 `UI/CinemaSelect.cs` — 오프닝 영상 + 챕터 1~20 오프닝/클로징 VN(대본 있는 것만) 스크롤 목록, 챕터 N 오프닝은 세이브 챕터 ≥ N, 클로징은 그 챕터 클리어(또는 지난 챕터)면 해금, 비밀코드(devUnlockAll)면 전부. 잠긴 카드 회색+토스트. 다시보기는 `ChapterVN.HoldBlackOnNext=false` 로 러너로 안 넘어감. 캡처 r42_more2(더보기 5항목), r42_cinema1(목록), r42_cinema2(CH1 오프닝 재생 → SKIP → 타이틀 복귀 확인), r42_coll1(포토카드 탭).
- 커밋 대상: UI/RunHudChrome.cs, Economy/CoinPickup.cs, UI/UI_FinalDestinationController.cs, Core/StageManager.cs, Visual/PickupFloat.cs, UI/CoastHudLayout.cs, UI/MainMenuController.cs, UI/CinemaSelect.cs(+meta), Assets/Resources/CoastRun/Fonts/Jua-Regular.ttf + LICENSE_Jua_OFL.txt(+meta), Docs/HANDOVER

### 2026-09-10 22:10 — 43차: 사용자 8건 (컴파일 0, 캡처 r43_*, 커밋은 사용자) — ②펫 3D 는 보류(아래)
- **① 낮→밤 전환**: 에디터에서 밤 챕터(6·9)를 직접·K-POP 두 경로로 돌려 봐도 조명은 첫 프레임부터 밤(`LoadStage` 의 `ResetLightingTo` 즉시 적용). 사용자가 본 "낮"은 **페이드 베일에 그려지는 로딩 그림(UI_Loading_Mock, 낮 풍경)** — 41차에서 카드를 베일 위(560>500)에 즉시 띄우자 낮 그림 위에 「챕터 N 시작」이 뜨고 걷히면 밤이 되어 그렇게 보였다. → `AnnounceStartCo` 가 베일 알파 < 0.45 까지만(최대 0.6초) 기다린 뒤 카드(41차의 3.5초 대기와 다름). 캡처 r43_s0: 챕터 6 첫 프레임 밤 + 카드.
- **② 펫 3D — 보류**: 진짜 3D 모델 생성은 Blender MCP 의 Hyper3D Rodin / Hunyuan3D 인데 (a) Blender 가 꺼져 있고 (b) `.env` 에 RODIN/HUNYUAN 키가 없다(KLING·PEXELS·TELEGRAM 만). 프로젝트에 있는 3D 펫은 `Pet_Sparrow.fbx`·`Pet_WildGoose.fbx` 2종(갈색 저폴리, 40차-c에서 사용자가 싫어해 그림으로 바꿈)뿐. **다음 세션**: Blender 켜고 Hyper3D 키(https://hyper3d.ai 무료 크레딧) 를 `.env` 에 넣으면 `generate_hyper3d_model_via_images` 로 Kling 정면·뒷면 그림 → GLB → `Resources/CoastRun/Pet_*.fbx` 로 7종 교체 가능(PetCompanion 은 FBX 있으면 그쪽 경로 유지). 그때까지 뒷모습 스프라이트.
- **③ 커서(주인공) 안 움직임**: `MobileSwipeInput.PollTouch` 가 `Input.touchCount` 만 읽어 에디터·PC 마우스 드래그로는 조작이 안 됐다 → 마우스 버튼을 손가락 하나로 흉내(누름 Began / 드래그 Moved / 뗌 Ended, fingerId −1 은 `IsPointerOverGameObject()` 로 UI 판정). 폰 터치 경로는 그대로.
- **④ 동전 두께 70% 축소**: FBX 코인은 Y 90° 회전이라 두께축 = 메시 로컬 X → `localScale.x *= 0.3`(몸통 ±0.17→±0.05), 면 그림 halfThick 0.18→0.062. 캡처 r43_fever2 확대: 얇은 원판.
- **⑤ 피버 3배**: `CoinSpawner` — 피버 중 3레인 금화 카펫 1.5 m 간격(평소 한 레인 7~9개/40 m → 카펫만 80개/40 m, 8배↑), `JellySpawner` — 3레인 지그재그 말랑이 1.2 m 간격. 둘 다 평소 패턴 위에 추가, 점유표 검사 없음(피버 흡입이 다 먹음). 캡처 r43_fever2: 코인 +475/6초.
- **⑥ 피니시 라인 Kling**: `Tools/KlingGen/out/finish/…_1.png`(기둥+FINISH 현수막+풍선) → rembg + 오른쪽 기둥(그늘져 반투명 판정) 흰색 마스크 강제 + 기둥 사이 도로 제거 → `Resources/CoastRun/Obs_FinishGate.png`(1013×573 RGBA). `FinishRibbon.Spawn`: 그림이 있으면 절차 기둥·현수막·풍선 대신 도로 폭(half×2+0.6) 고정 판 하나(ChromaUnlit, 양면, 빌보드 아님), 리본·관중·통과 뒤 위로 사라지는 규칙 그대로. 캡처 r43_gate2.
- **⑦ 클리어 얼굴**: 320→256(−20%), 중심 x 622→524(오른쪽 끝 652 ≤ 인셋 664 — 전엔 782 라 잘림), 말풍선 520/−490. 캡처 r43_clear1.
- **⑧ 챕터 선택 등급**: 별 4개 대신 `trackGrade` 로 「S 통과 / A 통과 / B 통과 / C 통과 / 미통과」(S 는 주황금). 캡처 r43_chsel1(에디터 프로필은 전부 S).
- 커밋 대상: Input/MobileSwipeInput.cs, Core/StageManager.cs, Economy/CoinPickup.cs, Economy/CoinSpawner.cs, Economy/JellySpawner.cs, UI/StageClearUI.cs, UI/KpopChapterSelect.cs, World/FinishRibbon.cs, Assets/Resources/CoastRun/Obs_FinishGate.png(+meta), Tools/KlingGen/out/finish*/, Docs/HANDOVER

### 2026-09-10 23:55 — 46차: 챕터 선택·미니게임 메뉴 시안 적용(Kling 메달·아이콘) · 차/버스 빈도 +50% (컴파일 0, 캡처 r46_*, 커밋은 사용자)
- **① 챕터 선택**(`UI/KpopChapterSelect.cs`) 사용자 시안대로: 카드 = 상단 「챕터 N」 + 가운데 그림 + 아래 상태 글자. **COMPLETED**(계절색 카드 + 등급 메달 `UI_Medal_S/A/B/C.png` — trackGrade 4/3/2/1) / **CURRENT**(금색 카드 + 자물쇠 `UI_Lock_Q.png`, 해금됐지만 미클리어) / **UNREACHED**(진회색 + 자물쇠). 선택 카드는 밝은 금 테두리+확대(시안의 글로우). 아래 핑크 버튼 「챕터 N 도전하기!」. 메달·자물쇠는 Kling 1:1 생성 → rembg(`Tools/KlingGen/out/chsel/`). A 메달은 첫 생성이 "44"로 나와 재생성. 에디터 프로필은 전부 S라 COMPLETED 만 보임(r46_chsel) — 새 프로필로 CURRENT/UNREACHED 확인 권장.
- **① 미니게임 메뉴**(`ChapterMissionUI.OpenMenu`): 카드 왼쪽에 Kling 아이콘 `UI_MG_{Marbles,Yut,Tuho,Ddakji,Mugunghwa}.png`(구슬 그릇·윷 4개·투호 항아리·딱지 2장·무궁화), 글자 오른쪽으로.
- **② 차·버스 빈도 +50%**(`Economy/ObstacleSpawner.cs`): 차 사이 줄 수 계산에 `CarFreqMul = 1/1.5` — 시작 5→3줄, 끝 3→2줄(챕터 난이도 배율·17챕터 이후 연속 2대 규칙 그대로). 종류 비율(밴/버스 30~40%/킥보드/귤)은 그대로.
- 커밋 대상: UI/KpopChapterSelect.cs, Raising/MissionMiniGames.cs, Economy/ObstacleSpawner.cs, Assets/Resources/CoastRun/UI_Medal_{S,A,B,C}.png · UI_Lock_Q.png · UI_MG_*.png ×5(+meta), Tools/KlingGen/out/chsel/*, Docs/HANDOVER

### 2026-09-11 00:45 — 47차: 펫 Blender 입체 4종 · 타이틀 로딩 → 옛 오프닝 버스 영상 · 2단 점프 · 옛 원고 정리 · 챕터 화면 비교 (컴파일 0, 캡처 r47_*, 커밋은 사용자)
- **① 펫 입체(Blender)** — Blender MCP 는 "Not connected"(Blender 창이 안 켜져 있음) → **헤드리스 파이프라인**으로 대체: `Tools/blender/pet_kit.py`(참새·기러기·흑돼지·오토바이 깡패 4종, Kling 정면 그림 색·비율 기준 치비 조형, 큰 눈·둥근 몸) + `Tools/blender/build_pets.bat`(blender.exe -b) + **Unity 메뉴 `Coast Run/Build Pets 3D (Blender)`**(`Editor/BlenderKitMenu.cs`, 원격 `unity_cmd "menu Coast Run/Build Pets 3D (Blender)"` 도 됨, 로그 `Tools/blender/pet_log.txt`) → `Resources/CoastRun/Pet_{Sparrow,WildGoose,BlackPig,BikerThug}.fbx` 생성 확인(exit 0).
  - `Player/PetCompanion.Build`: **FBX 최우선**(그림 Obs_Pet_* 는 FBX 없을 때만). 래퍼 `Body` 아래에 FBX, `PetFbxFix = Euler(0,0,180)*Euler(90,0,0)` — FBX 가 얼굴 +Y·정수리 +Z 로 들어와서(에디터 로그 `[Pet]` 로 확인) 세워 준 것. 결과: 얼굴이 진행 방향(+Z) = **카메라에는 뒷모습**(r47_pet8: 갈색 등·볏·날개, 정면 없음). 높이 참새 0.58·기러기 0.85·흑돼지 0.62·깡패 1.35. 스쿠터 바퀴 `WheelF/WheelB` 도 이름으로 찾아 스핀.
  - 남은 것: 참새만 실제 확인(에디터 세이브 펫). 기러기·흑돼지·깡패는 상점에서 바꿔 확인 필요. 조형이 마음에 안 들면 `pet_kit.py` 수치만 고치고 메뉴 다시 실행(1분). Hyper3D/Hunyuan 키는 여전히 없음.
- **② 타이틀 로딩 삭제 → 버스 영상**: 옛 컷씬 영상 중 「멀리 해안도로를 버스가 지나가는」 컷 = `Video/VID_CH01_Open.mp4`(6초) 를 `Resources/CoastRun/Title_Bus.mp4` 로 복사. `UI/MainMenuController.BuildSplash`: `Title_Bus` VideoClip 이 있으면 로딩 그림·퍼센트·바 없이 **영상 한 번(≈5.6초, 탭하면 건너뜀, "터치하면 건너뛰기")** → 페이드 → 타이틀. 없으면 옛 로딩 화면 그대로. **에디터 캡처로는 스플래시 구간이 안 잡혔음**(unity_shot 이 타이틀 완성 프레임만 돌려줌, 에디터 전용 `[Title]` 로그도 초반 로그가 유실) → **폰/에디터 눈으로 확인 필요**. 영상이 안 뜨면 Unity 가 mp4 를 VideoClip 으로 임포트했는지(Title_Bus.mp4.meta) 먼저 확인.
- **③ 2단 점프**: `Player/PlayerController.TryJump` — 공중(Air)에서 위 입력이 한 번 더 오면 `_doubleJumpUsed` 1회 허용, `_verticalVelocity = max(v,0)*0.15 + jumpForce*0.88`, `OnDoubleJumped` 이벤트(+OnJumped). 착지에서 리셋, 활공·피니시 제외. `Player/SkaterRig`: `HandleDoubleJump` → 0.45초 「허공 디딤」 포즈(`DoubleJumpPoseLate`: 앞 28% 두 무릎 가슴으로 웅크림 → 오른발로 허공을 아래로 쭉 밟고 왼무릎·양팔 위로 → 마지막 25% 복귀) + 스쿼시, `Visual/JuiceDirector.OnDoubleJump`(발밑 흰 퍼프+링+속도선+진동+SFX). 캡처 r47_dj4/dj5(timescale 0.15): 두 번째 도약·디딤 다리 확인. 원격 `unity_cmd jump` 두 번.
- **④ 옛 원고 정리** — 실제로 남아 있던 것: (a) **`Story/CutsceneController.cs` 옛 원고**("약속의 스마트폰 / 정류장 전광판 / 소녀의 결심 / 작년에도 / 16:40") → 문장 삭제, 새 대본(PRO)이 있으면 즉시 종료. (b) **`Core/SceneFlowController.OnStageClearContinue` 의 CH10·CH15 뒤 옛 시네마틱(CH1_Close·CH2_Close) 재생 분기 삭제**, 비스토리 경로의 `ChapterCutsceneBridge` 도 안 탐. (c) 파일: `Cut_*.png` 32장·`Video/VID_*.mp4` 10개·`StreamingAssets/Opening/open_*.mp4` 10개·`Docs/_archive_cutscenes_old/`(58씬)·`Docs/STORY_SCRIPT_2026-09-08.md`·`Docs/컷씬_스토리_요약_대화.md`·`Docs/_story_dump.txt` — **이 세션은 파일 삭제 불가** → `Tools/cleanup_old_cutscenes.bat` 갱신(Opening 클립 추가, Title_Bus 는 보존). **사용자가 더블클릭 1회**. 코드는 이미 참조 안 함.
  - 대본 txt 44~60(`END_A_SENSE/TRUST, END_B_TRUST/WEAK, END_TRUE, SIDE_*`)은 옛 원고가 **아님** — GameManager 엔딩 분기·사이드씬이 쓰는 현재 대본.
  - 남은 옛 스토리 문장(컷씬 아님, 게임 텍스트): `Meta/RandomEventTable`(“그 애가 병원 가는 날”, “서울 눈” 등 돌발 이벤트), `Raising/RaisingButler` 조언, `Content/PhotocardTable` 캡션, `AchievementTable`. 새 대본 톤으로 갈아끼울지 결정 필요 — 지우면 육성 이벤트가 비어서 손대지 않음.
  - **커밋 브릿지가 만든 중복 파일**: `Player/PlayerController-1.cs`·`Player/SkaterRig-1.cs`·`UI/MainMenuController-1.cs` 가 생겨 컴파일이 깨졌음 → 빈 파일로 덮어 컴파일 0. **`Tools/cleanup_dup_47.bat` 더블클릭으로 삭제**(안 지워도 동작엔 지장 없음).
- **⑤ 챕터 화면 vs 시안**: 비교 이미지 `Tools/_shots/r47_chsel2.png` ↔ `Tools/KlingGen/ref/ref_chapter.png`(39차 시안). 46차 시안(메달·COMPLETED/CURRENT/UNREACHED·「챕터 8 도전하기!」)은 **채팅 첨부라 파일로 안 남아 있음** → 픽셀 비교 불가. 다음 세션에 그 시안을 `Tools/KlingGen/ref/ref_chapter2.png` 로 저장하고 비교할 것. 이번엔 딤 0.66→0.70, 열려 있는 동안 타이틀 UI 숨김 훅(`KpopChapterSelect.TitleUi`) 추가 — 다만 제목 글자는 `_uiCg` 밖에 있어 아직 비침(시안도 K-POP Play 바는 비치므로 큰 차이는 아님). 그 외 배치(5×4·계절색·CHAPTER 금색·X·핑크 버튼)는 일치.
- 커밋 대상: Player/PlayerController.cs, Player/SkaterRig.cs, Player/PetCompanion.cs, Visual/JuiceDirector.cs, UI/MainMenuController.cs, UI/KpopChapterSelect.cs, Core/SceneFlowController.cs, Story/CutsceneController.cs, Editor/BlenderKitMenu.cs, Assets/Resources/CoastRun/Title_Bus.mp4(+meta), Assets/Resources/CoastRun/Pet_{Sparrow,WildGoose,BlackPig,BikerThug}.fbx(+meta), Tools/blender/pet_kit.py, Tools/blender/build_pets.bat, Tools/cleanup_old_cutscenes.bat, Tools/cleanup_dup_47.bat, Docs/HANDOVER, Tools/Telegram/queue.txt. (`*-1.cs` 3개는 bat 로 지운 뒤 커밋)

### 다음 세션 할 일(순서)
1. `unity_status` → `unity_compile` 0 확인. 사용자: `Tools/cleanup_dup_47.bat`, `Tools/cleanup_old_cutscenes.bat` 실행 후 커밋(47차).
2. 타이틀 버스 영상 스플래시 눈으로 확인(01_Title 플레이 첫 6초). 안 나오면 `Title_Bus.mp4` 임포트/`[Title]` 로그.
3. 펫 4종 확인: 더보기 › 상점(또는 devUnlockAll)에서 기러기·흑돼지·깡패 장착 → 러닝 캡처. 뒷모습·크기·색 조정은 `pet_kit.py` → 메뉴 재실행.
4. 챕터 화면: 46차 시안 이미지 받아 `Tools/KlingGen/ref/ref_chapter2.png` 저장 → 캡처와 나란히 비교 → 차이 나는 부분(카드 그림·글자·배경) Kling/코드로 맞추기.
5. 옛 스토리 문장(RandomEventTable·Butler·포토카드 캡션·업적) 새 대본 톤으로 교체 여부 결정.

### 2026-09-11 17:30 — 48차: 모드 리워크 설계 문서(GD만, 코드 변경 없음)
- 환경 점검(세션 시작): MCP 4종(unity·kling·photoshop·blender) 연결 OK, `unity_status` 브릿지 연결·컴파일 에러 0(씬 00_Boot, `[Title] Title_Bus clip=6.0s / splash video prepared` 로그 확인 → 47차 버스 스플래시는 재생됨), `kling_ping` OK. **Claude 앱 1.52386.0 — 컴퓨터 제어 승인 카드 정상(승인 버튼 보임)**, Telegram 승인 통과 → 텔레그램 보고는 큐가 아니라 데스크탑 창으로 직접 전송.
- ⚠ `device_bash` 폴더 마운트 실패(9/8 Windows 업데이트 영향, 앱 안내 메시지). 이 세션은 파일 읽기 = `device_stage_files`, 쓰기 = `SendUserFile` + `device_commit_files` 로 우회. 다음 세션도 같으면 이 방식으로.
- ⚠ 보안(사용자 지시): **이 프로젝트 작업은 폴더가 있는 PC(daniel)에 직접 링크된 세션에서만.** 이 세션은 다른 PC(같은 계정)에서 daniel 에 링크한 상태였고 사용자가 이번 건만 진행을 승인. 앞으로 원격 세션에서 작업 요청이 오면 먼저 그 점을 알릴 것. 사용자에게 안내한 조치: daniel 의 Claude 앱 설정 → 일반 「컴퓨터 사용 활성화」·원격 작업 파일 접근 토글 끄기, 다른 PC 로그아웃, 링크된 옛 대화 삭제.
- **산출물: `Docs/DESIGN_MODE_REWORK_v1.md`** — `Docs/DESIGN_BRIEF_KPOP_STORY_MISSIONS.md` 대응. 요지: ① K-POP = 「한 곡 달리기」(곡 길이 ≈90초 = 세션, 곡 끝 = 리본, 후렴 = 꼬마 피버 자동 제안) ② 「오늘의 런」(`ArcadeKind.Daily`)의 날짜 시드·조건 3·도장·스트릭 코드를 K-POP이 흡수, Endless 는 「자유 달리기」 토글로 ③ 스토리 = 챕터당 이야기 비트 1(연성, CH20만 경성) + 별 2, 새 `MissionKind` 6종(구간 무피격·편지 호위·초·돌탑·페이서·꼬마 없이), `[비트]` 컷씬 태그, CH1~20 표 ④ 미니게임 5개 유지(문구만 새 대본) ⑤ P0/P1/P2 페이징·리스크·Open questions 8개.
- 코드 확인 사실(설계 근거): `StageManager` 169·177·267·271·362행이 `!ArcadeRun.Active` 로 리본·피버·클리어를 막고 있어 **지금 K-POP 은 사망으로만 끝남**(`NoteKpopClear` 는 사실상 호출 경로 없음). 새 대본(`Tools/Story/script`) 인물은 하늘·바다·도윤·엄마·아빠인데 `StoryCond`(루아·만수·할머니·DJ)·`ChapterMission`(도담)·`RandomEventTable` 은 옛 인물 — 47차 미해결 5번과 같은 건.
- 48차-2(사용자 지시): **K-POP BGM = M2·M4·M7 중 매 런 랜덤 1곡**으로 설계 반영. `Resources/CoastRun/BGM`에 BGM_KPOP_* 파일은 없음(지금은 챕터 스템 폴백). 실측 길이 M2 232.9s / M4 97.1s / M7 201.6s → M4는 통째, M2·M7은 90초 창(`KpopTrackMeta start/length/chorus`). M2·M7이 보너스 레코드(S급 18/20 잠금)인 점은 Open question 1-b.
- 커밋 대상: Docs/DESIGN_MODE_REWORK_v1.md, Docs/HANDOVER.
- 다음: 사용자가 Open questions(트랙 확보·Daily 흡수·연성/경성·인물 정리) 답하면 P0 구현은 Cursor 세션에서.

### 2026-09-11 20:30 — 48차-3: K-POP P0 구현 (컴파일 0, 플레이 확인, 커밋은 사용자)
**텔레그램 보고 경로 변경(사용자 지시: "직접 입력하지 말고 .env 로 API 전송")**
- `Tools/Telegram/send_outbox.py`(신규) + `Editor/TelegramMenu.cs`에 **「Coast Run/Telegram/Send outbox」** 추가. 원격에서는 ① `Tools/Telegram/outbox.txt`에 본문을 커밋 → ② `unity_cmd "menu Coast Run/Telegram/Send outbox"` → PC의 python이 `.env` 토큰으로 sendMessage. 결과는 `outbox.result`·`sent.log`(`O\t…`)·에디터 로그 `[Telegram] outbox exit=0 True …`.
- ⚠ `device_commit_files`는 **같은 stagedPath를 다시 커밋하면 이전 내용이 올라가는 캐시**가 있음(첫 메시지가 2번 전송된 원인) → 메시지마다 `msg_<timestamp>.txt` 처럼 **새 파일명으로 커밋**할 것. 옛 큐(queue.txt 132~153번)는 미전송 상태 그대로(워처 안 켬).
- 샌드박스·VM 모두 api.telegram.org 403 확인(변함없음).
**K-POP 한 곡 달리기 P0 (설계 `Docs/DESIGN_MODE_REWORK_v1.md` §3, 사용자 확정: BGM = M2/M4/M7 랜덤, 미션 = 완주 고정 + 랜덤 2)**
- `Meta/ArcadeRun.cs`: `KpopTrackMeta`(num/start/length/chorusStart/chorusEnd) 표 3줄 — M2(0,90,55,80)·M4(0,97,55,82)·M7(0,90,55,80); `PickTrack`(직전 곡 제외, PlayerPrefs `CoastRun_KpopLastTrack`); `StartKpop`: `Seed=Today`, `MakeKpopConditions`(슬롯0 Finish 고정 + Coins/NearMiss/Combo/Hearts/MaxHits/Fever(+가끔 NoHit) 중 2), `ObstacleSpawner.SeedOverride = Today*7+chapter`, 코인 감쇠 `KpopCoinDecay`(오늘 4번째 런 ×0.5, 8번째 ×0.25 — `RunTuning.CoinMul`에 곱함); `TickKpop/KpopChorus/KpopOutro/MarkKpopFinished/NoteFever`; `OnKpopMissionDone` 이벤트; `SettleKpop`: 완주 시 오늘 첫 도장(`dailyStamps/dailyStreak/lastDailyDate` 재사용, +50G), 미션 비트 `kpopMissionDoneMask` 누적(런을 넘어), 3개 다 되면 올클리어(+100G, 장식 25%, 하루 1회), `dailyBestScore/kpopTodayBest/kpopBestByChapter`. 점수 = 기존 + 완주 300 + 무피격 200.
- `Meta/MissionTable.cs`: `MissionKind.Finish, Fever` 추가(스토리 별미션엔 안 씀). `Meta/SaveData.cs`(MetaProfile): `kpopMissionDoneMask/Date, kpopAllClearDate, kpopRunsToday/Date, kpopTodayBest/Date, kpopBestByChapter[20], kpopSongsFinished`. `Meta/RecordTable.TitleOf(num)`.
- `Core/StageManager.cs`: K-POP에서도 `FeverMode.ArmForStage`; `UpdateKpop()` — 후렴 진입 `FeverMode.ForceOffer()` + 배너 「후렴! 코인 ×2」, 마지막 8초 장애물 스폰 정지 + `FinishRibbon.Spawn(현재 + 속도×7.4s)` + BGM 3초 페이드, 리본 통과(또는 length+1.5s) → `KpopFinishCo`(리본 Break·FinishRun·픽업 정리·「한 곡 완주! ♪」) → `GameSession.EndKpopRun()`. `FinishPathZ`는 K-POP이면 리본 z. 시작 카드는 「♪ 곡 제목 / 미션 3」.
- `Core/GameSession.cs`: 아케이드 종료를 `ArcadeRunOver()`로 묶고 `EndKpopRun()` 공개. `Economy/FeverMode.cs`: `IsRacing` K-POP 허용, `ForceOffer()`, `Trigger`에서 `ArcadeRun.NoteFever()`. `Economy/CoinPickup.cs`: `KpopChorus`면 ×2. `Audio/CoastAudioManager.cs`: K-POP은 `ArcadeRun.KpopTrack.Clip`(BGM_M?)을 `time=start`부터, 재도전마다 reload, `SetRunBgmFade(초)`; `_kpopIndex` 제거.
- `UI/RunHudChrome.cs`: 미션 칩 3개(체력 바 아래, 달성 시 초록+체크+펀치), 결과 카드 K-POP 변형(「한 곡 완주! ♪」/부제 곡명/별=오늘 미션 수/도장·연속 N일째·+G·미션 3줄·올클리어/「한 곡 더 ♪」, 실패 시 「곡의 N%까지」). `UI/UI_FinalDestinationController.cs`: 여정 바 = 곡 진행, 타이머 「♪ m:ss」(후렴 분홍·마지막 10초 주황).
- 플레이 확인(캡처 `Tools/_shots/r48_*`): 타이틀 → K-POP Play(tap 0.5 0.13) → CH6 밤 코스, 칩 3개·♪1:27·꼬마 버튼 → 후렴 분홍 타이머 → 리본 → 「한 곡 완주!」 카드(도장 ✓ 1일째 +50G, 미션 1/3, 거리 1,107 m, 점수 1,877) → 「한 곡 더」 재도전 시 타이머 1:26 재시작·완주 칩 ☑ 유지. 곡은 M4가 나옴(랜덤). 에러 0.
- 남은 것(P0 밖): 타이틀 K-POP 바에 스트릭/오늘 미션 표시(`MainMenuController` 아트 바라 텍스트 자리 없음), 후렴 시각 실측(`KpopTracks` 상수만 고치면 됨), 피버 첫 제안이 8초에도 뜸(후렴 전용으로 하려면 `FeverMode.FirstOfferAfter`를 K-POP에서 크게), 「오늘의 런」 타이틀 진입 제거 여부, 도장 달력 UI.
- 커밋 대상: Assets/_CoastRun/Scripts/{Meta/ArcadeRun.cs, Meta/MissionTable.cs, Meta/SaveData.cs, Meta/RecordTable.cs, Core/StageManager.cs, Core/GameSession.cs, Economy/FeverMode.cs, Economy/CoinPickup.cs, Audio/CoastAudioManager.cs, UI/RunHudChrome.cs, UI/UI_FinalDestinationController.cs}, Assets/_CoastRun/Editor/TelegramMenu.cs, Tools/Telegram/send_outbox.py, Docs/HANDOVER. (`Tools/Telegram/outbox.txt·outbox.result`는 .gitignore에 추가 권장)

### 2026-09-11 21:10 — 48차-4: K-POP BGM 페이드아웃 = 리본 통과 기준 (사용자 지시, 컴파일 0, 플레이 확인)
- `Core/StageManager.UpdateKpop/KpopFinishCo`: 아웃트로 진입 때 걸던 3초 페이드 제거 → **리본 Break 순간 `SetRunBgmFade(2.2s)`**. 리본 위치 = 아웃트로 시점 속도 × 6.8초(곡이 끝나기 조금 전에 통과, 피격 감속 여유). 리본 도달 전엔 끝내지 않음(보호용 length+6초). 결과 카드는 1.8초 뒤(페이드 거의 끝난 뒤).
- 캡처 `Tools/_shots/r48_ribbon.png`: ♪ 0:01 에 리본 통과·「한 곡 완주! ♪」.
- 부수: HUD 미션 칩이 새 런 시작 때 오늘 누적 미션을 반영하지 않던 것(빌드 순서) → `ArcadeRun.IsKpopMissionDone(i)`(이번 런 ∪ 오늘 누적)로 칩 초기 상태 판정.
- 커밋 대상: Core/StageManager.cs, Meta/ArcadeRun.cs, UI/RunHudChrome.cs, Docs/HANDOVER.

### 2026-09-11 21:30 — 48차-5: 타이틀 BGM = M5 · K-POP 러닝 우하단 곡명 (사용자 지시, 컴파일 0, 캡처 확인)
- `Audio/CoastBgmLibrary.Menu()`: **BGM_M5 「돌아온 제주」**가 있으면 최우선(스플래시부터 메인 화면까지 `TitleAudio.PlayMenu`가 같은 곡을 튼다) → 없으면 BGM_Title → BGM_Menu.
- `Meta/ArcadeRun.KpopTrackMeta`: `Artist = "우히&히시"`, `Credit = 제목 + " — " + Artist`(M1. 같은 번호 없이 `RecordTable.TitleOf`). `UI/RunHudChrome.BuildKpopSongLabel`: 러닝 HUD **우하단 알약** 「♪ 제목 — 우히&히시」(글자 폭에 맞춤). 결과 카드 부제도 같은 표기. 캡처 `Tools/_shots/r48_song_label.png`.
- 커밋 대상: Audio/CoastBgmLibrary.cs, Meta/ArcadeRun.cs, UI/RunHudChrome.cs, Docs/HANDOVER.

### 2026-09-11 21:50 — 48차-6: 타이틀 BGM 켜자마자·끊김 없이 · 곡명 라벨 위치 (사용자 지시, 컴파일 0, 캡처 확인)
- `Audio/TitleAudio.cs`: BGM 소스를 **전역 `TitleBgm(Global)`(DontDestroyOnLoad)** 로 — `PlayMenuEarly()`(정적)를 `Core/BootLoader.Start`에서 호출해 **00_Boot 에서 M5 가 바로 시작**, 타이틀 씬 로드·스플래시·메인 화면까지 같은 소스가 이어진다. `PlayMenu`는 같은 곡이 이미 나오면 재시작하지 않음(끊김 방지). `StopMenu`(러닝/육성 진입, 레코드 재생)는 그대로.
- `UI/RunHudChrome.BuildKpopSongLabel`: 곡명 알약을 **우상단 동전 알약 바로 아래**(anchor 1,1 / y −132)로 이동. 캡처 `Tools/_shots/r48_song_label2.png`(「♪ 보조개 — 우히&히시」, 오늘 누적 미션 칩 ☑ 도 확인).
- 커밋 대상: Audio/TitleAudio.cs, Core/BootLoader.cs, UI/RunHudChrome.cs, Docs/HANDOVER.

### 2026-09-11 22:00 — 48차-7: K-POP 곡 시작 1초 지연 (사용자 지시, 컴파일 0)
- `ArcadeRun.KpopMusicDelay = 1f`. `CoastAudioManager.SetChapterStage`: K-POP이면 `PlayScheduled(dspTime + 1.0)`. `StageManager.UpdateKpop`: 곡 시계 `t = 경과 − 1초`로 후렴·아웃트로·리본·보호 종료를 판정(HUD ♪ 타이머도 1초 늦게 출발). 재도전도 동일.
- 커밋 대상: Meta/ArcadeRun.cs, Core/StageManager.cs, Audio/CoastAudioManager.cs, Docs/HANDOVER.

### 2026-09-11 22:50 — 48차-8/9: 첫 화면 버스 영상만·로딩 % 애니메이션 / 2단 점프 2배 높이 + 앞돌기 모션 (컴파일 0, 플레이 확인, 커밋은 사용자)
**48차-8 로딩 화면(사용자 보고: 폰 첫 실행 로딩 페이지가 잘못 나옴, 로딩 숫자 안 올라감)**
- `UI/MainMenuController.cs`: 옛 로딩 시안(UI_Loading_Mock)·로딩 키아트·로딩바·퍼센트 코드(`SetLoad`, `_loadFill/_loadCover/_loadPct/_loadLabel`, `LoadCoverW`) **전부 삭제**. 첫 화면 = `Title_Bus.mp4` 영상만, 영상 없으면 노을빛 단색 0.8초. 폰 첫 실행 디코더 준비 대기 3→6초.
- `Core/UIRoot.cs`: 씬 전환 베일(로딩 그림) 위에 **0→100% 숫자 + 바 애니메이션**(`LoadOverlay`, 720×1280 시안 좌표를 1.5배 컨테이너로, 1.3초 ease-out, 걷힐 때 100%). 
- ⚠ 폰에서 옛 로딩 화면이 보인 건 설치된 APK가 9/6 구버전(버스 영상은 47차)이기 때문일 가능성 큼 → **새 APK 빌드 후 확인**.
**48차-9 2단 점프**
- `Player/PlayerController.cs`: `DoubleJumpMul`(0.88) 삭제 → `DoubleJumpHeightMul = 2f`. 2단 점프 초기속도 = √(2g·(2·h1 − y0)) (h1 = 1단 최고 높이 jumpForce²/2g, y0 = 현재 높이; 최소 0.6·h1) → **최고점이 1단의 2배**.
- 모션 **두 가지 다 시도 → Mixamo 채택**:
  - Blender(MCP, 애드온 Edit→Preferences→Add-ons "MCP for Blender" 체크 필요했음): `Anim_Jump.fbx` 리그를 불러 19프레임 절차적 「웅크림+앞돌기」 액션을 키프레임 → `Anim_DoubleJumpBlender.fbx`(보관용, 컨트롤러에서 안 씀 — 빌드 용량 400KB, 지워도 됨).
  - Mixamo(내장 브라우저, Adobe ID 로그인은 사용자): **"Running Forward Flip"** In Place·30fps·Without Skin → `Downloads/Running Forward Flip.fbx` → **`Rig/Anim_DoubleJump.fbx`**. `Editor/MixamoImportSettings.cs`가 이 클립만 **10~27프레임(공중 구간, 15~23이 물구나무)**으로 트림, `BuildController`에 `DoubleJump` 트리거·상태(AnyState→DoubleJump, Grounded/끝나면 복귀, tag Jump) 추가. 메뉴 `Coast Run/Art/Build Skater animator (Mixamo clips)`로 두 컨트롤러 재생성함.
- `Player/SkaterRig.cs`: `HandleDoubleJump` — 컨트롤러에 `DoubleJump` 파라미터가 있으면 클립 트리거(Jump 트리거 리셋, 뒤따르는 OnJumped 의 Jump 트리거는 `DoubleJumpUsed` 면 생략), 없으면 47차 절차적 「허공 디딤」 유지. 공중 다리 거울 보정은 DoubleJump 상태에서도 건너뜀. 에디터 로그 `[DJ] double jump: clip=True`.
- 검증: 02_Run에서 `timescale 0.05` + `jump`(0.2초 뒤) `jump` → 애니메이터 상태 DoubleJump 0.6초 재생 확인, 캡처 `Tools/_shots/r48_fs4.png`(물구나무)·`r48_fl2.png`(2배 높이). ⚠ 원격 `jump` 두 번은 사이가 0.15초(게임 시간) 넘어야 2단으로 인식(코요테 0.12초).
- 커밋 대상: UI/MainMenuController.cs, Core/UIRoot.cs, Player/PlayerController.cs, Player/SkaterRig.cs, Editor/MixamoImportSettings.cs, Assets/Resources/CoastRun/Rig/{Anim_DoubleJump.fbx(+meta), RunnerAnimator.controller, SkaterAnimator.controller}, Docs/HANDOVER. (`Anim_DoubleJumpBlender.fbx`(+meta)는 제외/삭제 권장)

### 2026-09-11 23:10 — 48차-10: 비·눈 효과 강화 (사용자: "잘 안 보인다", 컴파일 0, 캡처 확인)
- `Content/WeatherFx.cs`: 비 = **속도 방향으로 늘어난 빗줄기**(`ParticleSystemRenderMode.Stretch`, velocityScale 0.09/0.11), 색·알파 ↑(0.82,0.90,1,0.85), 1600/s·속도 20~28·수명 1.6s(바닥까지만), 스폰 박스 24×36. 눈 = 흰색 α1, 크기 0.16~0.34, 700/s, 속도 1.8~3.2, 노이즈 흔들림. **근경 층 추가**(`RainNearFx` 320/s 굵은 줄, `SnowNearFx` 140/s 큰 송이, 카메라 앞 (0,−2.5,−5) 박스 9×8). 바람이면 눈 속도·양 ↑. 기울기(`_windTilt`) 근경에도 적용.
- `Content/SeasonWeatherDirector.cs`: 에디터 전용 **F9 = 비↔눈 토글**(`unity_cmd "key F9"`) — 원격 검증용.
- 캡처 `Tools/_shots/r48_wx1.png`(비)·`r48_wx2.png`(눈). 폰에서 과하면 `RainNearFx`/`SnowNearFx` emission 만 줄이면 됨.
- 커밋 대상: Content/WeatherFx.cs, Content/SeasonWeatherDirector.cs, Docs/HANDOVER.

### 2026-09-11 23:30 — 48차-11: 비·눈 절반으로 · 2단 점프 「뿡→쓩」 연출 (사용자 지시, 컴파일 0, 캡처 확인)
- `Content/WeatherFx.cs`: 48차-10 값의 **절반** — 비 800/s(근경 160), 눈 350/s(근경 70), 바람 눈 550/110. 빗줄기 알파 0.85→0.80.
- 2단 점프 연출: `Player/SkaterRig.HandleDoubleJump` → **Hips 뼈 뒤·아래**(엉덩이) 위치를 `JuiceDirector.OnDoubleJump(butt, transform)`에 넘김. `Visual/JuiceDirector`: 흰 퍼프 16 + 노르스름한 퍼프 8 터짐, 링 1.6, **`ButtJet` 코루틴 0.35초 동안 리그를 따라오며 아래로 퍼프 꼬리**, 스피드라인 30, FOV +4, 진동, 효과음 `CoastSfx.Boost`.
- `Audio/CoastAudioManager.cs`: `CoastSfx.Boost` + `ProceduralAudio.CreateWhoosh(0.34s)` — 앞 50ms 저음 툭(70→40Hz) + 220→1400Hz 지수 스윕 톤 + 저역 노이즈(「뿡→쓩」). `Resources/CoastRun/SFX/SFX_Boost.ogg` 를 넣으면 그 클립이 우선(ResourceClip).
- `Meta/ArcadeRun.ResetStatics`(RuntimeInitializeOnLoad SubsystemRegistration): 에디터 「도메인 리로드 없음」에서 KpopMode 등 정적 상태가 다음 플레이로 새던 것 방지(02_Run 직접 플레이가 K-POP 결과 카드로 끝나던 원인).
- 캡처 `Tools/_shots/r48_bj6.png`(엉덩이 퍼프 터짐).
- 커밋 대상: Content/WeatherFx.cs, Player/SkaterRig.cs, Visual/JuiceDirector.cs, Audio/CoastAudioManager.cs, Meta/ArcadeRun.cs, Docs/HANDOVER.

### 2026-09-11 23:45 — 48차-12: 비·눈 왔다 안 왔다 (러닝 시간의 약 50%) (사용자 지시, 컴파일 0, 캡처 확인)
- `Content/WeatherFx.cs`: 날씨가 비/눈일 때 **ON 10~18초 ↔ OFF 10~18초** 사이클(평균 50%), 2초에 걸쳐 방출량 페이드(`_precipMul` → `ApplyPrecipMul`). 비/눈으로 바뀌는 순간은 항상 '오는 중'(12~18초)으로 시작. HUD 날씨 칩은 「비」 그대로(날씨 자체는 유지, 강수만 쉼). 기본 방출량은 `_rainBase/_rainNearBase/_snowBase/_snowNearBase`(바람이면 눈 550/110).
- 캡처 `Tools/_shots/r48_pc1.png`(비 ON) → 22초 뒤 `r48_pc2.png`(같은 「낮·비」인데 강수 OFF).
- 커밋 대상: Content/WeatherFx.cs, Docs/HANDOVER.

### 2026-09-12 00:40 — 48차-13: 미니게임 3D 리디자인 파일럿 — 구슬치기 (사용자 지시 · 하이브리드 · 컴파일 0 · 캡처 확인)
- **방식(사용자 선택: 하이브리드, 구슬치기 1개 먼저)**: 게임 규칙·시뮬레이션·버튼·힘 게이지는 45차 2D(정규화 0..1) 그대로 두고, **보이는 것만** 3D 무대로 바꿨다.
  - `Raising/MiniStage3D.cs`(신규): 마당(`Field`) 안에 RawImage 1장 ← 전용 카메라(pitch 56°, FOV 38, 10 m) RenderTexture. 무대는 y=-3000 에 두어 러닝 씬과 안 겹침. **Kling 배경판**은 카메라 앞 40 m 고정 쿼드(그림 원근 그대로), 소품만 3D. `GroundPoint(정규화 xy)` = 뷰포트 레이 ↔ 바닥 평면 교점 → 2D 좌표와 항상 일치. `Spawn(res)`(FBX 를 피벗으로 감싸 임포트 회전 보존), `Bar`(분필 선), `Blob`(소프트 그림자), `Lit(color, smooth, metal, transparent)`(URP Lit, 환경반사 OFF), `OpaqueAlpha`.
  - 무대 카메라 렌더 중에만(beginCameraRendering) **안개 OFF·씬 조명 전부 OFF·환경광 Flat 고정** → 낮/밤/날씨와 무관하게 같은 밝기.
  - **RT 알파 함정**: 반투명(유리·그림자)이 RT 알파를 깎으면 RawImage 가 그 자리를 뚫어 뒤 크림 프레임이 비쳐 전부 파스텔로 바랬다 → 투명 머티리얼의 알파 블렌드를 `_SrcBlendAlpha/_DstBlendAlpha = One/One` 로(`OpaqueAlpha`).
- **에셋**: Blender 키트 `Tools/blender/minigame_kit.py` → `Resources/CoastRun/MG_Marble.fbx`(Glass 구 + Swirl 나선 리본 + Core 구), `MG_Arrow.fbx`(납작 화살표, Unity 에서 -Z 향해 코드에서 +180°). Kling(`kling_image`, 1:1 2장 `Tools/KlingGen/out/raw/mg_ground/`) → 1번 중앙 3:4 크롭 `Resources/CoastRun/UI_MG_Yard_Marbles.png`(768×1024, 조약돌 테두리 제주 마당).
- `Raising/MissionMiniGames.cs` `MarblesMission`: 구슬 8+1 = 3D 유리 구슬(색 유리 α0.38 + 어두운 소용돌이 + 코어, 반지름은 45차 34px 감각의 1.8배), 굴러갈 때 이동량만큼 회전, 밖으로 나가면 0.75배·유리 탁하게. 조준선 → 3D 주황 화살표. 분필 삼각형 = 흰 막대 3개. 발사 SFX Jump, 충돌 SoftHit. 힘 게이지·버튼·상태문·판정(5/8, 3발) 그대로.
- `Editor/MiniGameDevMenu.cs`(신규): 플레이 중 `Coast Run/Dev/Mission - Marbles|Yut|Tuho|Ddakji|Mugunghwa` → 원격 `unity_cmd "menu ..."` 로 바로 열기. 소개 카드 「시작!」은 `tap 0.5 0.41`, 발판 버튼은 `tap 0.8 0.095`.
- 캡처: `Tools/_shots/mg3d_13.png`(무대) · `mg3d_14/15.png`(발사 → 정지 → 다음 발). 러닝 중에 열면 뒤 런의 코인 팝업이 겹쳐 보이는 건 개발 메뉴 상황일 뿐(실제 흐름은 런 종료 후).
- **주의(에디터)**: 플레이 중 재컴파일하면 `ChapterMissionUI` 정적 `_canvas` 가 초기화돼 옛 미션 캔버스·무대가 남는다(유령 구슬). 정지 → 재생으로 확인할 것. `MiniStage3D.Create` 는 마당을 잃은 무대를 치우긴 한다.
- **남은 것 / 다음**: 사용자 스타일 확인 후 같은 키트로 윷놀이(윷가락·말판 3D)·투호(항아리·화살 포물선)·딱지치기(딱지 뒤집기)·무궁화(술래 NPC Kling) 확장. 육성 화면 `HomeMiniGames`(윷/구슬/공기)는 아직 2D — 같은 MarblesMission 3D 를 옮기면 됨. 유리 겉면 뿌연 느낌이 싫으면 `glassC` α 0.38→0.25.
- 커밋 대상: Raising/MiniStage3D.cs(신규), Raising/MissionMiniGames.cs, Editor/MiniGameDevMenu.cs(신규), Tools/blender/minigame_kit.py(신규), Resources/CoastRun/MG_Marble.fbx·MG_Arrow.fbx·UI_MG_Yard_Marbles.png(+.meta), Docs/HANDOVER.

### 2026-09-12 01:50 — 48차-14: 사용자 시안 4장 반영 (①인도 ②포토카드 ③뮤직 컬렉션 ④구슬치기) (컴파일 0, 캡처 확인)
- 원칙: ②③④는 **시안 이미지를 화면 배경 아트로 그대로** 깔고(`Resources/CoastRun/UI_Photocard_Mock.png`(1152×2128→1080×1920, 사진·캡션·수집수 영역은 종이색으로 비움), `UI_Music_Mock.png`(1056×2208, 좌우 93px 거울 확장 후 1080×1920, 행 제목 영역 비움)), 실제 데이터만 그 위에 얹었다. 시안 px→디자인(720×1280) 변환은 `CollectionUI.Mock.cs M()/MRect()`, `CollectionUI.Music.cs MM()/MMRect()`. 원본 시안: `Tools/Art/_mock/`.
- **④ 구슬치기**(`Raising/MissionMiniGames.cs` MarblesMission): 마당 0.29~0.92 · 발판 0~0.28 로 키움. 네온 삼각형(발광 URP Lit + 바닥 소프트 글로우 쿼드 `MiniStage3D.SoftDisc`), 보석 구슬(채도 8색 유리 α0.62 + 어두운 소용돌이 + 발광 코어 + 반짝 원판), 점선 조준(노란 구슬 7개 + 화살촉, 화살촉은 -Z 향해 +180°), 하단 패널 = [방향 선택 반원 게이지(0/90/180, 빨간 바늘, 각도)] [힘 선택 세로 무지개 게이지 + %] [발사! 큰 노란 버튼]. 버튼은 「방향 확정」→「발사!」→「굴러가는 중」. 배경 `UI_MG_Yard_Marbles.png` 는 황혼 톤으로 그레이딩. 벽 오른쪽 한계 0.90→0.97(힘 게이지가 마당 밖으로 나감).
  - `MiniStage3D.SoftDisc(color)`: Particles/Unlit 은 무대 RT 에서 α<0.55 가 안 보여(원인 미확인) URP Lit 투명 + `BlobShadow.SoftDisc()` 로 교체. 그림자·글로우·반짝이 모두 이것.
- **② 포토카드**(`UI/CollectionUI.Mock.cs`, partial): 컬렉션(포토카드) 열면 대표 카드(가장 최근 얻은 공개 카드) 화면. 카드 아트(`Card/Card_NN.png`)가 이미 폴라로이드(사진+캡션)라 종이 영역(시안 232~903 × 372~1195) 전체를 덮는다. SSR 배지는 시안 그림 그대로, 그 외 등급은 종이색으로 덮고 알약. COLLECTION n/9. 아래 3칸 = 다음 카드 3장(소유 = 썸네일+등급+NEW, 미소유 = 시안 자물쇠 그대로). 버튼은 시안 그림 위 투명 히트: 이미지 저장 공유(`SaveCardImage` 캡처 영역 = 폴라로이드+프레임) / → 바인더(기존 3×3 그리드) / ← 닫기. 그리드 카드 탭 → 이 화면(`OpenCard` → `ShowMockCard`; 옛 앞뒤 뒤집기 상세는 `OpenCardLegacy` 로 보존).
- **③ 뮤직 컬렉션**(`UI/CollectionUI.Music.cs`): 더보기›레코드(`CollectionUI.Open(_,0)`) → 시안 화면. 7행(시안 x147~920, y0=500+203.3·i, 높이 167) 제목(12자 넘으면 15pt)·부제(♪ noteKo)·NEW, PLAY(시안 693~887) 재생 중이면 주황 「■ STOP」, 잠김은 회색 「잠김」+행 어둡게. COLLECTED n/7. 「K-POP 전체재생」= 열린 곡 1→7 이어 재생(`PlayAllCo`, 진행 중이면 주황 「■ 전체재생 중지 · n/7 · 지금 곡명」). 좌상단 < = 닫기.
- **① 러닝 건물 옆 인도**(`World/PromenadeSegmentBuilder.cs`): 시안은 차도와 같은 돌포장이 건물 앞까지 이어진 모습 → `SidewalkL` 을 도로 텍스처 재질(`SidewalkMaterial()`, 타일 크기 동일·18% 밝게)로, 연석 `CurbL` 은 낮은 옅은 돌색 한 줄, 건물 기단 앞 0.5 m 크림 띠 `SidewalkBase`. 캡처 `run_side4.png`(전) ↔ 시안 비교 `run_side1.png`(후).
- 개발 메뉴(`Editor/MiniGameDevMenu.cs`): `Coast Run/Dev/Collection - Photocards | Records | Unlock all (F9)` 추가.
- 캡처: `Tools/_shots/mg3d_v2d.png`(구슬치기) `mg3d_v2b/c.png`(힘·발사) `pc_mock2.png`(포토카드) `mu_mock4.png`(뮤직·전체재생 중) `run_side4.png`(인도). 러닝 중 개발 메뉴로 열어 뒤 런의 코인 팝업·「거인 무적!」이 겹친 건 캡처 상황일 뿐.
- 주의: 시안 배경에 한국어 버튼 글자가 그려져 있어 영어 모드에서도 한국어로 보인다(「이미지 저장 공유 / 바인더 / 닫기 / K-POP 전체재생」). 영어판이 필요하면 시안을 영어로 재생성해 `_EN` 으로 두고 `CoastUiArt.Art` 이름만 갈아끼우면 됨.
- 커밋 대상: Raising/MissionMiniGames.cs, Raising/MiniStage3D.cs, UI/CollectionUI.cs, UI/CollectionUI.Mock.cs(신규), UI/CollectionUI.Music.cs(신규), World/PromenadeSegmentBuilder.cs, Editor/MiniGameDevMenu.cs, Resources/CoastRun/UI_Photocard_Mock.png·UI_Music_Mock.png·UI_MG_Yard_Marbles.png(+meta), Tools/Art/_mock/*, Docs/HANDOVER.

### 2026-09-12 02:20 — 48차-15: BGM 정리 — BGM_M1~M7 만 남김 (사용자 지시, 컴파일 0)
- `Resources/CoastRun/BGM/` 에서 M 번호 곡 7개(+meta)와 README 만 남기고 34개 삭제(챕터 스템 BGM_CH1~5_a~d, BGM_Cine_*, BGM_End_*, BGM_Memory_*, BGM_Menu, BGM_Opening, BGM_Title). 삭제는 에디터 메뉴 `Coast Run/Audio/Delete non-M BGM`(`Editor/BgmCleanupMenu.cs`, AssetDatabase.DeleteAsset — 원격 VM 마운트가 깨져 있어 이 방식) — 정규식 `^BGM_M\d+\.` 만 보존.
- 러닝 BGM 폴백: 스템이 없으므로 `CoastAudioManager.SetChapterStage` 가 앨범 Track_* 도 없을 때 `CoastBgmLibrary.ChapterFallback(metaStage)` = `BGM_M{(stage-1)%7+1}` 을 풀 트랙으로 튼다(스테이지마다 다른 곡). 타이틀은 그대로 M5, K-POP 은 M2/M4/M7. `CineOpen/CineClose/Memory` 헬퍼는 호출처가 없어 그대로 두었고(파일 없으면 null → 절차적 대체), README 갱신.
- 커밋 대상: Editor/BgmCleanupMenu.cs(신규), Audio/CoastBgmLibrary.cs, Audio/CoastAudioManager.cs, Resources/CoastRun/BGM/(삭제 34개 + README.md), Docs/HANDOVER.

---

### 2026-09-12 — 49차: BGM 5곡 추가 · 러닝 오른쪽(바다 쪽) 리빌드 · 미니게임 4종 3D + 진입 화면 리디자인 · 컬렉션 클릭음 제거 (컴파일 0, 캡처 확인, 커밋은 사용자)
- **환경**: `device_bash`(PC 리눅스 VM)는 여전히 불가 → PC 쪽 파일 작업은 **Blender MCP `execute_blender_code` 안에서 `subprocess` 로 PC 파이썬(3.12, PIL/numpy/cv2 있음)을 돌려** 처리(키잉·타일링·시트 만들기·FBX 내보내기). `Tools/Art/coast49_assets.py`, `Tools/blender/minigame_kit2.py` 가 그 산출물.
- **① BGM**: 업로드 wav 5곡 → -14 LUFS 통일·50ms 페이드인·Vorbis 128k → `Resources/CoastRun/BGM/BGM_M8~M12.ogg`(+meta).
  - K-POP 런 풀에 M8·M11·M12 추가(`ArcadeRun.KpopTracks`, 창 90초, 후렴은 파형 에너지 기준 기본값 — M8 37~62 / M11 50~75 / M12 63~88, M8 은 앞 10초 인트로 건너뜀). 제목은 `RecordTable.Extra`(레코드 목록엔 안 올림).
  - 스토리 러닝 BGM = M9(홀수 스테이지)/M10(짝수) 만(`CoastBgmLibrary.Story`, `CoastAudioManager.SetChapterStage`). 앨범 Track_*·M1~M7 돌려쓰기 삭제.
- **② K-POP 시작 「찌꺼기 소리」**: 원인 두 가지 — (a) 챕터 소개 카드에서 `SFX_ChapterClear` 스팅어가 곡보다 1초 먼저 울림 → K-POP 은 생략(`StageManager`), (b) 합성 앰비언트 드론(220Hz)+바람이 곡 앞 정적에서 들림 → K-POP 은 0, 소스 생성 시 초기 볼륨 0(`CoastAudioManager`).
- **③ 러닝 오른쪽 텍스처**(`World/PromenadeSegmentBuilder.Coast.cs` 신규 + Sea/Hill/Field/Rock 수정): 하늘색 철난간·돌 절벽 상판·색 박스 경사 → **짙은 나무 울타리 + 낮은 현무암 돌담 + 카툰 풀 텍스처 둔덕 + 억새/유채/현무암 더미 그림 포기(Kling, `Obs_SilverGrass/Canola/BasaltPile`) + 바위 해안 경사(`Tex_Shore_Basalt`)·포말·바위 + 검은 바위섬**. 킷 돌담 조각(L자로 보이던 것)은 이어지는 돌담 한 줄로. 텍스처 `Tex_Grass_Meadow`(돌 인페인트+타일링), `Tex_Shore_Basalt`(갈색→현무암 톤). 캡처 `Tools/_shots/r49_coast2.png`.
- **④ 미니게임**(`Raising/MissionMiniGames.Games3D.cs` 신규, 2D 4종 삭제): 공통 틀 `Stage3DMission`(마당 0.29~0.92 3D 무대, 발판 남색 카드 3분할 + 큰 노란 버튼, `VGauge/HalfGauge/Card/BigButton/Sprite/Chalk`).
  - 윷놀이: 멍석(`UI_MG_Yard_Yut`) 위 `MG_YutStick` 4개 튀어 올라 돌다 앉음(배=밝음/등=갈색), 말판 20칸 분필 원+선, `MG_Token` 말. 패널 [윷 결과][한 바퀴 경주 바][던지기!].
  - 투호: 한옥 마당(`UI_MG_Yard_Tuho`) `MG_Jar` + `MG_TuhoArrow` 포물선(꽂힘/누움), 패널 [방향 반원][힘 세로+흰 띠][방향 확정→발사!], 남은 화살 핍.
  - 딱지치기: 골목(`UI_MG_Yard_Ddakji`) `MG_Ddakji` 파랑(바닥)/빨강(손) 내리치기·뒤집힘, 패널 [타이밍 게이지+노란 띠][하트 3][내리치기!].
  - 무궁화: 운동장(`UI_MG_Yard_Mugunghwa`) 술래 = 집사 꼬마 앞/뒤 그림(`UI_Butler_Boy` / `MG_Char_ButlerBack`(Kling 키잉)), 나 = `MG_Char_GirlBack`(GirlSkater_Back 키잉), 패널 [술래 상태 카드(빨강/남색)][남은 거리 바+하트][달리기(꾹) → 끝에서 술래 터치!].
  - 배경이 원근 그림이라 **소품은 n.y ≤ 0.45 (바닥 영역)** 에만 둔다(구슬치기 마당은 탑다운이라 예외). 카메라 피치는 게임별(`Pitch` 오버라이드).
  - 진입 화면(`ChapterMissionUI.ShowIntro`) 리디자인: 마당 그림 배경 + 딤, 미션 리본, 둥실거리는 3D 아이콘+그림자, 큰 제목, 「이렇게 해요」 3단계 카드 + 목표 배지, 맥동하는 시작 버튼. 원격 탭 좌표: 시작 `tap 0.5 0.21`, 큰 버튼 `tap 0.8 0.11`.
  - 캡처: `r49_intro_yut/tuho/mug.png`, `r49_yut1~3`, `r49_tuho6~7`, `r49_ddakji3~4`, `r49_mug3~4`. 러닝 중 개발 메뉴로 열어 뒤 런의 팝업(「꽈당!」「거인 무적!」 코인)이 겹친 건 캡처 상황일 뿐. 검증은 `timescale 0` 걸고 하면 뒤 런이 안 끝난다.
- **⑤ 컬렉션 진입 클릭음**: 더보기 › 컬렉션/레코드 항목의 `PlayClick()` 제거(`MainMenuController`).
- **⑥ MCP**: `Tools/Mcp/mixamo_mcp_server.py`(MIXAMO_BEARER 토큰 방식, 검색/내보내기) · `pexels_mcp_server.py` 추가, `claude_desktop_config.json` 7개 서버 통합본. 실제 반영은 사용자가 `%APPDATA%\Claude` 로 복사 후 재시작(README 참조).
- **못 한 것 / 사용자 확인 필요**: 「구슬치기 진입화면 첨부처럼」「설정화면 첨부처럼」은 첨부가 러닝 화면 2장뿐이라 **시안 미수령** — 시안을 주면 그대로 반영. 억새 그림이 멀리서 흰 덩어리로 읽히면 `Obs_SilverGrass` 재생성(줄기 진하게) 권장. 새 곡 후렴 구간은 귀로 확인 필요.
- 커밋 대상: Audio/CoastBgmLibrary.cs, Audio/CoastAudioManager.cs, Core/StageManager.cs, Meta/ArcadeRun.cs, Meta/RecordTable.cs, UI/MainMenuController.cs, World/PromenadeSegmentBuilder.cs, World/PromenadeSegmentBuilder.Scenes.cs, World/PromenadeSegmentBuilder.Coast.cs(신규), Raising/MissionMiniGames.cs, Raising/MissionMiniGames.Games3D.cs(신규), Resources/CoastRun/BGM/BGM_M8~M12.ogg(+meta)·README.md, Resources/CoastRun/{Obs_SilverGrass,Obs_Canola,Obs_BasaltPile,Tex_Grass_Meadow,Tex_Shore_Basalt,UI_MG_Yard_Yut,UI_MG_Yard_Tuho,UI_MG_Yard_Ddakji,UI_MG_Yard_Mugunghwa,MG_Char_GirlBack,MG_Char_ButlerBack}.png(+meta), Resources/CoastRun/{MG_YutStick,MG_Token,MG_Jar,MG_TuhoArrow,MG_Ddakji}.fbx(+meta), Tools/Art/coast49_assets.py, Tools/blender/minigame_kit2.py, Tools/Mcp/*(mixamo·pexels·config·README), .cursor/mcp.json, .env.example, Docs/HANDOVER.

---

### 2026-09-12 — 50차: 더보기 › 「이용약관·정책」 (컴파일 0, 캡처 r50_policy1/2, 커밋은 사용자)
- `UI/PolicyUI.cs`(뷰어: 남보라 배경 + 탭 4개 + 종이 카드 스크롤, 한/영 Loc) + `UI/PolicyUI.Texts.cs`(본문): **이용약관**(제5조 = AI 기반 K-POP 음악·사이버 가수 「우히&히시」 조항 — 가상 인물 명시, 실존 인물 무관, 추출·재배포·AI 학습 금지, AI 표시) · **개인정보 처리지침**(회원가입 없음·기기 저장·마켓 결제·오류 보고, 만 14세 미만, 보유 5년) · **운영정책**(데이터·결제·금지행위·3단계 제재·AI 콘텐츠 운영·점검) · **청소년 보호정책**(전체이용가 기준·사이버 가수 표시·미성년 결제 보호자 동의·소통 기능 없음·과몰입·책임자).
- 진입: `MainMenuController` 더보기 마지막 항목 「이용약관·정책」(항목 4개를 따로 두면 더보기 열이 10칸이라 한 항목 안 탭 4개로). 개발 메뉴 `Coast Run/Dev/Policy - Terms|Youth`.
- **출시 전 교체할 자리표시자**(`PolicyUI.Texts.cs` 상단 상수): `Company`(코스트런 스튜디오), `Contact`(support@coastrun.app), `YouthOfficer`, 개인정보 보호책임자 성명. 시행일 `Effective`. 법률 검토 권장(특히 환불·미성년 결제 조항).
- 커밋 대상: UI/PolicyUI.cs(신규), UI/PolicyUI.Texts.cs(신규), UI/MainMenuController.cs, Editor/MiniGameDevMenu.cs, Docs/HANDOVER.

---

## 세션 마무리 (2026-09-15, Cowork 원격 세션 — 87차: 캐릭터 앵커 7개를 v3 컷씬 화풍으로 재생성 · 아빠 40대 초반)

### A. 이번 세션에 한 일
1. **앵커 7개 재생성**(`Tools/KlingGen/ref87/{H12,H19,D12,D20,KID,DAD,MOM}.png`, 768×1360): 옛 앵커는 화풍이 제각각(아빠 수채·꼬마 치비·도윤12 벡터)이고 아빠가 50대로 보여서, **v3 스틸(`Cut_S_*`)을 참조 이미지로** Kling 웹 IMAGE 3.0 Omni(1K SD · 9:16 · 2장)에서 「character reference sheet, full-body front view, plain cream background」 프롬프트로 뽑음. 참조 컷: H12 = N3_07·OP_5·N3_02 / D12 = N3_07·OP_4·OP_8 / H19 = N8_02·N1_03·N1_04 / D20 = N5_02·EA_02·N7_04 / KID = N1_01·N1_06 / DAD = N2_09(·N2_07) / MOM = N2_05·EA_02. 아빠는 사용자 선택 **40대 초반(41세)** — 검은 머리·흰머리 없음·마른 체형·수염 약간·왼눈 주름 미소(2차 생성 DAD2 #1 채택). 엄마는 v3 바이블대로 해녀복+물안경+주황 태왁, 42세. 후보 18장은 `Tools/KlingGen/out/web87/<ID>_<n>.png/.jpg`, 선택 = H12 #2 · D12 #2 · H19 #2 · D20 #1 · KID #2 · DAD2 #1 · MOM #2. 크레딧 466 → 448.
2. **기존 파이프라인 경로에도 덮어씀**: `ref77/{H12,H19,D12,D20,KID,DAD}.png` 와 `ref84/MOM.png` 를 새 앵커로 교체(옛 파일은 `ref77/_old87/`). Kling 웹 헬퍼 `__urls`(84차 B)는 그대로 ref77/ref84 를 가리키므로 다음 컷 생성부터 자동으로 새 앵커 사용. `ref77/MOM.png`(v3 해녀 원본)·`BOAT.png` 는 그대로.
3. 파일: `Tools/KlingGen/plan87.json`(7 프롬프트 + refs URL), `Tools/KlingGen/dl87.py`(urls.json 다운로드 → PNG/JPG; `--pick ID=n …` 로 ref87 복사·768×1360 통일), `Tools/KlingGen/out/web87/urls.json`.
4. 비교표 `r87_anchors_compare.jpg`(대화에 전송) — 옛/새 7쌍.

### B. Kling 웹 요령 추가(87차)
- 참조 썸네일 지우기: 이제 `.omni-material-pool__material-item a.close` 를 **pointerdown→mousedown→pointerup→mouseup→click 순서로 dispatch** 해야 지워진다(`.click()`·옛 `.mask div` 는 무반응 → 참조가 누적돼 9장으로 잘못 제출됨). 84차 헬퍼의 `__clearRefs` 를 이렇게 바꿔 쓸 것.
- 참조 이미지는 GitHub raw 의 `Assets/Resources/CoastRun/Cut_S_*.jpg` 도 바로 쓸 수 있다(`__anchorUrls(urls)`).
- 결과 목록은 「Back to Top」을 눌러야 새 작업이 `__groups()` 에 보인다.
- `device_commit_files` 로 같은 경로의 .py 를 두 번째 덮어쓸 때 내용이 안 바뀌는 경우가 있었다(mtime 만 갱신) → 새 파일명으로 커밋하고 bat 에서 `copy /Y` 로 덮는 것이 확실.

### C. 커밋 대상
`Tools/KlingGen/ref87/*.png`(7), `Tools/KlingGen/ref77/{H12,H19,D12,D20,KID,DAD}.png`, `Tools/KlingGen/ref84/MOM.png`, `Tools/KlingGen/ref77/_old87/*`(선택), `Tools/KlingGen/{plan87.json,dl87.py}`, `Tools/KlingGen/out/web87/urls.json`, `Docs/HANDOVER`. (`out/web87/*.png` 원본 18장은 제외 권장.)

---

## 세션 마무리 (2026-09-15, Cowork 원격 세션 — 86차: 사용자 요청 7건)

### A. 이번 세션에 한 일
1. **K-POP 미션 ×2**(`ArcadeRun.SettleKpop`): 미션 3개를 다 하면 **그 판의 육성 돈·젤리가 두 배**(매 판; 하루 1회 올클리어 보너스와 별개). 젤리는 `JellyWallet` 적립 + 젤리 경험치(`ExpJelly`, 처음 연결). 결과창 주황 띠에 「★ 미션 3개 달성 — 돈·젤리 ×2! 돈 +N · 젤리 +M」. 챕터 진행(`NoteKpopClear`)은 전처럼 **완주만** 조건 — 미션과 무관.
2. **컬렉션 클릭음 = 찰칵**: `CoastSfx.Shutter`(절차 합성 셔터 「찰·칵」 2클릭, `Resources/CoastRun/SFX/SFX_Shutter.ogg` 가 있으면 그것) — 포토카드 개봉 `CardReveal` 스팅어(음정 있는 코인음 1.5×, 배경음과 섞임)를 이것으로 교체.
3. **스토리 펫 → K-POP**: `PetCompanion.Create` 가 `RunTuning.Pet` 이 None 이면 `GameManager.I.PeekSave()`(디스크 세이브)의 장착 펫, 그것도 없으면 `PetShop.FirstOwned`. 캡쳐로 참새 확인(`shots86/r86_kpop.jpg`). Dev 메뉴 `Kpop - Start / Kpop - Log pet`.
4. **보스 원근·3D 움직임**(`BossDirector`): `YawBillboard` 대신 `BossBody`(요 빌보드 + **롤·피치·스핀·스쿼시**). `FollowBoss` 가 앞뒤 스윙(±6 m → 다가오면 커지고 멀어지면 작아짐)·옆이동 시 기울기(SmoothDamp 속도 → 롤)·오르내림 피치·종류별 리듬(갈매기 날갯짓 + 큰 바운스 / 골렘 무겁게 / 도깨비 팽이 회전 + 불규칙)·공격 0.32 s 전 윈드업 스쿼시 → 공격 때 `Recoil`·그림자 크기는 높이에 따라. (Blender 3D 모델 교체는 안 함 — 스프라이트 2겹 + 기울기로 「조각처럼」.)
5. **마을 대회 NPC 러너**(`ContestRivals`): `ObstacleHazard.Active` 로 앞 2.5~13 m 같은 레인 장애물을 보고 **빈 옆 레인으로 회피(82 %)** / 낮은 장애물은 점프(13 %) / 못 피하면 겹칠 때 `HitReact` — 속도 0.42 로 뚝(0.9 s)·앞으로 숙임·좌우 비틀(0.7 s)·붉게 번쩍(0.35 s)·`JuiceDirector.PlayRivalHit`(별·퍼프 파편 + 흰 링 + 「꽈당!」 + 꽈당 효과음, 가까울수록 카메라 살짝). 랜덤 레인 이동도 장애물 있는 레인은 피한다.
6. **기부 화면**(`DonateUI`): 시안을 배경 그림 `UI_Donate_BG.png`(720×1280 — 꽃 테두리·갈색 카드·컵만 구움, **글자는 전부 지움**(86차-2))으로 깔고 제목 「커피 한 잔 값, 기부 부탁드려요 ☕」(금색+갈색 외곽선, Loc.T)·본문 상자·「★ 선물 고르기」·알약 4개(선택 = 노랑 + ✦)·분홍 결제 버튼(아랫입술)·「다음에 할게요」·바닥 안내를 **시안 좌표**에 배치. Dev `UI - Donate`.
7. **캐릭터 정보**(`StatusUI`): 시안 배경 `UI_Status_BG.png`(얼굴·제목 알약 틀·칼/집 아이콘·스탯 아이콘 9개·보라 닫기 버튼 틀만 구움, **글자는 전부 지움**(86차-2)) + 코드 글자(「캐릭터 정보」·「스탯」·「생활」·라벨 체력/순발력/매력/감성/평판/말썽/스트레스/배부름/컨디션·「닫기」, 모두 Loc.T) + 레벨·이름·칭호 칩 / EXP 바 / 대회 배너(깃발 `UI_Status_Flag`) / 막대 7개(✦) / 돈·코인 보라 알약 / 생활 막대 / 재고 줄. 시안의 「일생」 자리는 **말썽**. 옛 힌트(레벨 보너스·경험치 얻는 법·호감도)는 시안에 없어 뺌. Dev `UI - Status`.
8. **대회 배너**(`StoryContest.ContestHud`): `UI_Contest_Banner.png`(노랑→하늘 그라데이션 + 흰 테두리, 900×170) + `UI_Contest_Flag.png`(시안에서 잘라냄) + 두 줄 파란 제목(흰 테두리) + 코인 아이콘 「63/120 · 2:38 · 3위」 + ✦ 3개. 캡쳐 비교 3회(`shots86/r86_banner4.jpg`). Dev `Contest - HUD test (ch1)`.
9. 캡쳐 비교(`Tools/KlingGen/out/shots86/`): 기부·캐릭터 정보·배너 모두 시안과 레이아웃·색 일치(글꼴은 게임 폰트).

### B. 알려진 이슈·남은 일
- ~~시안 글자를 그림으로 구운 부분은 한국어 고정~~ → **86차-2 에서 해결**: 기부·캐릭터 정보 배경의 글자를 전부 지우고 코드(`Loc.T`)로 옮김(문서 맨 위 「원칙」 참고). 캡쳐 `shots86/r86_status_t1.jpg`·`r86_donate_t1.jpg` 시안과 비교 확인. 옛 배경(포토카드·뮤직 컬렉션 버튼 글자)은 아직 구워진 상태 — 같은 방식으로 옮길 것.
- 기부 시안의 본문·선물 문구 오타(「업데이드」「모든 게임 일반」「OST 집합체」)는 바로잡아 넣음.
- 보스는 K-POP 6챕터 이상에서만 등장 — 이번 세션은 코드·컴파일만 확인, 플레이 캡쳐는 못 함. 이상하면 `BossBody.roll/pitch` 계수(FollowBoss)만 줄이면 됨.
- NPC 회피는 장애물 콜라이더 기준(전 레인 오리·달려오는 차는 못 피할 수 있음 → 꽈당).
- `unity_shot` 은 플레이 중 컴파일(도메인 리로드) 뒤엔 `GameManager.I` 가 비어 Dev 메뉴가 안 먹는다 → stop → play 로.

### C. 커밋 대상
`Scripts/Meta/{ArcadeRun,BossDirector,ContestRivals,PetShop,StoryContest}.cs`, `Scripts/UI/{RunHudChrome,CollectionUI,DonateUI,StatusUI}.cs`, `Scripts/Audio/CoastAudioManager.cs`, `Scripts/Player/PetCompanion.cs`, `Scripts/Visual/JuiceDirector.cs`, `Editor/MiniGameDevMenu.cs`, `Resources/CoastRun/{UI_Donate_BG,UI_Status_BG,UI_Status_Flag,UI_Contest_Flag,UI_Contest_Banner}.png(+meta)`, `Docs/HANDOVER`.

---

## 세션 마무리 (2026-09-15, Cowork 원격 세션 — 85차: 컷씬 대본 v4 전부 적용)

사용자 보고 「더보기 › 시네마가 과거 컷씬을 담고 있다」 → 확인: `CinematicTable` 이 v3(Cut_S) 그대로였음 → **v4 전부 적용**(사용자 선택).

### A. 이번 세션에 한 일
1. **`Story/CinematicTable.cs` 를 v4 로 전면 교체** — `Tools/Story/gen_cinematic_v4.py` 가 `Docs/CUTSCENE_SCRIPTS_v4.md` 를 파싱해 생성(자막·`Cut_T_*`·회상 태그(sepia)·컷 길이·채도). OPEN 14컷(10.6s, 카드 hold 154s) · CS1~8(13~16컷, 8.5s) · **EV1~10(4컷 × 7s)** · END_A/B/TRUE(9·9·11컷). 자막 >108자는 10~12초. 유지 컷은 `Cut_S_*` fallback. MV 절만 옛 파일에서 보존. **자막을 고칠 땐 md 를 고치고 `python Tools/Story/gen_cinematic_v4.py` 재실행**(`--txt` 를 붙이면 리더 txt 도).
2. **컷씬 챕터 ≠ 러닝 챕터**(`StoryProgress`): `CutsceneChapters = 1·3·5·7·10·12·17·20`(v4 배치표), `EventChapters = 2·4·6·8·9·11·13·14·15·19`(EV1~10), CH16·18 없음. `RunChapters`(1·4·7·10·13·15·18·20)는 그대로 — 대회는 그 챕터에서. `TamaRaisingUI.BoundaryRoutine` 이 CS 뒤 이어서 EV 도 튼다(`MarkEventSeen`, 경험치 25). 레벨 게이트 `IsLongCut`/`LevelForLongCut` 을 새 챕터로(CH3 Lv2 · 5 Lv4 · 7 Lv6 · 10 Lv8 · 12 Lv10 · 17 Lv13 · 20 Lv15), `LongCutIds` 도 교체.
3. **단서 시스템 `Story/ClueSystem.cs`**(신규) + `SaveData.clueMask`(6비트)·`pendingEndingId`: 컷씬 직후 육성 화면에 「단서」 카드(EventCardKit). CS2 하트 · CS3 라디오 · CS7 이름 · CS8 머리띠 는 한 버튼(자동 획득), **CS4 돌 세 개 · EV9 편지 스무 통 은 두 버튼**(다시 쌓는다/전부 읽는다 를 눌러야 획득, 그냥 두면 놓침). 엔딩 분기 `ClueSystem.EndingId(mask)`: 이름·라디오 중 하나라도 없으면 END_A, 여섯 개 전부면 END_TRUE, 그 사이 END_B.
4. **`GameManager.ResolveEnding` 재작성**: 전부 S / 양쪽 엔딩 조건 폐기 → `clueMask` 로 엔딩 id 결정, `CinematicPlayer.Play(END_*)` → 기존 엔딩 시퀀스. TRUE 만 Happy(타이틀), A·B 는 Tragic(타임라인·다음 회차). `endingMask` 비트: A=bit0 · B=bit3 · TRUE=bit6(옛 변형 비트 1·2·4·5 는 안 씀). 옛 VN 에필로그(END_A_SENSE 등)는 더 안 튼다(대본 파일은 남김).
5. **`CinemaSelect`**: 챕터 순서로 컷씬·이야기(EV) 카드 섞어 나열(EV 카드는 낮고 밝은 색, 「이야기 · 제 n화」, 4컷 · 28초), 표지 = 각 시네마 첫 컷 `Cut_T_*`, 길이 표시, 상단에 `단서 n/6 · …`. 엔딩 카드 라벨 「엔딩 A / 엔딩 B / 진엔딩」 + v4 제목. EV 의 「읽기」는 그 챕터 리더(`StoryReaderUI.OpenChapter`).
6. **`CinematicPlayer`**: 컷별 채도 키프레임(`Cut.sat`, -1 = Def 값) — END_TRUE 2컷부터 1.0 복귀(캡쳐 확인). 자막 >90자면 띠를 5~25% 로 높임(세 줄).
7. **리더 txt**(`Tools/Story/script/`): PRO·CH01~20_Open·END_A/B/TRUE 를 v4 자막으로(각 컷 = `CG | T_xx | Cut_T_xx` + `NARR`), Close 20개와 CH16·18 Open 은 빈 파일(ChapterVN 이 건너뜀). `cutscene_txt.py import` 가 빈 씬을 `new[] {}` 로 써서 컴파일이 깨지던 것 → `new VnLine[0]` 로 패치. 챕터 제목이 v4 제목으로 바뀜(CH2 손가락 사이로 … CH18 조용해). EN 줄 없음 → 영어 모드에도 한국어.
8. Dev 메뉴: `Cine - EV1/EV5/EV9/EV10/END_B/END_TRUE`, `Clue - Card CS4(돌)/CS7(이름)/Log`.
9. 검증: 컴파일 에러 0, 05_Raising 플레이에서 시네마 목록(캡쳐 `Tools/KlingGen/out/shots85/r85_sel.jpg`)·CS1(긴 자막 4줄)·END_TRUE(2컷부터 컬러)·EV1·단서 카드(돌 → 1/6 → END_A 로그) 확인, 콘솔 에러 0.

### B. 알려진 이슈·남은 일
- 편지 단서는 「편지 읽기 × 20」 육성 행동이 아니라 EV9 뒤 카드의 두 버튼으로 단순화. 돌 단서도 러닝 결과 화면이 아니라 CS4 뒤 카드. 실제 행동으로 바꾸려면 `ClueSystem.ShowAfterScene` 의 choice 분기만 갈아끼우면 됨.
- 기존 세이브는 `clueMask = 0` → 그대로 20챕터까지 가면 END_A. 컷씬을 이미 본 챕터(CoastRun_VN_CHnn_Open)는 v4 컷씬도 「본 것」으로 취급되어 안 튼다(시네마 다시보기로 볼 것).
- `unity_shot` PNG 는 만든 직후 `device_stage_files` 가 「hardlinked」로 거부 → PC 파이썬으로 JPG 변환(`Tools/_clip/run_shotcopy85c.bat` → `Tools/KlingGen/out/shots85/`) 후 **20~30초 뒤** stage 하면 됨(새 파일은 잠깐 nlink>1 로 보임).
- 02_Run 씬에서 Dev › Cine 를 틀면 뒤에서 러닝이 계속 돌아 결과창이 겹친다 → 05_Raising 에서 테스트.
- 회상 컷(sepia) 은 v3 처럼 세피아 0.85 로 — v4 컷의 「— 회상 —」 태그가 있는 컷 전부.

### C. 커밋 대상
`Assets/_CoastRun/Scripts/Story/{CinematicTable,CinematicPlayer,ClueSystem(신규),ChapterScript.Data,ChapterScript.En}.cs`, `Meta/{StoryProgress,SaveData,LevelSystem,GameManager}.cs`, `Raising/TamaRaisingUI.cs`, `UI/CinemaSelect.cs`, `Core/Loc.cs`(챕터 제목), `Editor/MiniGameDevMenu.cs`, `Tools/Story/{gen_cinematic_v4.py(신규),cutscene_txt.py}`, `Tools/Story/script/*.txt`(44), `Docs/HANDOVER`. (+ 84차분: `Resources/CoastRun/Cut_T_*.jpg` 192 + meta 등)

---

## 세션 마무리 (2026-09-14 밤, Cowork 원격 세션 — 84차: 컷씬 v4 스틸 116장 생성·적용)

### A. 이번 세션에 한 일
1. **v4.8 신규 스틸 116장을 Kling 웹(IMAGE 3.0, 1K SD · 9:16 · 2장씩)으로 생성**해 검수 → 720×1280 JPG 로 `Assets/Resources/CoastRun/Cut_T_*.jpg` 적용. 유지 76컷은 `Cut_S_*` → `Cut_T_*` 복사(`keep84.json`, 「**유지** 옛id」 64 + 옛id 표기 없는 같은 번호 12). **Resources 에 `Cut_T_*.jpg` 192장** 확인.
2. 검수·재생성: 1차 232장 중 19컷 재생성(OP_02·03·05·07·08·12, N1_05, N2_11, E2_01, N5_02·04·05·07·10·13, E6_03, N6_05, E10_04, ET_02), 그중 4컷(N5_05·E6_03·N6_05·E10_04) 2차 재생성, N8_16 은 수집 충돌로 재생성(아래 C-3). 크레딧 756 → 약 460.
3. 파이프라인 파일: `Tools/KlingGen/plan84.json`(116 프롬프트 {id,keys,prompt,sig}), `Tools/KlingGen/ref84/MOM.png`(42세 엄마 앵커), `Tools/KlingGen/out/web84/{urls.json(최종 116 URL), keep84.json, Cut_T_*.png(원본 116장 ≈ 150 MB, 커밋 제외)}`, `Tools/KlingGen/dl84.py`(다운로드 → JPG 변환 → 유지 컷 복사; `--force ID…` 재다운로드, `--keep-only`).
4. 코드 변경 없음 — `CinematicTable` 은 아직 `Cut_S_*`(v3) 를 쓴다. v4 적용(83차 B-1~3·5)은 다음 세션.

### B. Kling 웹 자동화 요령 (84차에서 확정 — 다음에 또 쓸 때)
- **내장 브라우저(Claude 앱 브라우저 패널)** 탭 `seed` 에서 kling.ai 로그인은 사용자가 직접. 이후 전부 `javascript_tool` 로: 헬퍼 전체가 `localStorage.__helpers84` 에 있으므로 새 세션에서는 `eval(localStorage.getItem('__helpers84')); await window.__boot()` 로 복구(`__boot` 이 GitHub raw 의 `plan84.json` 을 읽어 `window.__plan`, `window.__ids` 를 채움).
- 설정은 **페이지 새로고침 때마다 2K/3:4 로 돌아가므로** `1K SD · 9:16 · 2` 를 다시 맞출 것(하단 설정 알약 클릭). 한 번에 한 작업만 가능(Generate 버튼 비활성) → `await window.__cycle()` 을 **한 호출에 한 번**씩(30 초 대기 + 제출 ≈ 40 초, 도구 시한 50 초). 결과 수집 `__collect()` 는 `.creation-stream-item` 그룹 텍스트에서 `sig`(장면 문장 앞 60자) 로 매칭 → `window.__out[id] = [origin URL…]`(썸네일 `:360x638.webp` → `.origin`).
- **앵커 업로드**: base64 를 대화로 옮기면 깨지고 토큰이 많이 든다 → 앵커를 GitHub 에 푸시하고 페이지 안에서 `fetch('https://raw.githubusercontent.com/kk00701903-hub/game-costrunner/main/Tools/KlingGen/ref77/<K>.png')` → `File` → `input[type=file].el-upload__input` 에 DataTransfer 주입. 참조 순서 = `keys` 순서, 프롬프트에는 「Reference image 1 shows haneul's face」식 자연어(대문자 라벨 「DAD =」「Image1 is DAD」는 그림에 글자로 찍힘).
- **프롬프트 주의**: 「Korean webtoon key-visual style」은 만화 패널·말풍선·폰 화면을 자주 만든다 → 「Cinematic animated film still, one single wide shot filling the whole vertical 9:16 frame … Never a comic page, never a phone screen: no panels, no grid, no speech bubbles, no captions, no text」로. 실내·밤·병원 컷이 특히 잘 깨짐. 손 클로즈업 컷(OP_05·N5_07)은 앵커 없이(keys=[]) 뽑아야 얼굴이 안 끼어든다. 엄마·아빠는 「slim/lean, not overweight, not elderly」를 넣어야 40대로 나온다.
- 검수는 페이지 위 오버레이 `window.__show(ids)`(4컷 × 2후보 썸네일) 스크린샷 → `window.__hide()`. 선택 결과 `localStorage.__picks84`(id → 1|2), 최종 `localStorage.__final84`(id → URL, urls.json 과 SHA-1 일치 확인).
- 다운로드는 CDN 이 샌드박스에서 막혀 PC 파이썬(`dl84.py`)으로: `Tools/_clip/run.bat` 에 커밋(staged 파일명은 매번 새로) → `unity_cmd "menu Coast Run/Dev/Run - Tools/_clip/run.bat"`. 116장 ≈ 90 초, unity_cmd 는 타임아웃 나도 배치는 계속 돈다(`device_list_dir` 로 확인).

### C. 알려진 이슈 (이 세션)
1. `device_bash` 여전히 마운트 실패(9/8 이후) — stage/commit + RunBat 만 사용. `Tools/_clip/run.log` 는 실행 중엔 0 바이트·잠김, 끝난 뒤엔 「hardlinked」로 stage 거부 → 결과는 `device_list_dir` 로 확인.
2. Kling 작업 목록은 가상 스크롤이라 `__groups()` 에 최근 5개 안팎만 보인다 → **`__cycle()` 마다 `__collect()` 가 돌므로 결과는 그때그때 `localStorage.__out84` 에 쌓인다**. 되돌아가서 옛 결과를 다시 읽을 수는 없다(Assets 페이지에서 수동으로만).
3. **sig 충돌**: 한 컷의 sig 문장이 다른 컷 프롬프트에도 들어 있으면 나중 컷 결과가 먼저 컷을 덮어쓴다(N8_16 「19-year-old Haneul wearing the silver headband…」가 ET_01 프롬프트에 포함 → N8_16 결과 유실 → 재생성). 계획 파일을 만들 때 `sig in other.prompt` 검사 필수(E9_01↔E7_03 도 겹쳤으나 순서상 무사).
4. `unity_cmd` 300 초 타임아웃 응답이 와도 bat 는 끝까지 실행됨.

### D. 사용자가 할 것
1. 커밋: `Assets/Resources/CoastRun/Cut_T_*.jpg`(192 + meta — Unity 를 한 번 열어 meta 생성), `Tools/KlingGen/{plan84.json, dl84.py, ref84/MOM.png}`, `Tools/KlingGen/out/web84/{urls.json,keep84.json}`, `Docs/CUTSCENE_SCRIPTS_v4.md`, `Docs/CUTSCENE_SCRIPTS_v4_읽기용.md`, `Docs/HANDOVER`. (`out/web84/*.png` 원본은 제외 권장.)
2. Claude 데스크톱 앱 설정 → 개발자 → 구성 편집에 `android` 항목 붙여 넣기(82차 B-1, 아직 미완).
3. ~~다음 세션: 83차 B-1~3·5~~ → **85차에서 완료**.

---

## 세션 마무리 (2026-09-14 밤, Cowork 원격 세션 — 83차: 컷씬 대본 v4)

### A. 이번 세션에 한 일
1. **`Docs/CUTSCENE_SCRIPTS_v4.md` 신규(문서만, 코드 변경 없음)** — 사용자 제공 「서사 중심 컷씬 시나리오 재구성안」(OPEN + CS1~8 + END_A/B/TRUE)을 v3 대본에 입혀 전편 재작성. OPEN 14컷 · 컷씬 8편 각 13컷 · **보조 컷씬 EV1~EV10 각 4컷(스토리 모드 이벤트, CH2·4·6·8·9·11·13·14·15·19)** · 엔딩 A·B 9컷·TRUE 10컷 = **189컷**(v4.1: 감정선 점검으로 CS8 13→16컷, END_TRUE 9→10컷 · v4.2 위 5항). 유지 81컷(v3 그림 재사용) / **신규 108컷**(Kling 프롬프트 첨부).
2. 서사 골자 변경: 아빠의 죽음 = 보석 머리띠 외상값 · 엄마가 우비를 빼앗음 · 방수 통 · 꼬마 = 살아 있는 아이 「민재」에 남은 아빠의 마지막 기억(CS8에서 이름이 불리면 사라짐) · 「닿는 물건의 규칙」으로 유령 확증을 CS8까지 미룸 · 편지 19+1통 · 수색 카드 「고하늘 실종 1년」(CS7) · 세 가지 규칙(이름·물건·약속) · 엔딩 A/B 뜻 뒤집힘(A=못 만남, B=목소리만, TRUE=한 곡 동안 만남 + 방송국 + 넷째 돌).
3. 문서 안에 챕터 배치표, 엔딩 분기 조건(단서 6개 `clueMask`), 바이블 추가(머리띠·방수 통·수색 카드·완장·민재 엄마·할머니), 닿는 물건 규칙, 떡밥 체크리스트, 목소리 카드, 적용 메모 7항목.
4. v4.1 감정선 보강: CS8에 도윤의 「내일 건 누가 마셔」·엄마의 「어멍 왔져, 미역국은 못 끓여 줬져」(하늘이 엄마를 안으려다 통과)·아빠의 「늦게 와서 미안, 기다려 줘서 고맙고」·「잘 가, 아빠 — 이번엔 내가 소매를 놓았다」 단독 컷 추가. CS7-9 「늦어도 돼, 살아서만 와」+ 도윤이 처음으로 이름을 부름. EV9-1 편지 본문 한 줄(「나 아직 살아 있어. 너도 그러길」). END_TRUE 「살아 볼게, 아주 오래」 단독 컷.
5. v4.2(사용자 지시 3건): ① 머리띠 = 오일장 좌판 → **처음 가 본 시내 백화점 유리 진열장**(외상 → 이웃에게 빌린 돈) ② 아빠 사투리 「잡으민 되주」 → **「잡으민 뒈어」**(OPEN-4·CS8-10) ③ **CS7 이전 유령 티 제거** — 「손가락 사이로 빠져나갔다」류를 전부 「손에 힘이 안 들어갔다 / 손끝이 저렸다 / 미끄러졌다」로, 프롬프트에서 translucent·reflection 삭제(CS1-8, EV1-1, EV5-4, EV4-1·2, EV7-2·3, EV8-3, CS5-13, EV9-4). 「닿는 물건의 규칙」 절에 원칙 명문화: 독자는 CS6~7에서 하늘이 병약한 쪽일 수도 있다고 느껴야 한다. 통과 그림은 CS7-8부터.
6. v4.3: CS1-3 신설(사용자 지시) — 「달력에 내년 내 생일, 송전탑 아래서 보자는 한 줄. 누가 썼는지 모르지만 그 날짜만은 지켜야 할 것 같았다」(v3 `Cut_S_N1_03` 재사용) → CS1 14컷, 총 190컷.
7. v4.4: CS1-9 꼬마의 규칙 설명(「누나는 아무거나 못 만져…」) 삭제 → 「보드는 잘 잡히지?」 한마디로. 규칙 설명은 EV1-3에만 남김.
8. v4.5: EV6 「밴드」→「물때」 — 노란 완장 삭제(뜬금없음). 도윤은 새벽 물때마다 손전등으로 갯바위 틈을 비추며 걷는 것으로. 바이블 BAND 행 삭제, CS7-4 카드는 「초소 앞에서 꺼내 보던 코팅 카드」로.
9. v4.6: **CS6-13 오도 컷 신설**(사용자 지시) — 하늘이 절벽 길의 도윤 소매를 잡는데 쥐어지지 않고, **흐리게 그리는 쪽은 도윤**(하늘 손은 또렷). 자막 「사라져 가는 쪽은 그 애였다. 그렇게 보였다」 → 독자가 도윤이 죽어 간다고 믿게 한 뒤 CS7-5 카드에서 뒤집음. CS6 14컷, 총 191컷, 신규 109장. 규칙 절에 예외로 명시.
10. v4.7: **END_TRUE 결말 변경**(사용자 지시) — 9컷: 도윤이 「안 늦었네」에 **돌아본다**, 부스 유리 너머 하늘, 마이크를 내려놓고 일어섬 / 10컷: 「방송은 이어지지 않았다」+ 넷째 돌·빈 우유 두 병 / **11컷(마지막): 정류장 앞에서 손잡은 둘의 사진 한 장** — 찍은 사람·날짜 없음. 도윤도 죽어 만난 건지 하늘이 살아난 건지 열어 둠(사진의 두 사람은 똑같이 또렷하게). END_TRUE 11컷, 총 192컷, 신규 110장.
11. v4.8 **그림 지시 전면 추가**(사용자 지시): ① 「인물 고정 규칙」 절 — 앵커 사용법(얼굴만 subject 참조 0.35~0.5, 77차 구도 붕괴 원인은 강도 1.0), 나이·성별 태그 필수, **키 기준표**(H12 140 / D12 145 / KID 110 / H19 162 / D20 180 / DAD 172 / MOM 160), 인물별 얼굴 고정 문장, 금지 목록 ② **MOM 바이블 교체 — 42세, 갸름·날씬, 흰머리·주름·비만 금지** → `ref77/MOM.png` 먼저 재생성, **엄마 컷 11장 전부 신규**(OPEN-4·7·12, CS2-5, CS5-3·5, CS8-2~6) ③ 문서 끝에 「씬별 그림 지시」 표 22개(192컷 × 앵커·샷·핵심) ④ CS7-5 카드 컷 신규(옛 팔찌 그림 폐기). 신규 116장.
12. 그림 ID는 **`Cut_T_*`**(v4) — v3 `Cut_S_*`와 번호가 어긋나 같은 접두어로는 덮어쓰기 충돌.

### B. 다음 세션(적용) 순서
1. `Tools/Story/script/*.txt` 43개를 v4 배치표대로 다시 쓰고 `cutscene_txt.py import` → 컴파일. EV 컷 길이 7s·OPEN 10.6s 지원(`ChapterVN`/`CinematicPlayer` 컷별 길이).
2. `SaveData.clueMask`(6비트) + 컷씬 종료 콜백/편지 읽기 행동/돌 다시 쌓기 탭 → `CinemaSelect`·엔딩 분기.
3. `CinemaSelect` 엔딩 카드 제목·설명 교체(A 엇갈린 정류장 / B 우유 두 병 / TRUE 맞닿은 주파수 91.9).
4. ~~신규 스틸 Kling 웹 생성 → `Cut_T_*.jpg`, 유지 컷 복사~~ → **84차에서 완료**(116 신규 + 76 유지 = 192장).
5. END_TRUE 채도 0.25→1.0 컷 내 키프레임(셰이더) · M1 라디오 필터 버전.

### C. 커밋 대상
`Docs/CUTSCENE_SCRIPTS_v4.md`, `Docs/HANDOVER`. (82차 커밋 대상은 아래 절 참고.)

---

## 세션 마무리 (2026-09-14, Cowork 원격 세션 — 82차)

### A. 이번 세션에 한 일
1. **Figma MCP 커넥터 연결**(Claude 앱, songmj / (주)제때 Full 시트) — `whoami` 확인. 디자인 시안을 Figma 에서 바로 읽을 수 있음.
2. **MCP 점검**: Unity 브릿지 OK(02_Run, 에러 0) · Photoshop OK · Kling OK(bearer, SUCCEED) · **Blender 는 프로그램만 떠 있고 애드온 서버 미연결**(N 패널 → BlenderMCP → Connect to Claude 필요).
3. **82차 android MCP**: `Tools/Mcp/android_mcp_server.py` 신규 + `.env`/`.env.example` `ANDROID_*` 7항목 + `claude_desktop_config.json`·`.cursor/mcp.json`·`README.md` 에 `android` 등록 + `claude_desktop_mcp_android.json`(붙여넣기용). PC 셀프테스트 통과(3-6 절).
4. 텔레그램 보고는 outbox.txt → `menu Coast Run/Telegram/Send outbox` 경로가 정상 동작(82차 시작/50%/100% 전송).
5. **Blender MCP 연결(82차-2)**: 사용자가 마우스 권한을 주어 컴퓨터 사용으로 직접 처리 — 애드온이 비활성 상태였던 것이 원인, Python 콘솔에서 `addon_enable` + `save_userpref` → 9876 연결·`get_scene_info` 확인. 절차는 3-7 절.

### B. 사용자가 할 것
1. Claude 데스크톱 **앱 설정 → 개발자 → 구성 편집** 에 `android` 항목 붙여 넣고 앱 완전 재시작 → 새 세션에서 `android_ping` 으로 확인.
2. ~~Blender 애드온 서버 켜기~~ → 82차-2 에서 해결(userpref 저장, 다음 실행부터 자동).
3. 커밋(77~81차 미커밋분 + 82차: `Tools/Mcp/android_mcp_server.py`, `Tools/Mcp/claude_desktop_config.json`, `Tools/Mcp/claude_desktop_mcp_android.json`, `Tools/Mcp/env_android.txt`, `Tools/Mcp/README.md`, `.cursor/mcp.json`, `.env.example`, `Docs/HANDOVER`). `.env` 는 gitignore.

### C. 알려진 이슈 (이 세션)
- 9/8 Windows 업데이트 이후 원격 세션의 `device_bash` 가 `C:\dev\game` 을 마운트 못 함 → 파일은 `device_stage_files`/`device_commit_files`, PC 실행은 RunBat 메뉴로만. `.env` 는 remote 도구 쓰기 차단.
- 원격 세션 샌드박스는 `api.telegram.org` 403 — 봇 API 직접 전송 불가, outbox 경로 유지.
- **`device_commit_files` 는 같은 staged 파일명을 다시 커밋하면 이전 내용이 그대로 써진다**(outbox.txt 재커밋 → 「82차 시작」이 두 번 전송됨, 50% 보고 유실). 매번 새 파일명(`outbox_82_100.txt` 식)으로 만들어 `outbox.txt` 에 커밋할 것 — 기존 C-2 항목의 run.bat 주의와 같은 원인.

---

## 세션 마무리 (2026-09-13, Cowork 원격 세션 — 77~81차)

### A. 이번 세션에 한 일
1. **77차 그림 전면 교체**: 컷씬 스틸 **131장**을 Kling 웹(Standard 요금제)으로 전부 새로 생성 → 검수 후 8장 재생성 → `Resources/CoastRun/Cut_S_*.jpg`(720×1280) 적용. 시네마 표지·컷씬 재생 확인.
2. **78차 꼬마 집사 재디자인**: 검은 조끼 3D 아이 → 컷씬의 주황 우비 꼬마(전신·뒷모습·흉상·얼굴 뱃지 4종).
3. **79차 시네마 엔딩 임시 해금**: `CinemaSelect.OpenAllEndings = true`(false 로 되돌리면 원래 잠금 규칙).
4. **80차 대본 개정**: 18화까지 「도윤이 죽은 것 같다」로 오해 유도, 트럭 컷은 「옆으로 넘어짐」(그림도 재생성), 엄마 나이 통일(45세) + 그림 2장 재생성, 심장 → 심장병, 「스무 챕터 만에」 → 「여덟 해 만에」, 20화 환생 떡밥.
5. **81차 클리어 화면 정리**: 옛 수채화 회상 팝업 제거.
6. **APK 빌드**: `Builds/CoastRun.apk` (9/13 11:21, **478 MB**) — 77·78차까지 반영. **79~81차 변경(엔딩 해금·대본·팝업 제거)은 안 들어가 있으니 폰 테스트 전에 재빌드 필요.**

### B. 사용자가 할 것
1. **커밋** — 77~81차 전부 미커밋. 각 회차 절의 「커밋 대상」 참고. 새 파일: `Editor/{RunBatMenu,ClipboardDump}.cs`, `Resources/CoastRun/Cut_S_*.jpg`(131 + meta), `Tools/KlingGen/out/web77|web78|web80/*`, `Docs/CUTSCENE_SCRIPTS_v3.md`. (`Tools/KlingGen/out/web77/*.png` 원본 131장 ≈ 200 MB 는 커밋 제외 권장.)
2. **APK 재빌드**(원하면): `Library\Bee\Android\Prj` 삭제 → `Coast Run/Build/Android APK (IL2CPP, ARM64+ARMv7)`. 클린 상태면 20분대, IL2CPP 캐시가 남아 있으면 2~3분.
3. **폰 테스트** 후 피드백.

### C. 다음 세션 시작 절차
1. `unity_status` 로 브릿지 확인(안 붙으면 사용자가 **Unity Hub 에서 `C:\dev\game` 열기** — `unity_launch` 의 직접 실행은 라이선스 창에서 멈춘 적 있음).
2. PC 스크립트는 `Tools/_clip/run.bat` 를 `device_commit_files` 로 갈아끼우고 `menu Coast Run/Dev/Run - Tools/_clip/run.bat`. **같은 이름으로 다시 커밋하면 내용이 안 바뀌는 경우가 있으니 항상 새 파일명**(dl80b.py 식)으로.
3. Kling 웹은 내장 브라우저 tab-2 에 로그인돼 있음. **크레딧 6 남음.** 결과 URL 은 `https://kling.ai/app/user-assets/materials?ac=1`(최신순)에서 썸네일 토큰 → `.origin` 으로.

### D. 알려진 남은 이슈
- **APK 478 MB** — Resources 456 MB(옛 `Cut_V_*` 108 MB · `Cut_CH*` 74 MB). 새 `Cut_S_*` 로 대체됐으니 `Cut_V_*` 삭제로 100 MB 이상 감량 가능(테이블 폴백만 참조). 텍스처 압축(ASTC/ETC2)·최대 크기 제한도 여지 있음. 스토어는 AAB/PAD 필요.
- 컷씬 첫 컷 **image2video** 미완(API 크레딧 소진, 웹 영상은 크레딧 큼).
- 잔손 그림: `N7_08`·`N8_01` 빈 말풍선, `N8_03` 손목 글자 — 8.5초 컷이라 보류.
- 빌드 직후 에디터 로그 후크가 끊겨 `unity_log` 가 안 갱신될 수 있음(메뉴 명령은 정상) → 결과는 `device_list_dir` 의 mtime·크기로 확인.
- Photoshop MCP 는 `app.system` 이후 스크립트 실행이 매우 느림/타임아웃(파일은 나중에 생성됨). PC 작업은 `RunBatMenu` 로.

---

### 2026-09-13 — 81차: K-POP 스테이지 클리어의 옛 수채화 회상 팝업 제거 (컴파일 0, 캡처 r81_clear_*, 커밋은 사용자)
- 증상(사용자 캡처): K-POP 러닝 「STAGE CLEAR!」 위에 옛 수채화 컷씬 한 장 + 「화면을 탭하면 닫혀요」가 겹쳐 떠서 「다시 달리기」 버튼까지 가림. 정체는 **`UI_MemoryPopup`(기억 조각 회상)** — 54·68차 이전의 수채화 스틸을 쓰는 옛 시스템.
- 조치: ① `Core/SceneFlowController.OnStageClear…` 에서 `mem.PlayQueuedIfAny()` 호출 삭제(클리어 UI 가 없을 때만 바로 다음 단계로) ② `Core/StageManager.LocalClearWithMemory` 에서도 호출 삭제(정산 대기만 남김) ③ 초크 포인트로 **`Story/MemoryDirector.PlayQueuedIfAny()` 자체를 큐만 비우고 끝내도록** 바꿈 — 다른 경로가 남아 있어도 클리어 화면엔 못 뜬다.
- 남겨 둔 것: 해금 기록(`MemoryFragmentLog`)과 `ReplayFromGallery`(옛 타이틀의 「회상」 갤러리 — 현재 타이틀은 gate art 경로라 사실상 미사용). 이야기는 시네마(`CinematicPlayer`)로만 본다.
- 확인: 봄 러닝(ch3) → 원격 `clear` → 클리어 화면에 팝업 없음, 세 버튼 모두 정상(캡처 r81_clear_a/b).
- 커밋 대상: Scripts/Core/{SceneFlowController,StageManager}.cs, Scripts/Story/MemoryDirector.cs, Docs/HANDOVER.

---

### 2026-09-13 — 80차: 대본 반전 순서 재설계(18화까지 「도윤이 죽은 것 같다」) · 트럭 컷·엄마 그림 재생성 (컴파일 0, 커밋은 사용자)
- 사용자 지시 5가지: ① 엔딩1 자막의 「스무 챕터 만에」가 이상하다 ② 엄마 리어카 컷은 「심장」이 아니라 **심장병** ③ 엄마 그림이 늙었다 젊었다 한다 — 통일 ④ **우비 꼬마 = 기억이 남아 있는 환생한 아빠**라는 걸 20화쯤 긴가민가 알게 ⑤ **18화까지는 여주가 죽은 게 아니라, 남주가 죽은 것처럼** 보이게(특히 트럭이 그냥 통과하는 설정은 「트럭 옆으로 넘어졌다」로).
- `Story/CinematicTable.cs` 자막 14줄 수정(자세한 목록은 `Docs/CUTSCENE_SCRIPTS_v3.md` 「80차 개정」 절):
  - 유령 확증 삭제 — 1-4(스치고 지나감 → 인사에 대답 없음), 3-4(손이 통과 → 잡으려다 멈춤), 3-6(발자국 없음 → 돌아보면 눈에 덮임), 7-5·7-6(통과 → 눈을 안 맞춤), 6-11(「살아 돌아온 줄로만 알았다」 추가).
  - 의심을 도윤에게 — 7-3 「이 사람, 얼마나 남은 걸까」, 7-10 「그 꽃이 그를 위한 거라고 생각했다」, 7-12 「유리에 비친 게 나라고 믿었다. 그럼 옆에 선 그는」.
  - 5-3 트럭 「통과」 → 「바로 옆을 스쳤고 나는 길 옆으로 넘어졌다」 / 5-6 심장 → **심장병** / EA-5 「스무 챕터 만에」 → 「여덟 해 만에」.
  - 20화 환생 떡밥 — 8-8 「여섯 살이라던 아이가 여덟 해 전 그 밤을 알고 있었다」, 8-9 꼬마가 아빠 말버릇 「한 마리만 더 잡으민 되주」.
- **그림 재생성 3장**(Kling 웹 10크레딧, 14 → 6): `Cut_S_N5_03`(옆으로 지나가는 트럭·갓길에 넘어진 하늘·밀어낸 꼬마), `Cut_S_N2_04`·`Cut_S_N2_05`(엄마). 옛 파일은 `Tools/KlingGen/out/web80/_old_*.jpg`.
- **엄마 바이블 수정**: 40대 → 「**마흔다섯, 둥글고 볕에 그은 얼굴·통통하고 다부진 체격·검은 머리에 흰머리 몇 올**」, 프롬프트에 `only FORTY-FIVE years old … must NOT look elderly, no deep wrinkles, no white hair` 를 반드시 넣는다. (첫 시도에서 `weathered/crow's feet` 때문에 70대 할머니로 나왔다.)
- 커밋 대상: Scripts/Story/CinematicTable.cs, Resources/CoastRun/{Cut_S_N5_03,Cut_S_N2_04,Cut_S_N2_05}.jpg, Docs/CUTSCENE_SCRIPTS_v3.md, Tools/KlingGen/out/web80/*, Docs/HANDOVER.

---

### 2026-09-13 — 79차: 시네마 엔딩 3편 임시 해금 (컴파일 0, 캡처 r79_end_open, 커밋은 사용자)
- 사용자 지시: 「엔딩도 일단은 열어줘」 → `UI/CinemaSelect.cs` 맨 위에 **`public const bool OpenAllEndings = true`** 추가, 엔딩 카드 3장을 `endingMask` 와 상관없이 열어 둔다(제목·표지·길이 1:08 모두 노출, 눌러서 바로 재생).
- **되돌리는 법**: 그 상수만 `false` 로 바꾸면 77차 규칙(실제로 깬 엔딩만 열리고 나머지는 「???」 회색 잠금)이 그대로 살아난다. 비밀코드 `devUnlockAll` 로는 여전히 안 열린다.
- 확인: 시네마 목록 아래 「엔딩 1 · 스무 살 생일 · 만난다」 「엔딩 2 · 우유 두 병 · 못 만난다」 「엔딩 3 · 주파수 · 진엔딩」 노란 카드로 열림(캡처 r79_end_open).
- 커밋 대상: Scripts/UI/CinemaSelect.cs, Docs/HANDOVER.

---

### 2026-09-13 — 78차: 꼬마 집사를 컷씬의 주황 우비 꼬마로 재디자인 (컴파일 0, 캡처 r78_*, 커밋은 사용자)
- 사용자 지시: 「꼬마집사를 시네마틱에 나오는 주황색 우비 소년으로 다시 디자인해줘」. 옛 집사는 검은 조끼·흰 장갑·나비넥타이의 3D 아이 → 컷씬 KID 바이블(너무 큰 주황 우비·후드 아래 동그란 뺨과 큰 눈·노란 장화)로 교체.
- 교체한 4종(`Assets/Resources/CoastRun/`): `UI_Butler_Boy.png`(전신 정면·손 흔듦 — 육성 화면 조언 꼬마), `UI_Butler_Bust.png`(흉상 — 육성 튜토리얼 `RaisingTutorial`), `UI_Face_Butler.png`(원형 얼굴 뱃지 512² — 러닝 피버 `FeverMode`·윷놀이 말), `MG_Char_ButlerBack.png`(뒷모습 전신 — 무궁화 술래 뒷모습·대회 라이벌). 옛 파일은 `Tools/KlingGen/out/web78/_old_*.png` 에 백업.
- 생성: Kling 웹 6크레딧(22 → 14). 프롬프트는 컷씬 스타일 문장 + 「single isolated character on a FLAT SOLID MAGENTA background (pure #EA0072), no scenery, no ground, no cast shadow」 + KID 바이블. 뒷모습은 한 번에 안 되고(정면이 나옴) 「REAR VIEW ONLY … NO face, NO eyes … the child is walking away」로 2종×2장 재생성해서 BACK_A1 채택.
- 후처리 `Tools/KlingGen/out/web78/process78b.py`: HSV 색상 288~352°·채도 0.30↑ 를 알파 0 으로 키잉(마젠타 배경·바닥 그림자까지 제거) → 알파 bbox 트림 → 흉상은 위 62% 크롭, 얼굴은 머리 정사각 크롭 후 크림색 원 + 남색 링(14px) 뱃지로 합성. 얼굴 뱃지는 크롭이 턱을 자르기에 클라우드에서 다시 만들어(`face_badge_v2.png`) 덮어씀.
- 검증: 컴파일 0, 무궁화 미니게임 술래가 새 주황 우비 꼬마로 나옴(캡처 `r78_mugunghwa_play.png`).
- ⚠ 도구 함정: `device_commit_files` 로 **같은 이름의 파일을 다시 커밋하면 mtime 만 바뀌고 내용이 안 바뀌는 경우**가 있었다(urls78.json). 스크립트/데이터를 고쳐 보낼 때는 **새 파일명**(dl78b.py, urls78b.json, process78b.py)으로 보낼 것.
- **APK 빌드 결과(9/13 11:21)**: 성공 — `Builds/CoastRun.apk` **478 MB**(직전 9/13 02:09 빌드 307 MB). 첫 시도는 Gradle `AccessDeniedException`(`:unityLibrary:mergeReleaseNativeLibs`, merged_native_libs/release/out/lib/arm64-v8a)로 실패했고 **`Library\Bee\Android\Prj` 삭제 후 재시도로 통과**(IL2CPP 산출물이 남아 있어 2~3분 만에 끝남). 실패 시 `Builds/last_build.txt` 가 24 KB 짜리 Gradle 덤프, 성공 시 92 B.
- ⚠ **용량**: Resources 원본이 456 MB(Cut_V_* 108 MB · Cut_CH* 74 MB · Cut_S_* 20 MB · 기타 255 MB)라 APK 가 478 MB 까지 커졌다. 줄이려면 ① 새 Cut_S_* 로 대체된 **옛 Cut_V_* 스틸 삭제**(테이블 폴백만 쓰는 중, −108 MB) ② Cut_CH*(리더용)·Cut_S_* 텍스처 임포트 압축(ASTC/ETC2)·최대 크기 제한 ③ 오디오·영상 StreamingAssets 분리. 스토어 업로드는 AAB/PAD 필요.
- ⚠ 빌드 후 에디터 로그 후크가 끊겨 `unity_log`/`log n` 이 갱신되지 않았다(메뉴 명령은 정상). 새로 쓴 텍스트 파일이 `device_stage_files` 에서 계속 nlink>1 로 거부되기도 하니, 결과 확인은 파일 mtime·크기(`device_list_dir`)로 하는 편이 빠르다.
- 커밋 대상: Resources/CoastRun/{UI_Butler_Boy,UI_Butler_Bust,UI_Face_Butler,MG_Char_ButlerBack}.png(+meta), Tools/KlingGen/out/web78/*(원본·스크립트), Docs/HANDOVER.

---

### 2026-09-13 — 77차: 컷씬 전면 재작업 — 대본 v3(+20초·떡밥) · 캐릭터 바이블·앵커 · 카드 CHAPTER 삭제 · 시네마 엔딩 3편(본 것만 열림) · **새 스틸 131장 전부 Kling 웹으로 생성·적용** · APK 빌드 (컴파일 0, 캡처 r77_*, 커밋은 사용자)
- **Kling API 잔액 소진**(`code 1102`) → 사용자 지시로 **Kling 웹(app.klingai.com, Standard 요금제) 을 클로드 내장 브라우저로 자동화**해 131장 + 재생성 8장 = 139 크레딧 사용(161 → 22 남음). 파이어플라이는 로그인이 안 돼 있어 쓰지 않음.
- **웹 파이프라인**(다음에 또 쓸 때): ① 프롬프트 = `Tools/KlingGen/jobs/r77_cuts.json` 의 prompt(스타일 문장 + 장면 + `Characters:` 바이블). ② 브라우저 패널 탭에서 페이지 안 러너(`window.__q`/`__run`, 에디터 `.tiptap.ProseMirror` 에 `execCommand('insertText')` → `button.button-pay` "Generate" 클릭 → `.point-box .value` 크레딧이 1 줄면 성공, 6.5초 간격, 4회 재시도). 설정은 IMAGE 3.0 · 1K SD · 9:16 · 1장. ③ 결과 수집: feeds API 는 페이지 fetch 로 막힘(빈 응답) → 작업 목록(`.virtual-history-stream`, 가상 스크롤) 의 썸네일 `img[src*="kimg/"]` 에서 토큰을 읽고 프롬프트 문장으로 컷 id 매칭. 토큰은 urlsafe base64 protobuf(파일명 `..._ai_web_<workId>_<32hex><5>.png` 포함) — `:360x638.webp` 를 `.origin?x-kcdn-pid=112372` 로 바꾸면 원본(768×1360). ④ 다운로드는 클라우드에서 CDN 이 막혀 **PC 파이썬**으로: `Tools/KlingGen/out/web77/urls.json`{Cut_S_id: url} → `dl77.py`(전체, 있으면 skip) / `dl77b.py`(지정 컷 강제, 옛 것은 `_old_` 접두로 보관). ⑤ 검수는 `sheet77.py` 콘택트시트(`_sheet_OP|N1-N4|N5-N8|END.jpg`) 를 stage 해서 보기. ⑥ `copy77.py` 가 720×1280 JPG(q90) 로 `Assets/Resources/CoastRun/Cut_S_<id>.jpg` 저장(같은 이름 png 있으면 삭제).
- **PC 실행 통로**: Photoshop `app.system(powershell Get-Clipboard)` 가 걸린 뒤 Photoshop MCP 가 응답 없음(get_state 타임아웃, ping 만 됨 — 재시작 필요할 수 있음). 대신 **`Editor/RunBatMenu.cs`**: 원격 `menu Coast Run/Dev/Run - Tools/_clip/run.bat` → cmd 로 실행, 출력 `Tools/_clip/run.log`, 180초 대기 후 `[RunBat] exit=…` 경고 로그에 마지막 15줄. bat 은 `device_commit_files` 로 갈아끼운다(현재 내용: 스크린샷 복사). `Editor/ClipboardDump.cs`(클립보드 → Tools/_clip/clip.txt), `CoastRemote` 의 HTTP `POST /drop/<name>`(CORS 헤더 포함) 도 넣었지만 브라우저 패널이 localhost 를 못 불러 미사용.
- **검수 결과**: 131장 중 8장 재생성(OP_5 거꾸로 된 반사 → 도랑에서 가방 올려주는 정면 / N3_05·N4_11 액자 테두리 / N3_09 위 흰 여백 / N4_02 거대 손가락 / N5_03 그림 안 「HANEUL」 글자 / N7_03 거대 손 / EB_03 키스처럼 나온 손). 확인 항목 OK: 아이 컷은 아이 비율, 컷씬1 첫 컷 꼬마 얼굴 보임·꼬마만 우비·주인공 하늘색 후드, 아빠 배 = 조타실 있는 고기잡이 배(N2_07/08/11), 해녀복·주황 태왁(N2_04/05, N6_07~10), 우비 떨어지는 떡밥(N6_08/09). 남은 잔손: N7_08·N8_01 빈 말풍선, N8_03 손목 글자 — 8.5초 컷이라 그대로 둠(크레딧 22).
- **엔딩 카드 확인**(캡처 r77_cinema_tall3): 컷씬 8장 표지가 새 `Cut_S_N<n>_01` 로 바뀌고 아래 「엔딩 1·2·3 / ??? / 잠김 · 이 엔딩을 보면 열려요」 회색 카드. 개발 메뉴 `Coast Run/Dev/Cine - Select`·`Cine - END_A` 추가.
- **APK**: `Coast Run/Build/Android APK (IL2CPP, ARM64+ARMv7)` 로 빌드(아래 「빌드 결과」 참고). Resources 에 JPG 131장(≈30 MB) 추가됨 — 용량 이슈는 그대로(AAB/PAD 검토).
- **캐릭터 바이블**(`Docs/CUTSCENE_SCRIPTS_v3.md` 표 + 앵커 `Tools/KlingGen/ref77/{H19,H12,D12,D20,KID,DAD,MOM,BOAT}.png`): 하늘19 = 긴 검은 머리·노란 핀·**하늘색 후드**(주황 금지), 하늘12 = 단발·연하늘 티, 도윤12 = 흰 셔츠+남색 조끼(창백), 도윤20 = 회색 후드 코트+병원 팔찌, 꼬마 = 너무 큰 주황 우비·후드 아래 뺨 보임·노란 장화, 아빠 = 같은 주황 우비의 어부, 엄마 = 검은 해녀복+두건+물안경+**주황 태왁**, 배 = 흰·파랑 **연안 고기잡이 배**(조타실·그물·부표). 화풍 = 컷씬 7 기준 「clean soft anime, Korean webtoon key-visual」(수채 아님).
- **앵커 사용법 교훈**: `reference_type=subject`(fidelity 0.45)는 인물을 정면 전신으로 복제해 구도가 전부 깨진다(캡처 b1) → **참조 없이 바이블 문장으로만** 생성하고, 얼굴이 크게 나오는 컷만 `face` 참조를 시도할 것(face 는 잔액 소진과 겹쳐 검증 못 함). 배치 6건 동시 제출은 됨.
- **대본 v3**(`Story/CinematicTable.cs` 전면 교체, `Docs/CUTSCENE_SCRIPTS_v3.md`): 오프닝 11컷 × 10.6 s(≈1:57, hold 117, 음악 루프), 컷씬 8편 × 12컷 × 8.5 s(≈1:42), 엔딩 A/B/진엔딩 8컷 × 8.5 s. 추가 떡밥: 오프닝 괴롭힘 4컷(도시락 없음·생선 냄새·장날 좌판·가방 도랑) → 도윤이 건져 주고 앞에 서서 평화 / 컷씬 3 「우리 집 아저씨가 온다」 / 돌 세 개(너·나·아저씨) / 아빠 배·하트 두 개 부표 / **컷씬 6-8·9: 해녀 출근 아침마다 벽의 우비가 떨어짐(못은 멀쩡) → 세 번째 아침 안고 나간 게 마지막** / 컷씬 7 이름 없는 국화·「저 사람은 아직 몰라」 / 컷씬 8 개켜진 찢어진 우비. 컷씬1 첫 컷: 꼬마가 문가에 서고 **후드 아래 뺨·눈이 보이게**, 주인공은 하늘색 후드.
- 그림 파일명은 `Cut_S_<컷id>.png`(OP_1~11 · N1_01~N8_12 · EA/EB/ET_01~08 = 131장) — **없으면 fallback(옛 스틸)로 재생**되므로 지금도 새 대본으로 돈다(캡처 r77_cs1_a: 옛 주황 후드 그림이 폴백으로 나옴). 옛 cs1~cs8 영상은 새 그림과 안 맞아 clip 을 뺐다(MV 영상만 유지) — 새 스틸이 오면 image2video 로 다시.
- **카드**: 컷씬 카드 부제(CHAPTER n~m) 삭제 — cardSub 없음. 엔딩 카드는 「스무 살 생일 / 엔딩 A · 만난다」식.
- **시네마 엔딩**(`UI/CinemaSelect.cs`): 목록 끝에 엔딩 1·2·3 — `Profile.endingMask`(bit0~2 A, 3~5 B, 6 진엔딩)로 **실제로 본 엔딩만** 열림(비밀코드 devUnlockAll 로도 안 열림). 잠기면 회색 카드 + 제목 「???」 + 「잠김 · 이 엔딩을 보면 열려요」, 탭하면 토스트. 열리면 `CinematicPlayer.Play("END_A"|"END_B"|"END_TRUE")`(시네마틱 버전; 게임 내 엔딩은 여전히 ChapterVN 대사판). 캡처 r77_cinema_tall2.
- **남은 것**: 첫 컷 8장 image2video(`StreamingAssets/Opening/cs1..8.mp4` + 테이블 clip 복구) — API 크레딧 충전 후 또는 웹 영상(크레딧 큼). 옛 `Cut_V_*` 스틸은 폴백용으로 남겨 둠(정리 시 삭제 가능).
- 커밋 대상: Story/{CinematicTable,CinematicPlayer}.cs, UI/CinemaSelect.cs, Editor/{RunBatMenu,ClipboardDump,CoastRemote,MiniGameDevMenu}.cs, Resources/CoastRun/Cut_S_*.jpg(131 + meta), Docs/CUTSCENE_SCRIPTS_v3.md, Tools/KlingGen/jobs/r77_cuts.json, Tools/KlingGen/ref77/*.png(8), Tools/KlingGen/out/web77/{urls.json,dl77.py,dl77b.py,sheet77.py,copy77.py}, Tools/_clip/run.bat, Docs/HANDOVER. (Tools/KlingGen/out/web77/*.png 원본 131장은 커밋 제외 권장 — 200 MB.)

---

### 2026-09-13 — 76차: 「장애물에 부딪쳐도 HP 1 정도만」 진짜 원인 = 에디터 God mode + 무적프레임 · 최소 피해 게이지 30 확정 (컴파일 0, 로그 검증, 커밋은 사용자)
- **원인 ①(주범)**: 에디터 PlayerPrefs 에 옛 디버그 키 `CoastRun.Debug.God` 이 켜진 채 남아 있어 `PlayerController.Awake` 가 `Invincible = true` → **모든 장애물 피해가 무시**되고 초당 1.6 드레인만 보였다(진단 로그 `[Hit] 무시: Invincible=True god=True`). 켜는 메뉴는 이미 사라진 상태였음. 조치: 키를 `CoastRun.Debug.God2` 로 바꿔 자동 해제, 에디터·개발 빌드에서만 읽고 출시 빌드는 항상 false, 켜져 있으면 HUD 왼쪽에 빨간 「GOD · 피해 무시(Dev)」 배지, 개발 메뉴 `Coast Run/Dev/God mode - ON|OFF` 추가.
- **원인 ②**: 피격 후 무적프레임(순발력 0.8~2.0 s) 안에 두 번째 장애물을 치면 꽈당 연출만 나고 HP 는 안 닳았다(넉백으로 옆 레인 장애물에 연달아 부딪힐 때 특히). 조치(`PlayerController.SoftHitApplied`): **피해 이벤트 `OnHitDamage` 를 경직 이벤트 `OnSoftHit` 와 분리** — 무적프레임은 경직·넉백 중복만 막고 피해는 매 충돌마다 낸다(같은 장애물의 겹친 콜라이더만 0.15 s 디바운스). 피버·거인·보너스타임 무적은 그대로 피해 0. `HealthSystem.Bind` 는 `OnHitDamage` 를 듣는다.
- **최소 피해 규칙**(`HealthSystem.HandleHit`): `dmg = max × clamp(비율 × 스탯감소, 0.30, 0.60)` — **HUD 게이지(0~100) 기준으로 항상 30~60**. 전엔 절대값 30~60 이라 체력 스탯으로 최대 HP 가 169~200 이면 게이지가 15~18 만 빠져 보였다. 비율 ≥ 1 = 즉사 유지. 로그 `[HP] hit frac=… → dmg=51 = 게이지 30 (hp 90/169)` 는 이제 Warning(원격 `log` 로 보임).
- **장애물별 비율 재점검**(전부 ≥ 0.30, 호출처 전수): ObstacleCatalog.DamageFrac(버스 0.60 · 석상 0.50 · 관광객 0.45 · 허들/빨랫줄/등불 줄/바리케이드/스쿠터 0.40 · 상자/택배/슬라임 0.35 · 자전거/눈더미/콘/표지판/낙엽/웅덩이 0.30) · OncomingCar(버스 0.60 · 승합 0.50 · 킥보드 0.40 · 귤 0.30) · SkyHazards(바위 0.50 · 태풍 0.45 · 미사일 0.35) · DuckHazard 0.40 · DefaultFrac 0.30. SoftHit 를 부르는 곳은 ObstacleHazard·DuckHazard·원격 `hit` 뿐이고 셋 다 배율을 넣는다.
- **HUD**: 피격마다 게이지 오른쪽에 빨간 「−30」이 떠오른다(`RunHudChrome.DamagePopCo`, 게이지 단위) — 폰에서도 피해량을 눈으로 확인 가능. 육성 순발력 설명은 「피격 경직 ×… · 연속 경직 방지 N초」로(무적 표현 삭제).
- 검증: K-POP 봄 러닝(ch3)에서 원격 `hit` 2연타 → `[HP] … 게이지 30 (hp 90/169)` / `(hp 37/169)` 두 줄(무적프레임 안 2타 모두 적용). 입력 없이 자동 주행하면 3방에 죽는다 — 사용자가 원한 난이도.
- 커밋 대상: Player/PlayerController.cs, Progression/HealthSystem.cs, UI/RunHudChrome.cs, Raising/RaisingUI.cs, Meta/RunTuning.cs, Editor/MiniGameDevMenu.cs, Docs/HANDOVER.

---

### 2026-09-13 — 75차: 레코드 홈 버튼 수정 · 오프닝 어린 시절 재생성 · 회상 영화 연출(레터박스·플래시·그레인·큰 태그) · 컷씬 8편 10컷×2문장 확장 + Kling 스틸 24장 · Kling 영상 10편 · 엔딩/레코드 뮤직비디오 「Our frequency」 30초 (컴파일 0, 캡처 r75_*, 커밋은 사용자)
- **1) 레코드 홈 버튼**(`CollectionUI.Music.cs`): `CoastUiArt.GlossyPill` 은 `raycastTarget` 기본 false 라 Button 이 클릭을 못 받던 것 → `homePill.raycastTarget = true`. 캡처 `r75_after_home.png`(레코드 → 타이틀).
- **2) 오프닝 어린 시절**: `Cut_V_OP_1/2/6` 을 「small twelve-year-old children, childlike proportions」 + 부정 「adult, teenager」로 재생성해 덮어씀(유채밭 두 아이·첫 악수·벤치 노을). 캡처 `r75_op1/op2.png`.
- **3) 회상 연출**(`CinematicPlayer.cs`): 회상 태그 18 → **30 볼드**(「— 회상 · 여덟 해 전 —」, y −150, 640×56, 살짝 숨쉬듯 2 %), 회상 컷에서 **위아래 검은 레터박스**가 100 까지 내려오고(0.4초, `UpdateFlashback`), **진입 화이트 플래시** 0.55초(`FlashCo`), **필름 그레인**(256² 타일 스프라이트, α 0.10~0.16 펄린 흔들림 + ±6 지터), 비네트는 회상에서 더 진하게(72차 값). 세피아는 그대로 `UIDesaturate` (`_Keep` 로 빨강 하트 보존). 자막 여백 40 → 26 · 최대 27 → 25 로 두 문장이 **두 줄**에 맞음(세 줄째 한두 단어 고아 방지). 캡처 `r75_cs2_sepia*.png`(레터박스 192 px 확인).
- **4) 컷씬 확장**(`CinematicTable.cs`): CS1~CS8 각 **10컷 × 8초**, 자막은 **두 문장 ≤ 64자**(예전 한 문장 ≤ 34자의 1.5~2배). 새 Kling 스틸 `Cut_V_N<ep>_1~3`(24장, 9:16 수채 풀블리드, 어린 시절 컷은 KID 접두). 회상 태그 종류: 「— 회상 · 여덟 해 전 —」「— 회상 · 열세 해 전 —」「— 회상 · 태풍 전날 밤 —」「— 회상 · 열흘 뒤 —」「— 회상 —」. 첫 생성에서 어른처럼 나온 4장(N3_1 도시락 교실 · N5_2 리어카 · N5_3 병원 복도 · MV_3 두 손)은 재생성해 교체.
- **5) 영상**: 각 컷씬 첫 컷(`cs1..cs8`)과 MV 1·5컷(`mv1`, `mv5`)을 **Kling image2video**(kling-v1-6, 5초, std, 원본 스틸 그대로 살짝 움직임)로 만들어 `StreamingAssets/Opening/<clip>.mp4` 로 복사(6~18 MB ×10). `CinematicPlayer` 는 클립이 있으면 영상(길이 −0.15초), 4초 안에 준비 안 되면 스틸 켄번즈 폴백. Firefly 는 쓰지 않음(브라우저 자동화 부담) — Kling image2video 로 대체. Blender/Hyper3D 는 여전히 미연결.
- **6) 뮤직비디오 「Our frequency」**(`CinematicTable.MV`): BGM_M1, 6컷 × 4.5초(`Cut_V_MV_1~6`: 유채밭 두 사람 · 절벽길 · 두 손과 우유병 · 탑 아래 라디오 · 능선 실루엣 · 새벽 마주봄) + `holdToSeconds 30` → 약 30초. 마지막 카드 「Our frequency」 64 볼드 + 세 줄 부제 「스튜디오 우히&히시 / YouTube Music · Spotify · iTunes · TIDAL / 「Our frequency」 검색」(부제에 줄바꿈이 있으면 0.585 로 내려 제목과 안 겹침). **엔딩**: `SceneFlowController.CompleteEndingReturnToTitle` 이 타이틀로 가기 전에 MV 를 한 번 재생(`_mvShown`). **레코드**: 7번 트랙 아래 분홍 알약 「▶ 뮤직비디오 「Our frequency」 · 스튜디오 우히&히시」 → `CinematicPlayer.Play("MV", ShowMockRecords)`(끝나면 레코드로 복귀). 캡처 `r75_mv1/mv_c5/mv_card5.png`.
- **주의**: 에디터 원격 `tap`/`unity_shot` 이 게임 뷰를 클릭해 컷이 앞당겨진다(`Tapped()` 의 MouseButtonDown) — 실제 기기와 무관. `key Escape` 는 레거시 Input 에 안 들어가 컷씬 스킵이 안 됨(스킵은 「건너뛰기」 탭 또는 1초 홀드). 새로 만든 파일은 1분쯤 지나야 `device_stage_files` 가 읽는다(nlink>1 오류). Kling image2video 는 동시 5건 넘게 넣으면 `request failed` — 한 건씩 재시도.
- 커밋 대상: Story/{CinematicPlayer,CinematicTable}.cs, UI/CollectionUI.Music.cs, Core/SceneFlowController.cs, Resources/CoastRun/{Cut_V_N1_1…N8_3(24), Cut_V_MV_1~6, Cut_V_OP_1/2/6}.png(+meta), StreamingAssets/Opening/{cs1..cs8,mv1,mv5}.mp4, Docs/HANDOVER, Docs/CUTSCENE_SCRIPTS_v2.md.

---

### 2026-09-13 — 74차: 러닝 메시지 분홍 팝 · 장애물 피해 HP 30~60 고정 · 기부 선물 ④ OST 잠금해제·레코드 잠금·스트리밍 안내 (컴파일 0, 캡처 없음 — 사용자가 에디터 플레이 중, 커밋은 사용자)
- **1) 분홍 팝 메시지**(`UI/CoastToast.cs`): `CoastToast.Pop(msg)` 신규 — 화면 가운데 위(0.62)에서 분홍(1, 0.36, 0.66)+흰 테두리+그림자 글자가 0.6 → 1.15 → 1.0 배로 터지고 1.6초 살랑이다 위로 떠오르며 사라진다(여러 개면 70씩 아래로 쌓임). `CoastToast.Show` 는 **러닝 중(RunHudChrome.Instance 있음)이면 자동으로 Pop**, 밖에선 옛 위 띠(`ShowBar`). `RunHudChrome.ShowToast`(펫 발동 등)도 Pop. → 「포토카드 전부 모았어 — 코인 +50」, K-POP 미션 달성, 흑돼지 부활, 팡찌 출동 등이 전부 팝으로.
- **2) 피해 전수 점검**: 피해 경로는 `PlayerController.SoftHitApplied → OnSoftHit → HealthSystem.HandleHit` 하나뿐이고 배율은 `PendingHitDamageMul`. **누락 하나**: 허들류(`DuckHazard`: 봉·빨랫줄·등불 줄)는 배율을 안 넣어 직전 값이 남았다 → 0.40 명시. 최종 피해는 `Mathf.Clamp(100 × 비율 × 스탯감소, 30, 60)` 으로 **HP 30~60 절대값 고정**(최대 체력과 무관; 1 이상 = 즉사 그대로). 표(`ObstacleCatalog.DamageFrac`): 버스 0.60 · 석상 0.50 · 관광객 0.45 · 허들/바리케이드/스쿠터 0.40 · 상자/슬라임 0.35 · 자전거/눈더미/콘/표지판/낙엽/웅덩이 0.30. 마주 오는 차: 버스 0.60 · 승합차 0.50 · 킥보드 0.40 · 귤 0.30. 하늘: 바위 0.50 · 태풍 0.45 · 미사일 0.35. `DefaultFrac` 0.30. 피격마다 `[HP] hit frac=… dmg=…` 로그를 남기니 「1 정도」가 또 보이면 그 줄을 보면 된다(디버그 오버레이 아님 — 에디터/로그캣).
- **3) 기부 선물 ④ OST**(`Donation.Gift.Ost = 4`, `Donation.OstOpen`): DonateUI 선물 4개(카드 1010 → 1070, 알약 간격 56) — 「♫ OST 잠금해제 (레코드 전곡)」, 결제 후 상태문 「레코드의 OST 전곡이 열렸어요」, M2~ 전부 NEW. **레코드 잠금**(`RecordTable.IsUnlocked`): M1 만 기본 공개, 나머지는 `Donation.OstOpen` 으로만(컷씬·S급 해금 폐지 — `OnSceneWatched` 는 OST 열린 뒤에만 NEW 표시, 비밀코드 devUnlockAll 은 테스트용으로 그대로). 레코드 화면: 잠긴 곡도 **제목은 회색으로 보이고** 부제 「🔒 잠김 · 기부 선물 「OST 잠금해제」로 열려요」, 탭하면 안내 토스트. 상단 알약을 넓혀 두 줄: 「✦ AI 생성 음원 · 가상 듀오 우히&히시 / 유튜브뮤직 · 아이튠즈 · 타이달 · 스포티파이에서 「Our frequency」 검색해 들어요」(시안에 박힌 COLLECTED 배지를 덮는다).
- 관찰(입력 감시견 성과): 러닝에서 타이틀로 돌아오면 `[Title] _ready 가 팝업 없이 8초 꺼져 있어 다시 켰다` 가 찍혔다 — **71차 전이라면 타이틀이 영영 안 눌리던 경우**. 원인 후보: 러닝 시작 시 `_ready=false` 로 두고 씬을 나갔다가 돌아온 새 컨트롤러가 아니라… 정확한 경로는 다음에 로그로 추적(`ReadyWatch` 가 8초 뒤 풀어 준다).
- 커밋 대상: UI/{CoastToast,RunHudChrome,DonateUI,CollectionUI.Music}.cs, Progression/HealthSystem.cs, Economy/{DuckHazard,OncomingCar,SkyHazards,ObstacleHazard}.cs, Content/ObstacleCatalog.cs, Meta/{Donation,RecordTable}.cs, Docs/HANDOVER.

### 2026-09-13 — 73차: 상점 통합(일반+펫 탭) · 펫 버튼 삭제·홈 길게 · 러닝 펫 새 그림/배치/움직임 · 오프닝 9컷 수채 재생성 · 시네마 감상 후 선택 페이지 복귀 (컴파일 0, 캡처 r73_*, 커밋은 사용자)
- **1) 상점 통합**(`UI/ShopUI.cs` 신규, 시안 2장): 금테 크림 카드 위에 탭 알약 두 개 「🛒 일반상점」 / 「🛒 펫상점」(선택 = 노랑+반짝, 비선택 = 베이지), 「★ 돈/포인트 · Lv ★」, 줄 4개(흰 초상 틀·이름·설명·금 알약 「◆ 가격 · 보유 ◆」·파란 「구매」), 회색 「닫기」. 일반 탭 = 쌀 1주·쌀 4주·반찬·새 옷(옛 GroceryUI 규칙 `Survival.Buy*`, 아이콘 `UI_Goods_Rice/Side/Shirt.png` Kling 신규), 펫 탭 = 옛 PetShopUI 규칙(`PetShop.TryBuy/Equip`, 20주차 해금이면 버튼에 「20주차」). 육성 **장바구니 버튼 → ShopUI(일반 탭)**. `GroceryUI.Open`/`PetShopUI.Open` 은 ShopUI 로 넘기고 옛 화면은 `OpenLegacy` 로만 남김(개발 메뉴 `Pet - Shop` 도 통합 화면).
- **2) 육성 상단**: 「펫」 버튼 삭제, 홈 버튼 64 → **134 폭**(집 아이콘 + 「홈」 글자). 캡처 r73_raise3.
- **3) 러닝 펫**: 첨부한 뒷모습 4장(마젠타 배경)을 numpy 크로마키(마젠타도 (r+b)/2−g, 디스필) → `Obs_Pet_Sparrow/BlackPig/BikerThug/WildGoose.png`(1024, 덮어씀). `PetCompanion`: 오토바이(팡찌)만 **오른쪽 바닥**(1.0, 0, 0.6)을 같이 달리고 나머지 셋은 **오른쪽 위**(0.82, 1.55, 0.35)에 떠다님(둥둥 0.12 + 살랑 0.05 + 날갯짓 스쿼시 4 % + 기울기 ±5°), 크기 = 주인공 35 %(높이 0.56, 오토바이 0.64). 오토바이는 **몸으로 닿는 장애물(0.8 m)을 주인공처럼 팡 부순다**(`SmashNearby`, 횟수 제한 없음 · 기존 돌진 3회는 그대로). **3D 메시화는 못 함**: Blender MCP 서버가 안 붙어 있고(Blender 를 띄워도 애드온 서버 미시작) Hyper3D/Hunyuan 키도 없다 — 첨부 그림 자체가 3D 렌더라 빌보드 + 움직임으로 대체. 캡처 r73_run(팡찌 오른쪽 바닥).
- **4) 오프닝 9컷**을 컷씬과 같은 수채 풀블리드로 Kling 재생성(`Cut_V_OP_1~9.png`), `CinematicTable` OPEN 은 **영상(open_*.mp4) 대신 스틸 + 72차 움직임 효과**(폴백 = 옛 Cut_Open_*). 옛 3D 카툰풍 영상·스틸은 더 쓰지 않지만 파일은 남겨 둠(StreamingAssets/Opening, Cut_Open_*) — 지우려면 커밋 때 함께. 시네마 카드 표지도 새 스틸(Cut_V_*) 우선.
- **5) 시네마**: 카드 재생(시네마틱/리더/오프닝)이 끝나면 **시네마 선택 페이지를 다시 연다**(`CinemaSelect.Reopener` — 열 때의 gm/onPlayStart/onClose 를 붙들어 둠). 메인으로는 X 를 눌러야 간다(그때 타이틀 음악·_ready 복귀).
- 확인: 컴파일 0. 사용자가 에디터에서 직접 플레이 중이라([DJ] 로그) 상점·오프닝 캡처는 못 찍음 — 다음 세션 첫 확인: 육성 🛒 → 탭 전환, `Cine - OPEN`, `Pet - Equip Sparrow + run`(타이틀→스토리 진입 후).
- 커밋 대상: UI/{ShopUI(신규),PetShopUI,LifeUI,CinemaSelect}.cs, Raising/TamaRaisingUI.cs, Player/PetCompanion.cs, Story/CinematicTable.cs, Resources/CoastRun/{Cut_V_OP_1~9,UI_Goods_Rice,UI_Goods_Side,UI_Goods_Shirt}.png(신규, +meta), Obs_Pet_*.png(4 덮어씀), Docs/HANDOVER.

### 2026-09-13 — 72차: NOW PLAYING 이퀄라이저 알약 · 완주 얼굴 앞으로 · 컷씬 스틸 37장 Kling 재생성(풀블리드) + 크로스페이드·움직임 효과 · 육성 첫 진입 오프닝 · 레코드 홈 버튼 (컴파일 0, 캡처 r72_*, 커밋은 사용자)
- **1) K-POP 러닝 NOW PLAYING**(`UI/KpopNowPlaying.cs` 신규, `RunHudChrome.BuildKpopSongLabel` 이 호출): 우하단 262×60 보라→분홍 가로 그라데이션 알약(흰 테두리, 코드로 그린 스프라이트) + 왼쪽 어두운 사각 안 **이퀄라이저 막대 5개**(청록→파랑) + 「NOW PLAYING ♫」 / 「곡명 — 우히&히시」 두 줄 + 오른쪽 위 ✦ 반짝임. 막대는 `AudioListener.GetSpectrumData(256)` 5밴드(저역~중고역)로 멜로디를 따라 오르내림(공격 빠르게·감쇠 천천히, 음악 없으면 살짝 숨 쉼). 옛 우상단 「♪ 제목」 알약 삭제. 캡처 r72_run1/2(두 프레임 막대 높이 다름).
- **2) 한 곡 완주 얼굴**: `ShowSongComplete` 에서 얼굴 마스크(`FaceMask`)를 금테·반짝이 뒤에 만든 뒤 `SetAsLastSibling` — 얼굴이 맨 앞. 캡처 r72_song2.
- **3) 컷씬 「깜빡이며 잘못된 이미지」** — 원인 둘. ① `CinematicPlayer` 전환이 크로스페이드가 아니라 **새 컷이 위에 불투명으로 「탁」** 올라오고 아래 옛 컷만 페이드(안 보임) → 새 컷을 알파 0 으로 올려 0.8초 페이드인(진짜 크로스페이드), 자막도 0.6초 페이드인. ② 54차 수채 원화(Cut_CHnn_*)는 흰 데클 테두리·종이결이 있어 켄번즈로 움직일 때 가장자리가 비쳤다 → **컷씬에 쓰이는 37장 전부 Kling v1-5 로 다시 생성**(`Cut_V_<옛이름>.png`, 720×1280, 「full-bleed, NO border/paper edge」 프롬프트, CS 신규 10장과 같은 수채 화풍). `CinematicTable` 은 `Cut_V_*` 를 쓰고 옛 그림은 fallback. 옛 원화는 리더/VN 용으로 그대로 둠. 프롬프트·매핑은 `Tools/KlingGen/out/raw/kling_20260913_1239~1247*`(37장). 콘택트시트로 전부 확인(CH18_Mid 는 빨간 비행기가 나와 재생성).
- **3b) 육성 첫 시작에 오프닝**: `TamaRaisingUI.Bind` 에서 `chapter==1 && !prologueSeen && !boundaryPending` 이면 바로 `OpeningFirst()`(버튼 잠금 → `OpeningCinematic.Play` → prologueSeen). 1장 첫 「다음 턴」 경계의 프롤로그 분기는 이미 본 것으로 건너뛴다.
- **4) 이미지 움직임 효과**(`CinematicPlayer.BuildFx/UpdateFx`, 스틸 컷에만): 켄번즈 폭 1.5배 + 세로 드리프트 ±14 + ±0.8° 회전 · 떠오르는 빛 입자 26개(9초에 아래→위, 옆으로 흔들림, 세피아 땐 먼지색) · 7초마다 2.6초 지나가는 비스듬한 광선 · 숨 쉬는 비네트(세피아 0.45, 일반 0.30 ± 0.06). 전부 코드 텍스처(비네트 128², 광선 64×4). 영상 컷에선 Fx 끔. **Kling image2video 로 진짜 영상화**는 컷당 3~5분·65컷이라 미착수(요청 시 1·4·7컷만 먼저).
- **5) 레코드 버튼**: 시안 그림(`UI_Music_Mock.png`)에 **박혀 있던 「<」 동그라미 두 개**(좌상단 카드 위·화면 왼쪽 가장자리)를 Photoshop 콘텐츠 인식 채우기로 지우고, 옛 「뒤로」 투명 히트 영역·주황 「⌂ 홈」 알약 삭제 → 좌상단(옛 「<」 자리)에 **파란 둥근 홈 버튼**(새 집 아이콘) 하나. 캡처 r72_records.
- 커밋 대상: UI/{KpopNowPlaying(신규),RunHudChrome,CollectionUI.Music}.cs, Story/{CinematicPlayer,CinematicTable}.cs, Raising/TamaRaisingUI.cs, Resources/CoastRun/Cut_V_*.png(37, +meta), UI_Music_Mock.png(수정), Docs/HANDOVER.

### 2026-09-13 — 70·71차: 더보기 순서 · 챕터 선택 시안 · 게이지 50 % · 홈 아이콘 · 버전 라벨 · 건너뛰기 3배 · 입력 감시견 · 피해 = 체력 비율 (컴파일 0, 캡처 r70_*/r71_*, 커밋은 사용자)
- **70차** 더보기 순서: 새로하기 / 컬렉션 / 레코드 / **시네마 / 미니게임 / 보스전 / 설정 / 이용약관·정책**(`MainMenuController` 항목 블록 순서만 이동). 캡처 r70_more2.
- **71-1) 챕터 선택 화면**(`UI/KpopChapterSelect.cs` 재작성, 시안 첨부): 유채밭 배경 `UI_Chapter_Bg2.png`(Kling, 아래 70 % 를 확대해 하늘은 띠만) · 주황 「CHAPTER」 `UI_ChapterTitle2.png`(Photoshop SegoeUIBlack + 흰 10px 스트로크 + 그림자) · 부제 「너와 나의 주파수 COAST RUN · JEJU」 · 안내 한 줄(선택 챕터 번호 반영) · **4열×5행** 카드(인셋 폭에 맞춰 폭 계산) — 완료 = 파스텔 5색 순환 + **리본 메달 `UI_Rosette.png`**(흰 그림을 카드색의 진한 버전으로 틴트 `Deep()`) + 등급 글자 S/A/B/C + 초록 「✓ COMPLETED」(`UI_Check_W.png`), 다음 챕터 = 분홍 + 흰 원 재생 배지 `UI_PlayBadge.png` + 「READY!」, 잠김 = 회색 + 자물쇠(UI_Lock_Q) + 「LOCKED」 · 선택 = 노란 테두리 1.06배 · 아래 분홍 「챕터 n 도전하기!」 · 바닥 「K-POP 러닝 · JEJU」. READY/LOCKED 확인용 개발 메뉴 `Coast Run/Dev/Chapter select - preview (last clear 5)` / `preview off`(`KpopChapterSelect.DebugLastClear`). 캡처 r71_chapter(전부 완료 프로필), r71_chapter2(미리보기: 1~5 완료·6 READY·7~ 잠김).
- **71-2) 육성 HP·STRESS 게이지 50 %**(`TamaRaisingUI.SideGauge`): 트랙 26×560 → **20×280**(세로 중심 유지 460~740), 라벨 20 → 15, 아이콘 68 → 52. **홈 아이콘** `Icon_Home.png` 를 Photoshop 다각형으로 다시 그림(지붕+몸통+굴뚝, 문은 뚫림) — 육성 우상단 홈 버튼·마이룸 버튼·러닝 결과 「육성으로 돌아가기」가 같이 바뀐다. 캡처 r71_raise.
- **71-3)** 타이틀 「스튜디오 우히히시 v1.0」 y −12 → **−18**. 버스 영상 스플래시 「터치하면 건너뛰기」 **3배**(16 → 48, 상자 660×78, 아웃라인 3). 캡처 r71_splash2.
- **71-4) 「가끔 화면이 안 눌린다」**: 폰 로그가 없어 원인을 특정할 수 없었다 → 세 겹 안전장치. ① `Core/InputWatchdog.cs`(신규, 런타임 자동 생성): 터치마다 UI 레이캐스트 → 맨 위가 버튼/핸들러 없는 그림이면 `[Input] 헛터치 — <경로>(화면 비율·알파)` 경고, 4초 안 3번이면 화면 위에 작은 노란 안내 「터치 막힘: <오브젝트>」 4초(폰에서 사용자가 읽고 알려 줄 수 있게), 보이지 않는(알파<0.02) 전체 화면 그림이면 raycastTarget 을 끈다(18차의 `CoastRaycastWatchdog` 1초 주기 감시와 보완). 그리고 **EventSystem.pixelDragThreshold 를 dpi 비례**(S25U 500dpi ≈ 31px)로 — 기본 10px 은 고밀도 폰에서 손가락 떨림보다 작아 스크롤 안 버튼 탭이 드래그로 바뀐다. ② 타이틀 `_ready` 게이트: 팝업(기부·챕터·컬렉션·시네마·약관·미니게임·러닝) 하나도 안 열렸는데 8초 넘게 꺼져 있으면 다시 켠다(`ReadyWatch`, 콜백 누락 대비). ③ 에디터 전용이지만 원인 하나 확인: **K 키가 원격 클릭(CoastDebugClicker)과 「컬렉션 열기」 디버그 키 둘 다**여서 원격 `tap` 마다 컬렉션/레코드가 열렸다(플레이 시작 직후 레코드·챕터 화면이 떠 있던 것) → 디버그 키 **F7** 로(`MainMenuController`, `RaisingUI`). 폰에서는 해당 없음. **다음 폰 테스트에서 안 눌릴 때 화면 위 노란 안내 문구를 캡처해 주면 원인이 바로 나온다.**
- **71-5) 피해 = 최대 체력 비율**(`HealthSystem.HandleHit`, `ObstacleHazard.DamageMul` 의미 변경: 22차 배율 → **비율**, `DefaultFrac 0.25`): `ObstacleCatalog.DamageFrac(id)` — 버스 **0.60** · 석상 0.45 · 관광객 0.35 · **허들류(OverheadBar/Clothesline/LanternString)·바리케이드·스쿠터 0.30** · 상자/슬라임 0.25 · 자전거 0.22 · 눈더미 0.20 · 콘 0.18 · 표지판 0.15 · 낙엽 0.12 · 웅덩이 0.10. 마주 오는 차(`OncomingCar`): 버스 0.60(즉사 아님) · 승합차 0.45 · 킥보드 0.30 · 귤 0.12. 하늘 장애물(`SkyHazards`): 큰 바위 0.35 · 미사일 0.25 · 태풍 0.30. 육성 체력 스탯은 `hitDamage/(MaxHp×0.45)`(1.0 → 만렙 0.85) 만큼 덜 받는다. 상한 98 %, 1 이상 = 즉사. 물약 회복 40 → **30**. `DamageScale` 은 1(폐기). 확인: 개발 `hit` 한 번에 HP 100 → 75(드레인 별도), 캡처 r71_hit.
- 커밋 대상: UI/{KpopChapterSelect,MainMenuController}.cs, Raising/{TamaRaisingUI,RaisingUI}.cs, Progression/HealthSystem.cs, Economy/{ObstacleHazard,OncomingCar,SkyHazards}.cs, Content/ObstacleCatalog.cs, Player/PlayerController.cs, Core/InputWatchdog.cs(신규), Editor/MiniGameDevMenu.cs, Resources/CoastRun/{UI_Chapter_Bg2,UI_ChapterTitle2,UI_Rosette,UI_PlayBadge,UI_Check_W}.png(신규, +meta), Icon_Home.png(덮어씀), Docs/HANDOVER.

### 2026-09-13 — 69차: 컷씬 검수(콜드리드) → 자막 39줄 재작성 · 스틸 12곳 교체 · Kling 1장 · 카드 「제목 + CHAPTER 범위」 (컴파일 0, 캡처 r69_*, 커밋은 사용자)
- **무엇**: 사용자 「컷씬 검수해줘. 게임 독자가 이해되는지 확인해줘」. 사전 지식 없는 독자(서브에이전트)에게 온스크린 자막만 주고 콜드리드 → 오프닝 8, 1~8편 5/5/4/4/6/6/5/7. 막힌 곳: ① 이름 부재(주인공·소년) ② 우비 꼬마가 회상에 등장 ③ 설명 없는 숫자·소품(4.2, 돌 세 개, 16장, 열흘, 방언). 처방과 표는 `Docs/CUTSCENE_SCRIPTS_v2.md` 「69차 검수」 절.
- **코드**: `Story/CinematicTable.cs` 만 — 자막 39줄 재작성(≤34자, 도윤/「바다」/하늘 이름 구조, 4.2킬로 단위, 우비 = 아빠 복선), 스틸 12곳 교체(콘택트시트 63장을 Photoshop 으로 만들어 보고 고름), 카드 `cardMain = 제목`, `cardSub = "CHAPTER n ~ m"`. 재생기·셰이더는 손대지 않음.
- **그림**: `Resources/CoastRun/Cut_CS5_2.png`(Kling v1-5, 빗속 탑 아래 남자 뒷모습, 720×1280) 신규 — 「우산도 없이 탑 아래 선 뒷모습」에 맞는 원화가 없었다.
- **캡처**: r69_cs5_2(신규 스틸 + 자막 두 줄), r69_cs5_3(세피아 회상 + 태그 그대로), r69_cs1_4(1-5 꼬마 그림 + 새 자막).
- 남은 것(변동 없음): 각 편 1·4·7컷 Firefly 영상화, 엔딩 분기 재생기 이전, 러닝 HUD 22:9, APK 는 요청 시(23분).
- 커밋 대상: Story/CinematicTable.cs, Resources/CoastRun/Cut_CS5_2.png(+meta), Docs/{CUTSCENE_SCRIPTS_v2,HANDOVER}.

### 2026-09-13 — 68차: 오프닝·컷씬 8편 시네마틱 재구성(디렉터 컷) — 공용 CinematicPlayer · 채도 셰이더 · 수채 원화 재편집 + Kling 10장 (컴파일 0, 캡처 r68_*, 커밋은 사용자)
- **무엇**: 사용자 「30년차 게임디렉터 입장에서 오프닝과 컷씬 새로 적용해서 다시 만들어줘」. 오프닝(그동안 VN 「PRO」 텍스트)과 컷씬 8편(소설식 리더)을 **같은 시네마틱 형식**(스틸/영상 켄번즈 + 자막 한 줄 + 음악 한 곡 + 마무리 카드)으로 바꿔 흐름에 넣었다. 대본·디렉터 결정은 `Docs/CUTSCENE_SCRIPTS_v2.md`.
- **코드**: `Story/CinematicTable.cs`(대본 데이터: OPEN + CS1~8, 컷 = clip/still/fallback/caption/dur/켄번즈 패턴/sepia/tag), `Story/CinematicPlayer.cs`(OpeningCinematic 을 일반화한 재생기 — 크로스페이드·영상 준비 4초 폴백·탭 다음/길게 스킵·카드 유지·음악 페이드), `Resources/CoastRun/Shaders/UIDesaturate.shader`(_Sat 채도, _Sepia 회상, _Keep 빨강/주황 스폿 컬러 유지 — uGUI Image.material). `OpeningCinematic.Play` → `CinematicPlayer.Play("OPEN")`(VN PRO 는 폴백). `TamaRaisingUI.BoundaryRoutine`: 프롤로그 = 오프닝 시네마틱, 컷씬 N = `CinematicPlayer.Play("CS"+N)` 뒤 `StoryProgress.MarkCutsceneSeen`(리더와 같은 CoastRun_VN_ 흔적 + RecordTable). `CinemaSelect`: 카드 탭 = 시네마틱, 카드 우하단 「읽기」 = 소설식 리더. 개발 메뉴 `Coast Run/Dev/Cine - OPEN|CS1~CS8`.
- **그림**: 54차 수채 원화 41장 재편집(46컷) + Kling 신규 10장 `Cut_CS1_2, CS2_3, CS4_6, CS5_3, CS5_4, CS5_6, CS6_7, CS7_3, CS8_2, CS8_3`(720×1280 PNG). 오프닝은 옛 영상 open_1~10 + Cut_Open_* 스틸에 **새 스토리 자막** 9줄.
- **캡처**: r68_cs2_1/3(세피아 회상 + 태그), r68_cs5_3(채도 0.5 + 바퀴 빨강만 남음), r68_open_2/8/card(영상 재생·타이틀 카드 1:37 유지).
- 남은 것: 각 편 1·4·7컷 Firefly 영상화(clip 칸), 엔딩 분기 재생기 이전, 폰 확인용 APK(요청 시 빌드 — 23분).
- 커밋 대상: Story/{CinematicTable(신규),CinematicPlayer(신규),OpeningCinematic}.cs, UI/CinemaSelect.cs, Raising/TamaRaisingUI.cs, Meta/StoryProgress.cs, Editor/MiniGameDevMenu.cs, Resources/CoastRun/Shaders/UIDesaturate.shader(+meta), Resources/CoastRun/Cut_CS*.png(10, +meta), Docs/{CUTSCENE_SCRIPTS_v1,CUTSCENE_SCRIPTS_v2,HANDOVER}.

### 2026-09-13 — 67차: 폰 테스트 피드백 9개 — 러닝 화면 안 보임(스트리핑) · 스플래시/아이콘 · CH1 겹침(비율 앵커) · 빈 공간 탭 삭제 · 절전 방지 · 육성 동그라미/좁은 폰 Fit · 러닝 차례 팝업은 다음 턴 후 · APK (컴파일 0, 캡처 r67_*, 커밋은 사용자)
- **1) 폰에서 러닝 화면이 안 보임** — 원인은 폰 화면의 빨간 로그 그대로: `Can't add component because class 'MeshCollider' doesn't exist!`. **엔진 코드 스트리핑**이 씬에서 참조되지 않는 MeshCollider(물리 모듈)를 빼 버려 `GameObject.CreatePrimitive` 가 콜라이더 없는 오브젝트를 내놓고, 그 뒤 `GetComponent<Collider>()` 를 쓰는 월드/장애물/플레이어 생성 코드가 NullReference 로 멈춰 길·캐릭터가 안 그려졌다(에디터에선 스트리핑이 없어 정상). 수정 3중: ① `BuildMenu.Build` 에서 `PlayerSettings.stripEngineCode = false` + Managed Stripping Low ② `Assets/link.xml`(PhysicsModule·CoreModule MeshFilter/Renderer·ParticleSystem·Audio·Video·UI·Assembly-CSharp preserve) ③ `Core/DeviceBoot.cs` 의 `KeepTypes` 배열로 형식 직접 참조. 폰 화면의 빨간 로그 상자는 우리 디버그 오버레이(그대로 둠 — 다음 폰 테스트에서 오류가 또 보이면 그 문구가 단서).
- **2) Unity 로고 스플래시 → 스튜디오 우히히시**: Unity 6 부터 Personal 도 Unity 로고를 끌 수 있다. `Art/Brand/Splash_Studio.png`(첨부 로고, 배경 투명) 를 Sprite 로 임포트해 `PlayerSettings.SplashScreen`(showUnityLogo false · 로고 2.4초 · 크림 배경 · 정지 애니) 에 넣는다 — `BuildMenu.ApplyBranding()`(메뉴 `Coast Run/Build/Apply branding (splash + icon)`, 빌드 때 자동 호출). 타이틀 씬의 버스 영상 스플래시는 그대로.
- **3) 앱 아이콘**: 첨부(노란 둥근 사각 안 하늘이+스케이트보드)에서 둥근 사각을 잘라 `Art/Brand/AppIcon.png`(1024, 모서리는 노랑 채움 — 레거시/라운드) · `AppIcon_Bg.png`(노랑 단색) · `AppIcon_Fg.png`(86 % 축소 전경 — 어댑티브 안전영역) 로 만들고 `PlayerSettings.SetPlatformIcons(Android, 모든 kind)` 로 적용(adaptive 는 0=배경 1=전경). 기본 아이콘(Unknown 그룹)도 같은 그림.
- **4) CH1·CHAPTER 겹침**: 「CH 1. 이름」은 타이틀 배경 그림(`UI_Title_Mock.png`)에 **박혀 있던** 시안 칩이고, 우리 CHAPTER 칩은 인셋 캔버스 좌표라 16:9 에서만 정확히 겹쳐 가려졌다 — 폰(19.5:9)에선 그림이 세로로 늘어나 어긋남. 그림에서 옛 칩을 지우고(Photoshop: 아래 노란 바 조각을 늘려 덮고, 위는 청바지 조각+블러) CHAPTER 칩은 **배경 그림의 자식으로 그림 좌표 비율 앵커**(656~880/1080 × 1396~1486/1920) → 어떤 비율에서도 같은 자리. `CanvasGroup` 으로 타이틀 UI 와 같이 보였다 숨는다. CHAPTER 탭 → 챕터 선택 페이지 열림 확인(r67_chapter_s25u). 챕터 페이지 5열이 좁은 폰에서 오른쪽이 잘리던 것도 인셋 폭에 맞춰 카드 폭 축소(`KpopChapterSelect`).
- **5) 빈 공간 탭 → 러닝 진입 삭제**: 18차의 투명 「TapAnywhere」 버튼(TitleUI 맨 뒤, 빈 곳 탭 = 스토리 모드)과 폴백 UI 의 「화면을 터치하면 출발」 라벨·깜빡임 코드 전부 삭제. 그리고 **진짜 원인 하나 더**: 시안 배경 위의 투명 히트 영역(스토리/더보기/K-POP Play)도 인셋 좌표라 19.5:9 폰에선 그림의 버튼과 터치 영역이 어긋나 「빈 곳을 눌렀는데 Play 가 눌림」이 됐다 → 히트 영역도 배경 그림 비율 앵커로(`hit` 람다). 이제 S25U 비율에서 더보기·스토리 그림 자리를 눌러야 반응.
- **6) 절전 방지**: `Core/DeviceBoot.cs` `[RuntimeInitializeOnLoadMethod]` → `Screen.sleepTimeout = SleepTimeout.NeverSleep`(Android 는 권한 불필요, WAKE_LOCK 은 유니티가 자동).
- **7) 육성 동그라미 3개**: 채움이 밖으로 삐져나오던 원인 = 9-slice 반지름(44)이 지름 62 의 절반보다 커서 모서리 조각이 겹쳐 그려짐 → 반지름 31/25. 그리고 **좁은 폰 대응**: 육성 화면은 인셋 폭 664(16:9) 절대 좌표라 S25U(1440×3120, 인셋 ≈596)에선 동그라미가 장보기 버튼을 덮고 아래 버튼 줄이 잘렸다 → 폭 664 「Fit」 상자를 두고 화면 폭에 맞춰 통째로 축소(비율 유지; 배경/그늘은 밖). 캡처: r67_raise_s25u_3(19.5:9), r67_raise_fold(1080×2640 22:9) — 두 비율 모두 동그라미 안에서 채워지고 줄이 다 들어온다. 에디터 원격에 `res <w> <h>` 명령 추가(게임 뷰 해상도 전환, `CoastRemote.SetGameViewSize`).
- **8) 러닝 차례 팝업**: 육성 화면에 들어오거나 3번째 행동 뒤 경계(`boundaryPending`)가 돼도 **바로 띄우지 않는다**. 동그라미 3개를 다 찬 상태로 보여 주고 말풍선 「이번 주는 끝! 다음 턴을 누르면 이야기 → 대회」, 행동 버튼은 같은 안내만. **「다음 턴」을 누르면** `ResumeBoundary`(이야기 → 대회 안내 → 러닝). 자동 모드도 같은 길. 캡처 r67_boundary2(찬 상태) → r67_boundary3(다음 턴 뒤 대회 카드).
- **9) APK**: 빌드 전 `Library/Bee/Android/Prj` 삭제(Gradle AccessDenied 예방). `Builds/CoastRun.apk`.
- 알려진 남은 것: 러닝 HUD 는 22:9(폴드)에서 상단 알약이 겹친다(19.5:9 까진 괜찮음) — 육성처럼 Fit 상자를 두면 되지만 PickupFloat 등 월드→화면 좌표를 쓰는 요소가 있어 다음에.
- 커밋 대상: Assets/link.xml(신규), Core/DeviceBoot.cs(신규), Editor/{BuildMenu,CoastRemote}.cs, UI/{MainMenuController,KpopChapterSelect}.cs, Raising/TamaRaisingUI.cs, Art/Brand/{Splash_Studio,AppIcon,AppIcon_Fg,AppIcon_Bg}.png(+meta), Resources/CoastRun/UI_Title_Mock.png(수정), ProjectSettings/ProjectSettings.asset(스플래시·아이콘·스트리핑), Docs/HANDOVER.

### 2026-09-12 — 66차: 대회 라이벌 3명 · 포토카드 느낌표 · 대회 미달 시안 · CH 삭제 · 펫 상점/러닝 펫 Kling 뒷모습 · 육성 화면 HP·STRESS 세로 게이지 · 캐릭터 옆으로 도는 오류 · 한 곡 완주 시안 정합 (컴파일 0, 캡처 r66_*, 커밋은 사용자)
- **1) 대회 러닝 라이벌**(`Meta/ContestRivals.cs` 신규, `GameSession.HandleStageStart` 에서 `StoryContest.Active` 일 때 생성, `StoryContest.End` 에서 제거): **꼬마**(`MG_Char_ButlerBack`) + **보미**(`Rival_Girl.png`, 주황 티·양갈래) + **태오**(`Rival_Boy.png`, 보라 후드·거꾸로 쓴 모자) — Kling **턴어라운드 시트**(앞·뒤 나란히)로 만들고 오른쪽 절반을 잘라 키잉(뒷모습 프롬프트만으론 계속 정면이 나오는데, 시트로 시키면 뒷모습이 확실히 나온다). 뒷모습 쿼드(ChromaUnlit + YawBillboard) + 그림자 + 머리 위 이름표(월드 캔버스 알약). 움직임: 주인공 속도 × (1 ± 0.10 기분, 3~6초마다 바뀜), **고무줄** −10 m ~ +15 m(넘으면 0.78/1.28 배), 2.5~7초마다 레인 바꿈(가까울 땐 주인공 레인 회피·같은 자리 라이벌 회피), 3~9초마다 점프, 달리기 흔들림. `ContestHud` 에 **「N위」**(진행 거리 순위). 재도전(`IsRetry`)은 대회가 아니라 라이벌도 없음(기존 규칙).
- **2) 포토카드 느낌표**(`JellyPickup.Spawn` + `MissionMarker`): 사진 콘테스트(`Goal.Photos`) 중 스폰되는 포토카드 위 1.35 m 에 **노란 동그라미 빨간 !**(`Fx_BangMarker.png`, Photoshop 으로 그림) 쿼드 — 까딱까딱 + 맥동. 캡처 r66_run5(오른쪽 레인).
- **3) 대회 미달 화면**(`ContestResultUI.ShowFail`, 시안): 카드 648×1040 + **금색 띠 테두리**(Rim/RimIn, 스파클 아래) + 금 ✦ 5개 / 빨간 젤리 「대회 미달…」 78 / 아이콘 줄 2(대회 이름 · 조건 + 주황 「0 / 2」 배지) / **흰 상자 3개(얇은 금테)** `Box()`: 노란 「!」·파란 전구·청록 새로고침 / 버튼 주황 「지금 다시 도전」(화살표) · 파랑 **「스토리화면으로」**(`Icon_Book.png` 새로 그림). 개발 메뉴 `Contest - Fail screen`. 캡처 r66_fail1.
- **4) 주차 라벨**: 「23주차 · 여름 · CH4」 → **「23주차 · 여름」**(`TamaRaisingUI.Refresh`).
- **5) 펫**: 상점(`PetShopUI`, 65차 말에 시안대로 재작성: 금테 크림 카드·`UI_PetShop_Title.png`·행 4개 노랑/분홍/보라/초록·시안에서 오려낸 초상 `UI_Pet_<Kind>.png`·「◆ 800c · Lv3 ◆」 금 알약·파란 구매) 캡처 r66_petshop. **러닝 펫 4종 뒷모습**(`Obs_Pet_Sparrow/BlackPig/BikerThug/WildGoose.png` 덮어씀): Kling 턴어라운드 시트(참새·검은 돼지+바나나 껍질 모자·갈색 강아지 헬멧+가죽 재킷·거위 눈꽃 목도리) 오른쪽 절반 → 마법봉 키잉(가장자리 70px 간격 샘플, 비연속, 허용치 40~48; Select Subject 는 이 PC 에서 불가) → trim. `PetCompanion.Build` 는 스프라이트만(`PaintedProp.Attach("Pet_"+kind)`, FBX/절차 생성기·`PetFbxFix`·`BlenderKitMenu.BuildPets` 삭제, `Pet_*.fbx`·`Obs_Pet_*_prev`·`Obs_Pet_Cat/Puppy/Seagull`·`Tools/blender/pet_kit.py`·`build_pets.bat` 삭제). 개발 메뉴 `Pet - Shop`, `Pet - Equip <Kind> + run`. 캡처 r66_pet1(팡찌).
- **6) 육성 화면 시안**(`TamaRaisingUI`): 좌우 **세로 게이지** `SideGauge(right,…)` — 왼쪽 빨강 **HP**(하트 아이콘 `Icon_Heart`), 오른쪽 보라 **STRESS**(「×_×」 원) — 트랙 26×560(y 372), 채움 = stamina/200·stress/200, 라벨 y 936, 아이콘 y 968. 오른쪽 게이지는 **오른쪽 가장자리 앵커(x=1)** 로 잡아야 안전영역 여백에 안 잘린다(처음 x=686 절대좌표로 두니 STRESS 가 잘렸다). 경고 띠 글자 bestFit **최대 20**(한 줄에 「컨디션 0! 다음 주에 쓰러진다 — 쌀·잠·옷」). 캡처 r66_raise2.
- **7) 캐릭터가 옆으로 도는 오류**(`SkaterRig`): 원인 = 레인 킥·가방·착지·스쿼시 **스프링을 명시적 오일러로 프레임당 한 번** 적분 → 프레임이 튀거나(로딩·GC·에디터 렉·Kling 돌릴 때) timescale 이 높으면 dt > 0.1 s 에서 **발산**(강성 260~320) → `_laneKick` 이 수백 도로 튀어 몸이 옆으로 눕고 NaN 까지 감. 수정: dt 를 **1/90 s 이하로 잘게 나눠** 적분(최대 0.25 s), `_laneKick` ±28°·`_bounce` ±0.3 m 클램프, NaN 가드. 다른 회전 소스는 없음(플레이어 루트는 `ApplyPathPose` 로 길 방향 고정).
- **8) 한 곡 완주 화면**(`ShowSongComplete`) — 시안과 캡처를 **나란히 놓고 비교**(r66_song_compare.png, 개발 메뉴 `Fx - Song complete screen`(K-POP 런 중)) 3회 반복: 시안 좌표를 720×1280 로 환산해 부제 알약 616×54(y −188), 얼굴 (188,−416) r124 + 얇은 금테(+26), 별 112/124 (x 420+92i, y −388), **자주색+금테** 미션 알약(512,−476), 카드 648×652(아래 40) 안 「결과」 24 · 큰 배지 68(y −91) · 값 36 · 작은 줄 y −232/−304(아이콘 52, x 115/405) · 금주황 띠 −408~−502 · 버튼 64 높이(y 50). 은하 배경은 `AspectRatioFitter(EnvelopeParent)` 가 안전영역까지만 덮어 가장자리에 보라 띠가 남던 것을 직접 크기 계산으로 화면 전체 덮음. 결론: 구도·색·크기 시안과 일치, 남는 차이는 글꼴 렌더(제목 입체감)·금테 그림의 보석 디테일·상태에 따른 띠 문구뿐.
- **66차-2**(사용자): 러닝 시작 장애물 안내 **3초**(`StageManager.AnnounceStartCo` hold 3.0), 장애물 피해 **5배**(`HealthSystem.DamageScale = 5`, 상한 최대 체력 98 % — 일반 장애물 한 방이면 체력 2 % 남음, 버스 즉사 그대로). 한 방 즉사로 바꾸려면 상한(0.98)을 없애면 된다.
- 기타: 개발 메뉴 `Contest - Run CH4 (photos, rivals)` / `Contest - Run CH1 (coins, rivals)`(육성 화면에서 대회 러닝 바로). 러닝 화면 캡처는 Photoshop 으로 열어 미리보기(`device_stage_files` 는 hardlink 거부).
- 커밋 대상: Meta/{ContestRivals(신규),StoryContest}.cs, Core/GameSession.cs, Economy/JellyPickup.cs, Player/{SkaterRig,PetCompanion}.cs, Raising/{TamaRaisingUI,RaisingUI.Popups,HomeUI}.cs, UI/{PetShopUI,RunHudChrome}.cs, Editor/{MiniGameDevMenu,BlenderKitMenu}.cs, Resources/CoastRun/{Rival_Girl,Rival_Boy,Fx_BangMarker,Icon_Book,UI_PetShop_Title,UI_Pet_*(4),Obs_Pet_*(4 덮어씀)}.png(+meta), 삭제 파일들(git add -A), Docs/HANDOVER.

### 2026-09-12 — 65차: 시작 안내 장애물만 · 데미지 4배 · 생활 띠 · 결산 자동 닫힘 · 도로 네모 삭제 · 보스 설명 · K-POP 미션 글 삭제 · 윷놀이 시안2(얼굴 말) · 한 곡 완주 시안 · APK · 설정→더보기 (컴파일 0, 캡처 r65_*, 커밋은 사용자)
- **1) 시작 안내**(`PickupFloat.ItemGuide`): 「모으기」 줄 삭제, 「피하기」 한 줄(콘·점프 / 바리케이드 / 빨래줄·숙이기 / 버스·피하기)만 **반투명 남색 띠**(600×132, α0.55)로 **2초**(`StageManager.AnnounceStartCo` hold 2.6 → 2.0).
- **2) 장애물 데미지 4배**: `HealthSystem.DamageScale = 4f` — `HandleHit` 피해 = min(최대 체력 90 %, hitDamage×배율×4). 즉 일반 장애물 한 방이면 체력 10 %만 남는다(물약이 생명줄), 두 방이면 끝. 버스 즉사는 그대로. 너무 세면 DamageScale 만 낮추면 됨(2 = 두 방 반).
- **3) 생활 띠**(`TamaRaisingUI`): 「쌀이 없어 — 장보기」 글자 13 → **28**(bestFit, 띠 70 → 86 높이, y 266, ⚠ 36), 아래 작은 줄(`_gateLabel` 「챕터 4 송전탑까지 0주 · 체력 127 ▼48…」) **숨김** — 수치는 상태창에서.
- **4) 결산 카드**(`WeekPassUI`): 「터치해서 다음으로」 단추 삭제, **1초 뒤 자동 닫힘**(`AutoCloseSeconds`, `AutoCloser`), 우상단 **✕**(52px) + 카드/어둠 터치로 바로 닫기(`finish` 는 한 번만).
- **5) 도로 네모 오류**: 정체 = `SegmentDecorator` 의 계절 「도로 표면 액센트」 — 봄 물웅덩이(반투명 회청 상자 1.4×0.02×0.9) · 가을 낙엽 더미 상자 · 겨울 눈 둑 상자가 차도 위에 놓였다(밤엔 남색 반투명 네모로 보임). **세 개 다 삭제**(WorldDump 로 `Prop_LeafPile` x=1.25 확인).
- **6) 보스 효과 설명**: 등장 배너 뒤 **안내 띠**(`PickupFloat.InfoStrip`: 보스 그림 + 이름 + 두 줄, 3.2초) — 갈매기 「앞에서 미사일(내 레인 60 %) — 레인 옮겨 피하자」 / 골렘 「하늘에서 바위 — 바닥 그림자 보고 비키자」 / 도깨비 「태풍이 레인을 옮겨 다닌다 — 닿으면 피해」 + 「보스 동안 코인·장애물 없음 · 버티면 +100 코인」(`BossDirector.Effect`).
- **7) K-POP 챕터 카드**: 아래 미션 글(「곡 끝까지 달리기 · …」) 삭제 — 카드는 「챕터 N 시작!」 + 「♪ 곡 제목」만.
- **8) 윷놀이 시안 2**(`YutMission3D`): 마당 = Kling **한옥 마당 + 보석 금테 멍석**(`UI_MG_Yard_Yut2.jpg`, 있으면 자동) / 목표 띠 = **파란 알약** 「★ 꼬마보다 먼저 3바퀴! ★」 + **분홍 알약** 「♥ 나 0/3바퀴 · 꼬마 0/3바퀴」(`MiniKit.TwoPillStyle`) / 발판 제목 「✦♥ ★ 3바퀴 경주 — 내 차례! ★ ♥✦」 / 결과 카드 아래 분홍 알약 「✦ 꼬마」 / 경주 카드 = 나 **분홍 하트** 바퀴 3개 + 분홍 막대(♥) · 꼬마 **파란 별** 3개 + 파란 막대(★) · 초록 알약 「✓ 같은 칸 = 잡기!」 / 불꽃 버튼 「펑!! / 던지기!!」 두 줄. **상대는 도담 → 꼬마**(사용자: 나와 꼬마의 대결). **말 = 얼굴**: `MG_Token_Girl.png`(내 얼굴, 분홍 테) / `MG_Token_Kid.png`(집사 꼬마 얼굴 UI_Face_Butler, 파란 테) — Photoshop 으로 원형 테 26px. 세운 빌보드(Lit/Unlit 투명 쿼드 + Stand)는 이 무대에서 안 그려져(원인 미상, 배경판과 같은 재질도 안 나옴) **바닥에 눕힌 원판**(Euler 90, `Lay`)으로 — 위에서 보는 시점이라 자연스럽다. 진입 카드·규칙 글도 꼬마·3바퀴로.
- **9) 한 곡 완주 화면**(`RunHudChrome.ShowSongComplete`, KpopFinished 일 때만): Kling **보라 은하 배경**(`UI_Result_Galaxy.jpg`) + ✦ 반짝이 22개 / **금색 젤리 제목** 「한 곡 완주! ♪」(EventCardKit.JellyTitle 78) / **네온 분홍 알약 부제** / **보석 금테**(`UI_Ring_Gold.png`, Kling 키잉) 안 얼굴(UI_Face_Girl, 원형 마스크) / 큰 별 3개 + 금 알약 「★ 오늘 미션 N/3」 / 크림 카드: 「✦ 결과 ✦」 · 거리(보라 운동화 배지)·점수(금 트로피 배지) 큰 두 칸 · 코인·최고 콤보·아이템·하트 둥근 배지 4줄(`Icon_R_{Shoe,Trophy,Coin,Combo,Star,Heart}.png` — Kling 시트 1장을 Photoshop 으로 6조각) · 주황 젤리 띠 「★ 최고 점수 갱신!」/도장/완주 + 미션 체크 / 분홍 「♪ 한 곡 더 ♪」·회색 「나가기」. 실패 화면(아쉽지만 다음에!)은 그대로. 캡처 r65_song.
- **10) APK**: `Coast Run/Build/Android APK (IL2CPP, ARM64+ARMv7)` → **Builds/CoastRun.apk(305 MB, 성공)**, 탐색기로 폴더 열어 둠. 겪은 일 3가지: ① Burst AOT 컴파일러(bcl.exe)가 「응용 프로그램 제어 정책」(0x800711C7)으로 Burst.Compiler.IL 을 못 읽어 실패 → `ProjectSettings/BurstAotSettings_Android.json` 새로 만들어 **Android Burst AOT 끔**(EnableBurstCompilation false; 게임은 Burst 잡을 안 쓴다). ② Gradle `mergeReleaseNativeLibs` AccessDenied(unityLibrary → launcher) = 이전 실패 빌드가 남긴 `Library/Bee/Android/Prj` 찌꺼기 → 그 폴더를 통째로 지우고 다시(Photoshop `app.system('cmd /c rmdir /s /q …')`). ③ 첫 실패 뒤 에디터가 종료됐고 `unity_launch`(MCP 서버 컨텍스트)로 띄운 Unity 는 **Package Manager 서버 IPC 연결 실패**로 멈춤 → Unity.exe 를 taskkill 하고 Photoshop `app.system('start "" Unity.exe -projectPath C:\dev\game')`(사용자 세션)로 띄우니 정상. 이후 빌드는 3.8 분(증분).
- **11) 설정**: 우상단 톱니 아이콘 제거(`BuildSettingsIcon` 호출 주석), 더보기 목록에 「설정」 복귀(시네마 다음).
- 기타: 사용자가 에디터에서 직접 플레이 중일 땐 컴파일/빌드를 미룬다(도메인 리로드가 플레이를 끊는다). 스크린샷은 `unity_shot` 지연이 1~2초라 2초짜리 띠는 못 잡았다 — 개발 메뉴 `Fx - Item guide (6s)` 로 확인.
- 커밋 대상: Progression/HealthSystem.cs, Visual/PickupFloat.cs, Core/StageManager.cs, Raising/{TamaRaisingUI,MissionMiniGames.Games3D,MissionMiniGames,MissionMiniGames.Kit}.cs, UI/{LifeUI,RunHudChrome,MainMenuController}.cs, Meta/{BossDirector,ChapterMission}.cs, Content/SegmentDecorator.cs, Resources/CoastRun/{MG_Token_Girl,MG_Token_Kid,UI_Ring_Gold,Icon_R_*(6),UI_Result_Galaxy.jpg,UI_MG_Yard_Yut2.jpg}(+meta 자동), Docs/HANDOVER.

### 2026-09-12 — 64차: 윷놀이 시안·튜토리얼·3바퀴·승률 60% · 제주 집 4종(Blender) · 한국식 보도블록 · 설정 톱니 (컴파일 0, 캡처 r64_*, 커밋은 사용자)
- **1) 윷놀이**(`MissionMiniGames.Games3D.cs` YutMission3D 전면): 시안대로 **크림 발판** — 위 분홍 「♥ 내 차례 — [던지기!!] ♥」 제목(도담 차례엔 파란 「✦ 도담 차례… ✦」 + 버튼 흐림 0.45 + 맥동 정지) / 노란 젤리 「윷 결과」 카드(큰 「개 +2」, 「✦ 나/도담 · 한 번 더!」) / 흰 「🏁 3바퀴 경주」 카드(나 노랑·도담 파랑 칸 막대 + **바퀴 점 3개**, 「✓ 같은 칸 = 잡기!」) / 불꽃 「던지기!!」 큰 버튼(Kling 불꽃 `Resources/CoastRun/Fx_FireBurst.png` 650×940 + 「펑!」). 말은 **유리구슬**(빨강 나 · 파랑 도담, 하이라이트 점), 윷가락 0.26 → 0.30 wpn. 기본 상태문(Status)은 숨김.
  - **규칙**: 20칸 말판 **3바퀴(60칸) 먼저** 도는 쪽이 승리(`Cells 20 · Laps 3 · Total 60`, 바퀴 돌 때 「N바퀴!」 팝). 잡기 = 같은 칸(바퀴 무관, 그 바퀴 출발점에 서 있는 말은 제외) → 잡힌 말은 **그 바퀴 출발점**으로, 잡은 쪽 한 번 더. 윷·모 한 번 더.
  - **승률 60 %**: 배(평평한 면)가 위로 올 확률 나 `PMe 0.55` / 도담 `PAi 0.45` — 파이썬 시뮬레이션 2만 판 = 60.2 %(0.5/0.5 = 49.6 %, 0.58/0.5 = 63 %). 바꾸려면 두 상수만.
  - **턴 교대**: 실제 코드 결함은 없었고(로그로 확인: 나 → 도담(모=한 번 더) → 도담 → 나) 도담이 윷·모·잡기로 연달아 던지면 「번갈아 안 된다」로 보였다 → 교대는 `NextTurn(bool)` 한 곳에서만, 도담 차례 시작 전 0.55 s 대기, 팝에 「도담: 모 +5 한 번 더!」 표시, 제목·버튼 색으로 차례를 늘 보이게. 에디터 전용 `[Yut] tap/turn` 로그 남김.
  - **튜토리얼**(`ShowTutorial`): 시작 직후 어두운 오버레이 + 크림 카드 「윷놀이 하는 법」 — 배/등 윷가락 그림 4개 + 번호 4줄(던지기 · 도1~모5/한 번 더 · 잡기 · 번갈아 3바퀴) + 분홍 「알겠어! 시작 ▶」(맥동). 닫으면 탭 안내·「준비 — 시작!」. 진입 카드(`MissionMiniGames.IntroSteps/IntroGoal`)와 미션 규칙(`ChapterMission` ruleKo)도 3바퀴로 갱신.
- **2) 제주 집 4종**(`Tools/blender/jeju_house_kit.py` + `build_jeju_house.bat`, 메뉴 `Coast Run/Build Jeju Houses 3D (Blender)` — 에디터가 멈추는 게 싫으면 Photoshop `app.system('cmd /c start …bat')` 로도 됨): `JHouse_Thatch_A`(현무암 돌집 + 타원구 캡 두 겹 초가지붕 + 새끼줄 두 바퀴 + 세로줄 8 + 창호 + 굴뚝 + **정낭**) · `JHouse_Thatch_B`(ㄱ자 곁채 + 낮은 돌담 + 장독 3) · `JHouse_Tile_A`(현무암 기단 + 회벽 + 나무 기둥·도리 + **처마 끝 들린 우진각 기와지붕**(hip_roof: 밑변 8점 모서리 +0.22 m) + 용마루·추녀마루 + 툇마루) · `JHouse_Tile_B`(낮고 넓은 본채 + 돌 곁채 + 정낭 2단). 재질 이름 → `JejuKit.Build`: Thatch(0.74,0.60,0.36) / Tile / TileRidge / Paper / Plaster(+ 기존 Stone 텍스처·WoodDark·Wood·Rope). `JejuLots.JejuHouse(pivot,rng)`(직전과 다른 종류) — **집 필지의 절반**이 제주 집, 필지 비율 상가 45 → 40 / 집 22 → 27(`lotRoll < 40`). 확인용 `Coast Run/Dev/World - Jeju house showcase`(오른쪽 둔덕에 4채; timescale 먼저 낮출 것). 캡처 r64_jh_g.
- **3) 인도·설정**: 인도는 **한국 보도블록** `Resources/CoastRun/Tex_Sidewalk_KR.png`(PIL: 200×100 회색 블록 엇갈림 + 양쪽 가장자리 적갈색 2줄 + 가운데 포인트, 2.2 m 타일) + 연석 쪽 **노란 점자블록 띠**(`Tactile` 0.30 m) + 연석 콘크리트 회색 — 차도 크림 돌포장과 확실히 구분(`SidewalkMaterial` `_walkKr`, 그림 없으면 옛 방식). 캡처 r64_town_b/g. 설정 톱니는 (−38,−36) **64 px** + 반투명 남색 원판 받침(주변 하늘에 붙게). 캡처 r64_title.
- 기타: 봄 벚꽃 근경 층 크기 0.16~0.28 → 0.11~0.19(카메라 앞 분홍 덩어리 방지).
- 커밋 대상: Raising/{MissionMiniGames.Games3D,MissionMiniGames}.cs, Meta/ChapterMission.cs, World/{JejuKit,JejuLots,PromenadeSegmentBuilder}.cs, UI/MainMenuController.cs, Content/WeatherFx.cs, Editor/{MiniGameDevMenu,BlenderKitMenu}.cs, Tools/blender/{jeju_house_kit.py,build_jeju_house.bat}, Resources/CoastRun/{Fx_FireBurst.png(+meta 자동),Tex_Sidewalk_KR.png(+meta)}, Resources/CoastRun/Models/JHouse_*.fbx(+meta 자동) 4개, Docs/HANDOVER.

### 2026-09-12 — 63차: 육성 시안 정합 · 톱니 구석 · 계절 요소 · 보스 속도/귀여운 보스 · 발 접지 · 시작 안내 띠 · 좌우 커서 · 기부 $4 (컴파일 0, 캡처 r63_*, 커밋은 사용자)
- **1) 육성 화면**(`TamaRaisingUI`, 시안 그대로 — r63_r1 로 시안과 비교): 2줄 = ★ 상태 동그라미(Lv 소글자) · 「마이룸」 알약(집 아이콘) · 🛒 상점 동그라미 · 오른쪽 위 행동 동그라미 3개(−176+i·72, −115). 게이지 막대 없음(`_staminaFill` 등 null 가드) — 체력·기운 숫자는 게이트 안내 줄(y 254~280)에. 생활 띠 = 노란 테두리 알약 + 빨강/남색 띠 + ⚠/✦ + 글. 주인공 (0,372) 470×570. 행동 카드 y 118·높이 136 = 아이콘 70 + 이름 28 굵게 — **밥·놀기·알바 아래 작은 효과 글 삭제**. 아래 「자동 OFF」(176×100) + 분홍 **「다음 턴」**(글자는 이것만). **행동 3번 하면 자동으로 다음 턴**(말풍선 1.2 s + 0.6 s → `EndWeek`), 분홍 버튼은 행동이 안 끝났을 때 건너뛰는 용도. 말풍선 (−6, 880).
- **2) 설정 톱니**: 타이틀 우측 **맨 위 구석**(앵커 우상단 −50,−48 · 84px, `BuildSettingsIcon`) — 그림은 Kling(파란 젤리 톱니 버튼, 256², Photoshop 키잉) `Resources/CoastRun/Icon_Gear.png` 교체(meta GUID 유지). 인트로가 끝나야(`_ready`) 보인다(VisibleWhen). r63_title_gear2.
- **3) 계절 요소**(챕터 1~5 봄 / 6~10 여름 / 11~15 가을 / 16~20 겨울): **봄 벚꽃잎** `WeatherFx` `_petals`(원경 120/s, 0.14~0.26 m, 상자 22×30) + `_petalsNear`(카메라 앞 40/s, 0.16~0.28 m) — 진분홍~연분홍 두 색, 회전·노이즈, 비 아닐 때 봄이면 항상(`MakePetals`, 그림 `Fx_Petal.png` PIL). **여름 해바라기 / 가을 코스모스**: Kling → Photoshop 키잉(해바라기 Magentas+마술봉·안쪽 분홍은 비연속 마술봉, 코스모스 파란 배경 마술봉) → `Resources/CoastRun/Obs_Sunflower.png`(672×798) / `Obs_Cosmos.png`(550×680). `PromenadeSegmentBuilder.Scenes.SeasonFlowerKey`(여름 Sunflower · 가을 Cosmos · 그 외 Canola, 그림 없으면 Canola) 를 꽃밭 포기·오름 언덕·**해안 둔덕 드레싱**(`Coast.cs VergeDressing`, 겨울만 억새) 에 적용, 높이 해바라기 1.5~1.7 · 코스모스 1.05~1.1. **겨울 눈 덮인 건물**: `PromenadeSegmentBuilder.SnowCaps(root,index)` — 세그먼트 렌더러(이름 필터, 높이 ≥0.45) 위에 흰 Lit 슬래브(최대 48개). 개발 메뉴 `Coast Run/Dev/Season - Spring/Summer/Autumn/Winter run (ch3/8/13/18)`(타이틀에서 실행; 러닝 중엔 무시되므로 stop→play→메뉴). 캡처 r63_spring_c(꽃잎)·r63_summer_a/guide_e(해바라기)·r63_autumn_b(코스모스)·r63_winter_b(눈 슬래브).
- **4) 보스 구간 느려짐**: 실제 감속은 없었다 — 보스 등장 중 충돌 히트스톱 슬로모(0.6 s 이하 timeScale 0.04)와 빈 도로가 「느려짐」으로 읽힘. → `BossDirector` 보스 부스트 1.2 → **1.4**, 0.3 s 마다 `JuiceDirector.BossSpeedPulse()`(스피드라인 14 + FOV +4), 보스 중엔 `PlayerController.NoHitSlow` 로 히트스톱 짧게(`HitStop(0.6f,0.04f)` 분기). 토네이도/히트 슬로모는 timeScale 1 로 복귀.
- **5) 보스 3종 Kling 재작업**(통통·귀엽게, 펫과 안 겹치게): 해적 모자 갈매기(`Obs_Boss_Seagull.png` 847×837) · 이끼 골렘(`Obs_Boss_Golem.png` 1024×765) · 하늘색 머리 도깨비(`Obs_Boss_Dokkaebi.png` 697×871) — Photoshop Magentas Color Range + 마술봉 모서리, trim, 기존 meta 그대로. 러닝 중 확인 r63_autumn_a(도깨비)·r63_boss_b(갈매기).
- **6) 발 접지**(`SkaterRig` 스텝): 스텝마다 `JuiceDirector.StepThump()`(카메라 0.07 s·0.012 흔들림) + `_bounceVel −0.85`, 달릴 때 `localPosition.y = Clamp(_bounce,−0.08,0.04) − 0.035`(살짝 내려 앉힘), `PuffStep` 먼지 12개.
- **7) 시작 안내 띠**(`PickupFloat.ItemGuide`, `StageManager.AnnounceStartCo` hold 1.55 → 2.6 s): 챕터 카드 아래 크림 띠(600×236, y −300) 두 줄 — 초록 「모으기」 코인·말랑이·물약·하트 / 빨강 「피하기」 콘·점프 · 바리케이드 · 빨래줄·숙이기 · 버스·피하기(아이콘은 Obs_* 그림). 개발 `Coast Run/Dev/Fx - Item guide (6s)`. r63_guide_j.
- **8) 좌우 커서**: 에디터에서 `key LeftArrow/RightArrow`·`lane ±1`(MobileSwipeInput.Inject) 모두 레인이 바뀜([Lane] 로그), UI 레이캐스트 차단 없음(`Input - Probe UI raycast` 0 hits), 카메라가 플레이어를 가운데 두므로 캡처론 티가 안 난다. **코드 결함은 못 찾음** — 폰의 9/6 APK 가 옛 빌드이거나 게임뷰 포커스/마우스 드래그 문제일 가능성. MobileSwipeInput 에 에디터 전용 `[Touch] began onUi=… hits=…` 로그 남겨 둠(폰 로그로 확인 가능). 새 APK 로 재확인 필요.
- **9) 기부**: `Donation.PriceLabelFallback = "$4"`(₩5,500 / $3.99) — Play Console 상품 `coastrun_donate_coffee` 가격도 $4 로 맞출 것.
- 개발 메뉴 추가: `Fx - Weather probe`(WeatherFx 파티클 상태 로그), `Fx - Item guide (6s)`, `Season - * run`.
- 커밋 대상: Raising/TamaRaisingUI.cs, Meta/{Donation,BossDirector}.cs, Visual/{JuiceDirector,PickupFloat}.cs, Core/StageManager.cs, Player/{SkaterRig,PlayerController}.cs, Input/MobileSwipeInput.cs, Content/WeatherFx.cs, World/{PromenadeSegmentBuilder,PromenadeSegmentBuilder.Scenes,PromenadeSegmentBuilder.Coast}.cs, UI/MainMenuController.cs, Editor/MiniGameDevMenu.cs, Resources/CoastRun/{Icon_Gear,Fx_Petal(+meta),Obs_Boss_Seagull,Obs_Boss_Golem,Obs_Boss_Dokkaebi,Obs_Sunflower(+meta 자동),Obs_Cosmos(+meta 자동)}.png, Docs/HANDOVER.

### 2026-09-12 — 62차: 2단 점프 「구름 밟기」 연출 (컴파일 0, 캡처 r62_c8, 커밋은 사용자)
- 사용자: 「2단 점프가 아직 별 모양 — 별 말고 구름 같은 효과로」. 51차의 하늘색 공기 파문(`Fx_AirRing`)은 가장자리가 뾰족해 별처럼 읽혔다 → `JuiceDirector.OnDoubleJump` 를 **구름 밟기**로 교체: 발밑 뭉게구름(`Fx_Cloud_Puff`)이 0.12 s 퐁 부풀고(overshoot) → 밟힌 구름(`Fx_Cloud_Flat`)으로 바뀌어 납작·넓게(2.4 m) 퍼지며 뒤·아래로 흘러가 0.55 s 에 사라짐 + 구름 조각(`Fx_Cloud_Wisp`) 4개가 좌우로 포물선 + 0.3 s 동안 발 아래 따라오는 구름 꼬리 6개. 스피드라인·FOV 킥·진동·Boost SFX 는 유지. 구름 그림이 없으면 옛 퍼프 폴백. `AirRing/JetColumn` 코루틴은 미사용으로 남김.
- 구름 그림 3장: Kling(kling-v1-5, 마젠타 배경 「귀여운 뭉게구름 3종」) → **Photoshop MCP**(ExtendScript: Color Range 마젠타·레드 삭제 → 구름별 크롭 → 마술봉 모서리 배경 삭제 → Save for Web PNG) → `Resources/CoastRun/Fx_Cloud_{Puff,Flat,Wisp}.png`. .meta 는 Fx_AirRing 과 같은 설정(NPOT 그대로·알파 투명·밉맵 없음·비압축, GUID 유지).
- 개발: `Coast Run/Dev/Fx - Double jump cloud (slow x40)`(플레이 중, 연출을 40배 느리게 재생 — `JuiceDirector.DebugFxSlow`), 원격 `jump` 두 번은 사이가 게임 시간 0.15 s 넘어야 2단.
- 참고: 충돌 「꽈당!」 팡에는 여전히 노란 별·하트 파편(14차-14 `PlayObstaclePop`)이 나온다 — 사용자가 말한 「별」이 이것이면 다음에 구름/먼지로 바꿀 것.
- 커밋 대상: Visual/JuiceDirector.cs, Editor/MiniGameDevMenu.cs, UI/MainMenuController.cs(VisibleWhen CanvasGroup 수정), Resources/CoastRun/Fx_Cloud_*.png(+meta) 3개, Docs/HANDOVER.

### 2026-09-12 — 61차: 컷씬 8개로 정리(시네마) · 타이틀 버전 라벨 · 설정 톱니 아이콘 (컴파일 0, 캡처 r61_t*, 커밋은 사용자)
- **컷씬 구조**(사용자: 「오프닝/클로징 말고 그냥 컷씬, 8개」): 컷씬 = **8개**, 러닝 챕터(1·4·7·10·13·15·18·20)마다 하나. 컷씬 N 은 직전 러닝 챕터 다음 화부터 그 챕터까지의 이야기(오프닝+엔딩 대본)를 **한 편으로 묶어** 리더로 읽는다(컷씬 1 = 1화, 2 = 2~4화, 3 = 5~7화, 4 = 8~10화, 5 = 11~13화, 6 = 14~15화, 7 = 16~18화, 8 = 19~20화). 챕터가 바뀌는 자리엔 「제 n화 · 제목」 소제목. 러닝 없는 챕터 경계에선 이제 리더가 안 뜨고 그냥 다음 챕터로(`TamaRaisingUI.BoundaryRoutine`). `StoryProgress.CutsceneCount/CutsceneIndex/CutsceneChapter/CutsceneFirstChapter/CutsceneSceneIds/CutsceneRead/CutsceneTitle`, `StoryReaderUI.OpenCutscene(index)`(머리 「컷씬 N / 8」).
- **시네마**(`CinemaSelect` 전면): 프롤로그 영상(1:37) + 컷씬 1~8 카드(왼쪽 표지 그림 Cut_CHnn_Open, 「컷씬 N」·제목·「제 a~b화 · 장소」, 읽었으면 ✓). 해금 = 그 챕터에 닿았으면. 옛 오프닝/클로징 VN 항목(20×2) 삭제. 대본(ChapterScript 20챕터)·VN 재생기(ChapterVN)는 그대로 남아 있음(프롤로그 PRO·러닝 뒤 클로징 VN 은 옛 경로).
- **타이틀**: 「스튜디오 우히히시 v1.0」 라벨 y 2 → −12(살짝 아래). **설정**은 더보기에서 빼고 우상단 톱니 아이콘(`Icon_Gear.png`, 제목 아래·기부 컵 위, `BuildSettingsIcon`) — 메인에서만 보임(VisibleWhen).
- 커밋 대상: UI/{MainMenuController,CinemaSelect,StoryReaderUI}.cs, Meta/StoryProgress.cs, Raising/TamaRaisingUI.cs, Resources/CoastRun/Icon_Gear.png(+meta), Docs/HANDOVER.

### 2026-09-12 — 60차: 육성 페이지·이벤트 팝업 시안 반영 · 무궁화/투호 시안 · 육성 러닝 → 육성 복귀 (컴파일 0, 캡처 r60_t*, 커밋은 사용자)
- **육성 페이지**(`TamaRaisingUI`, 시안): 주차 알약 ✦ / 돈 알약 금색·남색 글자 / 왼쪽 버튼 3개에 아이콘(★·집·카트) / 오른쪽 위 **큰 동그라미 3개** = 이번 주 행동(하면 초록 ✓) / 생활 경고 알약 400 폭·줄바꿈 / 체력바 글자는 다시 막대 안 가운데 「127 / 게이트 48 ✓」(막대 안 눈금 없음, 위 ▼ 깃발만) / 기운 게이지 ⚡ 아이콘 + 「기운 92」 / 행동 카드 = 위 큰 아이콘 + 아래 이름 + 작은 효과 글 + ✦ / 아래 「자동 OFF」 + 큰 분홍 「다음 턴」(마감 주엔 「다음 턴 ▶ 이야기·대회」). 새 아이콘 `Resources/CoastRun/Icon_{Cart,Bolt,Eye,Camera,Card,Bang,Bulb,Tap}.png`(PIL).
- **이벤트 팝업 공통 키트** `UI/EventCardKit.cs`(신규): `Card`(어둠+흰 빛 테두리+크림 카드+모서리 ✦) · `JellyTitle`(그림자·두꺼운 테두리·하이라이트 겹 글자) · `Divider`(· · ✦ ✦ ✦ · ·) · `IconRow`(동그란 아이콘+글+오른쪽 알약 배지) · `InfoBox`(흰 안내 상자) · `IconButton`(아이콘 젤리 버튼). 적용: **대회 미달**(`ContestResultUI` — 시안 그대로: 빨간 젤리 제목·대회 이름·조건+0/2 배지·안내 3줄·주황 「지금 다시 도전」/파랑 「육성으로 (이 주 다시)」), **이번 주 대회 안내**(`ContestIntroUI` — 보라 젤리 제목·조건/제한시간 줄·안내 상자·주황 「출발!」), **육성 러닝 일시정지 카드**(아래).
- **육성 러닝 → 육성 복귀**: 러닝 일시정지 메뉴의 세 번째 버튼이 육성 모드에서도 「메인으로」(타이틀)였다 → 육성 모드(`GameManager.Active && !ArcadeRun.Active`)면 EventCardKit 카드(계속하기/다시 시작/**육성으로 돌아가기** + 「이 대회는 미달 — 이 주를 다시 키운다」) 로 `StoryContest.End(); GameManager.ContestFail()`(주차 유지). 정산 화면 버튼도 육성 모드면 「육성으로 돌아가기」(집 아이콘). 죽음(「육성으로 돌아가기」)·시간 초과·미달 화면은 이미 육성 복귀.
- **무궁화**(시안): 주인공을 앞(0.15)에 크게(0.30) / 네온 하늘색 레인 2줄(`MiniStage3D.Neon` = Lit+Emission) + 가운데 점선 화살표 3개 / 목표 띠 알약을 하트로(`MiniKit.Pips(…, sprite)`) / 아래 카드 = 빨강(👁 「술래 보고 있다!」 「멈춰!」 ⚠ 멈추면 안전; 등 돌리면 초록 「달려!」) · 파랑(맨 위 진행 막대 + ♥♥♥ + 「남은 거리」) · 분홍 「⚡ 달리기! (연타 누르기!) 👆👆」 / 조작을 **꾹 → 연타**로(탭 = 한 걸음 0.045 + 0.25 초 관성, 그 사이 술래가 보면 걸림).
- **투호**(시안): 항아리 1.45 배·가운데 / 화살에 분홍 빛 꼬리(TrailRenderer, Lit 투명+약한 발광 — 정점색은 URP Lit 이 안 쓴다) / 착지 반짝이(`Burst`) / 개발 토글 `Coast Run/Dev/Mission - Slow flight toggle`(비행 8 배 느리게 — 캡처용).
- **주의**: 이 세션 후반부터 원격 스테이징이 캡처·복사본까지 「hardlinked」로 거부 → 캡처 확인은 **Photoshop MCP**(`photoshop_open_image` → `photoshop_get_preview`)로 했다. `Coast Run/Dev/Tools - Copy recent shots`(_shots/_copy) 도 추가해 둠.
- 커밋 대상: Raising/{TamaRaisingUI,MissionMiniGames.Kit,MissionMiniGames.Games3D,MiniStage3D}.cs, UI/{EventCardKit(신규),RunHudChrome,StageClearUI}.cs, Meta/StoryContest.cs, Editor/{MiniGameDevMenu,ToolsCopyMenu}.cs, Resources/CoastRun/Icon_*.png(+meta) 8개, Docs/HANDOVER.

### 2026-09-12 — 59차: 체력바 정리 · 도로변 반복 상자 → Blender 소품 6종 (컴파일 0, 캡처 r59_*, 커밋은 사용자)
- **체력바**(`TamaRaisingUI.Gauge/Refresh`): 「127 · 게이트 48 ✓」 글자가 막대 위에 겹쳐 흰 눈금이 「1|27」처럼 보였다 → 숫자는 막대 오른쪽 칸(「127 ✓」, 굵게), 막대엔 채움 + 진한 테두리 흰 눈금 + 위 ▼ 깃발(통과면 초록, 부족이면 주황)만. 게이트 수치는 아래 안내 줄(「체력 ▼48 게이트 통과!」 / 「▼48 까지 N 더」). 기운 게이지도 같은 배치.
- **도로변 반복 상자**: 정체 = `PromenadeSegmentBuilder` 가로등 사이 15 m 마다 놓던 주황 상자 + 하늘색 「Bloom」 큐브(옛 자리표시 화분). → `Tools/blender/kerb_kit.py`(헤드리스, `build_kerb.bat`, 메뉴 `Coast Run/Build Kerb Props 3D (Blender)`) 로 **Kerb_*.fbx 6종**: 현무암 화분+수국 / 나무 화단+유채 / 귤 상자 2단+귤 / 옹기 항아리 두 개 / 부표 더미+밧줄 / 돌 화단+동백. `World/KerbProps.cs`(신규) 가 직전 둘과 다른 종류로 고르고, 회전·크기(0.88~1.16)·위치(±2 m)·꽃 색(MPB `_BaseColor`: 수국 파랑/보라/하양, 유채 노랑/주황, 동백 분홍/빨강)을 흔들며 5개 중 1개꼴로 비워 둔다. 재질 이름 Soil/Glaze/GlazeDark/BuoyOrange/BuoyWhite/Rope/Bloom* 를 `JejuKit.Build` 에 추가. 모델이 없으면 옛 상자.
- **도구**: `Editor/WorldDumpMenu.cs` `Coast Run/Dev/World - Dump nearby props`(플레이 중, 플레이어 앞 60 m 소품을 Tools/_shots/world_dump.txt 로) · `Editor/ToolsCopyMenu.cs` `Coast Run/Dev/Tools - Copy blender scripts`(Tools/blender/*.py|bat|txt → `_copy/`; 원격 스테이징이 「hardlinked」로 거부될 때 복사본을 읽는다) / `Apply _copy to blender`.
- 커밋 대상: Raising/TamaRaisingUI.cs, World/{KerbProps(신규),JejuKit,PromenadeSegmentBuilder}.cs, Editor/{BlenderKitMenu,WorldDumpMenu(신규),ToolsCopyMenu(신규)}.cs, Tools/blender/{kerb_kit.py,build_kerb.bat}(신규), Resources/CoastRun/Models/Kerb_*.fbx(+meta) 6개, Docs/HANDOVER. `Tools/blender/_copy/` 는 커밋 제외 권장(.gitignore).

### 2026-09-12 — 58차: 미니게임 사용성을 10억 다운로드급 캐주얼 문법으로 (컴파일 0, 캡처 r58_*, 커밋은 사용자)
- **비교 기준**(하이퍼캐주얼 UI/UX 가이드·FTUE 글 조사): ① 한 손·한 동작 조작 ② 글 없이도 첫 3초 안에 「뭘 누르는지」 보임(암묵적 튜토리얼) ③ 매 시도마다 즉각 피드백(큰 글자·진동·소리) ④ 목표·남은 기회·점수가 언제나 한 줄로 보임 ⑤ 화면에 글자 최소 ⑥ 실패 → 즉시 재시도. 우리 미니게임은 ①·⑥은 됐고 ②③④가 약했다(목표가 소개 카드에만, 남은 기회가 작은 글자, 결과가 상태줄 문장).
- **공통 키트** `Raising/MissionMiniGames.Kit.cs`(신규, `MissionMiniGames.MiniKit`): `Attach(yard, playKey)` → 마당 맨 위 **목표 띠**(왼쪽 목표 한 줄 · 가운데 남은 기회 알약 ●●○ · 오른쪽 점수 0/3, bestFit), `Goal/Score/Pips(total,left,color)`, `Flash("준비 — 시작!")`, `Pop("쏙!"/"빗나감", good)`(마당 가운데 큰 글자 튀어오름→위로 사라짐, unscaled), `TapHint(button,label)`(PlayerPrefs `CoastRun_MiniPlays_<게임>` 2판까지만: 큰 버튼 위 퍼지는 링+속 원, 마당 아래 흰 안내 알약 + ▼, 한 번 누르면 사라짐), 정적 `Pulse(button,on)`(대기 중 1±3.5% 맥동).
- **적용**: `Stage3DMission`(Games3D.cs) 기반에 `protected MiniKit Kit; protected RectTransform BigRect;` — `SetupStage` 가 자동 부착, `BigButton` 이 맥동 시작. 윷(목표·나/도담 점수·결과 팝), 투호(「5발 중 3발 항아리에!」·알약 5·쏙!/빗나감), 딱지(알약 3 빨강·넘어갔다!/약해!/튕겼어!), 무궁화(「술래에게 닿기! 돌아보면 멈춰」·알약 3·걸렸다!/이겼다!), 구슬치기(MissionMiniGames.cs `_kit/_fireRt`: 「3발로 구슬 6개를 밖으로!」·알약 3·밖으로!/성공!).
- 겹침 수정: 투호 안내 알약을 마당 아래 화살 알약 위로(y 74), 무궁화 구호(`_chant`)를 띠 아래 0.80~0.895 로(안내 Status 는 0.70~0.80).
- 확인: r58_tuho1/2/3, r58_ddakji, r58_marbles, r58_mugung/2, r58_yut — 5종 모두 띠·알약·점수·맥동 버튼·안내 알약·「종료하기」 표시.
- 커밋 대상: Raising/MissionMiniGames.Kit.cs(신규), Raising/MissionMiniGames.cs, Raising/MissionMiniGames.Games3D.cs, Docs/HANDOVER.
- 다음 후보(미적용): 소개 카드 대신 첫 판은 바로 시작+띠만(3초 절약) · 결과 카드에 「다시」 큰 버튼 하나만 · 진동 강도 2단계(성공 짧게/실패 길게).

### 2026-09-12 — 57차: 결산 카드 시안 · 미니게임 종료하기 · 체력바 · 생활 설계 문서 · 리더 글자 키움 (컴파일 0, 캡처 r57_*, 커밋은 사용자)
- **결산 카드**(`WeekPassUI` 전면): 달·해 글리프, 노란 큰 제목(진한 테두리), 분홍 「23주차 → 24주차 · 여름」, 아이콘 줄 목록(쌀·반찬·잠·옷 — `Resources/CoastRun/WK_Rice|WK_Side|WK_Sleep|WK_Shirt.png`, Kling 타일 3장 + 잠은 PIL 로 그림), 배부름(♥ 7칸)·컨디션(★ 7칸) 카드에 「↓ 100 → 60」, 컨디션 0 경고, 다음 턴 예고, 분홍 「터치해서 다음으로 ▶」 + ✦ 반짝임. `WeekReport.lines` 는 이제 안 씀(필드로 그림).
- **미니게임 종료하기**: `ChapterMissionUI` 가 숨기던 머리 「그만」 버튼을 항상 켜고 「종료하기」로 — 누르면 진 것으로 처리돼 「아쉽다」 카드(미션 모드: 다시하기/넘어가기, 다시하기 모드: 다시하기/그만).
- **체력바**(`TamaRaisingUI.Refresh`): 「123 / 36」이 헷갈려 막대 = 체력/200, 게이트 자리에 흰 눈금(`_gateMark`), 글자 「123 · 게이트 36 ✓ / 부족」.
- **생활 설계 문서** `Docs/DESIGN_RAISING_LIFE_v1.md`: 턴 구조·식사·잠·옷·컨디션/쓰러짐·돈 밸런스·자동 규칙·화면 + 다음 제안(잠 행동 분리·칸 이름 아침/낮/밤·반찬 등급·컨디션→성공률·낡은 옷→알바).
- **리더 글자**: 본문/대사 15 → 20(폰에서 ≈12pt), 이름표 12 → 15, 캡션 12 → 15, 지난 이야기 14 → 18, 속마음 14 → 18, 편지 14 → 18, 제주말 13 → 17, 줄 간격 14. A−/A+ 배율 0.9~1.6. 스크롤이 길어진 건 의도.
- 커밋 대상: UI/{LifeUI,StoryReaderUI}.cs, Raising/{MissionMiniGames,TamaRaisingUI}.cs, Resources/CoastRun/WK_*.png(+meta), Docs/DESIGN_RAISING_LIFE_v1.md.

### 2026-09-12 — 56차-2: UI/UX·글자 넘침 점검 (컴파일 0, 캡처 r56b_*, 커밋은 사용자)
- **점검 도구** `Coast Run/Dev/UI - Overflow audit`(플레이 중): 화면의 모든 Text 를 훑어 preferred 크기가 rect 보다 크면(bestFit 제외, 레이아웃 그룹 안 제외) 경로·글자·크기를 로그. 타이틀·더보기·챕터 선택·컬렉션·약관·투호·육성·상태창·마이룸(방/텃밭/놀이)·리더·러닝 HUD·대회 미달 화면을 돌려 봄.
- **고친 것**: ① `StageClearUI` 정산 설명 4줄이 아이템 칩에 가려짐 → 3줄(장소·등급 한 줄)로 합치고 상자 높이 88·칩 y −232. ② `KpopChapterSelect` 카드 아래 COMPLETED 글자가 옆 칸과 붙음 → 상자를 카드 폭 안으로 + bestFit. ③ `PolicyUI` 「✕ 닫기」 버튼이 피벗(가운데) 탓에 반이 화면 밖 → (−76, −46). ④ 미니게임 상태 문장(`Status`)이 캔버스 배율을 안 곱해 깨알 글씨 → Scaled(13)·bestFit·두 줄 띠(0.83~1.0), 카드/큰 버튼 0.04~0.80. 무궁화 「(꾹 누르기)」도 Scaled. ⑤ `RunHudChrome` 「노을까지 m:ss」가 상자보다 5px 넓음 → bestFit. ⑥ **UX** `StatusUI` 아래 빈 자리에 「생활」(배부름·컨디션 막대 + 쌀·반찬·옷·이번 주 잠) 추가, 카드 850.
- 남은 경고는 타이틀 기부 아이콘의 장식 ✦(36>32) 뿐 — 의도된 것.
- 커밋 대상: UI/{StageClearUI,KpopChapterSelect,PolicyUI,StatusUI,RunHudChrome}.cs, Raising/{MissionMiniGames,MissionMiniGames.Games3D}.cs, Editor/MiniGameDevMenu.cs.

### 2026-09-12 — 56차: 말풍선 오른쪽 · 투호 완화 · 텃밭 4작물(성공 확률·2~3주) · M 곡 외 음악 완전 제거 (컴파일 0, 캡처 r56_*, 커밋은 사용자)
- **말풍선**: 육성 화면 말풍선(`TamaRaisingUI` Bubble)을 하늘이 머리 오른쪽에 꼬리 달린 풍선(330×92, 우측 앵커 y 905)으로 — 위쪽 버튼·생활 알약과 안 겹친다.
- **투호**(`MissionMiniGames.Games3D.cs` TuhoMission3D): 힘 게이지 2.2 → 1.98 Hz(10 % 느리게, `PowerHz`), 방향 허용 ±6° → ±8°, 힘 띠 ±0.09 → ±0.12(들어갈 확률 ↑).
- **텃밭**(`HomeData.Seeds` 전면 교체 + `HomeUI` 「베란다」 탭 → 「텃밭」): 쿼터뷰 방(UI_Room_Iso) 안 화분 4개(`PotSpots`), 작물 4종 — 토마토 200G·2주·성공 70 %·반찬 2주분 / 감자 180G·3주·80 %·반찬 3주분 / 쌀(벼) 200G·3주·50 %·쌀 2주분 / 장미 180G·2주·60 %·스트레스 0. 물 주기 없음: 주가 바뀌면(`Survival.WeekTick` → `HomeData.WeeklyGrow`) 한 단계 자라고, 다 자라면 「수확 · 성공 N %」 탭 → `HomeData.Harvest` 가 확률을 굴려 성공(보상)/시듦. 트레이는 시안대로 아이콘·이름·값·「N주 · 보상」·「성공 N %」 카드(빈 화분 먼저 탭). 결산 카드에 「다 자란 작물 N개 — 마이룸에서 수확」.
- **음악은 M 곡만**: ① `Assets/Art/Audio` 의 옛 클립 6개(BGM_Drone_Low.wav 등, 코드 참조 없음) 삭제 — 메뉴 `Coast Run/Audio/Delete old music`, 확인 `Coast Run/Audio/List all clips`(남은 것: BGM_M1~M12 + SFX_* 7개). ② **합성 대체 음악 전부 제거**: `TitleAudio.PlayMenu`(M 곡 없으면 무음), `CoastAudioManager` 220 Hz 앰비언트 드론(클립 없음), `CutsceneController.PlayBgm`/`EndingController.PlayBgm`/`UI_MemoryPopup`(클립 없으면 무음), 엔딩 저음 펄스·합성 피아노·VO 톤 제거. ③ `CoastBgmLibrary.Alias`: 옛 키를 M 곡으로 — Menu/Title → M5, End_* → M3(Descent 는 M1), Memory_*/Cine_* → M6, CH*/Track_* → M9, KPOP_* → M8, 스팅어는 null. 바람·바퀴·발소리(노이즈)와 SFX 대체 블립은 음악이 아니라 그대로.
- 커밋 대상: Raising/{TamaRaisingUI,HomeUI,MissionMiniGames.Games3D}.cs, Meta/{HomeData,Survival}.cs, Audio/{CoastBgmLibrary,TitleAudio,CoastAudioManager}.cs, Story/{CutsceneController,EndingController}.cs, UI/UI_MemoryPopup.cs, Editor/BgmCleanupMenu.cs, 삭제된 Assets/Art/Audio/*.wav(+meta).

### 2026-09-12 — 55차-2: 「다음 턴」 버튼 · 미니게임은 져도 넘어감(이기면 돈) (컴파일 0, 캡처 r55b_next*, 커밋은 사용자)
- **다음 턴 버튼**: 육성 아래 「이번 주 행동 N/3 ●●○ ▶ 다음 턴」 알약(`NextTurn`)이 버튼. 행동 3번을 다 해도 자동으로 안 넘어가고 이 버튼을 눌러야 한 주가 간다(`ActionRoutine` 은 「행동 끝!」 말풍선만). 행동이 남았으면 첫 탭에 「N번 남았어, 한 번 더 누르면 넘어가」 말풍선, 6초 안에 다시 누르면 남은 행동을 버리고 넘어간다(잠을 안 잔 주로 계산됨). 자동 모드는 3번 끝나면 스스로 누른다. 원격: `tap 0.7 0.09`.
- **미니게임**: 격주 주말 미니게임·러닝 뒤 미션(CH3·6·9·12·14) 모두 **져도 다음으로**. 이기면 `ChapterMission.Reward` = 150 + 주차×5 G(`ChapterMissionUI.ShowWin` 에서 지급, 소개 카드에 「이기면 NG 보상 · 져도 넘어가요」). 진 화면의 「넘어가기」= `ChapterMission.MarkAttempted`(이번 회차 시도 비트만, 프로필 해금 비트는 안 켬) → `SceneFlowController` 의 미션 대기(Pending)가 풀린다. 「다시하기」로 돈을 노려도 됨.
- 커밋 대상: Raising/TamaRaisingUI.cs, Raising/MissionMiniGames.cs, Meta/ChapterMission.cs.

### 2026-09-12 — 55차: 턴 구조(3행동=1주 → 「일주일이 지났다」 → 다음 턴에 컷씬·대회) · 러닝=대회(미션·제한시간) · 생존 생태계(쌀·반찬·옷·컨디션·죽음) · 텃밭 채소 · 리더 시안 리디자인 (컴파일 0, 캡처 r55_*, 커밋은 사용자)
- **① 컷씬은 러닝과 무관** — 러닝 앞뒤 컷씬 전부 제거: `GameManager.StartStoryRun` 프롤로그 VN 안 틈(프롤로그는 육성 턴 사이 `TamaRaisingUI.BoundaryRoutine` 에서 첫 이야기 앞에), `SceneFlowController.EnterStageClear` 는 스토리 모드에서 회상(기억 조각) 팝업을 안 띄움(갤러리에서). 클로징 VN 은 52차에 이미 리더로 합쳐짐.
- **턴 구조**(`TamaRaisingUI.EndWeek/BoundaryRoutine`): 행동 3번 → `Survival.WeekTick`(생활 결산) → `GameManager.AdvanceWeek`(챕터 마지막 주면 `Save.boundaryPending=true`) → `WeekPassUI`「일주일이 지났다」(결산 줄 + 「다음 턴: 챕터 N 이야기 → 대회 「…」」 안내, 터치) → 다음 턴: `boundaryPending` 이면 레벨 게이트 → (1장 프롤로그) → 리더(안 읽었을 때만, `StoryProgress.ChapterRead`) → 러닝 없는 챕터 완료 / 러닝 챕터 = 체력 게이트 → `ContestIntroUI`(대회 안내, 출발!) → `StartStoryRun`. 앱을 껐다 켜도 `Bind` 에서 `boundaryPending` 이면 이어서(ResumeBoundary).
- **② 러닝 = 대회**(`Meta/StoryContest.cs` 신규): 러닝 챕터 8개에 대회 하나씩 — CH1 코인 120 / CH4 사진(포토카드) 2장 / CH7 보스 1 / CH10 코인 350 / CH13 사진 3 / CH15 보스 2 / CH18 코인 600 / CH20 완주, 제한시간 3:00(CH20 3:20). `GameSession.HandleStageStart` 맨 앞에서 `Begin`(스토리·비재도전만) → 상단 `ContestHud` 알약(이름·진행·남은 시간; 달성 = 초록, 20초 남으면 빨강) → 시간 초과·미달이면 `ContestResultUI.ShowFail`(timeScale 0, 「지금 다시 도전」=RetryCurrent / 「육성으로」=`GameManager.ContestFail`: 정산 없이 phaseIndex 0·**주차 그대로**). 결승선에서 `SceneFlowController.NotifyStageCleared` 가 `StoryContest.Succeeded` 아니면 정산 없이 미달 화면. 진행 집계: 코인 = `StageRunStats.CoinValue`, 사진 = `JellyPickup` 포토카드 `NotePhoto`, 보스 = `BossDirector` 퇴치 `NoteBoss`. 보스 대회는 `StoryContest.SpawnBoss`(GameSession 의 지난 BossDirector Destroy 뒤) — 목표 마리 수가 되도록 가짜 챕터 `6+3(n-1)`; `BossDirector.Plan` 의 곡 길이 L 은 K-POP 아닐 때 대회 제한시간×0.7. 사진 대회는 `JellySpawner` 포토카드 간격 230~320 m(평소 420~640).
- **③ 생존 생태계**(`Meta/Survival.cs` 신규, `SaveData` 필드 rice/sideDish/hunger/clothesWeeks/condition/sleepDebt/starveWeeks/dangerWeeks/deaths/restedThisWeek/boundaryPending/contestFails): 주마다 쌀 1·반찬 1 소비(없으면 허기↓·컨디션↓), 밥/휴식 행동 = 잠(`OnRestAction`, 안 자면 sleepDebt → 2주부터 컨디션 −15·스트레스 +10), 옷 12주(낡으면 컨디션 −8·매력 −1), 허기 → 컨디션, 컨디션 <30 이면 체력 −3/주. **컨디션 0 이 2주** 또는 4주 굶으면 사망 → `GameOverUI`(병원: 돈 절반·회복·이 주에서 계속 / 처음부터: `GameManager.RestartAfterDeath` = 같은 이동 모드 새 회차). 육성 화면: 마이룸 아래 **장보기** 버튼(`GroceryUI`: 쌀 60G·반찬 40G(4주분 묶음)·새 옷 300G) + 생활 알약(쌀·반찬·옷·배부름·컨디션, 위험하면 ⚠ 경고).
- **④ 텃밭**(`HomeData.Seeds` 에 먹는 작물 4종 상추/당근/감자/고구마 `food` 필드): 수확(`Sell`) = 반찬 재고 +food, 주가 바뀌면 `WeeklyGrow` 로 화분이 한 단계 자라고 다 자란 채소는 저절로 반찬으로(결산 줄 「텃밭에서 수확」). HomeUI 라벨 「수확 · 반찬 +N」.
- **⑤ 리더 시안 리디자인**(`UI/StoryReaderUI.cs` 전면): 종이 배경 위 분홍 리본 「제 N화 제목」+ A-/A+/건너뛰기 › · 위 42 % 삽화 고정(스크롤에 따라 장면 표식 `Mark` 의 컷/배경으로 교차 페이드) · 아래 어두운 반투명 패널 안 본문(지문 밝은 글씨 15pt, 대사 = 색 알약 이름표(하늘=하늘색·아빠=금색·그 외 분홍) + 「대사」, 편지지, 지난 이야기/제주말 풀이는 반투명 상자) · 오른쪽 표시용 스크롤바 · 맨 아래 흰 「다음 ▶」. 54차-2 산문 층(`Story/StoryProse.cs`: 지난 이야기·장면 캡션·소설 지문 130줄·제주말 풀이) 유지. 원격 스크롤 `key F8/F7`(PageDown 은 StageManager 가 씀).
- 개발 메뉴 `Coast Run/Dev/Life - Week pass|Grocery|Game over|Starve|Test turn end|Test boundary`, `Contest - Intro CH1|Fail screen|Close all`. 캡처: r55_reader1/2(리더), r55_grocery·weekpass·contest_intro·gameover(생활 화면), r55_flow2~7(턴 흐름 → 대회 안내), r55_run1(대회 HUD), r55_run2(정산, 컷씬 없음), r55_after(CH2 복귀).
- **주의**: 대회 코인 목표는 코인 **가치**(CoinValue, 배수 포함) 기준. 기존 세이브는 JsonUtility 가 새 필드를 기본값(쌀 2·반찬 2·옷 12주·컨디션 80)으로 채움. `RaisingUI.cs`(구 UI) 는 손대지 않음. 텍스트에 `\n` 을 파이썬으로 넣을 때 실제 개행이 들어가 컴파일 에러(CS1039) 났던 것 수정.
- 커밋 대상: Meta/{Survival,StoryContest}.cs(신규), UI/LifeUI.cs(신규), Meta/{SaveData,GameManager,HomeData,BossDirector}.cs, Raising/{TamaRaisingUI,HomeUI}.cs, Core/{GameSession,SceneFlowController}.cs, Economy/{JellyPickup,JellySpawner}.cs, UI/StoryReaderUI.cs, Story/StoryProse.cs(54차-2 신규), Editor/MiniGameDevMenu.cs, Docs/HANDOVER.

### 2026-09-12 — 54차: 컷씬 그림 21장 (Kling 수채, 컴파일 0, 캡처 r54_reader1)
- `Resources/CoastRun/Cut_CH01~CH20_Open.png`(20장) + `Cut_CH14_Close.png`(빠져 있던 것) — 기존 Cut_CHnn_Close 와 같은 수채 스토리북 화풍(810×1440, 송전탑 모티프, 얼굴 작게/뒷모습). 옛 `Cut_CH20_Open`(정류장·유채)은 `Cut_CH20_Mid` 로 보존. 프롬프트는 각 챕터 오프닝 대본 첫 지문 기준(`Tools/KlingGen/out/raw/r54_cut/*`).
- 연결: **리더**(`StoryReaderUI`)는 씬마다 `Cut_<sceneId>` 가 있으면 그 앞에 삽화(오프닝 그림 → 본문 → 엔딩 그림 → 본문). **VN 플레이어**(`ChapterVN.ShowBg`)는 그림 없는 컷(`BG | Blank`)의 첫 장면에 `Cut_<sceneId>` 를 깐다(씬당 한 장) — 더보기 › 시네마 재생에도 그림이 뜬다.
- 커밋 대상: Story/ChapterVN.cs, UI/StoryReaderUI.cs, Resources/CoastRun/Cut_CH01~CH20_Open.png(+meta, CH20 은 png 만), Cut_CH14_Close.png(+meta), Cut_CH20_Mid.png(+meta).

---

### 2026-09-12 — 53차: K-POP 돈 → 육성 돈(k 표기) · 레벨·스탯·경험치(상태창) · 마이룸(주인공+펫) · 레벨이 롱컷씬을 연다 (컴파일 0, 캡처 r53_*, 커밋은 사용자)
- **① 돈 합산**: K-POP 정산(`ArcadeRun.SettleKpop`)에서 그 판 코인 + 도장/올클리어 보너스 + 보스 코인이 `Save.stats.money` 에도 쌓인다(스토리 러닝은 원래 그랬음). 육성 화면 돈은 `LevelSystem.FormatK`(1,413 → 1.4k, 43,535 → 43.5k, 백만은 M) — 다마고치 HUD·마이룸·상태창.
- **② 레벨·경험치**(`Meta/LevelSystem.cs` 신규, `SaveData.level/exp`): 필요 경험치 `80 + 40·Lv`(Lv1→2 120 … Lv20→21 880), 상한 40. **젤리 = 경험치**(젤리 5 · 큰 젤리 15 — 52차 젤리 통장은 안 쓰고 펫은 코인 + 레벨 조건 Lv3/6/9/12). 그 밖의 경험치: 행동 10(대성공 20·실패 4), 쓰다듬기 조금, 이야기 한 편 50, 미니게임 승리 40, K-POP 완주 60, 보스 20/마리, 스토리 러닝 완주 120. 세이브 없이 모은 경험치는 PlayerPrefs 에 두었다가 육성 진입 때 합침(`FlushPending`). 레벨업: 체력 +2 순발력 +1 매력 +1 감성 +1 + 토스트, 러닝 코인 +1 %/Lv(최대 +30 %, `RunTuning.CoinMul` 에 곱함). 칭호 5레벨마다(새내기 → 초보 러너 → 제주 소녀 → 스무 살 준비 → 해안도로 스타 → 송전탑 러너 → 주파수의 주인).
- **상태창**(`UI/StatusUI.cs` 신규): 다마고치 HUD 주차 아래 [Lv N 상태창] 버튼 → 얼굴·레벨 배지·칭호·EXP 바·다음 롱컷 해금 힌트·스탯 6개 바(체력·순발력·매력·감성·평판·스트레스)·돈/코인·레벨 보너스·경험치 얻는 법. 그 아래 **[마이룸]** 버튼 → `HomeUI`(방 꾸미기·베란다·놀이). 마이룸에 **주인공 꼬마가 돌아다니고 장착한 펫이 옆에서 따라다닌다**(`ShowRoomGirl = true` 복구 + `_petRt`, 깊이 정렬 포함).
- **③ 레벨 킥**: 롱컷씬 챕터(4·7·10·13·15)와 엔딩(20)의 마지막 주에 **레벨이 모자라면 리더가 안 열리고 한 주 연장**(`TamaRaisingUI.EndWeek`, `LevelSystem.LevelForLongCut`: CH4 Lv3 · CH7 Lv5 · CH10 Lv7 · CH13 Lv9 · CH15 Lv12 · 엔딩 Lv15). 롱컷 7개가 K-POP 11챕터+를 여니 레벨이 「킥」. 비밀코드/기부 패스코드면 통과.
- 개발 메뉴: `Coast Run/Dev/Level - +200 EXP`, `Level - Status window`. 캡처 `r53_status1`(상태창 Lv2), `r53_room1`(마이룸 + 주인공).
- 커밋 대상: Meta/LevelSystem.cs·UI/StatusUI.cs(신규), Meta/{SaveData,PetShop,GameManager,ArcadeRun}.cs, Economy/JellyPickup.cs, UI/PetShopUI.cs, Raising/{TamaRaisingUI,HomeUI,MissionMiniGames,RaisingUI.Popups}.cs, Editor/MiniGameDevMenu.cs, Docs/HANDOVER.

---

### 2026-09-12 — 52차: 기부 팝업 · 스토리 모드 구조(웹소설 리더·격주 미니게임·러닝 8회) · K-POP 3분 · 레코드/컬렉션 AI 표시 · 보스 재배치 · 펫 상점(20주차·코인+젤리) · 딱지치기·챕터 화면 시안 · 육성 포즈 10장 (컴파일 0, 캡처 r52_*, 커밋은 사용자)
- **① 기부부탁**(`Meta/Donation.cs`, `UI/DonateUI.cs` 신규): 타이틀 우상단에 **커피잔 아이콘**(`UI_Donate_Cup`, Kling 키잉) + 「기부부탁」 라벨이 반짝이며 둥실 — 메인에서만 보이고 더보기·챕터 선택·컬렉션·미니게임·약관·설정 등이 열리면 숨는다(`MainMenuController` visible 람다). 첫 로딩(AI 고지 뒤) 팝업 1회 자동(`CoastRun_DonateSeen`), 이후 아이콘 탭. 팝업 = 개발자 메시지(광고·현질 없음, K-POP 세계화, 커피 한 잔 값) → 선물 3택(♪ 히든 트랙 2곡 / ★ 모든 게임 열림(히든 패스코드 **7799**) / ♥ 안 받기) → 「커피 한 잔 기부하기 · $3」(구글 결제 소모성 `coastrun_donate_coffee`, `IapBridge` 에 상품 추가, 여러 번 가능, 잔 수 `MetaProfile.donateCups`). 선물: 히든 트랙 = M9·M10 이 레코드 화면 얇은 금색 띠 + K-POP 런 풀(`ArcadeRun.HiddenTracks`)에; 모든 게임 열림 = `GameManager.DevUnlockAll` 이 `donateGiftMask` 도 본다(챕터·미션·시네마·펫 상점·K-POP 전 챕터). 패스코드는 설정 › 비밀코드에 넣으면 다른 기기에서도(`Donation.TryPasscode`). 에디터는 가짜 스토어라 가격 라벨을 $3 고정. **플레이 콘솔에 소모성 상품 `coastrun_donate_coffee`($2.99 티어) 등록 필요.** AI 고지 문구도 「광고도 강제 결제도 없음, 기부는 자율」로.
- **②③ 스토리 모드 구조**(`Meta/StoryProgress.cs`, `UI/StoryReaderUI.cs` 신규, `Raising/TamaRaisingUI.cs`·`RaisingUI.cs` 둘 다 수정 — **실제 육성 화면은 TamaRaisingUI**(RaisingSceneDriver 45차)):
  - 챕터 컷씬은 **챕터마다 하나**: 마지막 주 주말에 `StoryReaderUI.OpenChapter` — 오프닝+엔딩 대본을 이어서 **웹소설처럼** 종이 위 세로 스크롤(표지 삽화 `Cut_CHnn_*`, BG/CG 삽화 카드, 지문 문단, 「이름」+「대사」, 편지지 상자, A-/A+ 글자 크기(저장), 건너뛰기, 「다 읽었어요 ▶」, 아빠의 곡 M6 나지막이). 다 읽으면 ChapterVN 과 같은 흔적(`CoastRun_VN_<id>`, `RecordTable.OnSceneWatched`) → 롱컷 카운트·레코드 해금 공유. 런 뒤 CHxx_Close VN 은 리더에서 읽었으면 생략(`SceneFlowController`). 에디터/원격: Return = 다 읽음, Esc = 건너뛰기(`ReaderKeys`), 개발 메뉴 `Story - Reader CH1|CH4|close`.
  - **격주 주말 미니게임**(짝수 주, 5종 순환 `StoryProgress.WeeklyMinigame`): 행동 3번 뒤 `ChapterMissionUI.Play` — 이겨야 주가 넘어간다(`SaveData.weekMiniDone`). 지면 「이따가」(52차: 비-리플레이도 물러날 수 있음)로 주는 그대로, 다음 행동 탭이 주말만 다시(`WeekendOnly`). 이긴 미니게임은 `missionClearMask` 로 더보기 › 미니게임 다시하기 해금.
  - **러닝은 이벤트 8회**: `StoryProgress.RunChapters = {1,4,7,10,13,15,18,20}`. 다른 챕터는 리더를 읽으면 `GameManager.CompleteChapterNoRun()`(육성 하트로 등급 `ChapterGrading.SettleNoRun`, 목표에서 러닝 몫 30 제외) → 다음 챕터 첫 주. 러닝 한 판은 **≤1500 m**(`StoryProgress.MaxRunMeters`, `StageManager.LoadStage` 가 씌움 — 표는 1650~3300 m 그대로).
  - **K-POP 11챕터+ 잠금**: 롱컷씬 7개(`PRO, CH04/07/10/13/15_Open, END`) 본 수만큼(`KpopChapterSelect.IsUnlocked`, 잠금 토스트에 이유). 비밀코드/기부 패스코드면 전부.
- **④ K-POP 3분**: `KpopTracks` 창 180초, 짧은 곡은 스템 소스가 loop 라 처음부터 한 바퀴 더(M4 97s·M8 126s·M11 140s·M12 140.6s). 후렴 2회(`chorus2Start/End`, `KpopTrackMeta.InChorus`) — 두 번째 후렴에도 피버 제안+배너(`StageManager._kpopChorus2Offered`). 후렴 시각은 사운드 확인 전 기본값.
- **⑤⑥**: 약관 `Contact = sbs71656@gmail.com`. 레코드 화면: 「7/7」 삭제 → 그 자리 「✦ AI 생성 음원 · 가상 듀오 우히&히시」 필, 우상단 「⌂ 홈」 버튼(닫기와 같음). 포토카드 화면: 프레임 상단 띠 오른쪽 「✦ AI 생성 이미지 · 가상 인물」.
- **⑦⑧ 보스**(`BossDirector`): 앞 **28 m·높이 5.5 m**(전 14·2), 그림 **3배**(6.9/7.8 m), 그림자 5.5. 등장: **1~5챕터 없음**, 6챕터부터 1회, 3챕터마다 +1(최대 5, 곡 창 22~88 % 구간 균등). 퇴치 = **+100코인**(`CoinWallet.Add`) + 400점 + 배너. 「느려지는 보스」 = 가속 구간이 끝날 때 부스트가 1로 뚝 떨어지던 것 + 피격 감속 → **보스 중엔 피격 감속 없음**(`PlayerController.NoHitSlow`), 보스 중 ×1.2·가속 구간 ×1.35, 내릴 땐 천천히(`DriveBoost`). 보스전 최소 챕터 6, 보스 간격 26 s.
- **⑦′ 펫**: `PetShop.UnlockWeek = 20`(스토리 20주차, devUnlockAll 이면 열림), 값 **코인 + 젤리**(`JellyWallet` 신규 — 러닝 젤리 1/큰 젤리 3 적립, PlayerPrefs `CoastRun.Jelly`; `CoinWallet.TotalStatic/TrySpendStatic`). 참새 800c+30j · 흑돼지 1500+60 · 깡패 2000+90 · 기러기 4500+150. 다마고치 육성 HUD 에 「펫」 버튼 → `UI/PetShopUI.cs`(신규 오버레이). 옛 RaisingUI 상점도 같은 규칙.
- **⑨ 딱지치기**(`DdakjiMission3D` 재작성): 딱지 두 장이 **바닥에 납작**(탑다운 그림 쿼드 `MG_Ddakji_Blue/Red`, Kling 크롭) — 왼쪽 파랑(도담이) 위에 **하늘색 표적 링**(절차 텍스처, 맥동, 성공 시 터지며 사라짐), 오른쪽 빨강(나)은 들었다 내리친다. 발판 [타이밍 **가로** 게이지 + 노란 띠 + 큰 % 알약] [하트 3 + 「남은 기회 N개」] [내리치기! ↓]. 다른 미니게임은 손대지 않음(같은 Stage3DMission 틀).
- **⑩ 챕터 화면**(`KpopChapterSelect`): 별하늘·색종이·쿠키 배경(`UI_Chapter_Bg`, Kling 9:16), 완료 = 금색 카드 + S 메달 + 「✓ COMPLETED」, 현재 = 남색 카드 + 금테 + 자물쇠 + 노란 LOCKED, 잠김 = 남색 + 회색 LOCKED, 부제 「달릴 챕터를 고르세요 · 고른 챕터를 미션 플레이!」, 아래 「▶ 챕터 N 도전하기!!」.
- **⑪ 육성 포즈 10장**(`Raise_Girl_Pose_Sleep/Eat/Cafe/Delivery/Haenyeo/Laugh/Angry/Dance/Skate/Cry`, Kling v1-5 + `Tools/KlingGen/ref_girl.png` 피사체 참조 + 컨테이너 cv2 휴 키잉): 행동 중 활동 포즈(스케줄 id → 포즈 `PoseFor`), 대성공 웃음·실패 울음, 스트레스/체력 ≥ 0.95 화남, 미니게임 승리 웃음. 잠깐 붙잡았다가(`HoldPose`) 기분 그림으로.
- **주의/남은 것**: 원격 `tap` 은 이름이 같은 버튼(Home/Skip 등)을 잘못 잡는다 → 리더는 `key Return`. 이 세이브(9~10주차 CH1)는 게이트 연장으로 주차가 어긋나 있어 경계 흐름은 리더 dev 메뉴 + 격주 미니게임 진입까지만 확인. 플레이 콘솔 상품 등록, 후렴 시각, 러닝 8회 챕터 선택·거리 1500 m 체감은 사용자 확인.
- 커밋 대상: Meta/{Donation,StoryProgress}.cs·UI/{DonateUI,StoryReaderUI,PetShopUI}.cs·Economy/JellyWallet.cs(신규), Meta/{ArcadeRun,BossDirector,ChapterGrading,GameManager,IapBridge,PetShop,RecordTable,SaveData}.cs, Core/{StageManager,SceneFlowController}.cs, Economy/{CoinWallet,JellyPickup}.cs, Player/PlayerController.cs, Raising/{TamaRaisingUI,RaisingUI,RaisingUI.Popups,MissionMiniGames,MissionMiniGames.Games3D}.cs, UI/{CollectionUI.Music,CollectionUI.Mock,KpopChapterSelect,MainMenuController,PolicyUI.Texts}.cs, Editor/MiniGameDevMenu.cs, Resources/CoastRun/{UI_Donate_Cup,UI_Chapter_Bg,MG_Ddakji_Blue,MG_Ddakji_Red,Raise_Girl_Pose_*×10}.png(+meta), Tools/KlingGen/ref_girl.png, Docs/HANDOVER.

---

### 2026-09-12 — 51차: 2단 점프 FX · 더보기 자동 닫힘 · 투호 화살 낙하 · 구슬치기 규칙 · 하늘 낙석 · 버전 라벨 · K-POP 챕터 난이도(가속·보스 3종) · 더보기 「보스전」 · 약관 회사명 (컴파일 0, 캡처 r51_*, 커밋은 사용자)
- **① 2단 점프 FX**(`Visual/JuiceDirector.OnDoubleJump`): 충돌과 같던 흙먼지 대신 **공기를 아래로 쏘는 연출** — 발밑에서 퍼지는 하늘색 공기 링(`Fx_AirRing`, 0.6→3.2 m, 0.38 s 회전·페이드) + 발 아래 따라붙는 제트 기둥 빌보드(`Fx_JetBlast`, 0.42 s, 0.9→2.2 m) + 하늘색 퍼프 + 스피드라인 40 + FOV 킥 + Boost SFX. 텍스처 둘은 Kling 생성 후 컨테이너 cv2 로 키잉(휘도 키). 텍스처가 없으면 예전 퍼프로 폴백. 캡처는 타이밍상 못 잡음(코드 경로는 `SkaterRig → OnDoubleJump(butt, transform)` 확인).
- **② 더보기 자동 닫힘**: 컬렉션·설정(·보스전·약관)으로 나갔다 오면 더보기 열이 접혀 있음(`MainMenuController` 각 항목 진입 시 `if (_moreOpen) ToggleMore();`).
- **③ 투호 빗나감**(`MissionMiniGames.Games3D.cs` TuhoMission3D): 착지점 `endN.y` 0.06~0.42 로 고정(마당 바닥 영역), 화살은 **바닥에 누운 자세**(`lay.position = g - flatDir*len*0.5 + up*0.015`) + 길쭉한 블롭 그림자. 더 이상 공중에 떠 있지 않음.
- **④ 구슬치기 규칙**: 「3턴 안에 삼각형 안 구슬 **2개 이하**로 남기면 승리」 — `Need = 6`(8개 중 6개 밖으로), 상태 문구 「남은 구슬 N개(2개 이하면 승리)」, 소개 카드 목표 배지, `ChapterMission` rule 문구 동기화.
- **⑤ 하늘 낙석**(`Economy/SkyHazards.cs` 신규): `DropRock(z, lane, fall=1.15 s, r=0.62)` — **코인·젤리·장애물 없는 빈 레인**(`FindEmptyLane`, `RoadOccupancy`)에 먼저 **그림자 원판이 커지고**(0.25→0.7 α) 8 m 위에서 큰 바위(`Obs_BoulderBig`, Kling)가 `h = 8(1-u²)` 로 떨어져 **착지 시 흔들림·흙먼지·진동**(`JuiceDirector.PlayRockLand`) 후 **일반 장애물로 남는다**(ObstacleHazard + NearMissZone, 아웃라인). 스토리 러닝은 `RockRain` 컴포넌트(플레이어에 부착, 2챕터부터 22~40 s 간격, 보스 중·결승 직전 생략). K-POP 은 골렘 보스가 담당(RockRain 꺼짐). 캡처 `r51_rock_f.png`(착지한 바위 2 + 낙하 중 1).
- **⑥ 버전 라벨**: 「스튜디오 우히히시 v1.0」(`Loc` 영문 Studio Woohee-Heesi), 13pt, 위치 (-14, 2)로 살짝 아래.
- **⑦ K-POP 완주 화면**: 완주 시 갈색 빈 패널만 뜨던 것 → `RunHudChrome.ShowRunOver` kpopDone 분기에 얼굴 링(`UI_Face_Ring`+`UI_Face_Girl`) + 말풍선 「오늘도 찢었다! 오운완」 복구. 보스전 결과는 부제 「보스전 · 보스 N마리 퇴치!」(곡 크레딧까지 넣으면 한 줄 넘침 → 제거, BestFit 14~22).
- **⑧ K-POP 챕터 난이도 + 보스 3종**(`Meta/BossDirector.cs` 신규, `Core/GameSession.HandleStageStart` 에서 K-POP 이면 생성):
  - 난이도: 챕터 1~2 보스 없음 / 3~5 1회 / 6~9 2회 / 10+ 3회(곡 창의 30~85 % 구간에 균등), **가속 구간** 4챕터부터(×1.35, 6 s, 「가속 구간!」 배너, 18~26 s 후 첫 발동·이후 22~32 s 간격). 보스 종류는 챕터 가중 랜덤(낮으면 갈매기 60 %, 높으면 도깨비 45 %). 공격 간격은 챕터 3→15 에서 선형 감소.
  - **갈매기 해적**(`Obs_Boss_Seagull`): 앞 40 m 에서 미사일(`Obs_Missile`, 속도 v+13, 60 % 내 레인) — `SkyHazards.FireMissile`. **돌하르방 골렘**(`Obs_Boss_Golem`): 낙석 무한(`z+v·1.3+5`, 50 % 내 레인, 8챕터+ 40 % 2연발). **태풍 도깨비**(`Obs_Boss_Dokkaebi`): 예측 불가 2중 사인 횡이동 태풍(`Obs_Tornado`, 7 s, `SpawnTornado`). 보스 그림 3장·바위·미사일·태풍은 Kling 생성 + 키잉(도깨비 2번째 시안, 태풍 재생성).
  - 보스는 주인공 앞 14 m(40 m 에서 접근)·높이 2 m 에 떠다니며 가끔 레인 이동, 발밑 소프트 그림자. 등장 배너(이름만 — 8자 제한)+Horn+히트 임팩트, 16~22 s 뒤 위로 날아가며 퇴장 → 「보스 퇴치!」+RankS+`ArcadeRun.NoteBossCleared()`(점수 +400/마리).
  - **보스 중 최소화**: `ObstacleSpawner.SetSuppressed(true)` + `BossDirector.SpawnHold` 로 `CoinSpawner/JellySpawner` 패턴 정지(다음 스폰 z 를 앞으로 밀어 둠).
- **더보기 › 보스전**(`ArcadeRun.StartBossRush`): 챕터는 [내 K-POP 챕터-4, 챕터] 랜덤(최소 3), 곡은 K-POP 풀, 3 s 후부터 24 s 마다 보스 연속(가속 구간 없음). 개발 메뉴 `Coast Run/Dev/Boss - Rush`, `Sky - Drop rock|Missile|Tornado`. 캡처 `r51_boss*`, `r51_rock_d.png`(보스전 결과 「보스 4마리 퇴치」).
- **⑨ 약관 회사명**: `PolicyUI.Texts.Company = "스튜디오 우히히시"`, `CompanyEn = "Studio Woohee-Heesi"`. `Contact`·`YouthOfficer` 자리표시자는 그대로.
- **주의**: 히트 슬로모가 끝나면 `Time.timeScale` 을 1 로 되돌리므로 원격 `timescale` 검증은 **맞기 전에** 걸어야 함. 보스전은 챕터가 높으면(10+) 낙석·태풍이 촘촘해 실제 난이도 확인 필요 — 너무 어려우면 `BossDirector.AttackGap` 하한(골렘 0.9 s, 갈매기 1.0 s)을 올리면 됨.
- 커밋 대상: Economy/SkyHazards.cs(신규), Meta/BossDirector.cs(신규), Meta/ArcadeRun.cs, Meta/ChapterMission.cs, Core/GameSession.cs, Economy/CoinSpawner.cs, Economy/JellySpawner.cs, Visual/JuiceDirector.cs, UI/MainMenuController.cs, UI/RunHudChrome.cs, UI/PolicyUI.Texts.cs, Raising/MissionMiniGames.cs, Raising/MissionMiniGames.Games3D.cs, Editor/MiniGameDevMenu.cs, Resources/CoastRun/{Fx_AirRing,Fx_JetBlast,Obs_Boss_Seagull,Obs_Boss_Golem,Obs_Boss_Dokkaebi,Obs_BoulderBig,Obs_Missile,Obs_Tornado}.png(+meta), Docs/HANDOVER.

---

## 48차 세션 마무리 (2026-09-11 17:00 ~ 09-12 02:30, Cowork 원격 세션)

### A. 이번 세션 결과 한눈에
| 회차 | 내용 | 상태 |
|---|---|---|
| 48차 | 모드 리워크 GD 문서 `Docs/DESIGN_MODE_REWORK_v1.md` (K-POP 데일리 러닝 · 스토리 미션 강화) | 문서만 |
| 48차-3~7 | K-POP 「한 곡 달리기」 P0 — M2/M4/M7 랜덤, 90초 창, 일일 시드, 미션 3개(완주 고정+2), 스탬프/연속일, 후렴 피버×2, 코인 감쇠, HUD 칩, 결과 카드, 리본 통과 페이드아웃, 타이틀 M5, 곡명 라벨, 1초 지연 | 완료·플레이 확인 |
| 48차-8/9 | 첫 화면 버스 영상만 + 로딩 % 애니메이션 / 2단 점프 2배 높이 + Mixamo 앞돌기 클립 | 완료 |
| 48차-10~12 | 비·눈 강화 → 절반 → ON/OFF 50% 사이클, 2단 점프 「뿡→쓩」 연출 | 완료 |
| 48차-13 | 미니게임 3D 무대 `MiniStage3D` + 구슬치기 파일럿(Blender 구슬·Kling 마당) | 완료 |
| 48차-14 | 시안 4장 반영: 인도 돌포장 / 포토카드·뮤직 컬렉션 시안 배경 화면 / 구슬치기 시안 패널 | 완료·캡처 확인 |
| 48차-15 | BGM_M1~M7 만 남기고 34개 삭제, 러닝 BGM 은 M 곡 순환 | 완료 |

### B. 사용자가 할 것 (커밋·빌드는 사용자 담당)
1. **커밋** — 48차-3~15 변경분 전부 미커밋. 각 회차 절 끝의 「커밋 대상」 참고. 새 파일: `Raising/MiniStage3D.cs`, `UI/CollectionUI.Mock.cs`, `UI/CollectionUI.Music.cs`, `Editor/MiniGameDevMenu.cs`, `Editor/BgmCleanupMenu.cs`, `Editor/TelegramMenu.cs`(Send outbox), `Tools/Telegram/send_outbox.py`, `Tools/blender/minigame_kit.py`, `Resources/CoastRun/{MG_Marble,MG_Arrow}.fbx`, `UI_MG_Yard_Marbles.png`, `UI_Photocard_Mock.png`, `UI_Music_Mock.png`, `Rig/Anim_DoubleJump.fbx`, `Tools/Art/_mock/*`, `Docs/DESIGN_MODE_REWORK_v1.md`. 삭제: `Resources/CoastRun/BGM/` 34개(git 에서 `git add -A` 로 반영).
2. **삭제 권장**: `Resources/CoastRun/Rig/Anim_DoubleJumpBlender.fbx`(미사용 Blender 변형, 48차-9).
3. **새 APK 빌드** — 설치된 APK 는 9/6 빌드라 버스 영상 스플래시·K-POP·시안 화면이 모두 없다. version code 올려서 빌드.
4. **확인 요청 중인 것**: 구슬치기 3D/시안 스타일 승인 → 윷놀이·투호·딱지·무궁화(+육성 화면 윷/구슬/공기)도 같은 키트로 확장. 사운드팀에 M2/M4/M7 후렴 구간(`ArcadeRun.KpopTracks` chorusStart/End) 확인. 시안 배경의 한국어 버튼 글자(포토카드·뮤직 컬렉션) 영어판 필요 여부. GD 문서 §9 미결 질문.

### C. 다음 세션 시작 절차
1. `unity_status` → `unity_compile` 0 확인. 플레이 중 재컴파일하면 정적 상태(`ChapterMissionUI._canvas`, `MiniStage3D` 무대)가 남으니 **정지 → 재생**으로 확인할 것.
2. 텔레그램 보고: `Tools/Telegram/outbox.txt` 에 쓰고(커밋은 **매번 새 stagedPath 파일명**으로 — 같은 이름은 캐시로 안 바뀜) → `unity_cmd "menu Coast Run/Telegram/Send outbox"` → `unity_log` 에 `[Telegram] outbox exit=0 True …` 확인. 시작/50%/100%.
3. 원격 파일 IO: 9/8 윈도 업데이트 이후 `device_bash` 마운트가 깨져 있음 → `device_list_dir / device_stage_files / device_commit_files` 만 사용. 삭제는 에디터 메뉴(AssetDatabase.DeleteAsset)로.
4. 개발 메뉴(플레이 중, 원격 `unity_cmd "menu …"`): `Coast Run/Dev/Mission - Marbles|Yut|Tuho|Ddakji|Mugunghwa`(소개 카드 「시작!」= `tap 0.5 0.41`, 구슬치기 발사 버튼 `tap 0.8 0.11`), `Coast Run/Dev/Collection - Photocards|Records|Unlock all (F9)`(뮤직 전체재생 `tap 0.5 0.06`, 1행 `tap 0.72 0.735`). 러닝 중에 열면 뒤 런의 코인 팝업이 겹쳐 보이는 건 정상.
5. Blender/Kling/Mixamo: Blender 는 「MCP for Blender」 애드온 켠 채 열어 두면 `execute_blender_code` 로 바로(FBX 임포트는 `temp_override`, Action fcurve 는 `layers[].strips[].channelbags[]`). Kling 은 `kling_image`(PC 의 .env 키, `Tools/KlingGen/out/raw/`)가 되고 `image_reference` 는 kling-v1-5 모델에서만. Mixamo 는 내장 브라우저(민준 로그인, In Place/30fps/Without Skin, 다운로드 `C:\Users\ares2\Downloads`).
6. 무대 3D(MiniStage3D) 함정 요약: RT 알파(`OpaqueAlpha`) · FBX 임포트 회전(피벗으로 감쌈) · 씬 조명/안개(무대 렌더 중 OFF) · Particles/Unlit 소프트 원판은 α<0.55 가 안 보여 `SoftDisc`(URP Lit 투명) 사용.

### D. 알려진 남은 이슈
- 뮤직 컬렉션·포토카드 시안 배경은 9:16 고정 이미지라 20:9 기기에선 상하 여백이 배경색(보라/라벤더)으로 보인다. 필요하면 `Bg` 색을 시안 가장자리 색으로 맞추거나 시안을 상하 확장.
- `CoastBgmLibrary.CineOpen/CineClose/Memory` 는 파일이 없어 null 을 돌려주지만 호출처가 없다 — 컷씬 음악을 다시 넣으려면 M 곡으로 매핑하거나 파일 복구(git 이력에 있음).
- 포토카드 화면 3칸 슬롯 프레임이 시안 「???」 글자를 55px 정도 덜 덮어 y1 을 1800 으로 늘려 둔 상태 — 다른 해상도에서 어긋나면 `CollectionUI.Mock.cs` 슬롯 좌표 조정.
- 구슬치기 유리 겉면이 뿌옇게 느껴지면 `glassC` α(0.62) 만 낮추면 됨. 육성 화면 `HomeMiniGames` 는 아직 2D.
