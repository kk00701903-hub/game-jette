# 88~95차 세션 마무리 (2026-09-15 ~ 16, 원격 Cowork 세션) — HANDOVER_2026-09-07.md 뒤에 이어 붙일 절

## 한눈에 (다음 세션 시작 전에 읽을 것) — 103차 기준(2026-09-16)
- **지금 상태**: 컷씬 대본 **v7.1**(279컷 + 진엔딩 하얘짐 1컷)로 `CinematicTable` 재생성 완료, 신규 그림 **82장**(`컷씬이미지/97_v7신규/Cut_T_V7_*.jpg`) 적용, 기존 그림 107장 재사용. 시네마 화면은 주차 타임라인 시안(「메인스토리」 표기), 기부 컵 1cm 위. 엄마 앵커 3종 새로 만듦(`Tools/KlingGen/out/mom_r102/`). 컴파일 에러 0, 플레이 확인 캡쳐 있음. **커밋은 아직 안 함** — 102·103차 절의 커밋 대상 목록대로 사용자가 커밋.
- **먼저 할 것**: ① `git status` 확인 후 커밋 ② Unity 브릿지 47001 연결 `unity_status` ③ **포토샵 스크래치 디스크 비우기**(103차에 Firefly 문서 생성이 「스크래치 디스크 꽉 참」으로 실패 → 배경도 Kling 으로 만들었음) ④ Kling 웹 크레딧 약 31 남음(부족하면 충전) · Kling API 는 잔액 0(code 1102).
- **남은 일(우선순위)**: 1) 컷씬 전체 재생 검수 — 자막 64자가 현재 폰트 폭에선 3줄이 되는 컷 있음(예: CS1-3), 글자 크기/줄 기준 사용자 결정 대기. `V7_N7_16`(해녀복 하늘) 표정이 살짝 웃는 듯 → 필요 시 재생성 2) 첫눈(EV6)을 CH10(25주차, 여름 끝)으로 옮겨 계절과 어긋남 — 장소 이름도 곶자왈로 바뀜, 사용자 확인 3) 컷 길이 6.5/6/7초로 줄인 것 확인 4) 상점 재료 아이콘 25종·스케줄 카드·알바 소품 아이콘(95차 이월) 5) v7 영어판 자막(`ChapterScript.En` 은 아직 v6 영어).
- **규칙(사용자, 강하게)**: 요청한 부분 외에는 임의로 바꾸지 말 것, 바꿔야 하면 먼저 허락. 대본 문장·엔딩 구성은 사용자 것 그대로. 문체는 `Docs/CUTSCENE_STYLE_GUIDE.md` v2.
- **컷씬 파이프라인(v7)**: 자막 원본 = `Docs/CUTSCENE_SCRIPTS_v7_읽기용.md`(사용자 확정본, 손대지 않음) → `Tools/Story/build_v7_md.py 읽기용 v7_map.txt plan103.json → Docs/CUTSCENE_SCRIPTS_v7.md`(컷→그림 ID·앵커·폴백, BGM 은 `v7_map.txt` 2열, EV 는 `CH4+BGM_M1` 식) → `python Tools/Story/gen_cinematic_v7.py --txt` → `CinematicTable.cs` + `script/*.txt` → `python Tools/Story/cutscene_txt.py import Tools/Story/script` → `ChapterScript.Data/En.cs`. 챕터 배치는 `StoryProgress.CutsceneChapters/EventChapters` 와 `gen_cinematic_v7.CS_CH/EV_CH` 두 곳(103차: CS5 CH11, EV6 CH10). 그림 교체는 `Tools/KlingGen/dl103.py --apply`(`out/web103/urls103.json·picks103.json`, 다시 받을 ID 는 `force103.json`).
- **도구 요약**: Kling 웹 = 내장 브라우저 `seed` 탭, `eval(localStorage.__helpers84); eval(localStorage.__helpers95)` 뒤 103차 헬퍼는 `localStorage.__plan103`(계획)·`__out103`(결과 URL)·`__pick103`(선택)에 있고 함수(`__run103/__batch103/__sheet103`)는 103차 절의 setup 스크립트(`Tools/KlingGen/plan103.json` 의 P/S 와 같은 규칙)로 다시 정의. 앵커 URL: `__urls87`(H12·D12·H19·D20·KID·DAD), `__m_MOM40N`(40세 전신)·`__m_MOM40P`(40세 얼굴)·`__m_MOM48P`(48세)·`__m_MOMFB`(회상 뒷모습). 생성 설정(1K SD·9:16·2장)은 리로드마다 초기화. PC 스크립트 = `Tools/_clip/run.bat` 커밋(ASCII 만) 후 `Coast Run/Dev/Run - Tools/_clip/run.bat`, 로그 `Tools/_clip/run.log`. 텔레그램 = `Tools/Telegram/outbox.txt` → `Coast Run/Telegram/Send outbox`. 캡쳐 = `unity_shot`(같은 이름 재사용 금지, 20~30초 뒤 stage) 또는 `Library/CoastRun_CanvasCapture.txt` + play. `device_bash` 불가, stage/commit 만.

## 88차 — Firefly 앵커 시험 (포토샵 MCP)
- 포토샵 MCP `photoshop_generate_image`/`photoshop_generative_fill` 은 vbs 오류로 실패 → `photoshop_execute_script`(JSX) 에서 `syntheticFill` 액션(serviceID clio, workflow text_to_image)을 직접 호출하면 Firefly text-to-image 가 정상 동작. 새 문서 768×1360 + selectAll 후 실행, `photoshop_export_as` 로 PNG 저장.
- 산출: `Tools/KlingGen/ref87/ff_MOM_YOUNG.png`(20대 해녀), `ff_MOM_D12_a/b.png`(엄마×어린 도윤, a 후보), `ff_BOAT.png`(하트 부표 배), `ff_TOWER.png`(송전탑·억새·돌 3개). 비교 `Claude outputs/r88_ff_anchors.jpg`.
- 소감: 배·송전탑은 쓸 만함, 인물은 텍스트 프롬프트만으로는 ref87 얼굴과 안 맞음(참조 이미지 경로가 JSX 에서 안 열림). 인물 앵커는 Kling 웹 유지.

## 89차 — 엄마 앵커 재정의
- 사용자 지시: 엄마 = **40세(하늘 12살 때, 해녀복)** / **48세(현재, 가난한 아줌마 평상복)**. 기존 바이블(42세)과 다름 → 이후 컷은 이 두 나이대로.
- 산출: `ref87/ff_MOM40.png`(v2, 웹툰풍·주름 없음 — 1차 사실풍은 사용자가 거부), `ref87/ff_MOM48.png`(꽃무늬 블라우스+앞치마+몸빼+슬리퍼). 비교 `Claude outputs/r89_mom_anchors_v2.jpg`. 기존 `MOM.png` 는 아직 교체 안 함(사용자 결정 대기).

## 90차 — 컷씬 형상 수정 (README_수정요청.md, HIGH 8 + MID 7)
- 절차: Kling 웹(`seed` 탭, IMAGE 3.0 Omni, 1K SD·9:16·2장). **ref87 앵커는 GitHub 미커밋(404)** 이라 `out/web87/urls.json` 의 87차 Kling CDN 원본 URL(pick: H12_2 D12_2 H19_2 D20_1 KID_2 DAD2_1 MOM_2)을 `localStorage.__urls87` 에 두고 `fetch→File→input.el-upload__input` 으로 **순차 업로드**(동시 업로드 시 Image1/2 순서 뒤바뀜). 헬퍼: `eval(localStorage.__helpers84)` + `__anchorSeq/__go/__waitNew/__show2`(이 세션에서 정의, 저장 안 됨 — `plan90.json`/`plan90mid.json` 참고).
- 결과 수집: `.virtual-history-stream` 은 가상 리스트라 `scrollTop=0` 후 `__groups()[0]` 이 최신. 다운로드·교체는 PC 파이썬 `Tools/KlingGen/dl90.py`, `dl90mid.py`(RunBat 경유), 옛 파일 `out/shape_mismatch/final/_old/`, 후보 원본 `out/web90/`.
- 교체 15컷(720×1280, 같은 파일명): HIGH OP_02/03/04/07, N2_07, N5_05, N6_05, E6_03 · MID N2_05, N3_06, OP_12, N8_10, N8_14, N4_11, E10_04. 시트 `Claude outputs/r90_high_new.jpg`, `r90_mid_new.jpg`.
- 남은 이슈: OP_04 엄마 후드 쓴 상태 / E6_03 게시판 작은 글자 / N5_05 엄마 얼굴 작음 / N8_10 머리띠가 팔찌처럼 김 / OP_12·N5_05 는 "엄마 누움" 프롬프트가 인물 중복을 자주 냄(뒤에서 보는 구도로 해결). Kling 웹 크레딧 466 → 424.
- 커밋 대상: `Assets/Resources/CoastRun/컷씬이미지/**`(15컷), `Tools/KlingGen/{dl90.py,dl90mid.py,plan90.json,plan90mid.json,out/web90/*.json}`, `Tools/KlingGen/ref87/ff_*.png`, `Claude outputs/r88~r90*.jpg`, 이 문서.

## 94차 — 스토리모드(육성) 8페이지 시안 정합 (2026-09-15, 원격 Cowork 세션)
- 사용자가 준 시안 8장(놀이·일주일이 지났다·텃밭·방·하늘이 쓰러졌다·상점·캐릭터 정보·어디 알바?)을 플레이 캡쳐와 나란히 비교(`Claude outputs/r94_pages_compare.jpg`, 원본 `Tools/_shots/r94*_*.png`). 캐릭터 정보·놀이·텃밭·결산은 이미 시안대로였고, 방·상점·쓰러짐·알바 선택이 달랐음.
- **HomeUI**(`Raising/HomeUI.cs`): 어두운 딤 → 크림·라벤더 바탕 + 꽃 프레임(`SprinklePlayDecor`, 이제 public) / 보라 헤더(집 아이콘 「우리 집」 · 코인 아이콘 + 숫자 · 나가기) / 비선택 탭 라벤더 회색(`TabOff`) / **방**: 「★ 방 완성도 N/14」 게이지(`RoomScore` = 보유 펫 + 보유 장식 + 먹을 것) + 오른쪽 세로 버튼 3개(침대 모양 = 잠자는 포즈 토글 · 방 장식 꾸미기 = 보유 장식 안내 · 간식 바꾸기 = 가방) + 꼬마 숨김(`ShowRoomKid=false`) + 트레이 안내 문구 삭제·「조리!!」 노랑 버튼 / **텃밭**: 배너 크림 알약+꽃, 씨앗 카드 이모지(폰트 없음) → 미니 작물 그림(`SeedIcon`).
- **ShopUI**: 라벤더 파스텔 바탕+꽃 / 카드 안 보라 헤더(「상점」·코인 돈·♥하트·나가기) / 탭·지갑(보라 알약)·칩·안내 문구 `HeaderShift`(70) 만큼 하강, 안내 문구 크기 축소(겹침 해결) / 줄마다 노랑 장바구니(고르고 1개 즉시 구매) / 수량 바: 라벨 위·−1+ 아래·구매 오른쪽 위·닫기 오른쪽 아래 + 하단 안내 띠. 카드 1150(펫 1060).
- **GameOverUI**(`UI/LifeUI.cs`): 카드 620×980, 잠든 하늘 340×300, 제목 50, 버튼 520×104 두 줄(위 큰 글자/아래 괄호 설명 영역 분리 — 전엔 아래 줄이 안 보였음). **WeekPassUI**: 카드 높이 재계산(아래 빈 공간 제거), 리본 480·제목 30.
- **알바 선택**(`TamaRaisingUI.CardPickRoutine`): 딤 → 라벤더·분홍 파스텔 + 반짝이 10개, 배너 y150, 카드 300×300.
- Dev 메뉴 추가: `Life - Week pass (hold 60s)`(자동 닫힘 2초 → 60초로 캡쳐용), `UI - Home (Room)`, `UI - Shop`.
- 남은 차이(에셋 필요): 상점 재료 아이콘이 채소·고기·달걀·우유·생선 모두 같은 그릇 그림(`UI_Goods_Side`) → 품목별 아이콘 생성 필요 / 쓰러짐 화면의 누운 하늘 그림(`Raise_Girl_Pose_Sleep` 은 서서 자는 포즈) / 알바 카드 안 소품 아이콘. 시안 하단 「비교하려면 길게 누르세요」 토스트는 시안 캡쳐 도구의 것이라 게임엔 없음.
- 커밋 대상: `Assets/_CoastRun/Scripts/Raising/{HomeUI,TamaRaisingUI}.cs`, `Scripts/UI/{ShopUI,LifeUI}.cs`, `Editor/MiniGameDevMenu.cs`, `Claude outputs/r94_pages_compare.jpg`, 이 절.
- 참고: Unity 실행 시 「Unity Package Manager Error(local server process)」 대화상자가 세 번 반복됐고, 사용자가 프로세스 정리 후 Retry 로 열림.

## 95차 — 대본 v6.4 컷씬 재생성 + 육성 주인공 그림 세트 (2026-09-16, 원격 Cowork 세션)
- **대본**: `Docs/CUTSCENE_SCRIPTS_v6_읽기용.md` v6.4(188컷 — 오프닝 4컷·CS1 기억상실·CS8 엄마 얼굴/두 번 통과·「빌린 1년」 간접 암시) → `Tools/Story/gen_cinematic_v6.py --txt` 로 `CinematicTable.cs`(194컷) + 리더 txt, 이어서 `cutscene_txt.py import` 로 ChapterScript.Data/En 재생성. 그림 ID 는 폴더 번호와 무관한 리소스 이름(`Cut_T_V6_*` 신규 4장 포함, 매핑 `Docs/CUTSCENE_SCRIPTS_v6.md`).
- **컷씬 그림 51컷(Kling 웹, IMAGE 3.0 Omni, 1K SD·9:16·2장)**: 신규 4(V6_N2_11 뒤집힌 배, V6_N6_10 엄마 수화, V6_N8_06 엄마 고개 듦, V6_N8_07 물속 엄마 얼굴) + 대본 변경 21 + 검수 1·2순위 26. 엄마 앵커는 ANCHOR_MOM48B(웃지 않는 48세, Kling 생성)로 통일. 후보 `Tools/KlingGen/out/web95/`(urls95.json·picks95.json), 교체 `dl95.py --apply`(옛 파일 `out/shape_mismatch/final/_old95/`), 시트 `Claude outputs/r95_cuts_new.jpg`. 계획 `plan95.json`(+브라우저에서 고친 장면: N1_06 글자 없는 전단, N7_05 어깨너머 사진 카드, N3_02 빈 도시락통(검열 회피), N6_10/N8_07 단일 장면).
- **육성 주인공 그림 40장(Kling)**: 스타일 앵커 = 기존 `Raise_Girl_Normal.png`(수채 치비) + 얼굴 H19 → 검은 머리·노란 핀으로 통일. `Raise_Girl_S{1..4}_{Normal|Happy|Tired}`(봄 노란 티 → 여름 포니테일 → 가을 후드·라디오 → 겨울 코트) + `Raise_Girl_Pose_{SleepLying,Cook,Run,Raincoat,Birthday,Garden,Marbles,HugKid,NightWalk,Sick,Radio,Letter,Milk,Wave,Think,Shy,Surprised,Tower,Snow,BusStop}` + 스케줄 카드 `Sched_job_{sushi,patrol,store,market,library,radio,field,nets}`(4:3 1024×768). 마젠타 크로마키 → RGBA 는 `dl95r.py --apply`(bbox 는 침식 마스크로 — 처음엔 캔버스 전체가 잡혀 누운 그림이 작게 나왔음). 시트 `Claude outputs/r95_raising_new.jpg`, 플레이 확인 `r95_raising_play.jpg`.
  - 게임 알바 id 에 맞춰 복사: sushi→`Sched_job_sashimi`, radio→`Sched_job_dj_assist`, patrol→`Sched_job_tower_fix`, nets→`Sched_job_lighthouse`. `job_hall/job_salon/job_dangsan/job_night_delivery` 카드는 아직 없음(store/library/field 는 여분).
- **코드**: `TamaRaisingUI.RefreshGirl` — 주차 성장 단계(1~13 S1, 14~26 S2, 27~39 S3, 40~ S4) 그림 우선, 옛 52차 포즈 이름(Laugh/Cry/Sleep/Cafe/Delivery/Haenyeo/Dance/Skate/Eat/Angry)은 새 세트로 치환(그림체 섞임 방지), 놀이는 Cook/Marbles/Run 포즈. `LifeUI` 쓰러짐 카드는 `Raise_Girl_Pose_SleepLying`(가로 그림이면 560×260). `Editor/RunBatMenu.cs`: run.log 가 잠겨 있으면 run2/run3.log 로.
- **막힌 것·주의**: (1) Unity 원격 브릿지(47001)가 에디터 재시작 뒤 연결되지 않아 메뉴를 화면 조작으로 실행함 — 다음 세션에 `CoastRemote` 리슨 확인. (2) RunBat 가 안 돌던 원인은 옛 `kg_server.py`(최소화된 파이썬 창)가 `Tools/_clip/run.log` 를 잡고 있어서 리다이렉트 실패 → 그 파이썬 창은 닫아도 됨. (3) 메뉴 오클릭으로 `Build Jeju Houses 3D (Blender)` 가 한 번 실행됨(exit=0) — 변경된 에셋 있으면 되돌릴 것. (4) 육성 그림 일부는 2K HD·Auto 비율로 생성됨(리로드로 설정이 초기화) — 결과엔 지장 없고 크레딧만 더 씀(Kling 428 → 약 230). (5) Pose_Run/Milk 발밑 보라 그림자 잔상, Pose_Tower 위 작은 점 — 필요하면 재컷아웃.
- **커밋 대상**: `Assets/Resources/CoastRun/컷씬이미지/**`(51컷), `Assets/Resources/CoastRun/Raise_Girl_S*_*.png, Raise_Girl_Pose_*.png(20), Sched_job_*.png`, `Assets/_CoastRun/Scripts/Story/{CinematicTable,ChapterScript.Data,ChapterScript.En}.cs`, `Scripts/Raising/TamaRaisingUI.cs`, `Scripts/UI/LifeUI.cs`, `Editor/RunBatMenu.cs`, `Tools/Story/{gen_cinematic_v6.py,script/*.txt}`, `Tools/KlingGen/{dl95.py,dl95r.py,plan95.json,plan95r.json,cut_paths95.json,out/web95/*.json,out/web95r/*.json}`, `Docs/CUTSCENE_SCRIPTS_v6*.md`, `Claude outputs/r95_*.jpg`, 이 절.

## 96차 — MCP·인수인계 확인 + Unity/Blender/Photoshop 연결 점검 (2026-09-16, PC 링크 Cowork 세션)
- **연결 상태**: Unity 브릿지(47001) OK(`ping` → 6000.5.10f1, 01_Title, 컴파일 에러 0) · Blender 5.2.1 LTS MCP OK(애드온 프로토콜 5 / 서버 기대 7 → `uvx blender-mcp install-addon` 권장, 동작엔 지장 없음) · Photoshop 2026 MCP OK(`photoshop_ping`). Claude 앱 로컬 MCP 4개(unity·blender·photoshop·kling) 모두 announced. `device_bash` 는 여전히 불가 → stage/commit + RunBat.
- **브릿지 안 붙던 원인(95차 막힘 (1))**: `netstat` 에 47001 LISTENING 이 셋 — ① 어제(9/15 23:28) Unity 에서 RunBat 로 띄운 `kg_server.py`(python) 가 옛 에디터의 리슨 소켓 핸들을 상속해 계속 물고 있었고(에디터는 종료됐는데 소켓만 살아 CLOSE_WAIT → timed out), ② `AssetImportWorker`(-adb2 -batchMode) 도 Editor 어셈블리를 로드해 `CoastRemote` 정적 생성자가 돌아 ReuseAddress 로 같은 포트를 바인드. → kg_server.py 종료, `CoastRemote.cs` 에 `Application.isBatchMode || AssetDatabase.IsAssetImportWorkerProcess()` 면 리슨 안 하도록 가드 추가. 재발 시 `netstat -ano | findstr :47001` 로 LISTENING PID 가 에디터 하나뿐인지 확인.
- **RunBat 주의**: bat 에 한글(멀티바이트)을 넣으면 `chcp 65001` 뒤 cmd 가 줄을 잘못 읽어 깨진다 → bat 은 ASCII 만. `device_commit_files` 는 같은 **파일명**(경로가 달라도) 을 다시 커밋하면 옛 내용이 써짐 → `CoastRemote_r96.cs` 처럼 매번 새 이름으로.
- **git**: `3b00601 캔버스 전략 재정의: 제작 1080×2400(20:9) · 세이프존 1080×1920(16:9)` 까지 커밋됨, 추적 파일 변경 없음(미추적 17개: `Tools/_clip/`, `Tools/_shots/`, `Tools/Telegram/outbox*` 등). `Art3D` 변경 없음(95차 오클릭 영향 없음). 이 문서 §1 의 「1080×1920」 캡쳐 기준은 최신 커밋(20:9 제작·16:9 세이프존)으로 바뀐 상태 — 게임뷰도 Phone 20:9(1080×2400).
- **기타**: Claude 앱이 `unity_mcp_server.py`·`kling_mcp_server.py`·blender-mcp 를 각각 2개씩 띄우고 있음(9:22 시작, 세션 중복). 해는 없으나 정리하려면 앱 재시작.
- **커밋 대상**: `Assets/_CoastRun/Editor/CoastRemote.cs`, 이 절.

## 97차 — 캔버스 전략 점검 (2026-09-16, PC 링크 Cowork 세션)
- 사용자가 캔버스 전략(제작 1080×2400(20:9) · S25 1080×2340 상하 30px 허용 · 필수 UI 세이프존 1080×1920 중앙)을 다시 지시 → 확인 결과 **커밋 `3b00601` 에 이미 구현·검증돼 있었다**(`CoastUiCanvas` 의 ScalerMatch 0·BgRoot/FullBleedBackground/SafeZoneBox·`SafeZoneRectToBgAnchors`, `MainMenuController.BuildGateUi`, 점검기 `Editor/CanvasCaptureRunner.cs`). 코드 수정 없음.
- 재검증: `Tools/_shots/canvas_report.txt`(16:41) — 1080×2340/2400/1920/1440×3120 모두 StoryBtn·MoreBtn·KpopBtn·ChapterChip 세이프존 안, S25 상하 잘림 30px. 브릿지로 S25 게임뷰에서 육성 메인(05_Raising)·`UI - Home (Room)`·`UI - Shop` 캡쳐 → 잘림·늘어짐 없음. 시트 `Claude outputs/r97_canvas_check.jpg`, 원본 `Tools/_shots/r97_*.png`.
- 참고: `Coast Run/Dev/UI - *` 메뉴는 05_Raising 씬 플레이 중에만 UI 가 뜬다(타이틀 씬에선 캡쳐가 그대로 타이틀). 씬 전환은 `unity_cmd scene 05_Raising` → `play`.
- 남은 선택지(사용자 결정): 타이틀 키아트만 1080×2400 제작본이고 다른 화면 배경(컷씬 스틸 720×1280, UI 시안 720×1280)은 세이프존 크기 그대로 — 20:9 폰에서 위아래에 배경이 아닌 여백/카드 바탕이 보인다. 화면별로 20:9 제작본을 새로 뽑을지는 별도 작업.
- 커밋 대상: `Claude outputs/r97_canvas_check.jpg`, 이 절.

## 98차 — 타이틀 배경 원래대로 (2026-09-16, PC 링크 Cowork 세션)
- 사용자: 「메인페이지는 위아래 이상한데 원래대로 돌려줘」 — 79차에 1080×2400 으로 늘린 `UI_Title_Mock`(위 밋밋한 하늘 띠·아래 뒤집힌 길)이 어색함.
- 조치: `Assets/Resources/CoastRun/UI_Title_Mock.png` 을 `Tools/_bg20_backup/UI_Title_Mock_1080x1920.png`(원본)으로 되돌림. 늘림본은 `Tools/_bg20_backup/UI_Title_Mock_1080x2400.png` 에 보관. `MainMenuController` 에 `GateArt20x9 = false` 플래그 — false 면 67차 방식(원본을 인셋+HudPad = 안전영역에 꽉 채움, 버튼 히트·CHAPTER 칩은 720×1280 / 1080×1920 비율 앵커), true 면 79차 풀블리드(그때 그림도 2400 본으로 바꿔야 함). `CoastUiCanvas` 의 전략 코드(ScalerMatch 0·SafeZoneBox 등)는 그대로.
- 검증: `CanvasCaptureRunner` 로 20:9/19.5:9/16:9 캡쳐(`Tools/_shots/r98_c_*.png`, 시트 `Claude outputs/r98_title_restore.jpg`) — 세 비율 모두 그림이 화면을 채우고 버튼·칩이 그림과 맞음. 20:9 에선 세로로 25% 늘어남(원래 방식과 동일). 점검기 기준으론 Play 히트가 중앙 16:9 밖(화면 아래 63px)이지만 화면·안전영역 안이라 동작엔 문제 없음.
- 주의(도구): 같은 파일명을 다시 `device_stage_files` 하면 **옛 내용이 올라온다**(canvas_report.txt 가 16:41 판으로 나옴) → RunBat 로 `copy` 해 새 이름으로 stage. `unity_cmd res` 로 게임뷰를 바꾼 직후 `unity_shot` 은 레이아웃이 옛 크기로 남은 채 찍힐 수 있음 → 해상도별 캡쳐는 `Library/CoastRun_CanvasCapture.txt`(씬·해상도 목록) 커밋 후 해당 씬에서 `play` 가 확실.
- 커밋 대상: `Assets/_CoastRun/Scripts/UI/MainMenuController.cs`, `Assets/Resources/CoastRun/UI_Title_Mock.png`, `Tools/_bg20_backup/UI_Title_Mock_1080x2400.png`, `Claude outputs/r98_title_restore.jpg`, 이 절.

## 99차 — 20:9 기준 화면 선명도 통합 점검 (2026-09-16, PC 링크 Cowork 세션)
- 사용자: 「앞으로는 20:9 기준. 20:9 로 바꾸면서 전체적으로 또렷하지 않고 글자가 아른거린다 — 통합 체크」.
- **원인 1(주범, 모든 화면)**: `CoastUiCanvas.Create` 가 CanvasScaler 기준을 1080×1920 으로 두고 `HudInset` 에 localScale 1.5(DesignScale) 를 걸어 720×1280 좌표계를 맞추고 있었다. uGUI `Text` 는 캔버스 scaleFactor 만 보고 글자를 굽고 RectTransform 스케일은 모르므로, 1080 폭 폰(scaleFactor 1.0)에서 글자를 1.0 배로 구워 1.5배 늘려 그렸다 → 흐릿 + 움직일 때 텍셀 보간으로 아른거림. 아이콘·칩 그림도 같이 1.5배 확대. (79차 전 match 0.5 땐 S25 에서 1.1배로 구워 아주 조금 덜했을 뿐 같은 문제.)
  - **수정**: 기준 해상도를 배치 단위 그대로 **720×1280**(`RefWidth/RefHeight`)으로, 인셋·`CoastUiCanvasBox` 스케일 1(fit 만). 캔버스 단위 = 디자인 단위라 배치 코드는 무변경, scaleFactor 가 1.5(1080 폭)/2.0(1440 폭)이 되어 글자가 실제 픽셀로 구워진다. `DesignMetrics`/`SafeZoneMetrics` 도 같은 기준으로. `DesignScale` 상수는 시안 픽셀 환산(`MockPixelRectToBgAnchors`)에만 남음. 검증: 점검기 좌표(StoryBtn 등)가 98차와 1px 도 안 바뀜 = 레이아웃 동일, 글자만 선명(`Claude outputs/r99_text_crops.png`).
- **원인 2(육성 3D 배경·미니게임)**: `MiniStage3D.EnsureRt` 가 RT 를 1200×1600 으로 클램프 → 1080×2340 화면에 세로 1.46배 늘려 그림. 상한 1440×2560 으로.
- **남은 소프트 요소(그림 원본 문제, 코드 아님)**: 컷씬/오프닝/스플래시 비디오 RT 720×1280(`CinematicPlayer`·`OpeningCinematic`·`MainMenuController._splashRt`) — 영상 원본이 720p 라 1080 폰에서 1.5배. 컷씬 스틸 720×1280 처리본도 1080 폭에선 1.5배 확대(원본 1080×1920 이 있으니 Resources 를 1080 본으로 바꾸면 해결, 용량 ↑). 타이틀 시안에 구워진 글자(「스토리 모드」「더보기」)는 그림이라 규칙 2 대로 코드 글자로 옮겨야 선명.
- **20:9 기준 정리**: 제작 1080×2400, 게임뷰 기본 Phone 20:9, 세이프존 1080×1920. 타이틀 그림만 98차 요청으로 1080×1920 원본 유지(`GateArt20x9=false`).
- 참고: 에디터 플레이 중 육성 턴이 진행돼(42→43주차, 489→529G) 테스트 세이브가 바뀌었을 수 있음.
- **커밋 대상**: `Assets/_CoastRun/Scripts/UI/CoastUiCanvas.cs`, `Assets/_CoastRun/Scripts/Raising/MiniStage3D.cs`, `Claude outputs/r99_sharpness_full.jpg`, `Claude outputs/r99_text_crops.png`, 이 절.

## 100차 — K-POP 러닝 뮤직·미션 표시 키우기 (2026-09-16, PC 링크 Cowork 세션)
- 사용자: 「kpop 러닝에 뮤직하고 미션은 살짝 키워야 할 것 같다. 잘 안 보임」.
- `RunHudChrome.BuildKpopChips`: 미션 칩 230×32 / 14pt / 간격 36 → **290×42 / 18pt / 간격 48**(둥글기 12→15, 외곽선 1.2→1.4). 시작 y −134 그대로 — 체력 바·시간 알약 아래, 3개 합쳐 −272 까지(하늘 영역).
- `KpopNowPlaying.Build`: 알약 262×60 → **330×76**(둥글기 18→22), 이퀄라이저 상자 44→56(막대 5.5px·8~40), 「NOW PLAYING ♫」 10→13pt, 곡명 13→17pt, 우하단 오프셋 (−6,10)→(−8,12). 컷씬 좌상단 음악 표시(79차)도 같은 빌더라 같이 커짐.
- 검증: 컴파일 에러 0, S25 캡쳐 전후 `Claude outputs/r100_kpop_hud_crops.png`(전체 `r100_kpop_hud_full.jpg`). 다른 HUD 와 겹침 없음.
- 커밋 대상: `Assets/_CoastRun/Scripts/UI/RunHudChrome.cs`, `Assets/_CoastRun/Scripts/UI/KpopNowPlaying.cs`, `Claude outputs/r100_kpop_hud_*.{png,jpg}`, 이 절.

## 101차 — 컷씬 대본 v7.0 재구성 (2026-09-16, PC 링크 Cowork 세션) — **사용자 확인 대기**
- 사용자: 회상이 섞여 이해 어렵고 철학적 어투 → 순서·내용을 심플하고 감동적으로, **지정한 스토리라인을 한 줄도 바꾸지 말고** 다시 쓰기. 스토리라인(요지): 오프닝 괴롭힘 기억 + 「스무 살 생일 때 보자」 → 기억 상실 → 꼬마와 추억 → 엄마·남주 눈엔 다른 사람으로 보임(귀신 떡밥) → 사람들 무시 → 엄마가 딸 찾는 중이라며 밥 해 줌·꼬마 눈물 → 여주는 얼굴 기억 못 해 지나침 → 머리띠로 기억 폭발 → 회상 한 덩어리(머리띠→아빠 배→아빠 죽음→벙어리 엄마→괴롭힘→서울 남주가 구함→아지트→케이크 싸움→그리움→태풍·엄마 심장병·외딴 언덕·울부짖음·도움 없음→남주가 옴→리어카 병원→엄마 살림→남주 쓰러짐→다음 날 서울행→차에서 약속) → 반전(꼬마=아빠, 남자=매일 마주치던 사람, 엄마=밥 해 준 사람) → 육신 사라짐·엄마 못 안음·꼬마 전생 기억 상실 → 진엔딩은 남주가 알아보는 듯 아리송하게.
- 산출: `Docs/CUTSCENE_SCRIPTS_v7_읽기용.md`(163컷, 자막만). 맨 위에 **스토리라인 ↔ 컷 대응표**. 슬롯·재생 순서는 v6 과 동일(OPEN·CS1~8·EV1~10·END_A/B/TRUE) — 회상은 CS5~EV2(10~15챕터)에만. v6 문서는 그대로 둠.
- 내가 정한 것(사용자 확인 필요): ① 하늘의 사인은 CS7 9 한 컷(열아홉 생일, 부표 찾으러 들어간 바다). ② END_A/END_B 는 짧게만. ③ 엄마가 정해 준 「생일」= 진짜 생일(EV10 1) 떡밥 추가.
- **다음 단계(확인 후)**: 그림 지시 붙인 `CUTSCENE_SCRIPTS_v7.md` 작성 → `Tools/Story/gen_cinematic_v7.py`(v6 복제) → `CinematicTable` 재생성 → 새 컷 그림(회상 덩어리·잔치·텃밭 등 v6 에 없는 장면) Kling 생성. v6 컷 그림 상당수는 재사용 가능.
- 커밋 대상: `Docs/CUTSCENE_SCRIPTS_v7_읽기용.md`, 이 절.

## 101차 후속 — 대본 v7.1 확정 + 문체 가이드 v2 (2026-09-16, PC 링크 Cowork 세션)
- 사용자와 한 줄씩 다듬어 **v7.1(279컷, 한 컷 64자 이하)** 확정: 깨는 곳 = 송전탑 아래 담요(태풍 경보), 회상 엄마 얼굴 안 보임, CS6 싸움은 바닷가 불턱, 엔딩 A = 무덤·국화·우유 두 병 + 「그리고……」, END_B = 미역국·「엄마」, TRUE = A 1~6 + 버스·「도윤아?」·흰 빛. 개연성 점검은 사용자가 고른 항목만 반영.
- **규칙(사용자, 강하게)**: 요청한 부분 외에는 임의로 바꾸지 말 것. 바꿔야 하면 먼저 허락. → `Docs/CUTSCENE_STYLE_GUIDE.md` v2 §0 에 기록.
- 커밋 대상: `Docs/CUTSCENE_SCRIPTS_v7_읽기용.md`, `Docs/CUTSCENE_STYLE_GUIDE.md`.

## 102차 — 엄마 앵커 이미지 새로 (2026-09-16, PC 링크 Cowork 세션)
- 사용자: 회상 엄마(얼굴 안 보임) / 40세(얼굴 보임) / 48세(같은 얼굴, 야윔) 세 종.
- **Kling API 는 잔액 부족(code 1102 「Account balance not enough」)** — `kling_client._request` 가 429 를 재시도만 하고 마지막 오류를 안 남겨 「failed after retries: None」 으로 보였음(로그 `Tools/KlingGen/out/log/*.json` 에 원문 있음). kling-v1/v1-5/v2/v2-new 는 단종(1203). → Kling **웹**(`seed` 탭)으로 생성.
- 산출: `Tools/KlingGen/out/mom_r102/ANCHOR_MOM_MEMORY.png`(해질녘 뒷모습) · `ANCHOR_MOM40.png`(상반신 얼굴) · `ANCHOR_MOM48.png`(같은 얼굴·야윔) · `ANCHOR_MOM40_FULL.png`(전신) + 후보 10장, 시트 `Claude outputs/r102_mom_anchors.jpg`. 브라우저 `localStorage.__m_MOM40N/__m_MOM40P/__m_MOM48P/__m_MOMFB` 에 URL. 다운로드 `Tools/KlingGen/dl102.py`(RunBat).
- 커밋 대상: `Tools/KlingGen/dl102.py`, `Tools/KlingGen/out/mom_r102/**`, `Claude outputs/r102_mom_anchors.jpg`.

## 103차 — v7 컷씬 재구성 + 시네마 화면 시안 + 기부 버튼 (2026-09-16, PC 링크 Cowork 세션)
- 사용자: ① 새 원고로 컷씬 다시(부족한 그림은 앵커 비교해 Kling 인물·Firefly 배경) ② 기억이 돌아올 때 회상 엄마 얼굴 보이게 ③ 메인 기부해줘 0.5cm 위 ④ 첨부 시안대로 스토리 화면, 「컷씬」→「메인스토리」.
- **① 파이프라인**: `Docs/CUTSCENE_SCRIPTS_v7.md`(생성용 표) = `Tools/Story/build_v7_md.py`(읽기용 md + `Tools/Story/v7_map.txt`(컷→그림 ID, 신규는 `V7_xxx/폴백ID`) + `Tools/KlingGen/plan103.json`(프롬프트)) → `Tools/Story/gen_cinematic_v7.py --txt`(v6 복제 + 신규 컷 폴백 파싱 + 엔딩 카드 제목을 md 에서) → `CinematicTable.cs`(279컷+MV 6) → `cutscene_txt.py import` → `ChapterScript.Data/En.cs`. 컷 길이 CS 6.5s / EV 6s / 엔딩 7s(자막이 짧아져 8.5→6.5). 기존 그림 107장 재사용 + **신규 74장**(`Cut_T_V7_*`, 폴더 `컷씬이미지/97_v7신규`, `_paths.txt` 에 등록) — 없으면 폴백(비슷한 기존 그림)으로 재생됨.
- **② CS7-2** `Cut_T_V7_N7_02` = 40세 엄마 얼굴(ANCHOR_MOM40)이 빛 속에서 처음 보이는 컷. 회상 컷(CS5·EV4·EV8)의 엄마는 전부 뒷모습/얼굴 숨김(`MFB` 앵커).
- **Firefly 불가**: 포토샵 `create_document` 가 「스크래치 디스크가 꽉 참」 오류 → 배경 10장도 Kling 웹으로. 디스크 비우면 다음부터 Firefly.
- Kling 웹 절차(95차와 같음): `eval(localStorage.__helpers84); eval(localStorage.__helpers95)` 뒤 103차 헬퍼(`__PL103/__run103/__batch103/__sheet103`, `localStorage.__plan103/__out103/__pick103`) — 새 앵커 키 `M40/M40P/M48/MFB`. 결과 다운로드·교체 `Tools/KlingGen/dl103.py --apply`(urls103.json·picks103.json → `out/web103/`), 시트 `Claude outputs/r103_cuts_new.jpg`. 실패 유형: 말풍선/텍스트(N2_06-2, E10_01-1, EA_03) · 패널 분할(E3_06) · TIMEOUT(N7_07) → 프롬프트에 「no speech, no text」 넣고 재생성.
- **③** 기부 컵: `DonateIconAnim.Update` 가 매 프레임 y 를 **-286 으로 고정**하고 있어 79차 +65 도 이번 +55 도 화면에 반영된 적이 없었음(사용자 「안 올라갔다」). → `DonateUI.IconY = -286 + 109`(보이던 자리에서 1cm ≈ 164px ÷ 1.5) 로 통일, AttachIcon·Anim 둘 다 사용. 캡쳐 `Tools/_shots/r103_t11.png`(전 `r103_t7.png`).
- **④** `CinemaSelect.cs` 전면 재작성: 상단 보라 띠 + 「나가기」, 요약 줄(메인스토리 N편 + 이야기 N편 · 본 메인스토리 N · 단서), 「전체 펼치기 / 전체 접기」, **주차 타임라인**(왼쪽 주차 알약 = `Timeline.WeekStart(챕터)`, 점선, 마름모, 오른쪽 카드). 메인스토리/프롤로그/엔딩 = 펼침 카드(윗줄 「제N화 제목 : 장소 (N주차)」, 표지, 「메인스토리 N · 제목」, 읽기·보기, ✓, ▲ 접기), 이야기(EV) = 접힌 하늘색 줄(▼ 펼치기), 잠김 = 회색 「다음 콘텐츠 · 예정」. 아래 「팁: 이야기는 접혀있어요」. 펼침 상태는 세션 동안 유지(`_expanded`). `IsOpen` 유지. 캡쳐 `Tools/_shots/r103_c2.png`(기본)·`r103_c3.png`(전체 펼침).
- 커밋 대상: `Docs/CUTSCENE_SCRIPTS_v7.md`, `Tools/Story/{gen_cinematic_v7.py,build_v7_md.py,v7_map.txt,script/*.txt}`, `Tools/KlingGen/{plan103.json,dl103.py,out/web103/*.json}`, `Assets/_CoastRun/Scripts/Story/{CinematicTable,ChapterScript.Data,ChapterScript.En}.cs`, `Assets/_CoastRun/Scripts/UI/{CinemaSelect,DonateUI}.cs`, `Assets/Resources/CoastRun/컷씬이미지/97_v7신규/**`, `Assets/Resources/CoastRun/컷씬이미지/_paths.txt`, `Claude outputs/r103_*.jpg`, 이 문서.
- **103차 후속(사용자 10건)**: 꼬마가 작거나 둘로 나온 그림 교체(N1_10→`V7_N1_08`, E4_02→`V7_E1_01`, N1_14→`V7_E1_05`, E7_01→`V7_E6_01`) · 「아빠는 돌아오지 않았다」 OP_08(웃는 얼굴)→`V7_N5_20` · 「도윤아!」 `V7_N7_07` 쫓는 구도로 재생성 · 해녀복 하늘 N6_11→`V7_N7_16` · CS8-1·2 N8_01(탑 위 도윤)→`V7_N8_01` 땅에 선 모습 · CS8-22·23 `V7_N8_22` 엄마 얼굴 앵커로 재생성 · 진엔딩 끝 **자막 없는 10컷 `V7_ET_10`(하얗게 번짐)** 추가(읽기용 md 에는 없음, 생성용 md 에서 build 시 붙임). BGM: CS3 밥 → M5, EV2(4화)·EV4(8화) → M1(v7_map 의 `CH4+BGM_M1` 표기). **EV6 첫눈 CH11→10, CS5 파란 머리띠 CH10→11**(`StoryProgress.CutsceneChapters/EventChapters`, `gen_cinematic_v7.CS_CH/EV_CH`) — 시네마 목록에서 첫눈이 25주차, 파란 머리띠 27주차. Kling 크레딧 58 → 약 31.
- **103차 후속 2(사용자)**: 엔딩 A·진엔딩 3컷 「나는 그 옆에 서 있었다. 「도윤아, 이제 그만 기다려.」…」 → 주인공은 이미 사라진 뒤라 **「도윤이는 한참 비석을 봤다. 바람이 국화를 흔들었다.」**(사용자 선택)로 교체(읽기용 md 도 함께 수정), 그림 `V7_EA_03` 도윤 혼자로 재생성(비석에 십자가 무늬가 들어감 — 거슬리면 재생성). 스타일 가이드 §3 「간접화법」 예시 줄은 옛 문장 그대로(예시일 뿐). Unity 가 꺼져 있어 `unity_launch` 로 열어 적용, 컴파일 OK.
